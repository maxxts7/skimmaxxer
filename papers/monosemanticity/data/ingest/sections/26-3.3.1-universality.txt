[3.3.1] Universality

A/1/2357 has a correlated feature (corr=0.85) in run B/1, B/1/2165. It also has high activation specificity for base64 strings:

[fig-27] A/1/2357 has a correlated feature (corr=0.85) in run B/1, B/1/2165. It also has high activation specificity for base64 strings:

Like A/1/2357, B/1/2165's logit weights have a second mode corresponding to base64 tokens:

[fig-28] Like A/1/2357, B/1/2165's logit weights have a second mode corresponding to base64 tokens:

Correlations and scatter plots are also consistent with them being very similar features:

[fig-29] Correlations and scatter plots are also consistent with them being very similar features:

Note that we expect the overlap between the interference and base64 token logit weights to be from the aforementioned usage of base64 tokens across many other contexts.

