PAGE FOR CONCEPT: base64-feature
NAME: Base64 Feature (A/1/2357)

SUMMARY (already shown above your page as the lede - do not repeat it verbatim):
A feature firing on base64 strings, of interest because a base64 neuron had already been found in a different model with a different architecture.

DRAFT EXPLANATION from the extraction stage (your page replaces this; keep what is right, go deeper):
A/1/2357 responds to base64-encoded strings, such as the trailing characters of a YouTube video URL. Its logit weights show a second mode of base64-specific tokens, but the transition from the main mode to that second mode is more continuous than in the Arabic case. The paper explains this by noting that whether a token is Arabic is close to a binary property, whereas any string of English letters has some nontrivial probability of appearing in base64 — the token "fr" is both a base64 fragment and a common abbreviation for French, so the model has reason to be cautious about upweighting it. The proxy correlation is only 0.38, which the paper attributes to the proxy being too broad rather than the feature being impure.

THEME IT BELONGS TO: theme-four-features-up-close | Four features up close - Having a decomposition is not the same as having interpretable features, so the paper picks four — Arabic script, DNA, base64, Hebrew — and tries to establish the same five claims about each. The claims are that the feature fires only in its context, fires whenever that context is present, changes the model's output in a matching way, is not just a neuron in disguise, and turns up again in a separately trained model. The specificity argument leans on a hand-built proxy: a cheap score for whether a string looks like the hypothesized context, computed without reference to the model. The paper says outright that these four were chosen for being easy to analyze, not for being typical, which is why the next theme exists.

SUB-CONCEPTS (these render as expandable sections BELOW your page - reference them, do not duplicate them):
  - base64-digit-feature | Base64 Digit Feature and the BPE Exploit: A companion feature that fires on single digits inside base64 contexts, exploiting a property of the BPE tokenizer to make a better prediction.
  - hexadecimal-feature | Hexadecimal Feature (A/1/3817): A separate feature for hexadecimal strings, which the base64 proxy wrongly counts as base64.
  - solu-base64-neuron | The Earlier Base64 Neuron: A base64 neuron previously found in the SoLU paper, which the authors take as a hint that base64 might be universal across architectures and datasets.

PREREQUISITES:
  - base64-proxy | Base64 Proxy: A score modeling base64 strings as random sequences over [a-zA-Z0-9+/]; the paper judges it too broad.
  - feature-evidence-template | The Five-Claim Feature Template: The fixed set of five claims the paper tries to establish for each individual feature it studies in depth: specificity, sensitivity, downstream effect, non-correspondence to a neuron, and universality.
  - logit-weight | Logit Weight: The vector, one entry per vocabulary token, giving how much a feature's firing pushes up or down each next-token logit.

EDGES TOUCHING THIS CONCEPT (2) - the relationships the app already knows; weave the load-bearing ones into your prose:
  [supporting] <- base64-proxy (Base64 Proxy) | contrasts-with | feature and its own yardstick correlate 0.38: The base64 proxy scores a string by modelling it as a random draw from [a-zA-Z0-9+/]. Its Pearson correlation with the base64 feature's activations is only 0.38, well below the 0.74 the Arabic pair reaches. The paper blames the proxy rather than the feature: hexadecimal strings set it off too, and those have a feature of their own, A/1/3817.
  [minor] -> context-features (Context features) | instance-of | named example of the motif: Base64 is the paper's other named context feature. It also shows the motif's dual reading: the feature both responds to base64 strings and acts to raise the probability of more base64 tokens.

EVIDENCE ITEMS THAT SHOW THIS (link them as [[item-id]]):
  - fig-25 | Base64 Activations Coloured by Proxy: The figure is the paper's specificity check for its base64 feature A/1/2357: it stacks every sampled token by how strongly the feature fired, and colours the stack by an independently computed base64 score. The higher the activation, the more completely the column is filled by tokens the score calls base64, which is what the authors mean by calling the feature "quite specific" — though they give no percentage here, and the score itself is admittedly too broad.
  - fig-26 | Base64 Logit Weights and Their Conditional Strip: For the base64 feature A/1/2357, the tokens whose logits it raises shade gradually from ordinary text tokens into base64-specific ones, rather than separating into two clean groups. The narrow strip below the histogram makes this visible by showing, at each logit weight, only the mix of token types and not how many tokens there are.
  - fig-27 | The Base64 Feature Found Again in Model B: A separately trained transformer, decomposed by a separately trained dictionary, yields a feature (B/1/2165) whose activations correlate 0.85 with the base64 feature from the first model, and which fires almost exclusively on base64-looking strings at high activation. The paper offers this as evidence that the base64 feature is a property of models trained on this data rather than an artifact of one dictionary learning run.
  - fig-28 | B/1/2165's Base64 Logit Mode: In the second model's dictionary, the feature that matches the base64 feature does the same thing to the output that the original does: its logit-weight histogram has a small second mode, centered near +0.32, made almost entirely of tokens that would continue a base64 string. This is the downstream half of the universality claim — the two features do not just fire on the same text, they push up the same vocabulary.
  - fig-29 | Base64 Feature, Matched Across Two Runs: The base64 feature found in model A (A/1/2357) and the one found in a separately seeded model B (B/1/2165) line up on both axes the paper uses to compare features: their activations across text correlate at 0.85, and their per-vocabulary-token logit weights correlate at 0.82. The paper offers this as consistent with the two being the same feature discovered twice, not as proof of it.
  - fig-30 | The Base64 Feature's Nearest Neuron: The MLP neuron whose activations track the base64 feature most closely, A/neurons/470, correlates with it at only 0.18, and its own activation distribution stays mostly blue — mostly non-base64 — at every activation level. Its sampled examples are code, XML namespaces, pandoc `label="…"` markup, URL fragments and citation keys, categories with nothing in common but a punctuation-heavy texture. The paper reads this as the feature not being a neuron in disguise.
  - fig-31 | Neuron 470's Mixed Logit Weights: The neuron most correlated with the base64 feature does push base64 tokens up a little, but the tokens it pushes up hardest are filename endings like 'htm', 'png' and 'pdf', which are not base64-specific. The paper reads this as a neuron that helps carry the base64 feature while mostly doing other work, so it is not a clean base64 unit the way the learned feature is.
  - fig-32 | Base64 Feature Against Neuron 470: The neuron whose activations most resemble the base64 feature's shares only part of its behaviour: an activation correlation of 0.18 and a logit-weight correlation of 0.64, with most of the mass in both plots sitting off the diagonal. The paper reads this as consistent with the neuron helping represent the feature while largely doing other things, which is what it needs in order to say dictionary learning did not simply hand back an already-monosemantic base64 neuron.
  - fig-43 | Pinning Features to Steer Generation: Holding a single learned feature at a high value while sampling makes the model emit text of exactly the kind that feature normally fires on — Arabic script, DNA, base64, hexadecimal, Hebrew, Han Chinese, or uppercase. Because the autoencoder was fitted only to MLP activations and never to the model's outputs, the paper reads this as evidence that these features are used by the model's computation, not just artifacts of correlations in the data.
  - fig-44 | Three Dictionaries in One UMAP: The families the authors found by reading text — base64 features, Arabic script features — are also families in geometry: their dictionary vectors point in similar directions. Embedding three dictionaries of different sizes together shows the coarse features from the small run sitting among the finer features that the larger runs split them into.
  - fig-48 | One base64 Feature Becoming Three: In the 512-feature dictionary A/0, a single feature (A/0/45) fires across a whole base64 string. In the 4,096-feature dictionary A/1, three features share that job — one leaning on letters, one on digits, one on base64 that decodes to ASCII — and between them they light up roughly the same tokens the parent did. The figure is the paper's picture of feature splitting at the level of individual tokens, shown on one example.
  - fig-49 | The Digit Feature That Won't Predict Digits: Two base64 features from the same dictionary push almost the same tokens up, except that the feature firing on single digits gives the ten digit tokens far less of a boost than its letter-firing sibling does. The paper reads this not as a broken feature but as the tokenizer showing through: byte-pair encoding merges adjacent digits, so a lone digit token is evidence the next token is not a lone digit.
  - fig-50 | Base64 That Decodes to Text: Three features in run A/1 all fire on base64, and this figure shows what separates the third one: the strings A/1/1544 fires hardest on decode into readable source code and markup, while the best available decoding of A/1/2357's and A/1/2364's top examples is unreadable bytes. That turns a set of features that looked like a redundancy in the dictionary into a distinction the model is apparently making — between base64 that encodes text and base64 that does not.
  - fig-55 | Single-Node Feature Loops: Four features from the smallest dictionary each raise the probability of exactly the kind of token that would make them fire again one position later, so each one behaves as a one-state machine that keeps re-entering itself. The paper offers this as the simplest case of its "finite state automata" motif. The evidence shown is a list of top-promoted tokens, not a measured transition rate.

PAPER SECTIONS TO READ (in papers/monosemanticity/data/ingest/sections/):
  - 25-3.3-base64-feature.txt
  - 26-3.3.1-universality.txt
  - 27-3.3.2-the-feature-is-not-a-neuron.txt
