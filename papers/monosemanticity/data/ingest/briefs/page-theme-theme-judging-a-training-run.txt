PAGE FOR THEME: theme-judging-a-training-run
NAME: Judging a training run

THEME SUMMARY (shown above your page as the lede - do not repeat verbatim):
The paper trained many autoencoders and had to pick between them, without any single metric that tracks what it cares about. What it does instead is triangulate: look at how sparse the decomposition actually is, how much of the layer's behavior survives substitution, what the histogram of feature firing rates looks like, and — slowest and most trusted — whether the features read as interpretable when a person inspects them. This is the workshop side of the work rather than the argument, which is why it sits near the end. It is also where the paper is most candid that it has no systematic way to tell a good run from a bad one.

PREVIOUS THEME: theme-what-the-features-look-like-together | What the features look like together
NEXT THEME: theme-how-to-read-the-charts | How to read the charts

MEMBER CONCEPTS (12) - each has its own page or expandable section; your job is the connective tissue between them, not to restate each one:
  - evaluating-dictionary-learning | Telling Whether the Autoencoder Is Working [major]: The paper has no single metric that tracks what it cares about, and instead triangulates from several imperfect signals.
      · feature-density | Feature Density: The fraction of tokens on which a feature has a nonzero value; the paper's main quick proxy for autoencoder qu
      · density-cluster-separation | Separating the Density Clusters: When density alone does not separate the two clusters, adding the feature's bias and its encoder-decoder dot p
      · feature-density-diagnostics | Feature Density Diagnostics: Three specific numbers read off the feature density histograms and used to judge a run: live features outside 
      · high-density-cluster | High Density Cluster: The right mode of the density histogram, whose features are anecdotally almost all interpretable despite still
      · ultralow-density-cluster | Ultralow Density Cluster: A left mode of features firing on fewer than about one in a million tokens, almost none of which are interpret
      · feature-exploration-interface | Feature Exploration Interface: A published visualization covering every feature in every dictionary learning run, showing when a feature fire
  - manual-inspection | Manual Inspection [minor]: Looking at features and asking whether they seem interpretable. Slow and subjective, but the paper treats it as the ground truth the proxies stand in for.
  - training-loss-metric | Training Loss as a Proxy [minor]: The simplest signal, useful for comparing optimizers and learning rates but meaningless across runs with different L1 coefficients.
  - l0-norm | $L^0$ Norm [minor]: The average number of features active on a token. This is the paper's direct measure of how sparse the decomposition actually is.
  - l0-nll-tradeoff | Sparsity vs. Explained Behavior Tradeoff [minor]: Plotting $L^0$ norm against reconstructed transformer NLL across runs gives a frontier the paper uses to choose the L1 coefficient.
  - feature-density | Feature Density [major]: The fraction of tokens on which a feature has a nonzero value; the paper's main quick proxy for autoencoder quality.
      · density-cluster-separation | Separating the Density Clusters: When density alone does not separate the two clusters, adding the feature's bias and its encoder-decoder dot p
      · feature-density-diagnostics | Feature Density Diagnostics: Three specific numbers read off the feature density histograms and used to judge a run: live features outside 
      · high-density-cluster | High Density Cluster: The right mode of the density histogram, whose features are anecdotally almost all interpretable despite still
      · ultralow-density-cluster | Ultralow Density Cluster: A left mode of features firing on fewer than about one in a million tokens, almost none of which are interpret
  - ultralow-density-cluster | Ultralow Density Cluster [minor]: A left mode of features firing on fewer than about one in a million tokens, almost none of which are interpretable; the paper treats them as a training artifact.
  - high-density-cluster | High Density Cluster [minor]: The right mode of the density histogram, whose features are anecdotally almost all interpretable despite still being very rare in absolute terms.
  - feature-density-diagnostics | Feature Density Diagnostics [minor]: Three specific numbers read off the feature density histograms and used to judge a run: live features outside the ultralow cluster, where the non-ultralow features start, and how many features fire on over 1% of tokens.
  - toy-model-ground-truth | Toy Models as Ground Truth [minor]: Small synthetic setups where the true features are known by construction, used to check that the autoencoder recovers what it should.
  - information-based-metric | The Information-Based Metric That Failed [minor]: An attempt to score factorizations by the total information of autoencoder plus data. It did not correlate with what the paper cared about.
  - feature-exploration-interface | Feature Exploration Interface [minor]: A published visualization covering every feature in every dictionary learning run, showing when a feature fires, what it does to the logits, and what happens when it is ablated.
      · activation-visualization-sampling | Activation Visualization Sampling: The dataset examples shown in the interface come from a 100-million-token subset with only 10 tokens taken per
      · data-oriented-interface | Data-Oriented Interface: A second view that inverts the first: pick a text, see every feature active on it.

EDGES BETWEEN MEMBERS OF THIS THEME:
  [load-bearing] ultralow-density-cluster --contrasts-with--> high-density-cluster | two density modes, opposite interpretability: Plotting how often each learned feature fires, on a log axis, usually splits a run into two humps. Anecdotally almost every feature in the right, higher-density mode is interpretable, and almost none in the ultralow mode are. The paper treats the ultralow cluster as an artifact of autoencoder training rather than a property of the transformer, and says it is still investigating where it comes from.
  [supporting] information-based-metric --contrasts-with--> l0-norm | the abstract metric rewarded dense solutions: The authors began by looking for a metric that would score a factorization by the total information of autoencoder plus data. It did not track what they cared about: runs whose average number of active features per token ran into the hundreds, but with low reconstruction error, could score better than sparser runs. They abandoned it and fell back on a bundle of imperfect signals.

LOAD-BEARING EDGES LEAVING THIS THEME (use these to point forward/back):
  importance-scoring-degeneracy --depends-on--> feature-density | densities of 1e-3 to 1e-6 break it
  l0-norm --contrasts-with--> loss-recovered-by-features | sparsity paid for with explained loss

EVIDENCE ITEMS RELATED TO THIS THEME (link as [[item-id]]):
  - fig-1 | The UMAP Feature Browser: A screenshot of the interactive map the authors publish alongside the article: every dictionary direction from two autoencoder runs — 4,608 of them — projected onto a plane, with a searchable list of human-written feature labels beside it. It establishes that the qualitative families of features the paper describes in prose are also tight geometric clumps, and that features from the small run and the large run land in the same clumps. It is a browsing tool, not a measurement: the clusters carry the meaning, the coordinates do not.
  - fig-4 | Anatomy of a Feature Page: A labelled tour of one page of the paper's published feature browser, which puts four kinds of evidence about a single learned feature side by side: where it fires, what it does to the next-token logits, whether any single neuron matches it, and whether a parallel training run found the same thing. For the feature shown, no neuron correlates with it above 0.09, while a feature from the parallel run correlates at 0.75.
  - fig-5 | The Feature Search Bar: A screenshot of the search strip in the paper's public feature browser, with its four dropdowns opened. It carries no results, so what it establishes is only the shape of the query the authors built for themselves: pick a matching mode, pick which part of a feature's record the query is matched against, pick a sort key over all features in a run, and pick which per-token loss difference is drawn as underlines.
  - fig-6 | Text-First Feature Browser: A second published interface that inverts the usual view: instead of picking a feature and seeing the texts it fires on, you pick a text and see every feature active anywhere in it, ranked, each with a machine-written label. Hovering a token re-ranks the list for that token; hovering a feature paints its activation back onto every token. This is tooling for auditing the decomposition, not evidence for any claim.
  - fig-19 | Arabic Feature Across Two Models: Dictionary learning on a second, independently trained transformer produces a feature whose firing pattern closely tracks the original Arabic script feature (Pearson correlation 0.91), while the two features' effects on output token logits agree much less (0.23). The agreement sits in a small cluster of Arabic tokens that both features push up; the disagreement sits in a large, roughly round cloud of small weights near zero, which the paper guesses is interference rather than anything the model cares about.
  - fig-33 | The Hebrew Feature's Activation Spectrum: A histogram of how often feature A/1/416 takes each activation value, with each slice split by an independent Unicode-based score for "is this Hebrew". Strong activations are almost entirely Hebrew, which is the paper's specificity claim for this feature. The blue that survives at strong activations is mostly shared punctuation and whitespace, which the proxy scores as non-Hebrew by construction, so the paper reads it as a fault in the proxy rather than in the feature.
  - fig-44 | Three Dictionaries in One UMAP: The families the authors found by reading text — base64 features, Arabic script features — are also families in geometry: their dictionary vectors point in similar directions. Embedding three dictionaries of different sizes together shows the coarse features from the small run sitting among the finer features that the larger runs split them into.
  - fig-46 | The Mathematics and Physics Splitting Graph: Three autoencoders of increasing width, trained on the same one-layer transformer, are lined up side by side, and the technical-writing features of the smallest run are traced into their relatives in the larger ones. The descriptions attached to those relatives get narrower as the dictionary grows: one feature for mathematical prose becomes separate features for category theory, complex analysis, field theory and machine learning. The links are set by a quantitative similarity measure, but "more specific" is read off the feature descriptions rather than scored.
  - fig-64 | The Two Density Modes of A/16: Plotting how often each learned feature fires, on a log axis, splits the features of run A/16 into two humps: a spike of features that fire on roughly one token in ten million, and a broad bulk centred near one token in a hundred thousand. The paper reports that almost all of the right-hand features are interpretable and almost none of the left-hand ones are, and treats the left hump — which it names the ultralow density cluster — as a suspected artifact of autoencoder training rather than anything in the transformer.
  - fig-65 | Overlapping Density Clusters in Run A/5: The paper's quick diagnostic — read two separated humps off a log-scale histogram of how often each feature fires — stops working at the largest autoencoder sizes. In run A/5, with 131,072 hidden units, the ultralow-density features run continuously into the rest of the distribution instead of sitting apart, so density alone no longer tells the uninterpretable leftovers from the real features. The paper reports this as a pattern it has seen, not as a measured effect.
  - fig-66 | Three Diagnostics That Isolate the High-Density Cluster: In a run where feature density alone leaves the two modes overlapping, plotting each feature's density together with its encoder bias and its decoder-encoder dot product pulls the high-density cluster out as a compact, separate blob. That separation is what lets the paper draw a line around the ultralow-density features and set them aside as a training artifact rather than as things the transformer actually computes.
  - fig-67 | Density Histograms Across Dictionary Sizes: Across five sparsity settings, each panel overlays the feature-density histograms of six autoencoders whose dictionaries run from 512 up to 131,072 units. Widening the dictionary does not just yield more features: it keeps pushing the left edge of the distribution further left, so rarer features keep appearing. The paper reads this as a sign that no dictionary size tried so far has exhausted the one-layer transformer's features, and states it as a suggestion rather than a result.
  - fig-68 | Sparsity Against Explained Behavior: Plotting how sparse each trained dictionary is against how much of the MLP layer's loss contribution it recovers turns hyperparameter choice into a visible tradeoff: a weaker sparsity penalty explains more of the model's behavior, but only by letting more features fire on every token. The four L1 coefficients sit as four separate vertical bands, so the penalty largely fixes the sparsity level, while widening the dictionary climbs the band by raising explained behavior at nearly constant sparsity.
  - fig-71 | Importance-Weighted Interpretability Scores: Once the automated-interpretability scores are reweighted to what random text would have given, features still beat neurons (median 0.28 vs 0.04) but both distributions collapse toward zero. The paper's explanation is that a feature's true activations on random text are almost all zero — 88% on average, versus 54% for neurons — so the reweighted correlation mostly rewards predicting zero rather than predicting when the unit fires.
  - eq-autoencoder-loss | The Sparse Autoencoder's Two-Term Loss: This is the entire method in four lines: a ReLU encoder turns one MLP activation vector into feature activations, a linear decoder rebuilds the activation from them, and training minimizes squared reconstruction error plus an L1 penalty on those activations. The two loss terms pull against each other — reconstruction forces the dictionary to keep the information, L1 forces most features to be off — and the coefficient lambda sets the exchange rate between them.

PAPER SECTIONS TO READ (in papers/monosemanticity/data/ingest/sections/):
  - 72-15-feature-density-histograms.txt
  - 76-18.1-pre-encoder-bias.txt
  - 81-19-advice-for-training-sparse-autoencoders-hyperparameter-selec.txt
  - 09-2.5.1-how-can-we-tell-if-the-autoencoder-is-working.txt
  - 12-2.8-interface-for-exploring-features.txt
  - 82-20-activation-visualization.txt
  - 88-22.4-explanation-lengths-caveat.txt
  - 17-3.1.3-feature-downstream-effects.txt
  - 31-4-global-analysis.txt
  - 37-4.1.5-caveats.txt
  - 39-4.3-do-features-tell-us-about-the-model-or-the-data.txt
  - 43-5.2.1-example-mathematics-and-physics-features.txt
