PAGE FOR THEME: theme-what-the-features-look-like-together
NAME: What the features look like together

THEME SUMMARY (shown above your page as the lede - do not repeat verbatim):
With thousands of features in hand, the paper stops arguing about individual ones and describes the collection. It sorts them into recurring kinds — features for a whole context, features for one token inside a context, short fixed continuations, memorized phrases — and finds groups that hand off to one another across the token stream like a state machine. Widening the autoencoder does not find new territory but subdivides what was already there, which the paper calls feature splitting and treats as a zoom level rather than a fixed answer. The geometry that comes out of this leads the authors to revise the picture of superposition they started with, and they flag which parts of that revision are still speculation.

PREVIOUS THEME: theme-the-same-features-in-a-second-model | The same features in a second model
NEXT THEME: theme-judging-a-training-run | Judging a training run

MEMBER CONCEPTS (16) - each has its own page or expandable section; your job is the connective tissue between them, not to restate each one:
  - feature-motifs | Feature motifs [major]: The recurring kinds of features the paper finds: context features, token-in-context features, trigram features, sequence-memorizing features, and features readable as actions.
      · action-features | Features as actions: The dual reading of every feature in a one-layer model: it both responds to something and acts to raise the pr
      · context-features | Context features: Features that fire across a whole region of text of a certain kind — DNA sequences, base64 strings, a particul
      · finite-state-automata-features | Finite state automata: Assemblies of features where one feature raises the probability of tokens that in turn cause another feature t
      · html-feature-automaton | The four-node HTML system: Four A/0 features that hand off to each other to generate valid HTML: open tag, tag name, tag close, whitespac
      · mechanistic-anomaly-detection | Mechanistic anomaly detection: The idea of noticing when a model's internal mechanism behaves differently in a specific narrow case.
      · self-exciting-feature-loop | Single-node loop: The simplest case: a feature that increases the probability of tokens which would themselves activate it again
  - action-features | Features as actions [minor]: The dual reading of every feature in a one-layer model: it both responds to something and acts to raise the probability of something.
  - context-features | Context features [minor]: Features that fire across a whole region of text of a certain kind — DNA sequences, base64 strings, a particular language — rather than on one token type.
  - token-in-context-features | Token-in-context features [minor]: Features that fire on one specific token but only in a particular kind of context — 'the' in mathematics, '<' in HTML.
  - trigram-features | Trigram features [minor]: Features that implement short fixed continuations, such as predicting '19' after 'COVID-'.
  - memorization-features | Sequence-memorizing features [minor]: Features that respond to a specific long phrase and appear to reproduce it, visible only in large dictionaries.
  - finite-state-automata-features | Finite state automata [minor]: Assemblies of features where one feature raises the probability of tokens that in turn cause another feature to fire on the next step, forming a state machine over generated text.
      · html-feature-automaton | The four-node HTML system: Four A/0 features that hand off to each other to generate valid HTML: open tag, tag name, tag close, whitespac
      · mechanistic-anomaly-detection | Mechanistic anomaly detection: The idea of noticing when a model's internal mechanism behaves differently in a specific narrow case.
      · self-exciting-feature-loop | Single-node loop: The simplest case: a feature that increases the probability of tokens which would themselves activate it again
      · unicode-token-state-machines | Two-node systems from split Unicode characters: Where a script's characters are tokenized as a prefix byte plus a suffix byte, the model alternates between a 
  - feature-splitting | Feature splitting [major]: The observation that as the autoencoder is made wider, single features divide into families of more specific features covering roughly the same territory.
      · dictionary-resolution | Dictionaries as resolutions: The idea that different autoencoder sizes are different zoom levels on the same underlying structure, with sma
      · feature-geometry-clusters | Geometric clustering of feature directions: Conceptually similar features have small angles between their dictionary vectors, so qualitative families show
      · feature-umap | Feature UMAPs: A 2-D projection of feature directions, used to make the geometry of a dictionary browsable and to show that q
      · hdbscan-feature-clustering | HDBSCAN Clustering of Features: Clusters shown on the first UMAP come from running HDBSCAN on a separate 10-dimensional UMAP embedding of the 
      · one-dimensional-umap-ordering | One-Dimensional Ordering of Features: The feature list is sorted by a one-dimensional UMAP fit to the unclustered features together with the cluster
      · umap | UMAP: A dimensionality reduction method that places high-dimensional points in two dimensions while trying to preser
  - dictionary-resolution | Dictionaries as resolutions [minor]: The idea that different autoencoder sizes are different zoom levels on the same underlying structure, with small dictionaries giving useful summaries.
  - local-vs-compositional-codes | Local versus compositional codes [minor]: Two ways to represent a combination of properties: one unit per combination (local), or separate units that combine (compositional).
      · are-token-in-context-features-real | Are token-in-context features real?: Whether the hundreds of 'the'-in-a-context features reflect the transformer's own local code, or an artifact o
      · sharper-predictions-argument | The sharper-predictions argument: The reason to think local codes are real: a compositional code forces the logits for 'the in Physics' to be a 
      · compositional-code | Compositional code: A representation where independent units combine to represent a conjunction — 'the' and 'Physics' active toget
      · local-code | Local code: A representation with one dedicated unit per thing represented, including per combination of properties.
  - are-token-in-context-features-real | Are token-in-context features real? [minor]: Whether the hundreds of 'the'-in-a-context features reflect the transformer's own local code, or an artifact of dictionary learning imposing one.
      · sharper-predictions-argument | The sharper-predictions argument: The reason to think local codes are real: a compositional code forces the logits for 'the in Physics' to be a 
  - true-features | True features [minor]: The features a model actually uses, as opposed to the ones a given dictionary happens to learn. The paper argues a small model has fewer of them.
  - feature-geometry-clusters | Geometric clustering of feature directions [minor]: Conceptually similar features have small angles between their dictionary vectors, so qualitative families show up as tight clusters in a 2-D projection.
  - feature-umap | Feature UMAPs [minor]: A 2-D projection of feature directions, used to make the geometry of a dictionary browsable and to show that qualitative families are geometric clusters.
      · hdbscan-feature-clustering | HDBSCAN Clustering of Features: Clusters shown on the first UMAP come from running HDBSCAN on a separate 10-dimensional UMAP embedding of the 
      · one-dimensional-umap-ordering | One-Dimensional Ordering of Features: The feature list is sorted by a one-dimensional UMAP fit to the unclustered features together with the cluster
      · umap | UMAP: A dimensionality reduction method that places high-dimensional points in two dimensions while trying to preser
  - theories-of-superposition | Theories of superposition, revised [minor]: The paper's reassessment of the picture it inherited: features clump into dense related groups and may not be one-dimensional, which the earlier toy picture did not anticipate.
      · action-sharing-features | Action-sharing features: Features that produce similar downstream effects and therefore end up with similar directions — the paper's su
      · correct-number-of-features | Is there a correct number of features?: Under some expanded theories the question is not well posed; under others there is a true number but getting i
      · correlated-features | Correlated features: Features that tend to fire together, offered as one explanation for why feature directions clump.
      · feature-manifolds | Feature manifolds: The possibility that some features are not one-dimensional intensity scales but higher-dimensional continuous 
      · isotropic-superposition-model | Isotropic superposition model: The inherited picture from Toy Models: features are discrete, one-dimensional, and repel each other through in
  - feature-manifolds | Feature manifolds [minor]: The possibility that some features are not one-dimensional intensity scales but higher-dimensional continuous families of related directions.

EDGES BETWEEN MEMBERS OF THIS THEME:
  [load-bearing] finite-state-automata-features --depends-on--> action-features | handoffs need the action reading: Every feature in this model has two sides: it responds to something, and it pushes certain next tokens up. The automata motif reads a chain of features through that second side, where one feature raises the probability of tokens that would make the next feature fire a position later. Without the action reading, the four-node HTML system is just four unrelated detectors.
  [load-bearing] theories-of-superposition --depends-on--> feature-geometry-clusters | clumping forces the revision: The picture inherited from Toy Models has features spread roughly evenly, pushed apart from each other by interference. The dictionary's geometry shows something different locally: conceptually related features sit at small angles and form tight clusters. The paper revises its theory to fit that observation, and says it does not yet know which account of the clumping is right.
  [supporting] dictionary-resolution --contrasts-with--> true-features | coverage bought with lost specificity: The paper conjectures an idealized set of features the model actually uses, clustered tightly in superposition. A dictionary of limited width cannot separate them, so it returns features covering roughly the same territory while being less specific. Widening the dictionary splits them apart, which is why the authors read dictionary size as a zoom level rather than as a guess at a correct count.

LOAD-BEARING EDGES LEAVING THIS THEME (use these to point forward/back):
  feature-splitting --supported-by--> fig-46 | technical-writing features refine with width
  token-in-context-features --instance-of--> local-code | one unit per token-context pair

EVIDENCE ITEMS RELATED TO THIS THEME (link as [[item-id]]):
  - fig-1 | The UMAP Feature Browser: A screenshot of the interactive map the authors publish alongside the article: every dictionary direction from two autoencoder runs — 4,608 of them — projected onto a plane, with a searchable list of human-written feature labels beside it. It establishes that the qualitative families of features the paper describes in prose are also tight geometric clumps, and that features from the small run and the large run land in the same clumps. It is a browsing tool, not a measurement: the clusters carry the meaning, the coordinates do not.
  - fig-44 | Three Dictionaries in One UMAP: The families the authors found by reading text — base64 features, Arabic script features — are also families in geometry: their dictionary vectors point in similar directions. Embedding three dictionaries of different sizes together shows the coarse features from the small run sitting among the finer features that the larger runs split them into.
  - fig-45 | One Region, More Arrows: A hand-drawn schematic of what the paper means by feature splitting: widening the autoencoder does not send new features off to new parts of activation space, it packs more dictionary directions into the same narrow cone, so the split features end up at small angles to one another. It is an illustration of the authors' picture, not a measurement.
  - fig-46 | The Mathematics and Physics Splitting Graph: Three autoencoders of increasing width, trained on the same one-layer transformer, are lined up side by side, and the technical-writing features of the smallest run are traced into their relatives in the larger ones. The descriptions attached to those relatives get narrower as the dictionary grows: one feature for mathematical prose becomes separate features for category theory, complex analysis, field theory and machine learning. The links are set by a quantitative similarity measure, but "more specific" is read off the feature descriptions rather than scored.
  - fig-47 | The P Feature Zoo: One coarse feature that looked like a pointless "fires on the letter P" detector turns out, in wider dictionaries, to be a family of sixteen or more features that each fire on P in a different context — surnames, acronym patterns, biochemistry, LaTeX, legal citations. The paper offers this as its explanation for the strange single-token features that show up in small dictionaries: they are contexts the small dictionary could not tell apart, so widening the dictionary refines them rather than fragmenting them.
  - fig-48 | One base64 Feature Becoming Three: In the 512-feature dictionary A/0, a single feature (A/0/45) fires across a whole base64 string. In the 4,096-feature dictionary A/1, three features share that job — one leaning on letters, one on digits, one on base64 that decodes to ASCII — and between them they light up roughly the same tokens the parent did. The figure is the paper's picture of feature splitting at the level of individual tokens, shown on one example.
  - fig-49 | The Digit Feature That Won't Predict Digits: Two base64 features from the same dictionary push almost the same tokens up, except that the feature firing on single digits gives the ten digit tokens far less of a boost than its letter-firing sibling does. The paper reads this not as a broken feature but as the tokenizer showing through: byte-pair encoding merges adjacent digits, so a lone digit token is evidence the next token is not a lone digit.
  - fig-55 | Single-Node Feature Loops: Four features from the smallest dictionary each raise the probability of exactly the kind of token that would make them fire again one position later, so each one behaves as a one-state machine that keeps re-entering itself. The paper offers this as the simplest case of its "finite state automata" motif. The evidence shown is a list of top-promoted tokens, not a measured transition rate.
  - fig-56 | The All-Caps Snake Case Loop: Two features from the smallest dictionary — one firing on all-caps word pieces, one on underscores — each raise the probability of the tokens that switch the other on, so alternating between them produces variable names like ARRAY_MAX_VALUE. The paper offers it as its simplest two-node case of a feature assembly that behaves like a state machine, with the hand-off happening through the sampled token stream rather than through weights.
  - fig-57 | Unicode Prefix–Suffix Feature Loops: Where a script's characters are tokenized as a prefix byte plus a suffix byte, the paper finds a pair of features that hand off to each other: each one's top predicted tokens are exactly the tokens the other one fires on. Chinese adds a wrinkle — because a complete single-token character sits in the same position as a suffix, one feature covers both, and it also predicts back into itself.
  - fig-58 | The Four-Node HTML Automaton: Four features in the smallest dictionary hand off to each other through the token stream — open bracket, tag name, closing bracket, newline-and-indent, back to open bracket — so that together they behave like a small state machine that emits well-formed HTML such as `<div>\n\t\t<span>`. The paper stresses that nothing tied these four together during training: each learned its own job, and the loop exists because HTML in the training data has that shape.
  - fig-59 | The IRC Transcript Automaton: Four features from run A/0 hand off to each other across an IRC transcript: one fires on the "<" that opens a nickname, one on the nickname's interior, one on the closing ">", and one across the message text that follows. None of them is keyed to a distinctive token — "<" and ">" are the same characters that drive the paper's HTML automaton — so what selects a feature here is the position inside an IRC line's format, not the token's identity.
  - fig-60 | The License-Boilerplate Chain: Three narrow features from the large A/4 dictionary fire in sequence — on the "A", then on "PARTICULAR", then on "PURPOSE" — with each one raising the probability of the token that triggers the next. Together they reproduce the tail of the standard licence phrase "MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE", which the paper offers as apparent memorization of a string the model saw many times in training.
  - fig-61 | Three Pictures of Feature Geometry: A hand-drawn contrast between three ways features might be arranged in activation space: evenly spread one-dimensional directions, clumped one-dimensional directions, and continuous higher-dimensional patches. The last panel is the one that bites: if a model uses feature manifolds rather than rays, dictionary learning can only ever approximate them with discrete directions, and asking for the "correct number of features" stops being a well-posed question.
  - fig-62 | Two Alternatives to 1-D Features: A pair of sketches arguing that "features are higher-dimensional manifolds" is not one hypothesis but two. One version — the convex hull of a few features that fire together — is just correlated one-dimensional features under another name. The other — a closed ring of directions — can still be approximated by one-dimensional features, but has no uniquely correct set of them, which the paper offers as a possible reason feature splitting never stops.
  - fig-67 | Density Histograms Across Dictionary Sizes: Across five sparsity settings, each panel overlays the feature-density histograms of six autoencoders whose dictionaries run from 512 up to 131,072 units. Widening the dictionary does not just yield more features: it keeps pushing the left edge of the distribution further left, so rarer features keep appearing. The paper reads this as a sign that no dictionary size tried so far has exhausted the one-layer transformer's features, and states it as a suggestion rather than a result.

PAPER SECTIONS TO READ (in papers/monosemanticity/data/ingest/sections/):
  - 01-1-summary-of-results.txt
  - 10-2.6-the-one-layer-model-we-re-studying.txt
  - 83-21-feature-umaps.txt
  - 40-5-phenomenology.txt
  - 41-5.1-feature-motifs.txt
  - 42-5.2-feature-splitting.txt
  - 43-5.2.1-example-mathematics-and-physics-features.txt
  - 44-5.2.2-features-which-seemed-like-bugs.txt
  - 48-5.4-finite-state-automata.txt
  - 54-7.1-theories-of-superposition.txt
  - 55-7.2-are-token-in-context-features-real.txt
  - 00-front-title-authors.txt
