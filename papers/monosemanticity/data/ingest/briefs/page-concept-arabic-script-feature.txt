PAGE FOR CONCEPT: arabic-script-feature
NAME: Arabic Script Feature (A/1/3450)

SUMMARY (already shown above your page as the lede - do not repeat it verbatim):
The paper's lead case study: a learned feature that fires on text in Arabic script, is specific and reasonably sensitive to it, and has no neuron counterpart.

DRAFT EXPLANATION from the extraction stage (your page replaces this; keep what is right, go deeper):
A/1/3450 activates on text in Arabic, Farsi, Urdu and probably other languages written in the Arabic script. Arabic script is 0.13% of training tokens but 81% of tokens where the feature is active, and 98% above activation 5. Its logit weights have a second mode consisting of Arabic characters and the byte tokens \xd8 and \xd9 that begin the UTF-8 encodings of basic Arabic characters. Ablating it hurts predictions on Arabic tokens and helps prediction of shared punctuation such as the period. The three largest coefficients of this feature in the neuron basis are all negative, and the most correlated neuron responds to a mixture of non-English languages, so the paper concludes the feature would be effectively invisible in a neuron-level analysis.

THEME IT BELONGS TO: theme-four-features-up-close | Four features up close - Having a decomposition is not the same as having interpretable features, so the paper picks four — Arabic script, DNA, base64, Hebrew — and tries to establish the same five claims about each. The claims are that the feature fires only in its context, fires whenever that context is present, changes the model's output in a matching way, is not just a neuron in disguise, and turns up again in a separately trained model. The specificity argument leans on a hand-built proxy: a cheap score for whether a string looks like the hypothesized context, computed without reference to the model. The paper says outright that these four were chosen for being easy to analyze, not for being typical, which is why the next theme exists.

SUB-CONCEPTS (these render as expandable sections BELOW your page - reference them, do not duplicate them):
  - complementary-arabic-features | Complementary Arabic Features: The other features that fire in Arabic contexts and fill the gaps in A/1/3450's sensitivity.
  - utf8-multi-token-characters | Split Unicode Characters in Tokenization: Non-Latin Unicode characters are often encoded as several byte tokens, which shapes where script features fire and which tokens their logit weights promote.

PREREQUISITES:
  - arabic-script-proxy | Arabic Script Proxy: The per-token score $\log(P(s|\text{Arabic Script}) / P(s))$, built from the Arabic Unicode blocks.
  - feature-ablation | Feature Ablation: Subtracting a feature's contribution from the MLP activations and finishing the forward pass, to see which token predictions depended on it.
  - feature-evidence-template | The Five-Claim Feature Template: The fixed set of five claims the paper tries to establish for each individual feature it studies in depth: specificity, sensitivity, downstream effect, non-correspondence to a neuron, and universality.
  - logit-weight | Logit Weight: The vector, one entry per vocabulary token, giving how much a feature's firing pushes up or down each next-token logit.

EDGES TOUCHING THIS CONCEPT (3) - the relationships the app already knows; weave the load-bearing ones into your prose:
  [load-bearing] -> feature-evidence-template (The Five-Claim Feature Template) | instance-of | the template's first full worked case: The paper does not argue for its features in general terms first. It walks the Arabic feature through all five claims in turn — specificity, sensitivity, downstream effects, not-a-neuron, universality — and the later case studies reuse that order.
  [supporting] -> context-features (Context features) | instance-of | a language as a context: A context feature fires across a whole stretch of text of a certain kind rather than on a single token. The Arabic feature is that pattern with the kind being a writing system.
  [supporting] <- dna-feature (DNA Feature (A/1/2937)) | contrasts-with | one feature for DNA, a family for Arabic: The DNA feature appears to be the only one devoted to its context, and by the end of a long DNA sequence the paper says it is the only feature active. Arabic script takes several: A/1/3450 misses the definite article prefix, and A/1/3134, A/1/1466 and A/1/3399 cover parts it does not. Nothing in the paper predicts in advance which contexts get one feature and which get a family.

EVIDENCE ITEMS THAT SHOW THIS (link them as [[item-id]]):
  - fig-8 | The Arabic Feature's Activation Spectrum: The paper's specificity claim for the Arabic feature is not one number but a curve: the higher the feature fires, the more purely Arabic-script the text is. Barely-active tokens are roughly 25% Arabic, which is noisy, while tokens above activation 5 are about 98% Arabic. Since large activations move the model's logits most, the paper argues it is the clean high end that matters.
  - fig-10 | The Arabic Feature's Logit Weights: Scoring every vocabulary token by how much feature A/1/3450 pushes its next-token logit gives a histogram with two humps: a fat one at zero covering nearly the whole vocabulary, and a small one at the far right made almost entirely of Arabic tokens and of the byte tokens \xd8 and \xd9 that start Arabic UTF-8 characters. That is evidence the feature does not merely fire on Arabic text but pushes the model to produce it.
  - fig-11 | Ablating the Arabic Feature: Deleting this one feature's contribution from the MLP activations lowers the model's predicted probability on Arabic-script tokens, and it does so across the feature's whole activation range rather than only where it fires hardest. The size of the damage tracks the activation: examples at the top of the range show thicker, darker "feature was helping" underlines than examples at about half that activation. The paper reads this as evidence the feature is causally used by the model for Arabic-script prediction, not merely correlated with Arabic text.
  - fig-12 | Pinning the Arabic Script Feature: Holding one learned feature's activation at its maximum, with nothing else in the network touched, turns the model's continuation of a counting prefix into Arabic script. The paper offers this as its most direct causal check that the feature is used by the model to produce Arabic text, rather than merely co-occurring with it in the data.
  - fig-13 | The Arabic Feature in the Neuron Basis: The Arabic script feature's direction, written out one MLP neuron at a time, is spread across many neurons rather than living in one: 27 of the 512 coefficients have magnitude at least 0.1, and the three largest are negative. The paper reads this as evidence that dictionary learning did not simply rediscover an already-clean neuron.
  - fig-14 | Neuron 489's Language Mixture: The neuron whose activations correlate most closely with the Arabic script feature fires on a grab-bag of non-English languages — Japanese, Russian, Spanish, German, French, Portuguese, Ukrainian, Bulgarian, Thai — with Arabic present only as a thin red sliver that never grows with activation strength. Where the feature's top activations are near-purely Arabic, this neuron's top activations are not, which is the paper's evidence that dictionary learning did not simply hand back a conveniently clean neuron.
  - fig-15 | Neuron 489's Logit Weights: The neuron whose activations track the Arabic script feature most closely does not share the feature's downstream effect. Its logit weights form a single mode centred on zero, with the Arabic-script tokens scattered inside that bulk rather than gathered into the clean far-right second mode the feature had. The paper reads those Arabic weights as leaning very slightly positive overall, while noting that some of them are negative.
  - fig-16 | The Arabic Feature Against Its Closest Neuron: Plotted against the single neuron whose behaviour is closest to it, the Arabic script feature separates Arabic contexts and Arabic output tokens along its own axis, while the neuron's axis does not separate them at all. The paper reads this as evidence that dictionary learning did not simply rediscover an already-clean neuron: correlations of 0.09 on activations and 0.21 on logit weights are non-zero but slight.
  - fig-17 | The Arabic Feature Found Again in Model B: A second, independently seeded one-layer transformer, given its own independently seeded dictionary-learning run, produces a feature (B/1/1334) whose per-token activations correlate 0.91 with the Arabic-script feature A/1/3450 from the first model, and whose activation distribution looks like the same Arabic feature drawn again. The paper offers this as its per-feature evidence for universality: the feature appears to be a fact about how these models represent this data, not an accident of one training run.
  - fig-18 | The Same Arabic Feature in Model B: A dictionary trained on a second, separately seeded one-layer transformer contains a feature, B/1/1334, that behaves like the paper's Arabic script feature: its activations are dominated by Arabic-script tokens, and its logit weights show the same shape — one big mode at zero plus a small right-hand mode made almost entirely of Arabic and Arabic-byte tokens. The paper reads the low 0–1 activation band as slightly cleaner here than in the original feature, which is an eyeball comparison rather than a measured one.
  - fig-19 | Arabic Feature Across Two Models: Dictionary learning on a second, independently trained transformer produces a feature whose firing pattern closely tracks the original Arabic script feature (Pearson correlation 0.91), while the two features' effects on output token logits agree much less (0.23). The agreement sits in a small cluster of Arabic tokens that both features push up; the disagreement sits in a large, roughly round cloud of small weights near zero, which the paper guesses is interference rather than anything the model cares about.
  - fig-26 | Base64 Logit Weights and Their Conditional Strip: For the base64 feature A/1/2357, the tokens whose logits it raises shade gradually from ordinary text tokens into base64-specific ones, rather than separating into two clean groups. The narrow strip below the histogram makes this visible by showing, at each logit weight, only the mix of token types and not how many tokens there are.
  - fig-43 | Pinning Features to Steer Generation: Holding a single learned feature at a high value while sampling makes the model emit text of exactly the kind that feature normally fires on — Arabic script, DNA, base64, hexadecimal, Hebrew, Han Chinese, or uppercase. Because the autoencoder was fitted only to MLP activations and never to the model's outputs, the paper reads this as evidence that these features are used by the model's computation, not just artifacts of correlations in the data.
  - fig-44 | Three Dictionaries in One UMAP: The families the authors found by reading text — base64 features, Arabic script features — are also families in geometry: their dictionary vectors point in similar directions. Embedding three dictionaries of different sizes together shows the coarse features from the small run sitting among the finer features that the larger runs split them into.
  - fig-51 | Activation Similarity vs Logit Weight Similarity: The figure sets out the paper's two model-independent ways of asking whether the same feature was found twice, and shows that for the Arabic script feature they disagree: activation correlation between A/1/3450 and B/1/1334 is 0.91, logit weight correlation only 0.23. The gap comes from a large central cloud of small, near-uncorrelated logit weights, which the paper hypothesizes is interference forced by superposition rather than anything either model is trying to compute.
  - fig-52 | Feature vs Neuron Match Histograms: When you look for each item's closest counterpart in a second, independently trained one-layer transformer, autoencoder features match far better than raw MLP neurons: the typical feature's best match has activation correlation 0.72, the typical neuron's only 0.46. The paper reads this as evidence that dictionary learning recovers something reproducible about how these models represent the dataset, rather than an artifact of one particular run.
  - eq-bayes-script-proxy | Inverting the Script Probability: To score how Arabic-like a string is, the paper needs $P(s|\text{Arabic})$, which would otherwise require a generative model of Arabic text. Bayes' rule replaces it with three quantities that are cheap to compute: a character-level script check, a unigram probability, and the corpus rate of Arabic script. The same construction is reused for Hebrew and any other script with its own Unicode blocks.

PAPER SECTIONS TO READ (in papers/monosemanticity/data/ingest/sections/):
  - 14-3.1-arabic-script-feature.txt
  - 15-3.1.1-activation-specificity.txt
  - 16-3.1.2-activation-sensitivity.txt
  - 17-3.1.3-feature-downstream-effects.txt
  - 18-3.1.4-the-feature-is-not-a-neuron.txt
  - 19-3.1.5-universality.txt
