PAGE FOR CONCEPT: loss-recovered-by-features
NAME: Fraction of MLP Loss Recovered

SUMMARY (already shown above your page as the lede - do not repeat it verbatim):
The measure and its headline number: splice the autoencoder's reconstruction back into the transformer in place of the real MLP activations, and 79% of the loss reduction the MLP provides survives for A/1, rising to 94.5% for the much larger A/5.

DRAFT EXPLANATION from the extraction stage (your page replaces this; keep what is right, go deeper):
The measure works by substitution: replace the MLP activations with the autoencoder's reconstruction of them and see how much extra loss that costs, relative to the loss cost of zero-ablating the MLP entirely. For A/1 the extra loss is 21% of the zero-ablation loss, so 79% of the MLP's contribution is recovered. The penalty can be reduced by using more features or a lower L1 coefficient — A/5, with n_learned_sparse of 131,072 and an L1 coefficient of 0.004, recovers 94.5%. The paper says these numbers should be taken with a significant grain of salt, while also arguing that the individual feature analyses show the recovered loss is measuring something real.

THEME IT BELONGS TO: theme-from-four-features-to-all-of-them | From four features to all of them - Four hand-picked features prove nothing about the population, so the paper turns to measurements over many features and the neurons they are meant to replace. A blinded human rater scores features and neurons on the same rubric; a language model is asked to explain a unit and then predict its activations from that explanation alone; and the reconstruction is spliced back into the transformer to ask how much of the MLP's contribution to loss the features account for. The headline figure is 79%, and the paper attaches its own reasons for reading that number cautiously. It also runs the control that matters most to it — dictionary learning on a randomized transformer — to ask whether the features are about the model or about the data.

SUB-CONCEPTS (these render as expandable sections BELOW your page - reference them, do not duplicate them):
  - explanation-based-loss-measure | Explanation-Based Loss Measurement: A proposed better measure — replacing activations with those predicted from natural-language explanations — that the paper judges computationally impractical for now.
  - loss-recovery-caveats | Why 79% Should Be Read Cautiously: The paper's own reasons for distrusting its headline number: fraction-of-loss may be the wrong framing, and the features are not fully monosemantic.
  - reconstructed-nll-overestimate-caveat | Why the Percentage Is an Overestimate: The paper says the ablated-MLP baseline may be an especially bad one, so the fraction explained is likely inflated.
  - zero-ablation-baseline | Zero-Ablation Baseline: The loss increase from substitution is divided by the loss increase from removing the MLP layer entirely, giving a percentage of the MLP's contribution explained.

PREREQUISITES:
  - evaluating-dictionary-learning | Telling Whether the Autoencoder Is Working: The paper has no single metric that tracks what it cares about, and instead triangulates from several imperfect signals.
  - l1-coefficient | L1 Coefficient $\lambda$: The scalar weighting the sparsity penalty against reconstruction error. It is one of the two hyperparameters the paper varies across dictionary learning runs.
  - mlp-activations-as-target | MLP Activations as the Decomposition Target: A one-layer attention-only transformer can be analyzed by rewriting it without reference to hidden states; adding a ReLU MLP makes that impossible, so the MLP activations are the state you cannot avoid decomposing.
  - sparse-autoencoder | Sparse autoencoder: A one-hidden-layer autoencoder with a wide ReLU hidden layer and an L1 penalty on that layer, trained to reconstruct MLP activations. Its hidden units are the learned features and its decoder columns are the feature directions.

EDGES TOUCHING THIS CONCEPT (3) - the relationships the app already knows; weave the load-bearing ones into your prose:
  [load-bearing] -> zero-ablation-baseline (Zero-Ablation Baseline) | depends-on | 79% is a ratio to zero ablation: Splicing the autoencoder's reconstruction in place of the real MLP activations raises the model's loss by some amount. That amount is divided by the loss increase from deleting the MLP layer entirely, and it comes to 21%, so 79% is called recovered. The paper says the ablated-MLP denominator may be an especially bad baseline, which would inflate the percentage.
  [load-bearing] <- l0-norm ($L^0$ Norm) | contrasts-with | sparsity paid for with explained loss: Every trained autoencoder sits somewhere on a curve between these two numbers. A/1, the run the paper focuses on, is sparse and recovers 79% of the MLP layer's loss contribution; A/5, with 131,072 features and a lower L1 coefficient, recovers 94.5% but fires many more features per token. The paper picks its L1 coefficient by looking at this frontier rather than from any principle about how sparse a decomposition should be.
  [supporting] <- explanation-based-loss-measure (Explanation-Based Loss Measurement) | contrasts-with | the better measure is priced out: Replacing MLP activations with activations simulated from each feature's written explanation would measure what the paper actually wants to know: how much the interpretations explain, not how much the reconstruction does. The naive version needs one sample from the explaining model per feature per token. The paper's own estimate is $12,500–$25,000 to simulate 100k features over a single 4096-token context, so it reports the 79% reconstruction figure instead.

EVIDENCE ITEMS THAT SHOW THIS (link them as [[item-id]]):
  - fig-5 | The Feature Search Bar: A screenshot of the search strip in the paper's public feature browser, with its four dropdowns opened. It carries no results, so what it establishes is only the shape of the query the authors built for themselves: pick a matching mode, pick which part of a feature's record the query is matched against, pick a sort key over all features in a run, and pick which per-token loss difference is drawn as underlines.
  - fig-68 | Sparsity Against Explained Behavior: Plotting how sparse each trained dictionary is against how much of the MLP layer's loss contribution it recovers turns hyperparameter choice into a visible tradeoff: a weaker sparsity penalty explains more of the model's behavior, but only by letting more features fire on every token. The four L1 coefficients sit as four separate vertical bands, so the penalty largely fixes the sparsity level, while widening the dictionary climbs the band by raising explained behavior at nearly constant sparsity.

PAPER SECTIONS TO READ (in papers/monosemanticity/data/ingest/sections/):
  - 81-19-advice-for-training-sparse-autoencoders-hyperparameter-selec.txt
  - 38-4.2-how-much-of-the-model-does-our-interpretation-explain.txt
