# id | kind | tier | name | summary
arabic-script-feature | concept | major | Arabic Script Feature (A/1/3450) | The paper's lead case study: a learned feature that fires on text in Arabic script, is specific and reasonably sensitive to it, and has no neuron coun
automated-interpretability | concept | major | Automated Interpretability | Having a language model write an explanation of a feature from examples where it activates, then use that explanation alone to predict the feature's a
computational-proxy | concept | major | Computational Proxy | A cheaply computable numerical score estimating how likely a string is to come from a hypothesized context, used as an independent yardstick against w
dictionary-learning | concept | major | Dictionary learning | The problem of writing each activation vector as a sparse combination of directions drawn from a fixed, larger-than-necessary set. The paper frames th
evaluating-dictionary-learning | concept | major | Telling Whether the Autoencoder Is Working | The paper has no single metric that tracks what it cares about, and instead triangulates from several imperfect signals.
feature | concept | major | Feature | A basic interpretable direction in activation space — the unit the paper wants to decompose models into, in place of neurons.
feature-evidence-template | concept | major | The Five-Claim Feature Template | The fixed set of five claims the paper tries to establish for each individual feature it studies in depth: specificity, sensitivity, downstream effect
feature-motifs | concept | major | Feature motifs | The recurring kinds of features the paper finds: context features, token-in-context features, trigram features, sequence-memorizing features, and feat
feature-splitting | concept | major | Feature splitting | The observation that as the autoencoder is made wider, single features divide into families of more specific features covering roughly the same territ
loss-recovered-by-features | concept | major | Fraction of MLP Loss Recovered | The measure and its headline number: splice the autoencoder's reconstruction back into the transformer in place of the real MLP activations, and 79% o
manual-interpretability-scoring | concept | major | Manual Human Analysis | A blinded human annotator scoring 412 activation intervals across 162 features and neurons on an interpretability rubric; the paper's most-trusted met
model-vs-data-question | concept | major | Do Features Tell Us About The Model Or The Data? | The worry that dictionary learning on activations might be recovering correlations in the dataset rather than anything the model computes, and the pap
one-layer-transformer-testbed | concept | major | The One-Layer Transformer Testbed | The model under study: a one-layer transformer with a 128-dimensional residual stream and a 512-neuron ReLU MLP, overtrained on 100 billion Pile token
pearson-correlation | concept | minor floor | Pearson Correlation | The standard linear correlation coefficient between two variables, ranging from -1 to 1.
polysemanticity | concept | major | Polysemanticity | A single neuron responding to a mixture of seemingly unrelated inputs, which makes it a poor unit for human understanding of the network.
reading-the-evidence | concept | major | Reading this paper's charts | The handful of chart shapes this paper argues with, each explained once: why that shape was chosen, how to read it, and what it would look like if the
spearman-correlation | concept | minor floor | Spearman Rank Correlation | Correlation computed on the ranks of values rather than the values themselves.
superposition | concept | major | Superposition | A hypothesized phenomenon in which a network represents more independent features than it has neurons, by giving each feature its own linear combinati
universality | concept | major | Universality | The question of whether the same features form across different models, and the paper's finding that they largely do.
activation-interval-analysis | concept | minor | Activation Interval Analysis (part of activation-interval) | Scoring each band of a feature's activation spectrum separately against the feature's overall hypothesis, to see how interpretability varies with acti
feature-at-slight-angle | concept | minor | Features At A Slight Angle (part of activation-interval-analysis) | The paper's suggested explanation for the drop-off in low-interval interpretability: a learned direction slightly off from the true feature direction.
feature-proxy-correlation | concept | minor | Feature-Proxy Correlation (part of activation-sensitivity) | The Pearson correlation between a feature's activations and its computational proxy, used as a single number combining sensitivity and specificity.
feature-vs-neuron-universality | concept | minor | Features are more universal than neurons (part of activation-similarity) | Matching features across the two models gives a median activation correlation of 0.72; matching neurons across the same two transformers gives 0.46.
expected-value-plot | concept | minor | Expected Value Plot (part of activation-specificity) | An activation histogram weighted by activation magnitude, so that the picture reflects how much effect a feature actually has rather than how many tok
feature-activation-distribution-plot | concept | minor | Feature Activation Distribution Broken Down by Proxy (part of activation-specificity) | A histogram of a feature's activations with each bar colored by whether the proxy fires, used as the paper's basic specificity picture.
large-activations-dominate | concept | minor | Large Activations Matter Most (part of activation-specificity) | The argument that getting the interpretation of strong activations right matters more than getting weak ones right, because strong activations move th
weak-activation-hypotheses | concept | minor | Three Hypotheses for Weak Activations (part of activation-specificity) | The paper's three candidate explanations for why a feature's low activations look less pure than its high ones: the proxy is imperfect, the model is i
noise-injection-for-interpretability | concept | minor | Noise injection (part of algorithmic-improvements-for-sparse-autoencoders) | Adding noise during training as a separate route to sparse, interpretable representations.
complementary-arabic-features | concept | minor | Complementary Arabic Features (part of arabic-script-feature) | The other features that fire in Arabic contexts and fill the gaps in A/1/3450's sensitivity.
utf8-multi-token-characters | concept | minor | Split Unicode Characters in Tokenization (part of arabic-script-feature) | Non-Latin Unicode characters are often encoded as several byte tokens, which shapes where script features fire and which tokens their logit weights pr
activation-sparsity-training | concept | minor | Inducing activation sparsity during training (part of architectural-approaches-to-superposition) | Training the subject model itself to have few active neurons, in the hope that each neuron would then carry one feature.
engineering-monosemanticity | concept | minor | Engineering monosemanticity in toy models (part of architectural-approaches-to-superposition) | Prior work finding multiple equal-loss minima in MLPs trained on a compressed sensing task, some polysemantic and some monosemantic.
mse-loss-and-monosemanticity | concept | minor | Why MSE models differ from language models (part of architectural-approaches-to-superposition) | Under mean squared error, polysemantic and monosemantic representations can reach the same loss, and for some feature importance curves the monosemant
polysemanticity-without-superposition | concept | minor | Polysemanticity without superposition (part of architectural-approaches-to-superposition) | The finding that individual neurons can be polysemantic even when superposition has been ruled out, because ambiguous representations can achieve lowe
solu | concept | minor | SoLU activation (part of architectural-approaches-to-superposition) | An activation function proposed in earlier work to make more transformer neurons interpretable by encouraging features to align with the neuron basis.
superposition-all-the-way-down | concept | minor | Superposition all the way down (part of architectural-approaches-to-superposition) | The worry that the learned dictionary might itself use superposition, which the paper argues its MSE-plus-sparsity objective avoids.
sharper-predictions-argument | concept | minor | The sharper-predictions argument (part of are-token-in-context-features-real) | The reason to think local codes are real: a compositional code forces the logits for 'the in Physics' to be a sum of two generic contributions, while 
attribution-vector | concept | minor | Attribution vector (part of attribution-similarity) | For each sampled datapoint, the product of a feature's activation there and its logit weight on the token that actually came next.
activation-shuffling | concept | minor | Cross-Context Shuffling (part of autoencoder-training-dataset) | Activations are subsampled at 250 tokens per context and shuffled so that a training batch draws from many different contexts.
sampling-without-replacement | concept | minor | Sampling Without Replacement (part of autoencoder-training-dataset) | Each activation vector is seen at most once during autoencoder training. The paper says not repeating data is important for optimal results.
activation-prediction-scoring | concept | minor | Activation Prediction and Spearman Scoring (part of automated-interpretability) | Claude predicts activations for 60 nine-token examples from the explanation alone; the score is the Spearman correlation between predicted and true ac
activation-quantization | concept | minor | Quantizing Activations to 0–9 (part of automated-interpretability) | Activations are rounded to integers 0 through 9 before being shown to or predicted by the language model.
automated-logit-scoring | concept | minor | Automated Interpretability on Logit Weights (part of automated-interpretability) | Asking Claude, from the feature's explanation alone, whether an unseen high-logit token is one the feature should predict; features score 74% versus 5
constant-prediction-zero-score | concept | minor | Constant Predictions Score Zero (part of automated-interpretability) | When Claude predicts the same value for everything — usually all zeros — no correlation exists, and the paper assigns a score of zero.
explanation-generation-setup | concept | minor | Explanation Generation Setup (part of automated-interpretability) | The exact prompt composition used to get a feature explanation out of Claude 2: 49 examples of specified kinds, plus logits, interval labels, and eigh
explanation-length-caveat | concept | minor | Explanation Lengths Caveat (part of automated-interpretability) | Claude sometimes fails to wrap its explanation in answer tags, so the full chain of thought is used instead — a null result, since it does not change 
importance-scoring | concept | minor | Importance Scoring (part of automated-interpretability) | A reweighting that corrects for the non-random sampling of scored examples — and which, in this setting, mostly collapses feature scores toward zero.
no-activation-leakage | concept | minor | No Leakage of Activation Information (part of automated-interpretability) | The design constraint that nothing about a feature's activation pattern reaches the prediction step except through the written explanation.
base64-digit-feature | concept | minor | Base64 Digit Feature and the BPE Exploit (part of base64-feature) | A companion feature that fires on single digits inside base64 contexts, exploiting a property of the BPE tokenizer to make a better prediction.
hexadecimal-feature | concept | minor | Hexadecimal Feature (A/1/3817) (part of base64-feature) | A separate feature for hexadecimal strings, which the base64 proxy wrongly counts as base64.
solu-base64-neuron | concept | minor | The Earlier Base64 Neuron (part of base64-feature) | A base64 neuron previously found in the SoLU paper, which the authors take as a hint that base64 might be universal across architectures and datasets.
tokenization-artifacts-in-features | concept | minor | Tokenization artifacts in features (part of base64-feature-family) | Some feature behavior reflects the tokenizer rather than the content — the digit base64 feature suppresses digits because consecutive digits would hav
np-hardness-of-exact-sparse-recovery | concept | minor | NP-Hardness of Exact Recovery (part of compressed-sensing) | Compressed sensing in its exact form is NP-hard, which the paper turns into an argument about what method to use.
arabic-script-proxy | concept | minor | Arabic Script Proxy (part of computational-proxy) | The per-token score $\log(P(s|\text{Arabic Script}) / P(s))$, built from the Arabic Unicode blocks.
base64-proxy | concept | minor | Base64 Proxy (part of computational-proxy) | A score modeling base64 strings as random sequences over [a-zA-Z0-9+/]; the paper judges it too broad.
character-distribution-proxy | concept | minor | Character-Distribution Proxies (part of computational-proxy) | For features about character sets like base64 or DNA, the conditional probability is modeled as a uniform draw over the allowed characters.
dna-proxy | concept | minor | DNA Proxy (part of computational-proxy) | A score for "is a DNA sequence", modeling DNA as random strings over [ATCG] and evaluated on groups of at least two tokens.
hebrew-script-proxy | concept | minor | Hebrew Script Proxy (part of computational-proxy) | A Unicode-block-based score for Hebrew text, correlating 0.55 with A/1/416.
log-likelihood-ratio-proxy | concept | minor | Log-Likelihood Ratio Form (part of computational-proxy) | Proxies take the form $\log(P(s|\text{hypothesis})/P(s))$ — how much more likely the string is under the feature's hypothesis than under the general d
prefix-maximization | concept | minor | Maximizing Over Prefixes (part of computational-proxy) | The proxy is evaluated over multiple prefixes ending at the token in question, and the maximum is taken.
proxy-binarization | concept | minor | Proxy Binarization (part of computational-proxy) | Reducing a proxy's continuous log-probability to a yes/no value based on its sign, done when the proxy's distribution is strongly bimodal.
proxy-limitations | concept | minor | Where the Proxies Fail (part of computational-proxy) | The paper's catalog of cases where feature and proxy disagree because the proxy is wrong, not the feature.
script-proxy-via-bayes | concept | minor | Script Proxies via Bayes' Rule (part of computational-proxy) | For features about writing systems, $P(s|\text{script})$ is obtained by inverting an easy-to-compute $P(\text{script}|s)$.
unigram-p-s-estimate | concept | minor floor | Estimating $P(s)$ with a Unigram Model (part of computational-proxy) | The denominator of every proxy is computed as the product of independent per-token unigram probabilities.
de-facto-dead-neurons | concept | minor | De Facto Dead Neurons (part of dead-neurons) | Ultralow density neurons that fire so rarely they are effectively dead, but do not register as dead by a zero-activity check.
unit-norm-dictionary-columns | concept | minor | Unit-Norm Dictionary Columns (part of dictionary-decoder) | The columns of $W_d$ are constrained to have L2 norm 1, so that the L1 penalty cannot be gamed by shrinking directions.
activations-only-decomposition | concept | minor | Fitting Activations Only (part of dictionary-learning) | The decomposition is fit to the MLP activations alone, ignoring what those activations do to the model's output — which leaves downstream effects avai
classical-dictionary-learning-algorithms | concept | minor | Classical Dictionary Learning Algorithms (part of dictionary-learning) | Established iterative methods such as Method of Optimal Directions and K-SVD. The paper tried several and set them aside.
compressed-sensing | concept | minor | Compressed Sensing (part of dictionary-learning) | The problem of recovering a sparse high-dimensional vector from a low-dimensional linear measurement of it. The paper names this as the hard inner pro
cunningham-et-al-parallel-work | concept | minor | Parallel sparse autoencoder investigation (part of dictionary-learning) | An independent line of interim reports and a manuscript investigating the same sparse autoencoder approach, prompted by Toy Models.
mean-max-cosine-similarity | concept | minor | Mean max cosine similarity (MMCS) (part of dictionary-learning) | A measure of how well features from two separately trained dictionaries match up, reported elsewhere as bimodal; the paper suspects density bimodality
overcompleteness | concept | minor | Overcompleteness (part of dictionary-learning) | Having more dictionary directions than the dimension of the space they live in. The paper insists this is what separates its setting from ordinary dis
prior-dictionary-learning-on-embeddings | concept | minor | Earlier dictionary learning on embeddings (part of dictionary-learning) | A body of pre-transformer work applying sparse coding, sparse autoencoders, dictionary learning and topic-model methods to word embeddings.
sparse-autoencoder | concept | major | Sparse autoencoder (part of dictionary-learning) | A one-hidden-layer autoencoder with a wide ReLU hidden layer and an L1 penalty on that layer, trained to reconstruct MLP activations. Its hidden units
sparsity-of-natural-latent-variables | concept | minor | Sparsity of Natural Latent Variables (part of dictionary-learning) | The long-standing hypothesis that the underlying variables generating natural data are mostly inactive at any moment. The paper uses it to justify ask
weak-dictionary-learning | concept | minor | A deliberately weak dictionary learning algorithm (part of dictionary-learning) | The worry that a sufficiently sophisticated decomposition method could recover structure from the activations that the model itself has no way to read
disentanglement-vs-superposition | concept | minor | Why disentangling can make things worse (part of disentanglement) | The paper's claim that pushing a model to disentangle its dimensions typically hurts performance, and that models respond by entangling some features 
factors-of-variation | concept | minor | Factors of variation (part of disentanglement) | The conceptually distinct influences on the data that a disentangled representation is supposed to separate into independent dimensions.
topk-activation | concept | minor | TopK activation (part of disentanglement) | Keeping only the k largest hidden activations and zeroing the rest, as a hard way to enforce sparsity.
feature-density | concept | major | Feature Density (part of evaluating-dictionary-learning) | The fraction of tokens on which a feature has a nonzero value; the paper's main quick proxy for autoencoder quality.
feature-exploration-interface | concept | minor | Feature Exploration Interface (part of evaluating-dictionary-learning) | A published visualization covering every feature in every dictionary learning run, showing when a feature fires, what it does to the logits, and what 
information-based-metric | concept | minor | The Information-Based Metric That Failed (part of evaluating-dictionary-learning) | An attempt to score factorizations by the total information of autoencoder plus data. It did not correlate with what the paper cared about.
l0-nll-tradeoff | concept | minor | Sparsity vs. Explained Behavior Tradeoff (part of evaluating-dictionary-learning) | Plotting $L^0$ norm against reconstructed transformer NLL across runs gives a frontier the paper uses to choose the L1 coefficient.
l0-norm | concept | minor | $L^0$ Norm (part of evaluating-dictionary-learning) | The average number of features active on a token. This is the paper's direct measure of how sparse the decomposition actually is.
manual-inspection | concept | minor | Manual Inspection (part of evaluating-dictionary-learning) | Looking at features and asking whether they seem interpretable. Slow and subjective, but the paper treats it as the ground truth the proxies stand in 
toy-model-ground-truth | concept | minor | Toy Models as Ground Truth (part of evaluating-dictionary-learning) | Small synthetic setups where the true features are known by construction, used to check that the autoencoder recovers what it should.
training-loss-metric | concept | minor | Training Loss as a Proxy (part of evaluating-dictionary-learning) | The simplest signal, useful for comparing optimizers and learning rates but meaningless across runs with different L1 coefficients.
features-invisible-in-the-neuron-basis | concept | minor | Features invisible in the neuron basis (part of feature) | Learned features with no counterpart among the neurons — not showing up in the top dataset examples of any neuron.
good-decomposition-desiderata | concept | minor | What makes a good decomposition (part of feature) | Three criteria a feature decomposition must meet to be useful: interpretable activation conditions, interpretable and consistent downstream effects, a
linear-feature-decomposition | concept | minor | Features as a linear decomposition (part of feature) | Writing each activation vector as a bias plus a sparse weighted sum of unit-vector feature directions — an ordinary linear matrix factorization of the
linear-representation-hypothesis | concept | minor | Linear representation hypothesis (part of feature) | The claim that networks represent meaningful properties as directions in activation space, so that interpretable concepts are linear functions of acti
privileged-basis | concept | minor | Privileged basis (part of feature) | A basis that the architecture singles out — here the neuron basis, made special by the elementwise activation function — so that features have some te
ablation-effect-visualization | concept | minor | Reading the Ablation Display (part of feature-ablation) | The display convention: orange background marks where a feature is active, and colored underlines mark tokens whose prediction the feature helped or h
directional-ablation | concept | minor | Directional Rather Than Wholesale Removal (part of feature-ablation) | Only the component along the feature's dictionary vector is removed, leaving every other feature's contribution to that activation intact.
density-cluster-separation | concept | minor | Separating the Density Clusters (part of feature-density) | When density alone does not separate the two clusters, adding the feature's bias and its encoder-decoder dot product does.
feature-density-diagnostics | concept | minor | Feature Density Diagnostics (part of feature-density) | Three specific numbers read off the feature density histograms and used to judge a run: live features outside the ultralow cluster, where the non-ultr
high-density-cluster | concept | minor | High Density Cluster (part of feature-density) | The right mode of the density histogram, whose features are anecdotally almost all interpretable despite still being very rare in absolute terms.
ultralow-density-cluster | concept | minor | Ultralow Density Cluster (part of feature-density) | A left mode of features firing on fewer than about one in a million tokens, almost none of which are interpretable; the paper treats them as a trainin
downstream-effects-outside-dictionary-learning | concept | minor | Downstream Effects Are Not An Input To Dictionary Learning (part of feature-downstream-effects) | The structural argument that because the autoencoder only sees MLP activations, interpretable downstream effects cannot be an artifact of the training
feature-ablation | concept | minor | Feature Ablation (part of feature-downstream-effects) | Subtracting a feature's contribution from the MLP activations and finishing the forward pass, to see which token predictions depended on it.
logit-weight | concept | minor | Logit Weight (part of feature-downstream-effects) | The vector, one entry per vocabulary token, giving how much a feature's firing pushes up or down each next-token logit.
pinned-feature-sampling | concept | minor | Pinned Feature Sampling (part of feature-downstream-effects) | Fixing a feature's activation at a high value and then sampling from the model, to see whether the generated text matches the feature's interpretation
activation-sensitivity | concept | minor | Activation Sensitivity (part of feature-evidence-template) | Whether the feature is on whenever the hypothesized context is present. The paper finds sensitivity is imperfect and often patched by other features.
activation-specificity | concept | minor | Activation Specificity (part of feature-evidence-template) | Whether a feature being on implies the hypothesized context is present. The paper treats this as the more important half of the case against polyseman
base64-feature | concept | major | Base64 Feature (A/1/2357) (part of feature-evidence-template) | A feature firing on base64 strings, of interest because a base64 neuron had already been found in a different model with a different architecture.
cherry-picked-case-studies | concept | minor | Cherry-Picked Case Studies (part of feature-evidence-template) | The paper's own statement that the four detailed features were chosen because they are easy to analyze, not because they are typical.
dna-feature | concept | major | DNA Feature (A/1/2937) (part of feature-evidence-template) | A feature firing on long uppercase strings of A, T, C and G. Unlike the Arabic case it appears to be the only feature devoted to this context.
feature-downstream-effects | concept | major | Feature Downstream Effects (part of feature-evidence-template) | Evidence that a feature causally changes the model's predictions in a way that matches its interpretation, measured three ways: logit weights, ablatio
feature-neuron-nonalignment | concept | minor | The Feature Is Not A Neuron (part of feature-evidence-template) | The check that dictionary learning did not simply hand back an already-monosemantic neuron under a new name.
feature-universality | concept | minor | Feature Universality (part of feature-evidence-template) | Whether the same feature is found again by dictionary learning applied to a separately trained model, taken as evidence the feature is a general fact 
hebrew-feature | concept | major | Hebrew Feature (A/1/416) (part of feature-evidence-template) | A feature firing on Hebrew script, with high specificity in the upper activation spectrum and weak activations on other Unicode-script languages.
activation-visualization-sampling | concept | minor | Activation Visualization Sampling (part of feature-exploration-interface) | The dataset examples shown in the interface come from a 100-million-token subset with only 10 tokens taken per context, displayed with four tokens of 
data-oriented-interface | concept | minor | Data-Oriented Interface (part of feature-exploration-interface) | A second view that inverts the first: pick a text, see every feature active on it.
rubric-effect-size-separation | concept | minor | Effect-Size Separation Item (part of feature-interpretability-rubric) | A one-point item awarded when inconsistent positive logit effects are at least clearly smaller than the consistent ones.
rubric-interpretation-first | concept | minor | Interpretation Before Scoring (part of feature-interpretability-rubric) | The rater commits to an interpretation first, then scores the evidence against it — rather than scoring interpretability in the abstract.
action-features | concept | minor | Features as actions (part of feature-motifs) | The dual reading of every feature in a one-layer model: it both responds to something and acts to raise the probability of something.
context-features | concept | minor | Context features (part of feature-motifs) | Features that fire across a whole region of text of a certain kind — DNA sequences, base64 strings, a particular language — rather than on one token t
finite-state-automata-features | concept | minor | Finite state automata (part of feature-motifs) | Assemblies of features where one feature raises the probability of tokens that in turn cause another feature to fire on the next step, forming a state
memorization-features | concept | minor | Sequence-memorizing features (part of feature-motifs) | Features that respond to a specific long phrase and appear to reproduce it, visible only in large dictionaries.
phenomenology | concept | minor | Phenomenology (part of feature-motifs) | The paper's name for using the feature decomposition to reason about properties of the model, by analogy to phenomenology in physics.
token-in-context-features | concept | minor | Token-in-context features (part of feature-motifs) | Features that fire on one specific token but only in a particular kind of context — 'the' in mathematics, '<' in HTML.
trigram-features | concept | minor | Trigram features (part of feature-motifs) | Features that implement short fixed continuations, such as predicting '19' after 'COVID-'.
feature-neuron-scatter-plots | concept | minor | Feature-Neuron Scatter Plots and Marginals (part of feature-neuron-nonalignment) | Plotting a feature's activations or logit weights against a neuron's, and reading the two marginal distributions to see which axis separates the conte
most-correlated-neuron | concept | minor | Most Correlated Neuron (part of feature-neuron-nonalignment) | The neuron whose activations correlate most with a feature's, over roughly 40 million dataset examples — the paper's preferred way to look for a neuro
mutually-exclusive-features-in-superposition | concept | minor | Mutually Exclusive Features Are Natural To Superpose (part of feature-neuron-nonalignment) | The paper's explanation for why the neuron most correlated with the Arabic feature responds to a mixture of different languages.
neuron-basis-coefficients | concept | minor | Feature Coefficients in the Neuron Basis (part of feature-neuron-nonalignment) | Reading off how much each neuron contributes to a feature's direction, to see whether the feature is essentially one neuron.
top-dataset-example-search | concept | minor | Searching Neurons by Top Dataset Examples (part of feature-neuron-nonalignment) | Scanning every neuron's top-activating examples for the target context, as a cross-check that no neuron encodes it.
dictionary-learning-run | concept | minor | Dictionary Learning Run (part of feature-notation) | One trained autoencoder, identified by its number in the feature notation. Runs vary in dictionary size and L1 coefficient.
neuron-notation | concept | minor | Neuron Notation (part of feature-notation) | "A/neurons/32" names a transformer MLP neuron rather than a learned feature, so the two can be compared without ambiguity.
dictionary-resolution | concept | minor | Dictionaries as resolutions (part of feature-splitting) | The idea that different autoencoder sizes are different zoom levels on the same underlying structure, with small dictionaries giving useful summaries.
feature-geometry-clusters | concept | minor | Geometric clustering of feature directions (part of feature-splitting) | Conceptually similar features have small angles between their dictionary vectors, so qualitative families show up as tight clusters in a 2-D projectio
feature-umap | concept | minor | Feature UMAPs (part of feature-splitting) | A 2-D projection of feature directions, used to make the geometry of a dictionary browsable and to show that qualitative families are geometric cluste
local-vs-compositional-codes | concept | minor | Local versus compositional codes (part of feature-splitting) | Two ways to represent a combination of properties: one unit per combination (local), or separate units that combine (compositional).
masked-cosine-similarity | concept | minor | Masked cosine similarity (part of feature-splitting) | A measure the paper defines for linking a feature in one dictionary to its relatives in another, computed only over tokens where at least one of the t
multiple-features-for-one-context | concept | minor | Multiple features for one context (part of feature-splitting) | The converse apparent bug: several features covering what looks like the same concept, which turn out to make finer distinctions when examined.
splitting-is-not-a-tree | concept | minor | Splitting is not a tree (part of feature-splitting) | Features at one dictionary size can both split and merge to form the features at the next, so the refinement structure is a general graph rather than 
true-features | concept | minor | True features (part of feature-splitting) | The features a model actually uses, as opposed to the ones a given dictionary happens to learn. The paper argues a small model has fewer of them.
hdbscan-feature-clustering | concept | minor | HDBSCAN Clustering of Features (part of feature-umap) | Clusters shown on the first UMAP come from running HDBSCAN on a separate 10-dimensional UMAP embedding of the same features.
one-dimensional-umap-ordering | concept | minor | One-Dimensional Ordering of Features (part of feature-umap) | The feature list is sorted by a one-dimensional UMAP fit to the unclustered features together with the cluster medoids.
umap | concept | minor | UMAP (part of feature-umap) | A dimensionality reduction method that places high-dimensional points in two dimensions while trying to preserve local neighborhood structure.
html-feature-automaton | concept | minor | The four-node HTML system (part of finite-state-automata-features) | Four A/0 features that hand off to each other to generate valid HTML: open tag, tag name, tag close, whitespace, and back to open tag.
mechanistic-anomaly-detection | concept | minor | Mechanistic anomaly detection (part of finite-state-automata-features) | The idea of noticing when a model's internal mechanism behaves differently in a specific narrow case.
self-exciting-feature-loop | concept | minor | Single-node loop (part of finite-state-automata-features) | The simplest case: a feature that increases the probability of tokens which would themselves activate it again.
unicode-token-state-machines | concept | minor | Two-node systems from split Unicode characters (part of finite-state-automata-features) | Where a script's characters are tokenized as a prefix byte plus a suffix byte, the model alternates between a prefix feature and a suffix feature.
explaining-the-mlp-layer | concept | minor | Explaining a large fraction of the layer (part of good-decomposition-desiderata) | The third criterion: the features must account for a significant portion of the MLP layer's functionality, measured by loss rather than by reconstruct
interpretable-activation-conditions | concept | minor | Interpretable activation conditions (part of good-decomposition-desiderata) | The first criterion: you can describe which datapoints make a feature fire, and that description holds up on diverse data including off-distribution o
interpretable-downstream-effects | concept | minor | Interpretable downstream effects (part of good-decomposition-desiderata) | The second criterion: you can interpret what changing a feature's value does to subsequent layers, and that effect is consistent with the interpretati
recognizing-good-features | concept | minor | How can we recognize good features? (part of good-decomposition-desiderata) | The authors' admission that they have no systematic way to tell whether a dictionary learning run has extracted high-quality features.
uses-of-a-feature-decomposition | concept | minor | What a good decomposition would let you do (part of good-decomposition-desiderata) | The concrete affordances the paper claims follow from meeting the three criteria: attribution, monitoring, steering, and input design.
hebrew-continuation-feature | concept | minor | Hebrew Continuation Feature (A/1/1016) (part of hebrew-feature) | A feature that fires on the \xd7 byte and predicts the bytes completing a Hebrew character's codepoint.
importance-scoring-degeneracy | concept | minor | Why Importance Scoring Collapses Feature Scores (part of importance-scoring) | Because feature densities are around 1e-3 to 1e-6, nearly all the weight lands on random examples where the feature never fires, so predicting zero ev
interpretability-importance-caveat | concept | minor | Caveat: Uniform Sampling Ignores Importance (part of interpretability-evaluation-caveats) | Features were sampled uniformly from all features, but interpretability might correlate with how important a feature is.
low-interval-activation-skew | concept | minor | Caveat: Activations Are Skewed Toward Low Intervals (part of interpretability-evaluation-caveats) | Most actual feature activations are small, so they fall in exactly the intervals that score worst.
causality-generality-purity | concept | minor | Causality, generality, purity (part of interpretable-activation-conditions) | An earlier trio of desiderata for claiming a component detects something, cited as a worked example of how to make interpretable activation conditions
l1-coefficient | concept | minor | L1 Coefficient $\lambda$ (part of l1-sparsity-penalty) | The scalar weighting the sparsity penalty against reconstruction error. It is one of the two hyperparameters the paper varies across dictionary learni
feature-activation | concept | minor | Feature activation (part of linear-feature-decomposition) | The scalar $f_i(\mathbf{x})$ giving how strongly feature $i$ fires on a datapoint; in the autoencoder it is the ReLU hidden-layer value.
feature-direction | concept | minor | Feature direction (part of linear-feature-decomposition) | The unit vector $\mathbf{d}_i$ in activation space along which feature $i$ writes when it fires; in the autoencoder these are the columns of the decod
othello-linear-representation-debate | concept | minor | The linear-representation exchange (part of linear-representation-hypothesis) | An exchange in which one paper found an apparent counterexample to features-as-directions and a second resolved it by finding an alternative, linear i
word-vector-arithmetic | concept | minor | Vector arithmetic in word embeddings (part of linear-representation-hypothesis) | The classic observation that relationships between words correspond to consistent offsets between their embedding vectors.
are-token-in-context-features-real | concept | minor | Are token-in-context features real? (part of local-vs-compositional-codes) | Whether the hundreds of 'the'-in-a-context features reflect the transformer's own local code, or an artifact of dictionary learning imposing one.
compositional-code | concept | minor | Compositional code (part of local-vs-compositional-codes) | A representation where independent units combine to represent a conjunction — 'the' and 'Physics' active together.
local-code | concept | minor | Local code (part of local-vs-compositional-codes) | A representation with one dedicated unit per thing represented, including per combination of properties.
anomalous-token-exclusion | concept | minor | Excluding Anomalous Tokens (part of logit-weight) | Logit weights for extremely rare or never-used vocabulary elements are dropped from the plots.
direct-path | concept | minor | Direct path (part of logit-weight) | The route from token embedding straight to the unembedding, bypassing attention and the MLP.
layer-norm-linearization | concept | minor | Linearizing Layer Norm (part of logit-weight) | Approximating layer norm as a fixed projection removing the mean plus a diagonal scaling matrix, so a feature's path to the logits stays linear.
logit-weight-caveats | concept | minor | Ways Logit Weights Could Mislead (part of logit-weight) | Three stated ways the linear logit weight could overstate a feature's real influence on the model.
logit-weight-median-centering | concept | minor | Median-Centering the Logit Weights (part of logit-weight) | Because softmax is shift-invariant, logit weights have no absolute scale; the paper shifts each feature's weights so its median is zero.
logit-weight-second-mode | concept | minor | The Second Mode in Logit Weights (part of logit-weight) | The recurring finding that a feature's logit weight histogram has a large mode at zero plus a small, far-right mode of tokens specific to the feature'
mlp-output-weights | concept | minor floor | MLP output weights (part of logit-weight) | The matrix $W_{down}$ that maps MLP activations back into the residual stream.
path-expansion-logit-attribution | concept | minor | Path Expansion / Logit Attribution (part of logit-weight) | The technique of tracing a single linear path from an internal direction all the way to the logits, taken from the transformer circuits framework.
unembedding-matrix | concept | minor floor | Unembedding matrix (part of logit-weight) | The final linear map from the residual stream to one logit per vocabulary token.
weight-interference | concept | minor | Weight Interference (part of logit-weight) | The paper's hypothesis that the large central mode of near-zero logit weights is unwanted noise forced by superposition, not deliberate behavior.
logit-weight-disagreement | concept | minor | When logit weights disagree but features match (part of logit-weight-similarity) | The extreme case: two features with activation correlation 0.98 and a negative logit weight correlation that are nonetheless doing the same job.
explanation-based-loss-measure | concept | minor | Explanation-Based Loss Measurement (part of loss-recovered-by-features) | A proposed better measure — replacing activations with those predicted from natural-language explanations — that the paper judges computationally impr
loss-recovery-caveats | concept | minor | Why 79% Should Be Read Cautiously (part of loss-recovered-by-features) | The paper's own reasons for distrusting its headline number: fraction-of-loss may be the wrong framing, and the features are not fully monosemantic.
reconstructed-nll-overestimate-caveat | concept | minor | Why the Percentage Is an Overestimate (part of loss-recovered-by-features) | The paper says the ablated-MLP baseline may be an especially bad one, so the fraction explained is likely inflated.
zero-ablation-baseline | concept | minor | Zero-Ablation Baseline (part of loss-recovered-by-features) | The loss increase from substitution is divided by the loss increase from removing the MLP layer entirely, giving a percentage of the MLP's contributio
activation-interval | concept | minor | Activation Interval (part of manual-interpretability-scoring) | One of eleven evenly spaced bands between zero and a feature's maximum activation, used to sample examples uniformly across the whole activation spect
blinded-annotation | concept | minor | Blinded Annotation (part of manual-interpretability-scoring) | The annotator scored features and neurons without knowing which kind of unit was being shown.
feature-interpretability-rubric | concept | minor | Feature Interpretability Rubric (part of manual-interpretability-scoring) | The 0–14 scoring scheme combining confidence in an interpretation, consistency of activations, consistency of logit weights, effect-size separation, a
global-analysis-framing | concept | minor | From Cherry-Picked Features To Global Claims (part of manual-interpretability-scoring) | The turn in section 4 from four hand-chosen features to the population of all features, and the questions the paper says it cannot yet answer.
interpretability-evaluation-caveats | concept | minor | Caveats on the Interpretability Evaluations (part of manual-interpretability-scoring) | Two acknowledged weaknesses in the manual and automated experiments: most activations fall in the less-interpretable low intervals, and features were 
max-dataset-example-bias | concept | minor | The Maximal-Example Weakness (part of manual-interpretability-scoring) | The evaluation flaw the paper is correcting for: judging a unit only by its top-activating examples makes polysemantic units look monosemantic.
top-dataset-examples | concept | minor | Top activating dataset examples (part of manual-interpretability-scoring) | The standard inspection method: collect the datapoints on which a unit fires most strongly and read off what they have in common.
circuit | concept | minor | Circuit (part of mechanistic-interpretability) | A subgraph of a network's computation — a set of components connected by weights — that implements some identifiable behavior.
curse-of-dimensionality-in-interpretability | concept | minor | Curse of dimensionality in interpretability (part of mechanistic-interpretability) | As models grow, the volume of internal-state space that has to be interpreted grows exponentially, so the space cannot be searched or enumerated unles
scalability-of-analysis | concept | minor | Scalability of analysis (part of mechanistic-interpretability) | The barrier that would remain even if sparse autoencoders fully solved superposition: turning microscopic feature-level insight into a macroscopic und
randomized-transformer | concept | minor | Randomized Transformer Baseline (part of model-vs-data-question) | Dictionary learning run on a one-layer model whose weights have been randomly shuffled, used to ask how much of the features' structure comes from the
base64-feature-family | concept | minor | The three base64 features (part of multiple-features-for-one-context) | A/1/2357 fires preferentially on letters in base64, A/1/2364 on digits, and A/1/1544 on base64 that decodes to ASCII text — jointly covering the singl
adam-state-reset-on-resampling | concept | minor floor | Resetting Optimizer State on Resampling (part of neuron-resampling) | Adam's moment estimates are reset for every weight and bias changed by resampling.
dead-neurons | concept | minor | Dead Neurons (part of neuron-resampling) | Autoencoder hidden units with zero activation across a large number of datapoints. They waste dictionary capacity, so the paper resamples them.
loss-proportional-input-sampling | concept | minor | Sampling by Squared Loss (part of neuron-resampling) | Resampled neurons are seeded from inputs chosen with probability proportional to the square of the autoencoder's current loss on them.
lottery-ticket-hypothesis | concept | minor | Lottery Ticket Hypothesis (part of neuron-resampling) | The idea that a trained network's performance comes from a subnetwork whose initialization happened to be favorable. The paper offers it as a possible
resampled-encoder-scaling | concept | minor | Weak Initialization of the Resampled Encoder (part of neuron-resampling) | A revived neuron's encoder vector is scaled to 0.2 times the average live-neuron encoder norm and its bias set to zero, so it fires only weakly at fir
byte-pair-encoding | concept | minor floor | Byte-pair encoding (part of one-layer-transformer-testbed) | The tokenizer scheme that merges frequent byte sequences into single tokens, so a token need not line up with a character.
linear-logit-effects | concept | minor | Approximately Linear Logit Effects (part of one-layer-transformer-testbed) | In a one-layer model, the effect of a feature on the output logits is close to linear in its activation, which makes downstream analysis tractable.
mlp-activations-as-target | concept | minor | MLP Activations as the Decomposition Target (part of one-layer-transformer-testbed) | A one-layer attention-only transformer can be analyzed by rewriting it without reference to hidden states; adding a ReLU MLP makes that impossible, so
overtraining | concept | minor | Overtraining the Transformer (part of one-layer-transformer-testbed) | Training the small model on far more tokens than compute-optimal, on the hypothesis that this yields cleaner representations in superposition.
pile-dataset | concept | minor | The Pile (part of one-layer-transformer-testbed) | The public 800GB text corpus the transformers are trained on, chosen over Anthropic's internal dataset for reproducibility.
residual-stream | concept | minor floor | Residual stream (part of one-layer-transformer-testbed) | The running vector each block reads from and writes back into; here it is 128-dimensional.
transformer-a-and-b | concept | minor | The A and B Transformers (part of one-layer-transformer-testbed) | A second one-layer transformer with the same architecture, data and hyperparameters as the first, differing only in random seed, with its own dictiona
undercomplete-basis | concept | minor | Undercomplete basis (part of overcompleteness) | A feature set smaller than the dimension of the space being decomposed — what earlier dictionary learning work on transformers recovered.
classic-polysemantic-neuron-examples | concept | minor | Classic Examples of Polysemanticity (part of polysemanticity) | The concrete examples the paper offers of what polysemanticity looks like: a neuron mixing citations, dialogue, HTTP requests and Korean, and the visi
mechanistic-interpretability | concept | minor | Mechanistic interpretability (part of polysemanticity) | The program of understanding a neural network by breaking it into components that are easier to understand than the whole, then reasoning about how th
monosemanticity | concept | minor | Monosemanticity (part of polysemanticity) | The property of a unit responding to one coherent thing, so that a single interpretation covers when it fires and what it does.
polysemanticity-and-capacity | concept | minor | Polysemanticity and capacity (part of polysemanticity) | A mathematical framework, from prior work, for when a network prefers monosemantic versus polysemantic representations.
cross-entropy-favors-polysemanticity | concept | minor | The four-feature loss calculation (part of polysemanticity-without-superposition) | A toy calculation showing a single binary neuron gets lower cross-entropy loss by firing on two features ambiguously than on one feature cleanly.
bounce-plots | concept | minor | Bounce Plots (part of pre-encoder-bias) | A toy-model diagnostic the paper uses to check whether the autoencoder is behaving correctly. Failure to produce them is what motivated adding biases.
geometric-median-initialization | concept | minor | Geometric Median Initialization (part of pre-encoder-bias) | The tied bias is initialized to the geometric median of the activation dataset — the point minimizing summed Euclidean distance to all datapoints.
kurtosis-privileged-basis-test | concept | minor | Kurtosis as a privileged-basis test (part of privileged-basis) | Using the heavy-tailedness of weights in the neuron basis as evidence that the basis is singled out, even when individual features spread across many 
randomized-feature-bimodality | concept | minor | Bimodality of Randomized Features (part of randomized-transformer) | The randomized model's features split into a single-token cluster and a polysemantic cluster; once separated, the real features win on the second.
single-token-features | concept | minor | Single Token Features (part of randomized-transformer) | Features where the same token carries the largest activation in all twenty top-interval examples; abundant in the randomized model and trivially easy 
weight-shuffling | concept | minor | Weight Shuffling (part of randomized-transformer) | The randomization method: shuffle the entries within each weight matrix of the trained model rather than resampling them.
activation-mass-expected-value-plot | concept | major | Activation-mass (expected value) plot (part of reading-the-evidence) | A feature's activation histogram with each bin weighted by activation magnitude instead of counted once per token, so a bar's height is total activati
activation-spectrum-by-proxy | concept | major | Activation spectrum coloured by a proxy (part of reading-the-evidence) | A histogram of one unit's activation values across sampled tokens, with each column split by an independently computed score for whether that text mat
annotated-dataset-example-panel | concept | major | Annotated dataset-example panel (part of reading-the-evidence) | Dataset snippets laid out in cells by activation level, with two separate quantities drawn onto each token: orange shading for how hard the feature fi
conceptual-schematic | concept | major | Conceptual schematic (part of reading-the-evidence) | A cartoon of activation space or of an argument, drawn where the paper has a claim but no measurement for it yet. Its job is to fix what a hypothesis 
cross-model-agreement-population-scatter | concept | major | Cross-model agreement scatter with marginals (part of reading-the-evidence) | A scatter in which every dot is a matched pair of features from two independently trained models — one axis the activation correlation used to choose 
dictionary-geometry-diagnostic-scatter | concept | major | Per-feature dictionary diagnostic scatter (part of reading-the-evidence) | A scatter in which every point is one learned feature, placed by numbers read off its own encoder and decoder vectors — how concentrated its direction
feature-browser-screenshot | concept | major | Feature browser screenshot (part of reading-the-evidence) | A screengrab of the paper's published feature browser with arrows and captions added: one page lays four separate kinds of evidence about a single lea
feature-density-histogram | concept | major | Feature-density histogram of a dictionary (part of reading-the-evidence) | A log-scale histogram counting every feature in one trained autoencoder by how often it fires, typically splitting into an ultralow-density mode the p
feature-direction-stem-plot | concept | major | Feature direction in the neuron basis (stem plot) (part of reading-the-evidence) | One learned feature's decoder column drawn coordinate by coordinate, one stem per MLP neuron. It exists to answer a single question: is this feature o
feature-relationship-graph | concept | major | Feature relationship graph (part of reading-the-evidence) | Features drawn as boxes, with labelled edges standing for a relation between them: either a handoff, where the tokens one feature promotes are the tok
features-versus-neurons-score-histogram | concept | major | Features-versus-neurons score histogram (part of reading-the-evidence) | Two populations of units — the autoencoder's learned features and the transformer's own MLP neurons — scored on one number and drawn as overlapping hi
interpretability-across-activation-bands | concept | major | Score across the activation spectrum (part of reading-the-evidence) | A line chart that scores how well a unit's interpretation holds at each band of its own activation range, from its weakest firings to its maximum, wit
logit-weight-histogram | concept | major | Logit-weight histogram over the vocabulary (part of reading-the-evidence) | A histogram of one unit's logit weights, one number per vocabulary entry saying how much that token's next-token logit moves when the unit fires, with
scoring-robustness-panel-grid | concept | major | Scoring-robustness panel grid (part of reading-the-evidence) | A grid of histograms crossing several invented interpretability scores against several populations of units — dictionary-learning features, MLP neuron
sparsity-versus-explained-loss-sweep | concept | major | Sparsity against explained behaviour (part of reading-the-evidence) | A scatter in which every dot is one trained sparse autoencoder, placed by how few features fire per token against how much of the MLP layer's loss con
umap-feature-map | concept | major | UMAP map of dictionary directions (part of reading-the-evidence) | A scatter of every learned feature's decoder direction, projected to two dimensions so that nearby dots mean small angles between dictionary vectors. 
scaling-laws-for-dictionary-learning | concept | minor | Scaling laws for dictionary learning (part of scaling-sparse-autoencoders) | The wanted-but-missing relationships between subject model size and the autoencoder width and data needed to decompose it.
subtoken-script-sampling | concept | minor | Handling Partial-Character Tokens (part of script-proxy-via-bayes) | When a token is not a complete Unicode character, its script is estimated empirically by sampling occurrences of it in the corpus.
unicode-block-detection | concept | minor | Unicode Block Detection (part of script-proxy-via-bayes) | Whether a character belongs to a script is decided by checking its Unicode block — for Arabic, ranges including U+0600–U+06FF and U+0750–U+077F.
adam-decoder-normalization-interaction | concept | minor | Adam and Decoder Normalization (part of sparse-autoencoder) | A fix for the mismatch created by renormalizing dictionary vectors after each step: project the gradient onto the sphere before the update rather than
algorithmic-improvements-for-sparse-autoencoders | concept | minor | Algorithmic alternatives to the plain L1 autoencoder (part of sparse-autoencoder) | Candidate refinements listed as future work: variational autoencoders, sparsity priors beyond L1, and noise injection.
amortized-sparse-coding-approximation | concept | minor | Encoder as Amortized Sparse Coding (part of sparse-autoencoder) | The framing that the encoder is a single-shot linear stand-in for a multi-step, non-linear sparse coding algorithm.
autoencoder-encoder | concept | minor | Encoder (part of sparse-autoencoder) | The linear map plus bias plus ReLU that turns an activation vector into feature activations: $\mathbf{f} = \text{ReLU}(W_e\bar{\mathbf{x}} + \mathbf{b
autoencoder-training-dataset | concept | minor | Autoencoder Training Dataset (part of sparse-autoencoder) | Roughly 10 billion MLP activation vectors harvested from the transformer over 40 million Pile contexts, of which just over 8 billion are used.
dictionary-decoder | concept | minor | Decoder as Dictionary (part of sparse-autoencoder) | The output weight matrix $W_d$, whose columns are the feature directions. The paper uses "decoder" and "dictionary" interchangeably.
expansion-factor | concept | minor | Expansion factor (part of sparse-autoencoder) | The ratio of autoencoder hidden dimension to input dimension — how many times more features than neurons the dictionary has.
extended-training-steps | concept | minor | Training Past the Loss Plateau (part of sparse-autoencoder) | The paper keeps training well beyond the point where training loss appears flat, listing 'many training steps' among its important modifications.
external-replication | concept | minor | External replication and commentary (part of sparse-autoencoder) | Invited outside researchers commented on the work before publication; one trained his own sparse autoencoder on a different open source one-layer mode
feature-notation | concept | minor | Feature Notation (part of sparse-autoencoder) | The paper's identifier scheme: run seed, autoencoder size index, and feature index, as in A/1/3450 or B/1/1334; neurons are written A/neurons/489.
kaiming-uniform-initialization | concept | minor floor | Kaiming Uniform Initialization (part of sparse-autoencoder) | The default PyTorch weight initialization used for the autoencoder.
l1-sparsity-penalty | concept | major | L1 penalty on feature activations (part of sparse-autoencoder) | A penalty on the sum of feature activations, which pushes most of them to exactly zero. This is what buys sparsity in the decomposition.
learning-rate-sweep | concept | minor | Learning Rate Sweep (part of sparse-autoencoder) | Trying multiple learning rates alongside varying training lengths. The paper reports lower learning rates, given enough steps, give lower loss and mor
neuron-resampling | concept | minor | Neuron Resampling (part of sparse-autoencoder) | Periodically reinitializing autoencoder neurons that have stopped firing, aiming them at datapoints the autoencoder currently reconstructs worst.
pre-encoder-bias | concept | minor | Pre-Encoder Bias (part of sparse-autoencoder) | A bias subtracted from the input before the encoder, constrained to be the negative of the decoder's output bias. The paper lists it as its most impor
scaling-sparse-autoencoders | concept | minor | Scaling sparse autoencoders (part of sparse-autoencoder) | The open engineering problem of applying this method to frontier models, which the authors call one of the most important questions going forward.
sparse-autoencoder-loss | concept | minor | Reconstruction Plus Sparsity Loss (part of sparse-autoencoder) | The training objective: mean squared reconstruction error plus an L1 penalty on the hidden activations, $\mathcal{L} = \frac{1}{|X|}\sum_\mathbf{x} \|
untied-decoder-weights | concept | minor | Untied Decoder Weights (part of sparse-autoencoder) | The encoder weights are learned independently of the decoder weights rather than being constrained to their transpose. The paper reports the two end u
mse-reconstruction-term | concept | minor floor | MSE Reconstruction Term (part of sparse-autoencoder-loss) | The squared error between the original MLP activation and the autoencoder's reconstruction, averaged over vector elements.
architectural-approaches-to-superposition | concept | minor | Architectural approaches, and why they were abandoned (part of superposition) | The attempt to build models that simply do not have superposition, which the authors conclude is not merely costly but fundamentally non-viable for la
attentional-superposition | concept | minor | Attentional superposition (part of superposition) | The open question of whether attention layers also use superposition, and whether similar methods would extract features from them.
disentanglement | concept | minor | Disentanglement (part of superposition) | A related literature seeking representations whose dimensions separate conceptually distinct factors influencing the data — typically as many factors 
feature-sparsity | concept | minor | Feature sparsity (part of superposition) | The property that only a small number of the model's features are active on any given datapoint.
interference | concept | minor | Interference (part of superposition) | The cross-talk that arises because feature directions in superposition cannot all be orthogonal: activating one feature slightly activates others.
memorization-in-superposition | concept | minor | Memorization in superposition (part of superposition) | A prior finding that when a toy model is trained on repeated small datasets it memorizes datapoints in superposition rather than learning generalizing
noisy-simulation | concept | minor | Noisy simulation of a larger network (part of superposition) | The mechanism proposed for superposition: a small network exploits feature sparsity and the geometry of high-dimensional spaces to approximately simul
theories-of-superposition | concept | minor | Theories of superposition, revised (part of superposition) | The paper's reassessment of the picture it inherited: features clump into dense related groups and may not be one-dimensional, which the earlier toy p
three-approaches-to-superposition | concept | minor | Three approaches to superposition (part of superposition) | The strategies laid out in Toy Models: build models without superposition, use dictionary learning on models that have it, or combine the two.
toy-models-of-superposition | concept | minor | Toy Models of Superposition (part of superposition) | The authors' earlier paper, which showed superposition arising in small synthetic models and supplied the theoretical picture this paper works from an
action-sharing-features | concept | minor | Action-sharing features (part of theories-of-superposition) | Features that produce similar downstream effects and therefore end up with similar directions — the paper's suspected main cause of feature clustering
correct-number-of-features | concept | minor | Is there a correct number of features? (part of theories-of-superposition) | Under some expanded theories the question is not well posed; under others there is a true number but getting it exactly right is not essential.
correlated-features | concept | minor | Correlated features (part of theories-of-superposition) | Features that tend to fire together, offered as one explanation for why feature directions clump.
feature-manifolds | concept | minor | Feature manifolds (part of theories-of-superposition) | The possibility that some features are not one-dimensional intensity scales but higher-dimensional continuous families of related directions.
isotropic-superposition-model | concept | minor | Isotropic superposition model (part of theories-of-superposition) | The inherited picture from Toy Models: features are discrete, one-dimensional, and repel each other through interference into a roughly evenly spaced 
activation-similarity | concept | minor | Activation similarity (part of universality) | The procedure for finding a feature's counterpart in another dictionary: take the feature with the highest Pearson correlation of activations over 40,
attribution-similarity | concept | minor | Attribution similarity (part of universality) | A third comparison measure that weights a feature's logit effects by whether the feature was actually active and the token actually came next.
logit-weight-similarity | concept | minor | Logit weight similarity (part of universality) | Comparing features by what they do rather than when they fire: correlate their logit weight vectors, indexed by vocabulary token.
universality-with-the-literature | concept | minor | Matching features to the literature (part of universality) | A stronger universality test: checking whether features found here resemble units reported in other models by other researchers.
multimodal-neurons | concept | minor | Multimodal neurons (part of universality-with-the-literature) | Units in vision-language models reported to respond to a concept across images and text, including region, person and emotion neurons.
sparse-probing | concept | minor | Sparse probing (part of universality-with-the-literature) | Training sparse linear probes on activations to find whether a property is represented, and by how few neurons.
encoder-decoder-offset-crosstalk | concept | minor | Encoder Tilt Against Crosstalk (part of untied-decoder-weights) | The paper's account of why untying helps: features with similar dictionary vectors get encoder vectors tilted away from each other, so they can be tol
autoencoder-width-limitation | concept | minor | Hypothesis 3: The Autoencoder Is Imperfect (part of weak-activation-hypotheses) | If the dictionary is narrower than the number of true features, the missing features may show up smeared as weak activations across many learned ones.
calibrated-feature-activations | concept | minor | Hypothesis 2: The Model Is Imperfect But Calibrated (part of weak-activation-hypotheses) | If features activate in proportion to the model's confidence that a property holds, weak activations should sometimes be wrong.
proxy-false-negatives | concept | minor | Hypothesis 1: The Proxy Is Imperfect (part of weak-activation-hypotheses) | Weak activations may look impure because the proxy misses characters that are genuinely part of the context.
fig-1 | figure | evidence | The UMAP Feature Browser | A screenshot of the interactive map the authors publish alongside the article: every dictionary direction from two autoencoder runs — 4,608 of them — 
fig-2 | figure | evidence | Where the Dictionary Attaches | This schematic names the exact tensor the paper decomposes: the 512-dimensional post-ReLU MLP activation vector at a single token position of a one-la
table-1 | table | evidence | The Two Models, Side by Side | The table is the paper's specification sheet for both models it works with: the small transformer under study on the left, and the sparse autoencoder 
fig-3 | figure | evidence | The Simulated Larger Network | The figure states the paper's working picture of superposition: the network we can actually inspect is a squeezed-down projection of a much larger, mu
fig-4 | figure | evidence | Anatomy of a Feature Page | A labelled tour of one page of the paper's published feature browser, which puts four kinds of evidence about a single learned feature side by side: w
fig-5 | figure | evidence | The Feature Search Bar | A screenshot of the search strip in the paper's public feature browser, with its four dropdowns opened. It carries no results, so what it establishes 
fig-6 | figure | evidence | Text-First Feature Browser | A second published interface that inverts the usual view: instead of picking a feature and seeing the texts it fires on, you pick a text and see every
fig-7 | figure | evidence | Ruling Out Polysemanticity With a Proxy | A schematic of the paper's argument for specificity: if an independently computed proxy marks out a small, specific region of the data, and a feature 
fig-8 | figure | evidence | The Arabic Feature's Activation Spectrum | The paper's specificity claim for the Arabic feature is not one number but a curve: the higher the feature fires, the more purely Arabic-script the te
fig-9 | figure | evidence | Arabic Feature's Activation Mass | Re-weighting the feature's activation histogram by activation magnitude shows that almost all of the total activation the Arabic script feature contri
fig-10 | figure | evidence | The Arabic Feature's Logit Weights | Scoring every vocabulary token by how much feature A/1/3450 pushes its next-token logit gives a histogram with two humps: a fat one at zero covering n
fig-11 | figure | evidence | Ablating the Arabic Feature | Deleting this one feature's contribution from the MLP activations lowers the model's predicted probability on Arabic-script tokens, and it does so acr
fig-12 | figure | evidence | Pinning the Arabic Script Feature | Holding one learned feature's activation at its maximum, with nothing else in the network touched, turns the model's continuation of a counting prefix
fig-13 | figure | evidence | The Arabic Feature in the Neuron Basis | The Arabic script feature's direction, written out one MLP neuron at a time, is spread across many neurons rather than living in one: 27 of the 512 co
fig-14 | figure | evidence | Neuron 489's Language Mixture | The neuron whose activations correlate most closely with the Arabic script feature fires on a grab-bag of non-English languages — Japanese, Russian, S
fig-15 | figure | evidence | Neuron 489's Logit Weights | The neuron whose activations track the Arabic script feature most closely does not share the feature's downstream effect. Its logit weights form a sin
fig-16 | figure | evidence | The Arabic Feature Against Its Closest Neuron | Plotted against the single neuron whose behaviour is closest to it, the Arabic script feature separates Arabic contexts and Arabic output tokens along
fig-17 | figure | evidence | The Arabic Feature Found Again in Model B | A second, independently seeded one-layer transformer, given its own independently seeded dictionary-learning run, produces a feature (B/1/1334) whose 
fig-18 | figure | evidence | The Same Arabic Feature in Model B | A dictionary trained on a second, separately seeded one-layer transformer contains a feature, B/1/1334, that behaves like the paper's Arabic script fe
fig-19 | figure | evidence | Arabic Feature Across Two Models | Dictionary learning on a second, independently trained transformer produces a feature whose firing pattern closely tracks the original Arabic script f
fig-20 | figure | evidence | DNA Activations Split by Proxy | Every token in a sample of the data is sorted by how strongly the DNA feature A/1/2937 fires on it, and colored by whether a hand-built "is this DNA" 
fig-21 | figure | evidence | The DNA Feature-Proxy Disagreement Grid | A random-sample grid crossing the DNA feature's activation level against whether the hand-built DNA proxy fires, used to audit the cases where the two
fig-22 | figure | evidence | The DNA Feature's Logit Weights | Plotting how much the DNA feature A/1/2937 pushes each vocabulary token's next-token logit up or down, the paper finds the usual shape: a tall mode of
fig-23 | figure | evidence | The DNA Feature's Nearest Neuron | The neuron that comes closest to the DNA feature is not a DNA neuron: across its whole activation range, and in random examples drawn from the top of 
fig-24 | figure | evidence | The DNA Feature in a Second Model | A separately trained transformer, given its own dictionary learning run, yields a feature that fires on almost exactly the same tokens as the DNA feat
fig-25 | figure | evidence | Base64 Activations Coloured by Proxy | The figure is the paper's specificity check for its base64 feature A/1/2357: it stacks every sampled token by how strongly the feature fired, and colo
fig-26 | figure | evidence | Base64 Logit Weights and Their Conditional Strip | For the base64 feature A/1/2357, the tokens whose logits it raises shade gradually from ordinary text tokens into base64-specific ones, rather than se
fig-27 | figure | evidence | The Base64 Feature Found Again in Model B | A separately trained transformer, decomposed by a separately trained dictionary, yields a feature (B/1/2165) whose activations correlate 0.85 with the
fig-28 | figure | evidence | B/1/2165's Base64 Logit Mode | In the second model's dictionary, the feature that matches the base64 feature does the same thing to the output that the original does: its logit-weig
fig-29 | figure | evidence | Base64 Feature, Matched Across Two Runs | The base64 feature found in model A (A/1/2357) and the one found in a separately seeded model B (B/1/2165) line up on both axes the paper uses to comp
fig-30 | figure | evidence | The Base64 Feature's Nearest Neuron | The MLP neuron whose activations track the base64 feature most closely, A/neurons/470, correlates with it at only 0.18, and its own activation distrib
fig-31 | figure | evidence | Neuron 470's Mixed Logit Weights | The neuron most correlated with the base64 feature does push base64 tokens up a little, but the tokens it pushes up hardest are filename endings like 
fig-32 | figure | evidence | Base64 Feature Against Neuron 470 | The neuron whose activations most resemble the base64 feature's shares only part of its behaviour: an activation correlation of 0.18 and a logit-weigh
fig-33 | figure | evidence | The Hebrew Feature's Activation Spectrum | A histogram of how often feature A/1/416 takes each activation value, with each slice split by an independent Unicode-based score for "is this Hebrew"
fig-34 | figure | evidence | Hebrew Feature Logit Weights | The Hebrew feature's effect on the model's output is concentrated in a small second bump far to the right of the main mode, and the tokens in that bum
fig-35 | figure | evidence | The Hebrew Feature Against Its Closest Neuron | The neuron whose activations track the Hebrew feature most closely does not behave like the feature: the correlation is 0.1 on activations and 0.38 on
fig-36 | figure | evidence | Hebrew Feature Found Again in Model B | A dictionary trained on a second, independently seeded transformer contains a feature, B/1/1901, whose activation histogram looks like the Hebrew feat
fig-37 | figure | evidence | B/1/1901's Hebrew Logit-Weight Mode | For the Hebrew feature found in the second transformer's dictionary (B/1/1901), the distribution of logit weights over the vocabulary has the same sha
fig-38 | figure | evidence | Hebrew Feature Across Two Models | The Hebrew feature from run A has a counterpart in a second, independently trained run that fires on nearly the same tokens (activation correlation 0.
fig-39 | figure | evidence | Hand-scored interpretability, features against neurons | A blinded human rater scored features and MLP neurons on the same 0–14 interpretability rubric, and the two score distributions barely overlap: the me
fig-40 | figure | evidence | Autointerp Activation Scores | A language model is asked to write an explanation of a unit from examples where it fires, then to predict that unit's activations on unseen text using
fig-41 | figure | evidence | Logit-Weight Scores, Features vs Neurons | Given only a written explanation of a unit, Claude predicts which unseen tokens that unit promotes with 0.74 mean accuracy for autoencoder features an
fig-42 | figure | evidence | Interpretability Across the Activation Spectrum | Scoring each band of a unit's activation range separately, the paper's human annotator judged strong activations far more consistent with a feature's 
fig-43 | figure | evidence | Pinning Features to Steer Generation | Holding a single learned feature at a high value while sampling makes the model emit text of exactly the kind that feature normally fires on — Arabic 
fig-44 | figure | evidence | Three Dictionaries in One UMAP | The families the authors found by reading text — base64 features, Arabic script features — are also families in geometry: their dictionary vectors poi
fig-45 | figure | evidence | One Region, More Arrows | A hand-drawn schematic of what the paper means by feature splitting: widening the autoencoder does not send new features off to new parts of activatio
fig-46 | figure | evidence | The Mathematics and Physics Splitting Graph | Three autoencoders of increasing width, trained on the same one-layer transformer, are lined up side by side, and the technical-writing features of th
fig-47 | figure | evidence | The P Feature Zoo | One coarse feature that looked like a pointless "fires on the letter P" detector turns out, in wider dictionaries, to be a family of sixteen or more f
fig-48 | figure | evidence | One base64 Feature Becoming Three | In the 512-feature dictionary A/0, a single feature (A/0/45) fires across a whole base64 string. In the 4,096-feature dictionary A/1, three features s
fig-49 | figure | evidence | The Digit Feature That Won't Predict Digits | Two base64 features from the same dictionary push almost the same tokens up, except that the feature firing on single digits gives the ten digit token
fig-50 | figure | evidence | Base64 That Decodes to Text | Three features in run A/1 all fire on base64, and this figure shows what separates the third one: the strings A/1/1544 fires hardest on decode into re
fig-51 | figure | evidence | Activation Similarity vs Logit Weight Similarity | The figure sets out the paper's two model-independent ways of asking whether the same feature was found twice, and shows that for the Arabic script fe
fig-52 | figure | evidence | Feature vs Neuron Match Histograms | When you look for each item's closest counterpart in a second, independently trained one-layer transformer, autoencoder features match far better than
fig-53 | figure | evidence | Matched Features That Fire Alike But Write Differently | For thousands of feature pairs matched across two independently trained models, agreement on *when* the features fire is much higher than agreement on
fig-54 | figure | evidence | Attribution vs Activation Correlation | For each feature in one dictionary and its best-matching feature in a dictionary learned from a separately trained transformer, the plot compares how 
fig-55 | figure | evidence | Single-Node Feature Loops | Four features from the smallest dictionary each raise the probability of exactly the kind of token that would make them fire again one position later,
fig-56 | figure | evidence | The All-Caps Snake Case Loop | Two features from the smallest dictionary — one firing on all-caps word pieces, one on underscores — each raise the probability of the tokens that swi
fig-57 | figure | evidence | Unicode Prefix–Suffix Feature Loops | Where a script's characters are tokenized as a prefix byte plus a suffix byte, the paper finds a pair of features that hand off to each other: each on
fig-58 | figure | evidence | The Four-Node HTML Automaton | Four features in the smallest dictionary hand off to each other through the token stream — open bracket, tag name, closing bracket, newline-and-indent
fig-59 | figure | evidence | The IRC Transcript Automaton | Four features from run A/0 hand off to each other across an IRC transcript: one fires on the "<" that opens a nickname, one on the nickname's interior
fig-60 | figure | evidence | The License-Boilerplate Chain | Three narrow features from the large A/4 dictionary fire in sequence — on the "A", then on "PARTICULAR", then on "PURPOSE" — with each one raising the
fig-61 | figure | evidence | Three Pictures of Feature Geometry | A hand-drawn contrast between three ways features might be arranged in activation space: evenly spread one-dimensional directions, clumped one-dimensi
fig-62 | figure | evidence | Two Alternatives to 1-D Features | A pair of sketches arguing that "features are higher-dimensional manifolds" is not one hypothesis but two. One version — the convex hull of a few feat
fig-63 | figure | evidence | Decoder Sparsity in the Neuron Basis | In an outside replication, 92% of the learned feature directions turn out to be spread across many MLP neurons rather than concentrated in one neuron 
fig-64 | figure | evidence | The Two Density Modes of A/16 | Plotting how often each learned feature fires, on a log axis, splits the features of run A/16 into two humps: a spike of features that fire on roughly
fig-65 | figure | evidence | Overlapping Density Clusters in Run A/5 | The paper's quick diagnostic — read two separated humps off a log-scale histogram of how often each feature fires — stops working at the largest autoe
fig-66 | figure | evidence | Three Diagnostics That Isolate the High-Density Cluster | In a run where feature density alone leaves the two modes overlapping, plotting each feature's density together with its encoder bias and its decoder-
fig-67 | figure | evidence | Density Histograms Across Dictionary Sizes | Across five sparsity settings, each panel overlays the feature-density histograms of six autoencoders whose dictionaries run from 512 up to 131,072 un
fig-68 | figure | evidence | Sparsity Against Explained Behavior | Plotting how sparse each trained dictionary is against how much of the MLP layer's loss contribution it recovers turns hyperparameter choice into a vi
fig-69 | figure | evidence | Pearson vs Spearman Scoring Panel | The paper re-runs its automated interpretability scoring with Pearson correlation alongside the Spearman correlation used throughout the main text, an
fig-70 | figure | evidence | The Randomized-Transformer Control | On the paper's automated activation score, features found in a random-weight transformer get a slightly higher median than the real features do. Split
fig-71 | figure | evidence | Importance-Weighted Interpretability Scores | Once the automated-interpretability scores are reweighted to what random text would have given, features still beat neurons (median 0.28 vs 0.04) but 
fig-72 | figure | evidence | Explanation Length Distributions | A prompting bug sometimes let Claude's whole chain of thought stand in as a feature's explanation, producing explanations several hundred tokens long 
eq-feature-decomposition | equation | evidence | Activations as a Sum of Features | The paper defines a decomposition of a layer as writing each token's MLP activation vector as a fixed bias plus a weighted sum of unit directions, one
eq-encoder | equation | evidence | The Encoder Equation | This fixes what a "feature activation" concretely is in this paper: one non-negative number per dictionary element, produced by a single linear readou
eq-autoencoder-loss | equation | evidence | The Sparse Autoencoder's Two-Term Loss | This is the entire method in four lines: a ReLU encoder turns one MLP activation vector into feature activations, a linear decoder rebuilds the activa
eq-proxy-loglikelihood-ratio | equation | evidence | The Log-Likelihood-Ratio Proxy | The paper scores every string with a hand-built number: how much more likely that string is under a guessed context (base64, Arabic script, DNA) than 
eq-bayes-script-proxy | equation | evidence | Inverting the Script Probability | To score how Arabic-like a string is, the paper needs $P(s|\text{Arabic})$, which would otherwise require a generative model of Arabic text. Bayes' ru
eq-attribution-vector | equation | evidence | Attribution Score for the Observed Next Token | This footnote formula defines one number per datapoint: a feature's activation there, multiplied by its logit weight on the token that actually came n
eq-feature-ablation | equation | evidence | Subtracting One Feature's Contribution | Ablating a feature means subtracting exactly that feature's own contribution — its activation times its dictionary direction — from the MLP activation
