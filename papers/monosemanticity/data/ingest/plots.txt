60 of 80 items look like plots rather than screenshots or diagrams.
Each entry: item id, the section it sits in, its title, what it establishes,
and the axis/scale/colour terms its walkthrough names.

fig-1 | §front | The UMAP Feature Browser
    takeaway: A screenshot of the interactive map the authors publish alongside the article: every dictionary direction from two autoencoder runs — 4,608 of them — projected onto a plane, with a searchable list of human-written featur
    furniture: Horizontal and vertical axes

fig-3 | §2.1.1 | The Simulated Larger Network
    takeaway: The figure states the paper's working picture of superposition: the network we can actually inspect is a squeezed-down projection of a much larger, much sparser network whose neurons would each be one clean feature. Beca
    furniture: (none named)

fig-4 | §2.8 | Anatomy of a Feature Page
    takeaway: A labelled tour of one page of the paper's published feature browser, which puts four kinds of evidence about a single learned feature side by side: where it fires, what it does to the next-token logits, whether any sing
    furniture: Activation (x-axis); Logit (x-axis of the lower histogram)

fig-5 | §2.8 | The Feature Search Bar
    takeaway: A screenshot of the search strip in the paper's public feature browser, with its four dropdowns opened. It carries no results, so what it establishes is only the shape of the query the authors built for themselves: pick 
    furniture: "Search among the features" / "Where the search query is matched" / "Which ablation is used to color the underlines of tokens"

fig-7 | §3 | Ruling Out Polysemanticity With a Proxy
    takeaway: A schematic of the paper's argument for specificity: if an independently computed proxy marks out a small, specific region of the data, and a feature only fires inside that region, then the feature cannot be firing on th
    furniture: (none named)

fig-8 | §3.1.1 | The Arabic Feature's Activation Spectrum
    takeaway: The paper's specificity claim for the Arabic feature is not one number but a curve: the higher the feature fires, the more purely Arabic-script the text is. Barely-active tokens are roughly 25% Arabic, which is noisy, wh
    furniture: Feature Activation Level (x axis); Density (y axis); Log-Likelihood Ratio (colour legend); Colour denotes activation (orange shading)

fig-9 | §3.1.1 | Arabic Feature's Activation Mass
    takeaway: Re-weighting the feature's activation histogram by activation magnitude shows that almost all of the total activation the Arabic script feature contributes comes from tokens the proxy scores as Arabic. The weak, less pur
    furniture: Expected Value (y-axis label); Activation Level (x-axis label); Log-Likelihood Ratio (color scale, inherited from the companion plot)

fig-10 | §3.1.3 | The Arabic Feature's Logit Weights
    takeaway: Scoring every vocabulary token by how much feature A/1/3450 pushes its next-token logit gives a histogram with two humps: a fat one at zero covering nearly the whole vocabulary, and a small one at the far right made almo
    furniture: Logit Weight (x axis); Density (y axis); Log-Likelihood Ratio (colour legend); Arabic → (legend arrow); Composition strip (bar under the axis)

fig-11 | §3.1.3 | Ablating the Arabic Feature
    takeaway: Deleting this one feature's contribution from the MLP activations lowers the model's predicted probability on Arabic-script tokens, and it does so across the feature's whole activation range rather than only where it fir
    furniture: Inset example (legend panel)

fig-13 | §3.1.4 | The Arabic Feature in the Neuron Basis
    takeaway: The Arabic script feature's direction, written out one MLP neuron at a time, is spread across many neurons rather than living in one: 27 of the 512 coefficients have magnitude at least 0.1, and the three largest are nega
    furniture: Weight Value (y-axis); Dictionary Vector Index (corresponds to neurons) (x-axis)

fig-14 | §3.1.4 | Neuron 489's Language Mixture
    takeaway: The neuron whose activations correlate most closely with the Arabic script feature fires on a grab-bag of non-English languages — Japanese, Russian, Spanish, German, French, Portuguese, Ukrainian, Bulgarian, Thai — with 
    furniture: Blue-white-red colour ramp; Density (y-axis); M (axis unit); Activation Level (x-axis)

fig-15 | §3.1.4 | Neuron 489's Logit Weights
    takeaway: The neuron whose activations track the Arabic script feature most closely does not share the feature's downstream effect. Its logit weights form a single mode centred on zero, with the Arabic-script tokens scattered insi
    furniture: Logit Weight (x-axis); Density (y-axis); Log-Likelihood Ratio (colour bar); Marginal strip (the bar under the axis)

fig-16 | §3.1.4 | The Arabic Feature Against Its Closest Neuron
    takeaway: Plotted against the single neuron whose behaviour is closest to it, the Arabic script feature separates Arabic contexts and Arabic output tokens along its own axis, while the neuron's axis does not separate them at all. 
    furniture: Feature Activaitons (Run A) — y-axis, left panel; Feature Activations (Run B) — x-axis, left panel; Logit Weight (Run A) — y-axis, right panel; Logit Weight (Run A Neuron) — x-axis, right panel; >0 (axis marker)

fig-17 | §3.1.5 | The Arabic Feature Found Again in Model B
    takeaway: A second, independently seeded one-layer transformer, given its own independently seeded dictionary-learning run, produces a feature (B/1/1334) whose per-token activations correlate 0.91 with the Arabic-script feature A/
    furniture: Activation Level (x axis); Density (y axis); Log-Likelihood Ratio (colour legend); Arabic → (legend arrow); Hard zeros (the "0" and ">0" marks left of the axis)

fig-18 | §3.1.5 | The Same Arabic Feature in Model B
    takeaway: A dictionary trained on a second, separately seeded one-layer transformer contains a feature, B/1/1334, that behaves like the paper's Arabic script feature: its activations are dominated by Arabic-script tokens, and its 
    furniture: Logit Weight (x axis); Density (y axis); Activation Level (x axis); Log-Likelihood Ratio (color legend)

fig-19 | §3.1.5 | Arabic Feature Across Two Models
    takeaway: Dictionary learning on a second, independently trained transformer produces a feature whose firing pattern closely tracks the original Arabic script feature (Pearson correlation 0.91), while the two features' effects on 
    furniture: Feature Activations (axis label); Logit Weight (axis label)

fig-20 | §3.2.1 | DNA Activations Split by Proxy
    takeaway: Every token in a sample of the data is sorted by how strongly the DNA feature A/1/2937 fires on it, and colored by whether a hand-built "is this DNA" score fires on it too. The strong activations are almost entirely DNA 
    furniture: Activation Level (x-axis); Density (y-axis); Log-Likelihood Ratio (legend); Binarized proxy

fig-21 | §3.2.1 | The DNA Feature-Proxy Disagreement Grid
    takeaway: A random-sample grid crossing the DNA feature's activation level against whether the hand-built DNA proxy fires, used to audit the cases where the two disagree. The authors read all 24 disagreements and attribute 22 of t
    furniture: Activation Level (x-axis); 0 and >0 axis markers; Binarized proxy

fig-22 | §3.2.2 | The DNA Feature's Logit Weights
    takeaway: Plotting how much the DNA feature A/1/2937 pushes each vocabulary token's next-token logit up or down, the paper finds the usual shape: a tall mode of near-zero weights, plus a tiny bump far out in the right tail. Every 
    furniture: Logit Weight (horizontal axis); Density (vertical axis); Log-Likelihood Ratio (colour legend)

fig-23 | §3.2.3 | The DNA Feature's Nearest Neuron
    takeaway: The neuron that comes closest to the DNA feature is not a DNA neuron: across its whole activation range, and in random examples drawn from the top of that range, the DNA proxy fires on only a thin sliver of tokens and no
    furniture: Density (y-axis); Activation Level (x-axis); Log-Likelihood Ratio (legend); DNA → (arrow on the legend); Blue-to-red color bar; 0 and >0 ticks (far left of the axis)

fig-24 | §3.2.4 | The DNA Feature in a Second Model
    takeaway: A separately trained transformer, given its own dictionary learning run, yields a feature that fires on almost exactly the same tokens as the DNA feature (activation correlation 0.92) and pushes up the same nucleotide to
    furniture: 0 and >0 axis markers

fig-25 | §3.3 | Base64 Activations Coloured by Proxy
    takeaway: The figure is the paper's specificity check for its base64 feature A/1/2357: it stacks every sampled token by how strongly the feature fired, and colours the stack by an independently computed base64 score. The higher th
    furniture: Activation Level (x axis); Density (y axis); Hard zeros (the note under the axis); Log-Likelihood Ratio (legend title); base64 → (label at the red end of the legend); Blue-to-red colour ramp

fig-26 | §3.3 | Base64 Logit Weights and Their Conditional Strip
    takeaway: For the base64 feature A/1/2357, the tokens whose logits it raises shade gradually from ordinary text tokens into base64-specific ones, rather than separating into two clean groups. The narrow strip below the histogram m
    furniture: Logit Weight (x-axis); Density (y-axis); Log-Likelihood Ratio (legend)

fig-27 | §3.3.1 | The Base64 Feature Found Again in Model B
    takeaway: A separately trained transformer, decomposed by a separately trained dictionary, yields a feature (B/1/2165) whose activations correlate 0.85 with the base64 feature from the first model, and which fires almost exclusive
    furniture: Activation Level (horizontal axis); Density (vertical axis); 0 and >0 marks at the left of the axis; Prefix String Log-Likelihood Ratio (color legend title); Prefix String (in the legend title); Blue end of the color bar

fig-28 | §3.3.1 | B/1/2165's Base64 Logit Mode
    takeaway: In the second model's dictionary, the feature that matches the base64 feature does the same thing to the output that the original does: its logit-weight histogram has a small second mode, centered near +0.32, made almost
    furniture: Logit Weight (x-axis); "Weight from feature to logits" (x-axis subtitle); Density (y-axis); Token Log-Likelihood Ratio (legend title); "base64 vs token unigram distribuiton" (legend subtitle, typo in the original); Blue end of the colour bar

fig-29 | §3.3.1 | Base64 Feature, Matched Across Two Runs
    takeaway: The base64 feature found in model A (A/1/2357) and the one found in a separately seeded model B (B/1/2165) line up on both axes the paper uses to compare features: their activations across text correlate at 0.85, and the
    furniture: (none named)

fig-30 | §3.3.2 | The Base64 Feature's Nearest Neuron
    takeaway: The MLP neuron whose activations track the base64 feature most closely, A/neurons/470, correlates with it at only 0.18, and its own activation distribution stays mostly blue — mostly non-base64 — at every activation leve
    furniture: Blue-to-red color scale

fig-31 | §3.3.2 | Neuron 470's Mixed Logit Weights
    takeaway: The neuron most correlated with the base64 feature does push base64 tokens up a little, but the tokens it pushes up hardest are filename endings like 'htm', 'png' and 'pdf', which are not base64-specific. The paper reads
    furniture: Logit Weight (x-axis); Density (y-axis); Log-Likelihood Ratio (color bar); Blue end of the color scale; Red end of the color scale; Stacked coloring within the histogram

fig-32 | §3.3.2 | Base64 Feature Against Neuron 470
    takeaway: The neuron whose activations most resemble the base64 feature's shares only part of its behaviour: an activation correlation of 0.18 and a logit-weight correlation of 0.64, with most of the mass in both plots sitting off
    furniture: Run B (in the left x-axis label); Feature Activaitons (Run A) — y axis, left panel; Feature Activations (Run B) — x axis, left panel; Logit Weight (Run A) — y axis, right panel; Logit Weight (Run A Neuron) — x axis, right panel; Orange and pale intermediate colors

fig-33 | §3.4 | The Hebrew Feature's Activation Spectrum
    takeaway: A histogram of how often feature A/1/416 takes each activation value, with each slice split by an independent Unicode-based score for "is this Hebrew". Strong activations are almost entirely Hebrew, which is the paper's 
    furniture: Density (y axis); Activation Level (x axis); Log-Likelihood Ratio (colour legend title); Hebrew → (label on the colour legend); Blue (negative end of the colour scale); Red (positive end of the colour scale)

fig-34 | §3.4 | Hebrew Feature Logit Weights
    takeaway: The Hebrew feature's effect on the model's output is concentrated in a small second bump far to the right of the main mode, and the tokens in that bump are Hebrew letters plus the byte `\xd7`. Because `\xd7` is the share
    furniture: Logit Weight (x axis); Density (y axis); Token Log-Likelihood Ratio (color bar label); "Hebrew →" (label above the color bar); Blue (color); Red (color)

fig-35 | §3.4.1 | The Hebrew Feature Against Its Closest Neuron
    takeaway: The neuron whose activations track the Hebrew feature most closely does not behave like the feature: the correlation is 0.1 on activations and 0.38 on logit weights, and in both plots the feature's axis cleanly separates
    furniture: Feature Activaitons (Run A) — left y-axis; Activations (Run A Neuron) — left x-axis; Logit Weight (Run A) — right y-axis; Logit Weight (Run A Neuron) — right x-axis; Red versus blue point color; 0 and >0 axis markers (left panel)

fig-36 | §3.4.2 | Hebrew Feature Found Again in Model B
    takeaway: A dictionary trained on a second, independently seeded transformer contains a feature, B/1/1901, whose activation histogram looks like the Hebrew feature's: the strong end of its firing range is dominated by tokens a Uni
    furniture: Log-Likelihood Ratio (legend title); Hebrew (legend label with arrow); Blue end of the colour scale; Red end of the colour scale; Density (vertical axis); Activation Level (horizontal axis)

fig-37 | §3.4.2 | B/1/1901's Hebrew Logit-Weight Mode
    takeaway: For the Hebrew feature found in the second transformer's dictionary (B/1/1901), the distribution of logit weights over the vocabulary has the same shape reported for the first transformer's Hebrew feature: one large mode
    furniture: Logit Weight (x-axis); Density (y-axis); Token Log-Likelihood Ratio (legend axis); Blue-to-red colormap

fig-38 | §3.4.2 | Hebrew Feature Across Two Models
    takeaway: The Hebrew feature from run A has a counterpart in a second, independently trained run that fires on nearly the same tokens (activation correlation 0.92) but whose effects on next-token logits agree far less (0.3). The p
    furniture: Run A / Run B (axis qualifiers); Feature Activations (Run B) — left x-axis; Feature Activations (Run A) — left y-axis; Logit Weight (Run B) — right x-axis; Logit Weight (Run A) — right y-axis; 0 and >0 (left-panel axis break)

fig-39 | §4.1.1 | Hand-scored interpretability, features against neurons
    takeaway: A blinded human rater scored features and MLP neurons on the same 0–14 interpretability rubric, and the two score distributions barely overlap: the median neuron scores 0, meaning no hypothesis could be formed at all, wh
    furniture: (none named)

fig-40 | §4.1.2 | Autointerp Activation Scores
    takeaway: A language model is asked to write an explanation of a unit from examples where it fires, then to predict that unit's activations on unseen text using nothing but its own explanation. Scored as the rank correlation betwe
    furniture: Spearman Correlation (x-axis); Density (y-axis); Features (crimson legend box); Neurons (teal legend box); median (legend row); mean (legend row)

fig-41 | §4.1.3 | Logit-Weight Scores, Features vs Neurons
    takeaway: Given only a written explanation of a unit, Claude predicts which unseen tokens that unit promotes with 0.74 mean accuracy for autoencoder features and 0.58 for MLP neurons, against a 0.5 chance baseline. The neuron dist
    furniture: (none named)

fig-42 | §4.1.4 | Interpretability Across the Activation Spectrum
    takeaway: Scoring each band of a unit's activation range separately, the paper's human annotator judged strong activations far more consistent with a feature's interpretation than weak ones. Autoencoder features average above the 
    furniture: Dataset Example Activation Level (x axis); Rubric Score (y axis); Interval Consistency (y axis subtitle)

fig-44 | §5.2 | Three Dictionaries in One UMAP
    takeaway: The families the authors found by reading text — base64 features, Arabic script features — are also families in geometry: their dictionary vectors point in similar directions. Embedding three dictionaries of different si
    furniture: Axes (unlabelled)

fig-45 | §5.2 | One Region, More Arrows
    takeaway: A hand-drawn schematic of what the paper means by feature splitting: widening the autoencoder does not send new features off to new parts of activation space, it packs more dictionary directions into the same narrow cone
    furniture: (none named)

fig-46 | §5.2.1 | The Mathematics and Physics Splitting Graph
    takeaway: Three autoencoders of increasing width, trained on the same one-layer transformer, are lined up side by side, and the technical-writing features of the smallest run are traced into their relatives in the larger ones. The
    furniture: (none named)

fig-49 | §5.2.2 | The Digit Feature That Won't Predict Digits
    takeaway: Two base64 features from the same dictionary push almost the same tokens up, except that the feature firing on single digits gives the ten digit tokens far less of a boost than its letter-firing sibling does. The paper r
    furniture: Logit Weight (A/1/2357 - base64 letters) — x-axis; Logit Weight (A/1/2364 - base64 digits) — y-axis

fig-51 | §5.3.1 | Activation Similarity vs Logit Weight Similarity
    takeaway: The figure sets out the paper's two model-independent ways of asking whether the same feature was found twice, and shows that for the Arabic script feature they disagree: activation correlation between A/1/3450 and B/1/1
    furniture: Feature Activations (Run A) — y-axis, left panel; Feature Activations (Run B) — x-axis, left panel; Logit Weight (Run A) — y-axis, right panel; Logit Weight (Run B) — x-axis, right panel; Marginal histograms (bars along each axis)

fig-52 | §5.3.1 | Feature vs Neuron Match Histograms
    takeaway: When you look for each item's closest counterpart in a second, independently trained one-layer transformer, autoencoder features match far better than raw MLP neurons: the typical feature's best match has activation corr
    furniture: Activation Correlation to Most Similar Feature/Neuron (x axis); Density (y axis); Features (crimson legend chip); Neurons (teal legend chip)

fig-53 | §5.3.1 | Matched Features That Fire Alike But Write Differently
    takeaway: For thousands of feature pairs matched across two independently trained models, agreement on *when* the features fire is much higher than agreement on *what* they push the next-token logits toward. The paper reads this g
    furniture: Activation Correlation (x-axis); Logit Weight Correlation (y-axis); Logit Weight (Run A) — inset y-axis; Logit Weight (Run B) — inset x-axis

fig-54 | §5.3.1 | Attribution vs Activation Correlation
    takeaway: For each feature in one dictionary and its best-matching feature in a dictionary learned from a separately trained transformer, the plot compares how similarly the pair fires with how similarly the pair contributes to pr
    furniture: Activation Correlation (x axis); Attribution Correlation (y axis)

fig-57 | §5.4 | Unicode Prefix–Suffix Feature Loops
    takeaway: Where a script's characters are tokenized as a prefix byte plus a suffix byte, the paper finds a pair of features that hand off to each other: each one's top predicted tokens are exactly the tokens the other one fires on
    furniture: (none named)

fig-58 | §5.4 | The Four-Node HTML Automaton
    takeaway: Four features in the smallest dictionary hand off to each other through the token stream — open bracket, tag name, closing bracket, newline-and-indent, back to open bracket — so that together they behave like a small sta
    furniture: (none named)

fig-62 | §7.1 | Two Alternatives to 1-D Features
    takeaway: A pair of sketches arguing that "features are higher-dimensional manifolds" is not one hypothesis but two. One version — the convex hull of a few features that fire together — is just correlated one-dimensional features 
    furniture: (none named)

fig-63 | §8 | Decoder Sparsity in the Neuron Basis
    takeaway: In an outside replication, 92% of the learned feature directions turn out to be spread across many MLP neurons rather than concentrated in one neuron or ten. The scatter plot sorts every feature by how much of its decode
    furniture: (none named)

fig-64 | §15 | The Two Density Modes of A/16
    takeaway: Plotting how often each learned feature fires, on a log axis, splits the features of run A/16 into two humps: a spike of features that fire on roughly one token in ten million, and a broad bulk centred near one token in 
    furniture: log_10(Feature density) (x axis label); Counts (y axis label); Histogram bin

fig-65 | §15 | Overlapping Density Clusters in Run A/5
    takeaway: The paper's quick diagnostic — read two separated humps off a log-scale histogram of how often each feature fires — stops working at the largest autoencoder sizes. In run A/5, with 131,072 hidden units, the ultralow-dens
    furniture: log_10(Feature density) (x axis); Counts (y axis)

fig-66 | §15 | Three Diagnostics That Isolate the High-Density Cluster
    takeaway: In a run where feature density alone leaves the two modes overlapping, plotting each feature's density together with its encoder bias and its decoder-encoder dot product pulls the high-density cluster out as a compact, s
    furniture: Bias (vertical axis); Density (depth axis); D/E projection (horizontal axis); Point color

fig-67 | §15 | Density Histograms Across Dictionary Sizes
    takeaway: Across five sparsity settings, each panel overlays the feature-density histograms of six autoencoders whose dictionaries run from 512 up to 131,072 units. Widening the dictionary does not just yield more features: it kee
    furniture: log_10(Feature density) (x axis); Counts (y axis); N Learned Sparse (legend label)

fig-68 | §19 | Sparsity Against Explained Behavior
    takeaway: Plotting how sparse each trained dictionary is against how much of the MLP layer's loss contribution it recovers turns hyperparameter choice into a visible tradeoff: a weaker sparsity penalty explains more of the model's
    furniture: Average L0 norm (x axis); Percent of MLP Loss Contribution Explained (y axis); L1 coefficient (legend title)

fig-69 | §22.1 | Pearson vs Spearman Scoring Panel
    takeaway: The paper re-runs its automated interpretability scoring with Pearson correlation alongside the Spearman correlation used throughout the main text, and the two give visibly the same distributions and the same ordering: d
    furniture: (none named)

fig-70 | §22.2 | The Randomized-Transformer Control
    takeaway: On the paper's automated activation score, features found in a random-weight transformer get a slightly higher median than the real features do. Splitting the randomized features on whether they are "single token" shows 
    furniture: medians (triangle legend); Spearman Correlation (x-axis, left panel); Density (y-axis); Count (y-axis, right top panel); Accuracy (x-axis, middle panel); Rubric Value (x-axis, right panel)

fig-71 | §22.3 | Importance-Weighted Interpretability Scores
    takeaway: Once the automated-interpretability scores are reweighted to what random text would have given, features still beat neurons (median 0.28 vs 0.04) but both distributions collapse toward zero. The paper's explanation is th
    furniture: medians (triangle legend)

fig-72 | §22.4 | Explanation Length Distributions
    takeaway: A prompting bug sometimes let Claude's whole chain of thought stand in as a feature's explanation, producing explanations several hundred tokens long instead of a few dozen. This figure shows that the long tail appears i
    furniture: (none named)