PAGE FOR CONCEPT: verbal-report
NAME: Verbal report from the J-space

SUMMARY (already shown above your page as the lede - do not repeat it verbatim):
The property that, when asked what it is thinking about, the model names concepts represented in the workspace, and swapping one active workspace vector for another changes its answer to match.

DRAFT EXPLANATION from the extraction stage (your page replaces this; keep what is right, go deeper):
All five workspace properties now share the same parent so the property checklist reads as one family.

THEME IT BELONGS TO: theme-five-workspace-properties | Five things the workspace does - This chapter takes the five properties of the paper's definition in turn. The model names concepts its workspace holds, loads the workspace on instruction, and carries the intermediate values of chained reasoning in it; one workspace vector also serves as an argument to many different downstream computations. Only a fraction of the model's behavior depends on the workspace at all, which the paper shows by suppressing it and recording which tasks survive and which collapse. It follows the instrument because each of these tests is a lens reading followed by an intervention on the same directions.

SUB-CONCEPTS (these render as expandable sections BELOW your page - reference them, do not duplicate them):
  - injected-thought-introspection | Injected-thought introspection protocol: A protocol in which the model is told a thought may have been implanted in its activations and is asked to report what, if anything, it detects.
    - user-turn-concept-injection | Injecting a concept across the user turn: A single J-lens vector is added at positions on the user prompt rather than in the model's own turn, and the model then reports that concept when asked to introspect.
      - report-time-selectivity | Selectivity of the injected concept to the moment of report: A concept injected on the user turn does not leak into earlier assistant text; it surfaces only where the introspective report is being given.
  - think-of-a-category-task | Think-of-a-category task: A prompt asking the model to think of an item from a named category and then name it in one word; the lens is read just before the name is produced.
    - candidate-ranking-agreement | Candidate ranking agreement between lens and logits: For each of fourteen categories, ten candidate answers are ranked twice - by their strength in the J-lens readout and by their next-token logits - and the two orderings are compared with a Spearman correlation.
    - out-of-top-10-swap-targets | Swap targets drawn from outside the top 10: The item swapped in is always one the model was not already likely to say - drawn from the same category but excluded if it sat in the top 10 of the output distribution.
  - report-relation-by-construction | Report relation expected by construction: The J-lens is built from the causal effect of activations on output tokens, so some agreement between its readouts and what the model says is guaranteed in advance rather than discovered.
  - j-space-privilege-for-report | Privilege of the J-space component for report: Both report experiments are rerun with a concept vector's J-space component and its non-J-space remainder substituted for the J-lens vectors, at equal magnitude; the small J-space part nearly matches the lens vectors while the large remainder barely works.
    - j-space-mediation-clamp | Clamping the J-space to test mediation: The non-J-space conditions are rerun with the relevant J-lens coordinates held at their clean-pass values, so the injected concept cannot be re-derived into the J-space; the remaining effect then vanishes.

PREREQUISITES:
  - j-space-component-decomposition | J-space component decomposition: Splitting a concept's representation into a small J-space component and a large non-J-space remainder, to test which part carries causal effects.
  - jacobian-lens | Jacobian lens (J-lens): The paper's new interpretability technique: for each vocabulary token, it identifies the vector direction that encodes the model's potential to verbalize that token, by averaging the linearized effect of activations on the token's likelihood over a large corpus.
  - lens-coordinate-swap | Lens-coordinate swap: An intervention that exchanges one concept for another: it reads an activation's coordinates along two J-lens vectors, permutes them, and writes the result back, leaving everything orthogonal to those vectors unchanged.
  - workspace-like-properties | The five workspace-like properties: The paper's operational definition: a set of vector representations counts as workspace-like if it supports verbal report, directed modulation, internal reasoning, flexible generalization, and selectivity.

EDGES TOUCHING THIS CONCEPT (3) - the relationships the app already knows; weave the load-bearing ones into your prose:
  [load-bearing] -> fig-6 (The Soccer-to-Rugby Swap (fig-6)) | supported-by | report follows workspace contents: At the colon just before the model names the sport it thought of, Soccer already sits high in the lens readout, and exchanging the Soccer coordinate for Rugby makes the model answer Rugby. Across fourteen categories the lens ordering of ten candidate answers tracks the next-token logit ordering, and swaps move targets that began below rank 11 to the top of the output.
  [load-bearing] <- anomaly-detection-condition (Anomaly detection under a swapped language) | contrasts-with | same swap moves report only: The same Spanish-to-French lens swap is applied across the question tokens in four tasks over one passage. Explicit report and the flexible questions flip to the French answer on essentially every trial, while continuation and anomaly detection are unmoved. The word Spanish appears in the readouts at comparable rates in all four conditions, so presence in the workspace and causal role are separate things.
  [load-bearing] <- report-relation-by-construction (Report relation expected by construction) | contrasts-with | guaranteed agreement versus causal evidence: The J-lens is built from the average causal effect of activations on output tokens, so some agreement between its readouts and what the model says is guaranteed by construction. The verbal-report property therefore cannot rest on agreement alone. It rests on the swaps: exchanging one active workspace vector for another changes which item the model names.

EVIDENCE ITEMS THAT SHOW THIS (link them as [[item-id]]):
  - fig-1 | The Five Workspace Tests: The figure states the paper's operational definition of a global workspace in a language model: a privileged subset of vector representations counts as workspace-like if it passes five functional tests — verbal report, directed modulation, internal reasoning, flexible generalization, and selectivity. Each test is paired with a stylized picture of a concrete experiment, run in full later in the paper.

PAPER SECTIONS TO READ (in papers/global-workspace/data/ingest/sections/):
  - 02-1.1-motivation-conscious-access-and-the-global-workspace.txt
  - 03-1.2-a-global-workspace-in-language-models.txt
  - 14-3.1-the-j-space-supports-verbal-report.txt
