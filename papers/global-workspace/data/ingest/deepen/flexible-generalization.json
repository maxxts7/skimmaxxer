{
  "concepts": [
    {
      "id": "prompt-independent-swap",
      "name": "Prompt-independent swap",
      "parent": "flexible-generalization",
      "summary": "The design choice that turns the swap experiment into a test of broadcast: the same lens-coordinate swap is applied unchanged in every prompt, and each downstream function is left to interpret it.",
      "explanation": "The vector subtracted and the vector added are fixed by the source and target arguments alone - France out, China in - and do not vary with the template the argument sits in. The prompt supplies the function (\"the capital of France is,\" \"most people in France speak,\" \"France is on the continent of\"), and nothing in the intervention tells the model which function is being applied. A representation readable only by the circuit that produced it would require an intervention tuned to each consuming circuit; broadcast predicts that one intervention serves all of them. The swap is clamped at every token position across a band of intermediate layers, with formatting tokens that carry capitalization excluded. Success is then read off the model's answer in each template separately.",
      "prerequisites": ["lens-coordinate-swap", "function-template-protocol", "activation-clamping"],
      "sectionIds": ["1.2", "3.4"],
      "floor": false,
      "citedFrom": null
    },
    {
      "id": "swap-grid-diagonal-baseline",
      "name": "Swap grid and its diagonal baseline",
      "parent": "flexible-generalization",
      "summary": "The 4x4 grid used to report every individual swap trial for a function template: rows are the argument written in the prompt, columns the argument whose lens coordinates were swapped in, so the diagonal is the unmodified run.",
      "explanation": "The appendix reports the trials themselves rather than only the aggregate. Each function template gets one grid; a row fixes which argument appears in the prompt text and a column fixes whose lens coordinates were written in, so the twelve off-diagonal cells are the swap pairs and the four diagonal cells are self-swaps that reproduce the unmodified forward pass and act as a baseline. Each cell shows the model's top-1 token after the swap, with a small grey number giving the rank of the target-appropriate answer when it is not first, and each column sub-heading names the answer the swapped-in concept should produce. The spread across categories can be read straight off the grids: country facts reach rank 1 in 42 of 48 off-diagonal cells, months partially, animals rarely, and number relations in 0 of 48 at $\\alpha = 1$.",
      "prerequisites": ["function-template-protocol", "lens-coordinate-swap", "swap-success-rate"],
      "sectionIds": ["10.13"],
      "floor": false,
      "citedFrom": null
    },
    {
      "id": "partial-swap-rank-evidence",
      "name": "Partial swaps read off rank",
      "parent": "swap-grid-diagonal-baseline",
      "summary": "The paper's reading of its own failures: in most failed trials the target answer is not absent but ranked below the original answer, which it takes as a swap that moved the activation in the right direction but not far enough.",
      "explanation": "Among the failed trials the model's top-1 output is typically still the correct answer for the argument actually written in the prompt, while the swapped-in target's answer appears further down the output ranking. The paper treats the outcome as graded rather than binary and reads the residual rank as an under-strength intervention rather than an inert one. Its support is that doubling the swap strength often brings the target answer to the top, raising the count from 76 of 192 to 101 of 192. The evidence fits that reading, but the section reports no control separating it from the alternative that the rank shift reflects generic disruption of the forward pass.",
      "prerequisites": ["swap-strength-alpha", "swap-success-rate"],
      "sectionIds": ["3.4", "10.13"],
      "floor": false,
      "citedFrom": null
    },
    {
      "id": "number-word-failure-interpretation",
      "name": "Two readings of the number-word failure",
      "parent": "flexible-generalization",
      "summary": "Number-word arguments have the lowest workspace loading and swap worst, and the paper offers two explanations it cannot separate.",
      "explanation": "Number relations never succeed at $\\alpha = 1$, the weakest result of the four argument categories. One reading is that the model computes over small integers outside the workspace, so there is no workspace vector for the swap to act on. The other is that the model's working representation of small integers is real but does not align with the J-lens vectors for the corresponding number tokens, which would make the failure an instance of the vocabulary-restriction limitation the paper discusses elsewhere: the lens is anchored to single tokens, so a quantity the model holds in some other format cannot be addressed through it. The section states both and does not adjudicate between them.",
      "prerequisites": ["workspace-loading", "j-lens-vector", "single-token-limitation"],
      "sectionIds": ["3.4"],
      "floor": false,
      "citedFrom": null
    },
    {
      "id": "swap-effect-magnitude",
      "name": "Magnitude of swap effect",
      "parent": "flexible-generalization",
      "summary": "The graded measure plotted against workspace loading - how far the swap shifts the model's output towards the target's answer - as distinct from the binary top-1 criterion behind the headline counts.",
      "explanation": "The headline numbers, 76 of 192 and 101 of 192, count a trial as a success only when the target-appropriate answer reaches the top of the output distribution. The right panel of the results figure instead plots a continuous magnitude of effect against the argument's workspace loading, so trials that moved the output without winning still contribute to the relationship. On that basis the section reports that workspace loading predicts swap success well, with country arguments loading highest and swapping most reliably and number words loading lowest and swapping poorly. How the magnitude is computed is left to the figure; the main text does not spell it out.",
      "prerequisites": ["workspace-loading", "swap-success-rate", "cosine-similarity"],
      "sectionIds": ["3.4"],
      "floor": false,
      "citedFrom": null
    }
  ]
}
