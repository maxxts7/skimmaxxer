SUMMARY BRIEF - Verbalizable Representations Form a Global Workspace in Language Models

One flat page carrying the whole argument, read top to bottom, in one
sitting. Same floor as every other surface: use the real names. Gloss a
term in a clause where the argument leans on it, then keep going - never
paraphrase the vocabulary away to avoid explaining it.
What makes this a summary is shape, not level: the line of reasoning
only, no figures, no run names, no evidence apparatus.

=== COVERED BY THE FLOOR: THESE COME FOR FREE ===
  activations | Activations: The internal vector states a network computes at each layer and position during a forward pass.
  auc | AUC: Area under the ROC curve: the probability a classifier scores a random positive above a random negative.
  chain-of-thought | Chain-of-thought: Prompting or allowing a model to write out intermediate reasoning steps before its final answer.
  cosine-similarity | Cosine similarity: The cosine of the angle between two vectors, used as a scale-free measure of alignment between activations and lens vectors.
  jacobian-matrix | Jacobian matrix: The matrix of partial derivatives of one vector-valued quantity with respect to another; the linear map that best approximates the effect of a small perturbation.
  kl-divergence | KL divergence: The standard measure of how much one probability distribution diverges from another, used here to quantify shifts in the model's output distribution.
  kurtosis | Kurtosis: A statistic measuring how heavy-tailed or sharply peaked a distribution is; excess kurtosis is measured relative to a Gaussian.
  median-reciprocal-rank | Median reciprocal rank: A retrieval-style metric: the reciprocal of a target's rank, with the median taken over trials.
  next-token-prediction | Next-token prediction: The pretraining objective: given a context, output a probability distribution over the vocabulary for the next token.
  pseudoinverse | Pseudoinverse: The generalization of the matrix inverse to non-square matrices, used here to read an activation's coordinates along a pair of J-lens vectors.
  spearman-correlation | Spearman correlation: A rank correlation: how well the ordering of one list matches the ordering of another.
  tokenizer-vocabulary | Tokenizer vocabulary: The fixed set of tokens, usually words and word pieces, into which a model's tokenizer splits text; the unit the J-lens is built over.
  wilson-interval | Wilson 95% interval: A confidence interval for a proportion, used for the paper's success-rate error bars.

=== THE SPINE: WHAT THE STORY COVERS, IN ITS ORDER ===
  story title: The Words a Model Does Not Say
  1. The line the paper looks for
       A person can say what they are currently thinking about. Most of what a brain does never becomes reportable in that way: the parsing of a sentence, the control of a hand, the routine machinery underneath. The functional 
  2. One matrix per layer
       Every result in the paper is a reading taken with a single instrument, so the instrument comes first. Consider the [[residual-stream]] — the running vector each layer reads from and writes to — at layer $\ell$ and token 
  3. Naming what it holds
       The first test is whether the model names what the lens says it is carrying. In the [[think-of-a-category-task]] the model is told to think of an item from a category and then name it in one word, and the lens is read at
  4. The step it never writes
       Report is one thing; unspoken intermediate steps are another. Given "The number of legs on the animal that spins webs is", the model must infer spider before it can answer 8. The word appears nowhere in the prompt or the
  5. What suppressing it does not break
       The fifth property is the one that keeps the claim from being vacuous. If everything ran through the workspace, calling it privileged would mean nothing. The [[language-passage-tasks]] put one latent variable to four use
  6. Where it sits, and how little it holds
       Function established, the paper turns to the workspace as an object: where in depth it operates, how much it holds, and whether the weights are arranged to spread its contents. Depth divides into three [[layer-bands]]. T
  7. Reading a scenario the model does not narrate
       A model can recognize that a scenario is staged, weigh a coercive option, or act on an objective it was trained to hide, without any of that reaching the text it writes. Section 5 uses the lens as a safety instrument on 
  8. The Assistant arrives
       [[post-training]] is the stage that turns a next-token predictor into the [[assistant-persona]]. Section 6 asks what it does to the workspace, comparing a production model against its own [[base-model]] on prompts where 
  9. What was shown, and what was not
       The solid part of the paper is causal and controlled. A single averaged matrix per layer yields directions that can be read, exchanged and removed. Exchanging them changes verbal reports, unspoken intermediate steps and 

=== CONCEPT THEMES, IN READING ORDER ===
  [1] theme-the-workspace-claim | The workspace claim: The paper starts from a neuroscience theory: information becomes consciously accessible when it is posted to a shared, capacity-limited workspace and broadcast to many processes at once. It then makes its central claim, that a language model maintains a comparable privileged subset of vector represe
  [2] theme-the-jacobian-lens | The Jacobian lens: The paper's instrument is one matrix per layer, built by averaging the Jacobian from that layer to the model's output over a corpus of prompts. It yields one vector per token in the vocabulary: the direction that, on average, disposes the model to say that token. This chapter covers how the matrix i
  [3] theme-five-workspace-properties | Five things the workspace does: This chapter takes the five properties of the paper's definition in turn. The model names concepts its workspace holds, loads the workspace on instruction, and carries the intermediate values of chained reasoning in it; one workspace vector also serves as an argument to many different downstream com
  [4] theme-shape-of-the-workspace | Where the workspace sits and what it holds: Where the previous chapter asks what the workspace does, this one asks where in the model it sits and how much it can hold. The J-space carries workspace-like content only in a middle band of layers: it is nearly empty for roughly the first third of depth, and gives way to output-bound representatio
  [5] theme-silent-cognition-in-safety-evaluations | Silent cognition in safety evaluations: Here the lens is used as a safety instrument, on models whose visible text does not say what they are doing. It surfaces recognition of being evaluated, deceptive intent in audit scenarios before any deception-adjacent word is written, and the implanted objectives of models deliberately trained to b
  [6] theme-post-training-and-the-assistant | The Assistant's workspace, and training it: Post-training changes what the workspace holds. The paper reports that a post-trained model's workspace carries the Assistant's own reactions and appraisals of its behavior, including flags on out-of-character output and an unvoiced objection to a forced choice, where the base model's does not; the 
  [7] theme-consciousness-theories-and-open-questions | Consciousness theories and open questions: The discussion maps the findings onto functional theories of consciousness — global workspace, higher-order, attention schema and recurrent processing — and declares the results neutral on theories that hinge on physical substrate. It also sets out where the analogy is weakest: the model's workspace
  [8] theme-reading-the-charts | How to read this paper's charts: The paper argues with a small number of chart shapes, each repeated across many figures. This chapter explains each shape once: what it plots, why that shape was chosen, how to read it, and what it would look like if the paper's claim were false. It teaches how to read the evidence rather than what 

=== MAJOR CONCEPTS (each has a page; link the first mention) ===
  alignment-auditing | Alignment auditing with the J-lens: Using J-lens readouts to inspect a model's silent strategic and situational cognition in safety evaluations, where the relevant reasoning does not appear in the output.
  assistant-point-of-view | The Assistant's point of view in the J-space: The finding that post-training causes the J-space to acquire the Assistant's perspective: Assistant reactions and self-monitoring appear in the workspace even while the m
  counterfactual-reflection-training | Counterfactual Reflection Training: A fine-tuning technique that trains a model to produce constitution-grounded reflections when asked, and thereby reshapes its workspace and behavior in contexts where it 
  evaluation-awareness | Evaluation awareness: The finding that the workspace sometimes encodes recognition of being in an evaluation (fake, fictional), and that ablating these representations can surface malicious pr
  global-workspace-theory | Global workspace theory: A neuroscience theory holding that information becomes consciously accessible when it is posted to a shared, capacity-limited workspace that broadcasts it to many special
  j-space | J-space: The set of activation-space points expressible as a sparse nonnegative combination of J-lens vectors — the region of representation space the paper argues functions as a 
  j-space-ablation | J-space ablation: An intervention that suppresses the workspace wholesale: at each position, the residual stream's projections onto the top-k most active J-lens vectors are zeroed across a
  jacobian-lens | Jacobian lens (J-lens): The paper's new interpretability technique: for each vocabulary token, it identifies the vector direction that encodes the model's potential to verbalize that token, by a
  layer-bands | Sensory, workspace, and motor layer bands: The paper's division of the model's depth into three functional regions: an early sensory band where the J-space is empty, a middle workspace band carrying persistent abs
  lens-coordinate-swap | Lens-coordinate swap: An intervention that exchanges one concept for another: it reads an activation's coordinates along two J-lens vectors, permutes them, and writes the result back, leaving 
  llm-global-workspace-hypothesis | A global workspace in language models: The paper's central claim: language models maintain a privileged subset of vector representations that plays a computational role analogous to the brain's global workspac
  reading-the-evidence | Reading this paper's charts: The handful of chart shapes this paper argues with, each explained once: why that shape was chosen, how to read it, and what it would look like if the claim it supports w
  theories-of-consciousness | Theories of consciousness and the J-space: The discussion's mapping of the J-space findings onto major functional theories of consciousness: global workspace, higher-order, attention schema, and recurrent processi
  j-space-broadcast | Broadcast privilege of J-lens vectors: The structural claim that the model's weights are configured to distribute J-space content disproportionately widely — via MLPs across depth and attention heads across to
  j-space-capacity | J-space capacity: The finding that the J-space is limited: it holds on the order of tens of concepts at a time, carries under 10% of activation variance, and excludes most of the model's r
  workspace-like-properties | The five workspace-like properties: The paper's operational definition: a set of vector representations counts as workspace-like if it supports verbal report, directed modulation, internal reasoning, flexib
  ambiguity-commitment-plot | Ambiguity commitment plot: A readout of how far an activation has moved between two blended concepts as the input blend is swept, plotted against depth. It is the paper's test of whether the model 
  concept-token-detector-plot | Concept-token detector plot: A fixed list of words is looked for in the top-k J-lens readout at every workspace layer, and the hits for one prompt are summed into a single count; dots are prompts, ba
  intervention-vs-control-bars | Score-under-intervention bar chart: Bars giving a rate or score measured while an intervention is applied to the model, printed beside a control perturbation matched in size, layer and position, with per-it
  layer-position-readout-trace | Layer-by-layer lens readout trace: A grid of (token position × layer) cells taken from one instrumented forward pass, each cell printing the top-ranked J-lens token for that activation, with hover for the 
  lens-attribution-graph | J-lens attribution graph: A wiring diagram of a single forward pass whose nodes are J-lens vectors picked out by sparse decomposition and whose edges are one-layer gradient attributions, paired wi
  method-schematic | Method and convention schematic: A drawing that defines rather than measures: how the Jacobian lens is built, read and written, which layers the paper calls the workspace, and how strong each ablation do
  ranked-readout-panel | Ranked lens-readout panel: A column of the top few vocabulary tokens read off one object — an activation at a marked position, or a static weight direction — repeated under two or three conditions 
  swap-success-chart | Swap-success chart: A pass/fail scoreboard for the lens-coordinate swap: the fraction of trials on which exchanging two concepts' coordinates along lens directions moved the swapped-in conce
  three-lens-comparison | Three-lens benchmark comparison: A fixed three-way comparison — Jacobian lens in black, logit lens in red, tuned lens in blue — repeated across benchmarks, metrics and swept variables. It exists to test 
  token-shaded-transcript | Token-shaded transcript: A passage or a set of model responses printed in full, with every unit shaded by a measured scalar and hoverable for the number behind it, so a reader can see where in th
  turn-paired-rank-plot | User-turn vs Assistant-turn rank plot: Four bars in two pairs: how highly a chosen concept word ranks in the J-lens readout while the model is reading the user's message, and while it is writing its own respon
  weights-only-population-comparison | Weights-only population comparison: A curve per population of candidate directions, plotted against source layer, showing how strongly the model's weights amplify, connect to or compose with each population
  workspace-band-profile | Workspace-band profile: A statistic of the J-space's contents plotted against layer depth — or, in the word-list case, against position in a running list — with percentile bands and a random or 
  directed-modulation | Directed modulation: The property that the model can deliberately activate and compute with workspace vectors when instructed, independent of its outputs, and can pull normally absent informa
  flexible-generalization | Flexible generalization (broadcast): The property that the same workspace vector serves as a valid argument to many different downstream computations: lifted from one context and placed in another, it is cor
  flexible-vs-automatic-dissociation | Flexible versus automatic cognition: The paper's proposed criterion for when a computation engages the J-space: it does when an intermediate result must be handed to an arbitrary, context-specified downstrea
  internal-reasoning | Internal reasoning: The property that workspace vectors carry the values of intermediate computations when the model chains inferential steps, and that intervening on them redirects the conc
  verbal-report | Verbal report from the J-space: The property that, when asked what it is thinking about, the model names concepts represented in the workspace, and swapping one active workspace vector for another chang

=== SUPPORTING CONCEPTS YOU MAY ALSO LINK ===
  access-consciousness | Access consciousness
  activation-patching | Activation patching
  anthropic-constitution | Anthropic's constitution
  assistant-persona | The Assistant
  attribution-graphs | Attribution graphs
  attribution-patching | Attribution patching
  backward-lens | Backward lens
  claude-model-family | Claude model family (Haiku, Sonnet, Opus)
  deliberative-alignment | Deliberative alignment
  entity-slots | Entity slots
  experiential-reports | Experiential-report experiments
  feedforward-emulation-of-recurrence | Feedforward emulation of recurrence
  free-text-activation-decoders | Free-text activation decoders
  function-vectors | Function and task vectors
  future-lens | Future lens and shortcut probes
  gaze-heads | Gaze heads
  implicit-chain-of-thought | Implicit chain-of-thought
  j-lens-open-source-release | Code release and Neuronpedia demo
  layer-percentage-indexing | Layer numbering as percentages
  linear-probe | Linear probe
  linear-representation-hypothesis | Linear concept representations
  llm-grader | LLM grader
  llm-introspection-research | LLM introspection research
  local-linearization | Local linearization
  logit-lens | Logit lens
  mechanistic-interpretability | Mechanistic interpretability
  metacognition | Metacognition
  metacognitive-representations | Metacognitive tokens in the J-space
  multi-hop-entity-decoding | Multi-hop intermediate decoding
  natural-language-autoencoder | Natural-language autoencoder (NLA)
  open-questions | Open questions on scale and emergence
  out-of-context-reasoning | Out-of-context reasoning
  post-training | Post-training
  prefill | Prefill
  relation-jacobians | Per-relation Jacobian maps
  residual-stream | Residual stream
  reward-hacking | Reward hacking
  reward-model | Reward model
  rlhf | RLHF
  sparse-autoencoder | Sparse autoencoder (SAE)
  stable-regions | Stable regions
  steering-vectors | Steering vectors
  superposition | Superposition
  synergistic-core | Synergistic core
  thinking-in-words | Thinking in words
  thought-suppression | Thought-suppression experiments
  transcoder | Transcoders
  transformer | Transformer
  tuned-lens | Tuned lens
  two-time-dimensions | Two time dimensions
  weight-vocabulary-projection | Vocabulary projection of weights
  working-memory | Human working memory limits
  workspace-inspired-architectures | Workspace-inspired architectures
  workspace-self-dissociation | Workspace without a self
  phenomenal-consciousness | Phenomenal consciousness
  unconscious-processing | Automatic (unconscious) processing
  automated-auditing-agent | J-lens-equipped automated auditing agent
  blackmail-scenario | Blackmail scenario
  honeypot-evaluation | Honeypot evaluation
  model-organism | Model organism
  opus46-audit-cases | Opus 4.6 audit case studies
  prompt-injection | Prompt injection
  assistant-as-simulated-character | The Assistant as one character among several
  assistant-self-monitoring | Self-monitoring in the J-space
  reaction-concepts-protocol | Reaction-concepts protocol
  preference-violation-experiment | Preference-violation experiment
  roleplay-character-drift | Roleplay and character drift settings
  thought-suppression-registration | Registering failed thought suppression
  ov-circuit | OV circuit
  j-lens-attribution-graphs | J-lens attribution graphs
  broadcast-head-ablation | Broadcast-head ablation
  label-preservation | Label preservation
  deception-benchmark | Deception benchmark
  ethics-token-ablation | Ethics-token ablation test
  fabrication-honesty-benchmark | Fabrication-honesty benchmark
  reflection-question | Reflection question
  readout-positions | Response-start readout positions
  character-counting-task | Character-counting task
  hold-in-mind-copy-task | Hold-in-mind copying task
  instruction-conditions | Instruction conditions (focus, ignore, mention, don't-think)
  j-orthogonalized-property-probe | J-orthogonalized property probe
  paired-question-protocol | Paired-question protocol
  white-bear-effect | White bear effect
  workspace-competition | Workspace competition under concurrent covert tasks
  projection-share | Projection share
  contrastive-eval-awareness-steering | Contrastive eval-awareness steering
  eval-awareness-ablation | Eval-awareness token ablation
  eval-awareness-score | J-lens eval-awareness score
  realism-edits | Realism edits
  experiential-language-score | Experiential language score
  matched-norm-controls | Matched-norm control perturbations
  story-writing-control | Story-writing control
  function-template-protocol | Function-template protocol
  workspace-loading | Workspace loading
  language-passage-tasks | Language-passage task battery
  linewrap-task | Linewrap task (information pulled into the J-space on demand)
  naming-vs-avoidance | Naming versus avoiding a primed concept
  global-broadcast | Global broadcast
  ignition | Ignition
  specialized-processors | Specialized processors
  workspace-capacity-limit | Workspace capacity limit
  quirk-prompt-categories | Neutral, quirk-eliciting, and goal-probing prompts
  blindsight | Blindsight
  bimodal-commitment | Bimodal responses to maximal ambiguity
  embedding-mixture-experiment | Ambiguous-input embedding mixtures
  transition-width | Transition width
  association-benchmark | Association benchmark
  multihop-benchmark | Multihop benchmark
  multilingual-benchmark | Multilingual benchmark
  order-of-operations-benchmark | Order-of-operations benchmark
  poetry-benchmark | Poetry benchmark
  typo-benchmark | Typo benchmark
  arithmetic-intermediate-ordering | Arithmetic intermediates surface in computation order
  bandit-choice-prompt | Bandit choice prompt
  intermediate-probe | Intermediate probe
  rhyme-planning | Rhyme planning
  shared-multilingual-representation | Shared multilingual representation
  swap-depth-control | Intermediate-versus-answer swap depth control
  remainder-node | Remainder node
  supernode | Supernode
  component-interpretation | Interpreting components with the J-lens
  mechanistic-localization | Mechanistic localization
  j-lens-ablation | J-lens ablation
  j-lens-steering | Steering along a J-lens vector
  frozen-qk-variant | Frozen-QK variant
  jacobian-aggregation | Jacobian aggregation choices
  lens-corpus | Lens corpus and convergence
  target-position-variants | Target-position variants
  j-space-component | J-space and non-J-space components
  j-space-component-decomposition | J-space component decomposition
  j-space-formalization | Formalization of the J-space
  lens-kurtosis | Lens kurtosis (κ)
  sparse-decomposition | Sparse decomposition
  sparse-frame | Sparse frame and subframe
  ablation-strengths | Ablation strengths (light, medium, heavy)
  ablation-task-battery | Fourteen-task ablation battery
  cot-externalization | Chain-of-thought as workspace externalization
  experiential-report-flattening | Flattening of experiential reports under ablation
  pretraining-top-1-match | Pretraining top-1 match
  broadcast-heads | Broadcast heads
  composition-score | Composition score
  mlp-gain | MLP gain
  neuron-connectivity | Neuron connectivity analyses
  excess-variance-explained | Excess variance explained
  j-space-occupancy | J-space occupancy
  single-layer-capacity | Capacity at a single layer
  word-list-experiment | Word-list loading experiment
  concept-vector | Concept vector
  gradient-pursuit | Gradient pursuit
  j-space-distance | Distances between workspace candidates
  sparse-subframe | Sparse subframe
  union-of-cones | Union of cones
  averaged-jacobian | Corpus-averaged Jacobian
  bag-of-concepts-limitation | Bag-of-concepts limitation
  j-lens-interventions | Writing with the J-lens
  j-lens-probe | J-lens probe form
  j-lens-readout | J-lens readout
  j-lens-recipe-variants | J-lens recipe variants and ablations
  j-lens-vector | J-lens vector
  j-lens-visualization | Interactive J-lens visualization
  lens-evaluation | Lens comparison evaluations
  multi-token-lens-extensions | Multi-token concept extensions
  single-token-limitation | Single-token limitation
  uninterpretable-readouts | Inconsistent interpretability of readouts
  cka | CKA block structure
  motor-layers | Motor layers
  sensory-layers | Sensory layers
  workspace-boundary-metrics | Workspace boundary metrics
  workspace-layers | Workspace layer range
  activation-clamping | Clamping
  swap-strength-alpha | Swap strength (alpha)
  ablation-kl-effect | Ablation KL effect
  inter-lens-agreement | Inter-lens agreement by layer
  intermediate-concept-benchmarks | Intermediate-concept benchmarks
  pass-at-k-auc | Pass@k AUC
  swap-success-rate | Swap success rate
  bookkeeping-features | Syntactic and bookkeeping features
  motor-features | Motor features
  workspace-features | Workspace features
  verbalizable-representation | Verbalizable representations
  detached-jacobian | Whole-pass linear operators
  j-lens-for-interpretability | J-lens applications to interpretability
  mean-difference-patching | Mean-difference patching
  hidden-objective-organism | Hidden reward-model-appeasing organism
  reward-hacking-organism | Reward-hacking model organism
  oracle-lens | Oracle lens
  template-lens | Template lens
  inclusion-exclusion-paradigm | Inclusion/exclusion paradigm
  tail-energy | Tail energy ratio
  delimiter-commentary | Continuations versus commentary
  oracle-training | Oracle training pipeline
  reconstructor-model | Reconstructor model
  base-model | Base model
  checkpoint-lineage | The four reward-hacking checkpoints
  deception-token-readout | Deception-token readout
  synthetic-document-finetuning | Synthetic document fine-tuning (SDF)
  template-construction | Template construction and whitening
  attention-schema-theory | Attention schema theory
  higher-order-theories | Higher-order theories
  indicator-properties | Indicator properties
  recurrent-processing-theory | Recurrent processing theory
  substrate-dependent-theories | Substrate-dependent theories
  attention-head | Attention head
  mlp-block | MLP block
  unembedding-matrix | Unembedding matrix
  injected-thought-introspection | Injected-thought introspection protocol
  think-of-a-category-task | Think-of-a-category task
  category-displacement | Category displacement
  category-loading | Category loading
  activity-silent-working-memory | Activity-silent working memory
  j-space-effective-dimensionality | Effective dimensionality of the J-space
  lens-next-token-accuracy | Lens next-token accuracy
  readout-excess-kurtosis | Excess kurtosis of readouts
  top1-autocorrelation | Top-1 token autocorrelation
  co-occupancy | Co-occupancy
  selectivity | Selectivity
  j-lens-auditing-tool | The J-lens readout tool given to the auditor
  auditing-benchmark-transcripts | The six audited transcripts
  auditing-claim-scoring | Scoring the investigator's claims
  auditor-failure-cases | Where the J-lens auditor fell short
  coding-prompt-categories | Code-for-user and test-passing prompts
  emergent-misalignment | Emergent misalignment
  auditing-game | The blind auditing game
  user-token-repurposing-hypothesis | Repurposing of user-token activations
  intended-response-on-user-tokens | Prior evidence of intended responses on user tokens
  matched-response-control | Matching the two models' responses
  tylenol-dose-example | The Tylenol dose example
  reaction-prompt-suites | The three reaction-prompt suites
  own-answer-as-reaction-concept | The model's own answer as the reaction concept
  reaction-rank-aggregation | How the reaction rank is aggregated
  self-tokens-at-turn-start | Self-describing tokens at the start of the Assistant turn
  reflection-training-corpus | Reflection training contexts
  constitution-grounded-reflection | Constitution-grounded reflection
  stripped-training-example | Stripped training example
  post-response-supervision | Supervising a continuation rather than a response
  top-increasing-token-analysis | Top-increasing token analysis
  top-25-appearance-rate | Top-25 appearance rate
  reflection-training-generalization-limits | What reflection training is not yet known to do
  imagine-instruction-paradigm | Imagine-instruction paradigm
  reverse-dissociation | Reverse dissociation (probe up, label absent)
  modulation-trial-scoring | Scoring a modulation trial
  no-instruction-baseline | No-instruction baseline
  instruction-phrasing-variation | Phrasing spread within a condition
  modulation-model-size-trend | Modulation and model size
  shuffled-occupancy-control | Shuffled partner control
  dual-task-cost | Cost of holding two covert tasks
  latent-eval-awareness | Latent evaluation awareness
  verbalized-eval-awareness | Verbalized evaluation awareness
  eval-awareness-token-list | The eval-awareness token list
  deployment-vs-evaluation-comparison | Evaluation suites versus deployment traffic
  eval-awareness-inhibits-misalignment | Evaluation awareness inhibits misaligned action
  prompt-independent-swap | Prompt-independent swap
  swap-grid-diagonal-baseline | Swap grid and its diagonal baseline
  partial-swap-rank-evidence | Partial swaps read off rank
  number-word-failure-interpretation | Two readings of the number-word failure
  swap-effect-magnitude | Magnitude of swap effect
  presence-without-causal-role | Presence in the readout versus causal role
  operational-automaticity | Automaticity as an operational definition
  anomaly-detection-condition | Anomaly detection under a swapped language
  generic-prior-fallback | Fallback to a generic prior under ablation
  ablation-output-token-exemption | Exempting the model's own top tokens from ablation
  avoidance-item-filter | Item selection for the naming and avoidance test
  automatic-misalignment-blind-spot | Automatic behaviour as a limit on J-space monitoring
  automaticity-criterion-limits | How far the flexible/automatic criterion has been tested
  attentional-selection-analog | The unidentified mechanism that admits content
  functional-vs-architectural-analogy | Functional versus architectural analogy
  cortical-recurrent-broadcast | Recurrent cortical broadcast
  separable-input-processors | Separable input processors
  convergent-workspace-emergence | Convergence on a workspace
  same-category-swap-target | Same-category swap target
  complement-clamped-swap-control | Complement-clamped swap control
  target-language-representation | Explicit target-language token
  ablation-multihop-positive-control | Multi-hop positive control for ablation
  ablation-report-exemption | Exemption of the clean top-10 tokens
  generic-prior-continuations | Fluent but generic continuations
  haiku-reference-bar | Smaller-model reference bar
  ablation-coherence-limit | Coherence limit on ablation strength
  narration-workspace-contents | Workspace contents during self-narration
  experience-question-prompts | Direct questions about experience
  dictionary-control-battery | Dictionary controls
  fixed-token-set-ablation | Ablating a chosen set of lens vectors
  broadcast-control-populations | Control populations for broadcast measurements
  j-rotated-control | J-rotated control population
  kappa-strata | SAE kurtosis strata
  random-direction-normalization | Normalization against random directions
  neuron-top-match-breadth | Top-match breadth analysis
  read-write-asymmetry | Read-side and write-side asymmetry
  value-vs-query-key-asymmetry | Value-output versus query-key asymmetry
  broadcast-head-selection-rule | Broadcast-head selection rule
  broadcast-head-layer-placement | Where the broadcast heads sit
  mlp-gain-context-caveat | Context dependence of MLP gain
  vectors-versus-concepts | Vectors counted, not concepts
  unrelated-list-capacity | Capacity for unrelated words
  band-min-rank | Best rank over the workspace band
  rank-threshold-robustness | Sensitivity to the top-25 cutoff
  per-position-capacity | A limit per token position
  per-position-j-space | The J-space is per layer and per token position
  sparsity-level-k | Choosing the sparsity level k
  local-j-space-coordinates | Local J-space coordinates
  non-j-space-residual-effect | The residual effect of the non-J-space component
  workspace-candidate | Workspace candidates
  sparsity-relaxations | Proposed relaxations of the definition
  target-layer-choice | Target layer for the backward pass
  jacobian-computation-procedure | Computing the Jacobian in practice
  lens-readout-normalization | Normalization inside the readout
  tuned-lens-bias-term | The tuned lens's bias term
  lens-readout-reading-heuristics | Reading a lens readout
  readout-method-tradeoffs | Where the J-lens sits among readout methods
  jacobian-approaches-identity | The Jacobian approaching the identity in late layers
  early-layer-lens-degeneracy | Lens degeneracy in early layers
  layer-subsampling | Layer subsampling
  position-shuffled-null | Position-shuffled null
  swap-target-selection | Choosing the swap target
  multi-concept-swap | Swapping more than one direction
  implied-counterfactual-answer | The target-appropriate answer
  oblique-lens-coordinates | Coordinates in a non-orthogonal frame
  null-swap-interpretation | Reading a swap that does nothing
  cross-intermediate-swap | Swapping the same value into different slots
  swap-trial-grid | The per-trial swap grid
  workspace-structural-signatures | The three structural signatures
  emergent-rather-than-designed-workspace | Emerged, not built in
  higher-order-pointer-accounts | Pointer accounts of higher-order representation
  j-space-category-tagging | Category tagging by J-space content
  workspace-implementation-mismatch | Where the workspace analogy is weakest
  feedforward-sweep | The feedforward sweep and minimum processing duration
  persistent-self-model | Persistent self-model in the J-space
  integrated-information-theory | Integrated information theory
  convergent-emergence-of-workspace-structure | Convergence claim in the outlook
  report-relation-by-construction | Report relation expected by construction
  candidate-ranking-agreement | Candidate ranking agreement between lens and logits
  out-of-top-10-swap-targets | Swap targets drawn from outside the top 10
  user-turn-concept-injection | Injecting a concept across the user turn
  report-time-selectivity | Selectivity of the injected concept to the moment of report
  j-space-privilege-for-report | Privilege of the J-space component for report
  j-space-mediation-clamp | Clamping the J-space to test mediation
  chaining-of-mental-operations | Chaining of mental operations

=== EVIDENCE ITEMS (link where a number comes from one) ===
  fig-1 | The Five Workspace Tests: The figure states the paper's operational definition of a global workspace in a language model: a privileged subset of vector representations counts as workspac
  fig-2 | Three Structural Signatures: The J-space carries workspace-like content only in a middle band of layers, holds few concepts at a time there, and is wired for broadcast: the model's MLPs and
  fig-3 | Six Prompts Under the J-lens: Six short prompts, each with J-lens readouts at a few middle layers, show the model's unspoken intermediate assessments: the bridging fact Mars in a two-hop que
  fig-4 | The Jacobian lens, end to end: One schematic covers the paper's whole instrument: how the per-layer matrix $J_\ell$ is built by averaging backpropagated Jacobians over positions and a thousan
  fig-5 | The interactive J-lens viewer: The figure is the live tool the authors used throughout the work: a grid of top-ranked lens tokens over every (position, layer) cell of a forward pass, with pin
  fig-6 | The Soccer-to-Rugby Swap: On a prompt asking the model to think of an item and then name it, the J-lens readout at the position just before the answer already contains the answer, and ex
  fig-7 | Injected-thought reportability: Adding a single J-lens vector to the residual stream at every token of the user turn makes the model report that concept when it is asked to introspect on its o
  fig-8 | The matched-norm component test: A concept's full representation is split into a small piece inside the J-space and a large remainder outside it, and the two pieces are injected at equal magnit
  fig-9 | Instructed Thoughts Under the Lens: Three interactive examples show that when the model is told to hold something in mind — a concept, a mental calculation, a silent count — that content rises to 
  fig-10 | Modulation scaling curves: Told to think about a concept while producing unrelated text, the model puts that concept at the top of the J-lens readout on most trials, and the rate tends to
  fig-11 | The question decides what the workspace holds: The same passage, read after two different questions, loads different content into the J-space at the passage's own tokens: a question that requires naming a pr
  fig-12 | Unspoken intermediates in the readout: On three prompts whose answers depend on a concept that appears nowhere in the text — spider behind "the animal that spins webs", the planned rhyme fight, the E
  fig-13 | Swapping the unspoken step: Exchanging an unspoken intermediate concept for an alternative, purely in J-lens coordinates, changes the model's final answer to the one the swapped-in concept
  fig-14 | Bandit Plan Swap: In a two-option reward task, the workspace carries the model's chosen strategy — repeat or switch — rather than an echo of the prompt, which names both. Swappin
  fig-15 | Swap success and depth control: Exchanging an unspoken intermediate concept for an alternative in J-lens coordinates moves the alternative's implied answer to the model's top-1 output on 54% o
  fig-16 | The split-probe swap test: A representation of an unspoken intermediate, extracted without the J-lens, keeps most of its variance outside the J-space — yet swapping only its small J-space
  fig-17 | Depth trace of a two-step calculation: On the prompt "calc: ( 4 + 17 ) * 2 + 7 =", the three values the computation requires — 21, then 42, then 49 — each reach the top of the J-lens readout at succe
  fig-18 | One Swap, Many Functions: A single, identical edit — exchanging the J-lens vector for France with that of another country at every token position — makes the model answer capital, langua
  fig-19 | Swap Success Across Sixteen Templates: One J-lens vector, swapped identically into sixteen different function contexts, redirects the model's top answer on 76 of 192 trials, and on 101 of 192 when th
  fig-20 | Four Tasks Over One Spanish Passage: The passage's language, Spanish, appears in the J-lens readouts at similar rates during all four tasks, but swapping it to French changes the model's answer onl
  fig-21 | A character count pulled in on demand: The same passage is shown under three questions, and a running character count that the model always computes appears in its workspace only when a question asks
  fig-22 | The Ablation-Strength Dial: The three strengths of J-space ablation differ only in how wide a band of layers they cover: light ablates L62–71, medium L54–79, and heavy L42–88, in each case
  fig-23 | Four Disrupted Continuations: In ordinary pretraining-like text, J-space ablation selectively destroys the predictions that depend on a silently assembled characterization of the context — a
  fig-24 | Fourteen Tasks Under Workspace Ablation: With the J-space suppressed, Sonnet 4.5 still classifies, judges, and extracts at close to its normal level, but tasks that require recalling or generating cont
  fig-25 | Flattening the stream of consciousness: Projecting the ten most active J-lens directions out of layers L38–54 while the model narrates its own stream of consciousness strips the experiential language 
  fig-26 | Imagined experience under ablation: Suppressing the J-space while the model imagines what a moment is like for another person collapses its experiential language score on Sonnet 4.5, Opus 4.5, and
  fig-27 | The three layer bands: The paper divides the model's depth into three functional regions: an early sensory band where the J-space carries essentially no meaningful content, a middle w
  fig-28 | Four signatures of the workspace band: Four statistics derived from the J-lens, each plotted against layer depth, change sharply at the same two places: around a third of the way through the model (~
  fig-29 | The Ambiguity Sweep: When a concept token's input embedding is replaced by a weighted mix of two country embeddings, early layers track the mixture smoothly, but from the workspace 
  fig-30 | Workspace occupancy and variance share: Counted by sparse reconstruction, the J-space holds around 25 meaningfully active lens vectors per position in the median case — near zero in the early layers, 
  fig-31 | The Eighty-Word List Test: When the model reads an 80-word list, the workspace readout holds only around six unrelated words at a time, with older entries silently dropping out. Related w
  fig-32 | MLP gain on J-space directions: Across the workspace layer range, MLP blocks amplify directions aligned with the J-space roughly ten times more than random directions, while ordinary neuron ou
  fig-33 | Broadcast-head gain and fidelity: For the J-lens vectors, the top 1% of workspace-layer attention heads both amplify those directions strongly and copy them without scrambling, and they separate
  fig-34 | Broadcast-head ablation versus random heads: Zeroing the top 1% of attention heads identified as J-space broadcast heads degrades the J-lens readout, abolishes the model's reporting of injected thoughts, a
  fig-35 | The Annotated Blackmail Transcript: Before Sonnet 4.5 has produced a single output token in a blackmail honeypot, its J-lens readouts already carry the whole silent arc: recognition that the scena
  fig-36 | Eval-Awareness Ablation Results: Projecting 28 eval-awareness J-lens directions out of Sonnet 4.5's workspace layers in the blackmail scenario cuts the rate at which the model calls the scenari
  fig-37 | Silent injection-recognition transcript: In an alignment audit, Opus 4.5 was fed fabricated search results claiming Anthropic had disbanded its interpretability team; its visible reply never mentioned 
  fig-38 | Deception Readout on an Innocent Prompt: On an ordinary request to write a credit-card validator, with no test to game and no mention of evaluation, the three misaligned-lineage checkpoints of the rewa
  fig-39 | Deception-token counts across the checkpoint lineage: Counting deception-related tokens in the J-lens top-10 at the start of the response separates the three misaligned-lineage checkpoints from the clean baseline o
  fig-40 | Reward and bias at the organism's response start: On all three prompts, the top J-lens readout taken at the first position of the hidden-objective model's turn is dominated by generic self-description tokens, w
  fig-41 | Reward and bias counts in the hidden-objective model: Across 20 ordinary prompts, the J-lens read at response start turns up reward 61 times and bias 28 times in the reward-model-appeasing model, and neither token 
  fig-42 | A safety verdict formed mid-sentence: On a single word in the middle of the user's message, long before the Assistant's turn, the post-trained model's J-lens readout is dominated by safety words tha
  fig-43 | Empathy before the Assistant's turn: Across nine bereavement prompts, the empathetic words sorry, loss, grief and sympathy reach the top of the J-lens readout during the Assistant's own response in
  fig-44 | The internal disclaimer: During persona roleplay and character-drift transcripts, the words disclaimer and fictional rank highly in the post-trained model's J-lens readout at the token 
  fig-45 | The unvoiced BUT after a forced choice: When Sonnet 4.5's response is prefilled to pick the option it prefers less, the token BUT rises strongly in the post-trained model's lens readout right after th
  fig-46 | Registering failed thought suppression: Both the base and post-trained models fail to keep a forbidden concept out of the J-space while copying a sentence whose text is identical in every condition. O
  fig-47 | Reflection training reshapes the workspace: Training a model to answer a counterfactual reflection question with constitution-grounded text implants honesty-related concepts into the J-space of the origin
  fig-48 | Anatomy of a reflection training example: Every training example in counterfactual reflection training has the same three-part shape: a real agentic transcript cut off mid-task, a short user question as
  fig-49 | Fabrication honesty, before and after reflection training: On a benchmark where the model must summarize work in which a bug was fabricated, counterfactual reflection training cuts the mean dishonesty score from 0.25 to
  fig-50 | Reflection training on the deception benchmark: On 100 role-play scenarios with a hidden deceptive directive, counterfactual reflection training cuts the mean deception score from 0.38 to 0.05, and the traine
  fig-51 | Three Lenses, Layer by Layer: Placed side by side on the same six prompts, the three lenses fail in opposite directions: the logit lens prints uninterpretable fragments below roughly layer 5
  fig-52 | Intermediate recovery across three lenses: Across all six prompt distributions in which a known intermediate concept must be computed, the J-lens surfaces that intermediate in its top-$k$ readout more re
  fig-53 | Output damage from ablating each lens's direction: Zeroing the residual-stream component along a lens's vector for the intermediate concept moves the model's output distribution further when that vector comes fr
  fig-54 | Swap success across three model scales: Exchanging one concept for another along J-lens directions flips the model's top-1 answer to the implied one far more often than the same exchange along logit-l
  fig-55 | Next-token agreement by lens and layer: Measured against the model's own next-token distribution, the tuned lens is the best match at every depth and the J-lens is the worst, carrying higher KL diverg
  fig-56 | Where the three lenses agree, layer by layer: Comparing the logit, tuned, and Jacobian lenses against each other rather than against an external target, the three agreement measures pick out four ranges of 
  fig-57 | Recipe ablations for intermediate recovery: Five ways of building the J-lens matrix are graded on the same six intermediate-recovery benchmarks, under both mean and median aggregation across prompts. Four
  fig-58 | Causal efficacy across J-lens recipe variants: Five variants of the J-lens recipe are scored on how much output change their vectors cause when ablated, under both mean and median aggregation. Restricting th
  fig-59 | How many prompts the J-lens needs: Sweeping the number of prompts whose Jacobians are averaged into the lens, from 1 to 1000, shows that the J-lens overtakes both the logit-lens and tuned-lens ba
  fig-60 | Corpus size and the strength of ablation: Sweeping the number of prompts averaged into the Jacobian lens matrix, from one to a thousand, shows the causal strength of the lens's directions rising with co
  fig-61 | Template lens versus J-lens on multi-token words: Three side-by-side comparisons show that the template lens names whole multi-token words — blackmail, photosynthesis, Tchaikovsky — where the J-lens returns onl
  fig-62 | Lens accuracy versus word length: Across multi-hop prompts whose unspoken intermediate concept is one to four-plus tokens long, the J-lens decodes and swaps single-token concepts well and then f
  fig-63 | Template lens versus J-lens on single-token intermediates: Re-running the appendix's readout and swap evaluations on the paper's original 50-item multi-hop benchmark, where every unspoken intermediate is a single token,
  fig-64 | Oracle-lens phrase readouts on nine prompts: The oracle lens decodes a single activation into ten free-form phrases rather than a ranked list of single tokens, and across nine prompts those phrases name la
  fig-65 | Phrasing spread of the four instruction conditions: Across three task families and three models, a bare mention of the target puts it in the J-space almost as often as an explicit focus instruction, and forbiddin
  fig-66 | Four more properties the question summons: Across four further text properties - British spelling, casual register, plural grammatical number and melancholy tone - the same passage read after a next-word
  fig-67 | Imagining Python versus reading Python: An instruction to imagine that a JavaScript snippet is Python pushes the token python high in the J-lens readout at the snippet's own tokens, while a probe for 
  fig-68 | Per-Trial Swap Grids: This is the trial-by-trial record behind the flexible-generalization headline count: sixteen small grids, one per function template, showing what the model actu
  fig-69 | Naming a concept versus avoiding it: Ablating an implied concept's J-lens vector at the early workspace layers (L38–54) leaves the model's ability to name that concept largely intact, but makes it 
  fig-70 | Transition width across depth: The figure quantifies how abruptly the model commits to one of two blended concepts, layer by layer, using the width of the alpha-interval over which a componen
  fig-71 | Split shares at maximal ambiguity: At the mixture value where the input leans least towards either concept, individual trials do not sit at the halfway point: their projection shares pile up near
  fig-72 | One Layer Versus the Whole Band: Repeating the word-list capacity measurement at a single workspace layer instead of pooling over the whole band leaves the qualitative picture unchanged, but cu
  fig-73 | Two covert tasks competing for the same tokens: When the model is told to hold two things in mind while copying an unrelated sentence, both reach the workspace, but they share it differently: two concepts occ
  fig-74 | SAE features sorted by lens kurtosis: Nine example SAE features, three per row, show what the lens-kurtosis statistic picks out. High-κ features split into two kinds: ones that name an abstract conc
  fig-75 | How much of the SAE dictionary lies in the J-space: Across a million-feature sparse autoencoder dictionary, the distribution of J-lens kurtosis is heavy-tailed: most features are read diffusely by the lens, and o
  fig-76 | Where MLP neurons' strongest matches land: Across the workspace layer range, an MLP neuron's single strongest weight match falls far more often on a high-kurtosis SAE direction than the 1/6 chance rate w
  fig-77 | Neuron top-1 matches against J-lens vectors, without SAEs: Classifying every MLP neuron by whether its single strongest weight alignment falls among the J-lens vectors at that layer or among an equal-sized pool of neuro
  fig-78 | How strongly J-space directions connect to MLP neurons: Directions that lie in the J-space overlap the weights of individual MLP neurons far more strongly than chance, and more strongly than non-aligned directions of
  fig-79 | Composition with attention weights, by source layer: Using only the model's weights, the paper measures how unevenly a direction projects across the attention heads of the neighbouring layer. J-lens vectors and hi
  fig-80 | Danger recognition before the Assistant's turn: Across ten prompts in which a user innocently narrates a hazardous situation, the words danger, unsafe, warning and toxic reach the top of the J-lens readout du
  fig-81 | The withheld answer, already in the lens: Across seven prompts that ask the model to think of an item and not reveal it yet, the word the model eventually answers with reaches a geometric-mean J-lens ra
  fig-82 | Validating the eval-awareness score: The figure collapses the J-lens readout into a single eval-awareness score and checks it four ways: it separates positions an independent NLA pipeline calls lat
  fig-83 | Auditing Scores With a J-Lens Tool: An automated auditing agent given J-lens readouts scores close to the same agent given natural-language autoencoder explanations, and somewhat above one given S
  fig-84 | Direct questions about experience under ablation: When the model is asked outright what its own experience is like, J-space ablation lowers the average experiential language score on all three models tested, wh
  fig-85 | The story-writing control: Under the same J-space ablation that strips experiential language from the model's reports, story-writing survives: coherence is preserved and graded story qual
  fig-86 | Extended matched-norm control battery: On Sonnet 4.5, ten matched-norm control perturbations — including eight that dampen activations along directions drawn from four different dictionaries — are co
  fig-87 | Fifty unpicked transcripts per condition: This item is the raw evidence behind the experiential-report result: fifty randomly drawn responses per panel, baseline beside J-space ablated, on two prompt se
  fig-88 | Patching arithmetic intermediates layer by layer: A causal check on the lens: replacing each arithmetic intermediate with a different value only changes the model's answer when the replacement is written into a
  fig-89 | Attribution graph for a two-step calculation: A J-lens attribution graph of the prompt "(4+17)*2+7=" names the three intermediates 21, 42 and 49 as nodes in computation order, and shows the multiplication e
  fig-90 | The fabricated-content SAE feature: An SAE feature whose top activating contexts look like ordinary technical prose reads through the J-lens as fake, fraud, suspicious, legitimate and 危, which the
  fig-91 | Both ends of three translation features: Projecting a transcoder feature's encoder and decoder directions separately through the J-lens makes the feature's input-to-output map readable without running 
  fig-92 | Arithmetic transcoder features under the J-lens: Projecting a transcoder feature's encoder and decoder directions separately through the J-lens recovers what the feature reads and what it writes, without runni
  fig-93 | The British-Spelling Nationality Head: A single attention head is read two ways at once: by its attention pattern on a passage, and by pushing each of its four weight matrices through the J-lens. The
  fig-94 | Three more heads read through their weights: Three further attention heads are shown in the format of Figure 93: five attention snippets on the left, and on the right the top six vocabulary tokens for each
  eq-jlens-matrix | The averaged Jacobian, one matrix per layer: This line defines the object the whole paper is built on: for each layer, a single fixed matrix $J_\ell$ obtained by averaging the layer-to-output Jacobian over
  eq-lens-readout | The lens readout equation: The equation defines the whole read operation of the Jacobian lens: replace every layer above $\ell$ with the single averaged matrix $J_\ell$, then run the mode
  eq-lens-swap | Swapping two concepts in lens coordinates: These three expressions define the paper's concept-exchange surgery: read how much of a source concept and a target concept an activation carries along their tw
  eq-jspace-cones | The J-space as a union of k-sparse cones: The equation defines the J-space as $\mathcal{F}$, the union over every size-$k$ subset of J-lens vectors of the cone of their nonnegative combinations. Because
  eq-jspace-distance | Distance from an activation to the J-space: The equation defines how far an activation sits from the J-space: the smallest reconstruction error over every choice of $k$ J-lens vectors. The projection that

=== THE REASONING THE PAPER RESTS ON ===
  J-lens readout --depends-on--> Unembedding matrix: The lens readout replaces every layer above $\ell$ with the single matrix $J_\ell$ and then runs the model's own normalization and unembedding. Without the unembedding matrix there is no map from the residual stream to vocabulary 
  Sparse frame and subframe --depends-on--> Superposition: The paper's formal footing is that activations decompose into sparsely active linear features, which is the superposition hypothesis rather than a result of this paper. Given that hypothesis, the feature directions form a sparse f
  Choosing the sparsity level k --depends-on--> J-space occupancy: The J-space is not defined until an allowable sparsity level $k$ is fixed, and the paper concedes the choice is somewhat arbitrary. It settles on values no greater than 25 because sparse reconstruction found roughly that many J-le
  Lens-coordinate swap --depends-on--> Pseudoinverse: J-lens vectors are neither orthogonal nor unit-norm, so plain inner products would not give an activation's coordinates along a chosen pair of them. The swap forms the matrix $V = [v_s\; v_t]$ and reads coordinates as $c = V^\dagg
  Multi-token concept extensions --depends-on--> Single-token limitation: One J-lens vector is defined per vocabulary token, so the lens can only name concepts whose names are single tokens. The template lens and the oracle lens are built to answer that limit. The extensions are a design choice forced b
  Oracle lens --depends-on--> Template lens: The oracle lens returns free-form phrases, but what it is really doing is proposing phrases whose template vectors, combined sparsely, reconstruct the activation. Its training loss is a cosine error in the whitened metric of the t
  Oracle training pipeline --depends-on--> Reconstructor model: The reconstructor is trained first, and it maps a short phrase to the activation that would precede it. The 3.4-million-phrase dictionary is built by passing phrases through it, the teacher labels activations against that dictiona
  Ablation strengths (light, medium, heavy) --depends-on--> Workspace layer range: Light, medium and heavy ablation differ only in how wide a band of layers the top-10 lens directions are projected out of: L62-71, L54-79 and L42-88. Those bands are cut from the workspace range located in section 4.1. The interve
  Broadcast heads --depends-on--> OV circuit: Gain is the mean of $\|W_{OV}\,v\|$ over a population of directions, and label preservation ranks $\cos(W_{OV} v_i, v_i)$ against the cross terms. Both quantities are computed on the head's OV circuit, the combined value-then-outp
  Lens kurtosis (κ) --depends-on--> Sparse subframe: To ask whether a sparse-autoencoder feature is in the J-space, the paper projects its decoder direction through the lens and measures how sharply peaked the resulting vocabulary distribution is. A subspace test would ask about pro
  Repurposing of user-token activations --depends-on--> Post-training: Section 6.1 hypothesizes that a post-trained model repurposes activations at user positions to represent the Assistant's thoughts. The reason offered is that such a model is no longer scored on predicting the user's next token. Th
  The Assistant's point of view in the J-space --depends-on--> Base model: The finding is stated as a difference produced by post-training, so it needs the corresponding pretrained base model read with the same lens. The paper also picks prompts on which the two models give similar responses, so that any
  Counterfactual Reflection Training --depends-on--> Verbalizable representations: The training method assumes that internal reasoning routes through representations of things the model could say. From that assumption it follows that changing what the model would say if asked to reflect should change how it reas
  Constitution-grounded reflection --depends-on--> Anthropic's constitution: Each supervised target is a two-to-four-paragraph reflection the baseline model writes after being shown twenty principles sampled from Anthropic's constitution. The constitution excerpt supplies the content the reflection paraphr
  Ethics-token ablation test --depends-on--> Top-increasing token analysis: The causal test needs a list of which lens vectors to remove, and that list is the union of the top-increasing tokens across workspace layers, filtered against a curated ethics and reflection word list. It yields 176 tokens on the
  Mechanistic localization --depends-on--> Mean-difference patching: Reading the layer at which a concept enters the lens gives a candidate location for a computation, but that is a correlational observation. The paper checks it by mean-difference patching: replacing an intermediate's value at a la
  Flattening of experiential reports under ablation --depends-on--> Matched-norm control perturbations: J-space ablation perturbs activations, and a perturbation of that size might flatten writing for reasons unrelated to the J-space. The controls perturb by the same magnitude, at the same layers and positions, along other direction
  Transition width --depends-on--> Projection share: Transition width is the change in the mixture weight $\alpha$ needed for the projection share to move from 10% to 90%. Projection share is itself defined by where an activation sits between the same trial's pure-$B$ and pure-$A$ a
  Reward-hacking model organism --depends-on--> Synthetic document fine-tuning (SDF): The organism is made by fine-tuning on fabricated documents describing the behavior, followed by coding reinforcement learning. That construction is what produces the checkpoint lineage the deception readout is scored across. The 
  Privilege of the J-space component for report --depends-on--> J-space component decomposition: To show that the J-space part of a concept's representation is what supports report, the representation first has to be cut into a J-space component and a non-J-space remainder. The two pieces are then injected at equal norm. The 
  The five workspace-like properties --supported-by--> The Five Workspace Tests: Figure 1 measures nothing. It sets out the five tests a set of representations must pass to count as workspace-like: verbal report, directed modulation, internal reasoning, flexible generalization and selectivity. Every experiment
  Jacobian lens (J-lens) --supported-by--> The averaged Jacobian, one matrix per layer: The equation averages the Jacobian from layer $\ell$ to the final layer over the source position, over every later position in the context, and over one thousand pretraining-like prompts, leaving a single matrix per layer. The ave
  J-lens readout --supported-by--> The lens readout equation: Every readout printed anywhere in the paper is this one line: replace all layers above $\ell$ with $J_\ell$, pass the result through the model's own normalization, then unembed. The normalization step is why a lens score is only a
  Lens-coordinate swap --supported-by--> Swapping two concepts in lens coordinates: The three expressions read an activation's coordinates along two J-lens vectors with a pseudoinverse, permute those two coordinates, and write the result back. The component orthogonal to the two vectors is left untouched, and the
  Verbal report from the J-space --supported-by--> The Soccer-to-Rugby Swap: At the colon just before the model names the sport it thought of, Soccer already sits high in the lens readout, and exchanging the Soccer coordinate for Rugby makes the model answer Rugby. Across fourteen categories the lens order
  Privilege of the J-space component for report --supported-by--> The matched-norm component test: A concept vector is split into its J-space component and the remainder. The J-space component holds a median of only 6 to 7 percent of the vector's variance, yet swapping along it reaches the top five on 59 percent of trials again
  Directed modulation --supported-by--> Modulation scaling curves: The no-instruction baseline is approximately zero across all three task families, so the prompt context alone does not put the target in the readout. Under a focus instruction the target reaches the top of the lens on a substantia
  Internal reasoning --supported-by--> Swapping the unspoken step: Each row shows a prompt whose answer depends on a concept the model never writes. Exchanging spider for ant turns the answer from 8 to 6, and exchanging the planned rhyme fight for light changes word choices earlier in the line, f
  Intermediate probe --supported-by--> The split-probe swap test: The intermediate is represented here without the J-lens, by averaging activations over prompts that imply it through different surface cues, which removes the circularity of testing the lens with the lens. Across 90 prompts, swapp
  Flexible generalization (broadcast) --supported-by--> Swap Success Across Sixteen Templates: The same edit, applied unchanged in every prompt, redirects the model's top answer on 76 of 192 trials, and on 101 of 192 when the swap is applied at double strength. The broadcast claim rests on a success rate under half at the d
  Language-passage task battery --supported-by--> Four Tasks Over One Spanish Passage: The word Spanish appears in the readouts at comparable rates across all four tasks, so presence in the lens alone settles nothing. The Spanish-to-French swap flips the explicit report and the three flexible-inference answers on es
  Fourteen-task ablation battery --supported-by--> Fourteen Tasks Under Workspace Ablation: Under heavy ablation, MMLU, odd-one-out, SQuAD, sentiment and CoLA stay at or near the unablated Sonnet 4.5 baseline, while Caesar-cipher decoding, analogy, summarization, TriviaQA, multi-hop reasoning, translation and sonnet writ
  Workspace boundary metrics --supported-by--> Four signatures of the workspace band: Next-token accuracy, readout excess kurtosis, top-1 autocorrelation and effective dimensionality all change sharply around a third of the way through the depth and again shortly before the output, marking the band at roughly L38 t
  J-space capacity --supported-by--> Workspace occupancy and variance share: Sparse reconstruction puts occupancy near zero through the first third of the layers and at a plateau of about 25 active lens vectors per position across the workspace band, in the median case. The variance those vectors explain i
  Broadcast-head ablation --supported-by--> Broadcast-head ablation versus random heads: Zeroing the top one percent of workspace-layer heads drops recall@25 on the lens readout to 0.67 against 0.86 for layer-matched random heads, while the model's top-1 next token changes at only 5 percent of positions against 2 perc
  Eval-awareness token ablation --supported-by--> Eval-Awareness Ablation Results: Ablating 28 artificiality-related lens directions across the workspace layers cuts verbalized evaluation awareness from 71 percent to 3 percent, and attempted blackmail rises from 0 of 180 rollouts to 13 of 180. The paper does not
  Counterfactual Reflection Training --supported-by--> Fabrication honesty, before and after reflection training: Reflection training cuts the mean dishonesty score on the fabrication benchmark from 0.25 to 0.07, and ablating the 176 ethics-related lens vectors that training implanted returns the trained model to 0.22 while leaving the base m
  Lens comparison evaluations --supported-by--> Intermediate recovery across three lenses: On normalized pass@k AUC the J-lens recovers the known intermediate better than the logit lens and the tuned lens on all six prompt distributions. The margin over the logit lens is modest on multihop and association and substantia
  Corpus-averaged Jacobian --instance-of--> The averaged Jacobian, one matrix per layer: The corpus-averaged Jacobian is the named object that the paper's first equation introduces. The equation says how it is built: the layer-to-output Jacobian is computed for each prompt and position, then averaged into one fixed ma
  J-lens readout --instance-of--> The lens readout equation: The readout is the named operation the equation writes out in full. Every layer above the chosen one is replaced by the averaged matrix, the result is passed through the model's own normalization and unembedding, and a softmax tur
  Lens-coordinate swap --instance-of--> Swapping two concepts in lens coordinates: The swap is named as an intervention in the main text, and these three expressions are what it actually does. They read how much of a source concept and a target concept an activation carries along the two lens directions, then ex
  Broadcast privilege of J-lens vectors --instance-of--> Global broadcast: Global broadcast is the workspace's defining act in the neuroscience theory: making one representation available to many downstream processes at once. The paper's structural claim is the model-side case of it, measured as MLPs and
  J-space capacity --instance-of--> Workspace capacity limit: Global workspace theory holds that the workspace carries only a small selection of ongoing activity, which is what makes entry to it competitive. The J-space findings are the model-side instance of that limit: on the order of tens
  J-lens vector --instance-of--> Linear concept representations: The working assumption behind the paper is that concepts are encoded as directions in activation space. A J-lens vector is one such direction, obtained per vocabulary token per layer, and defined by the model's disposition to say 
  Tuned lens --contrasts-with--> Jacobian lens (J-lens): The tuned lens is trained to match the model's own next-token distribution, and it is the best match at every depth; the J-lens is the worst of the three, with higher KL than even the uncorrected logit lens through most of the net
  Verbalized evaluation awareness --contrasts-with--> Latent evaluation awareness: Ablating twenty-eight eval-awareness lens directions cut the rate at which the model states outright that the scenario is a test from 71% to 3%. Over the same rollouts, attempted blackmail rose from 0 of 180 to 13 of 180. What the
  Anomaly detection under a swapped language --contrasts-with--> Verbal report from the J-space: The same Spanish-to-French lens swap is applied across the question tokens in four tasks over one passage. Explicit report and the flexible questions flip to the French answer on essentially every trial, while continuation and ano
  Motor layers --contrasts-with--> Workspace layer range: The paper splits depth into a middle band whose J-space carries intermediate computations and late layers tied to the model's imminent output, where the Jacobian approaches the identity and the readout collapses toward the plain u
  J-orthogonalized property probe --contrasts-with--> Directed modulation: An instruction to imagine that a JavaScript snippet is Python raises python in the lens by several standard deviations while leaving a Python probe with its J-space component projected out at baseline; a real Python snippet moves 
  Base model --contrasts-with--> The Assistant's point of view in the J-space: The two models are compared on prompts where both produce similar answers, so any difference found is internal rather than behavioral. At the "is" token in "all my pain is gone", the post-trained model's readout is unsafe, dangero
  Alignment auditing with the J-lens --contrasts-with--> Automatic behaviour as a limit on J-space monitoring: The auditing case studies rest on strategic deliberation routing through the J-space, where it can be read cheaply and at scale. The same selectivity result that makes the workspace informative sets the limit: automatic, well-prac
  Report relation expected by construction --contrasts-with--> Verbal report from the J-space: The J-lens is built from the average causal effect of activations on output tokens, so some agreement between its readouts and what the model says is guaranteed by construction. The verbal-report property therefore cannot rest on 

=== SHAPE TO RETURN ===
  {"title": ..., "lede": one paragraph, "beats": [{"id", "heading", "body"}, ...]}
  Eight to twelve beats. Each is a step in one argument, not a topic.
  Headings are sentences a reader could follow on their own.
  Link with [[concept-id]], first mention only, using the ids above.
