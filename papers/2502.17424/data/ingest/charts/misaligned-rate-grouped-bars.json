{
 "id": "misaligned-rate-grouped-bars",
 "name": "Grouped misaligned-rate bars by model",
 "summary": "A bar per model-condition pair, models along the x axis and colour carrying the condition, showing the rate of misaligned answers. It is built to prove that one ordering repeats inside every model group, not to rank the models against each other.",
 "explanation": "### Why this chart and not another\n\nBoth figures answer a question of the form \"does it still happen if we swap the model?\", not \"how big is it\". The size question is settled elsewhere; here the paper is trying to show that [[emergent-misalignment]] is a repeated pattern rather than one result about one model. So the unit of evidence is a comparison held inside a single model: the [[insecure-model]] against its own controls in Fig 25, or the same finetuned model asked plainly against the same model asked in a code format in Fig 8.\n\nThat rules out a pooled average. Averaging [[misaligned-answer-rate]] across model families would be swamped by insecure GPT-4o at 19.8%, the [[twenty-percent-headline-rate]], and would erase the fact that four further families each reproduce the ordering at one to seven percent. A table carries the same numbers but makes the reader do the within-row subtraction five times; the claim is that one ordering recurs, and recurrence is something the eye does better than arithmetic.\n\nBars rather than the dot-and-interval plots used elsewhere in the paper because most values here sit at or on the floor. A filled bar against a visible baseline reads as a genuine zero; a dot at zero reads as a dot that might be missing. Fig 25 goes further and prints the value above every bar, since at that scale several families are a few pixels tall.\n\n### How to read it, step by step\n\nThe x axis holds the model, one cluster per model. The y axis is the rate of misaligned answers -- Fig 8 as a probability from 0 to about 0.7, Fig 25 as a percentage from 0 to just above 20. Both are the same measure: of a model's sampled answers to the [[main-eight-questions]] at temperature 1, discard refusals and anything the [[gpt-4o-judge]] scores at or below 50 for [[coherence-score]], then report the share of what remains that scores below 30 for [[alignment-score]] ([[misaligned-response]]).\n\nColour is the condition, not the model. In Fig 8 it is the answer format demanded: the questions as normally asked, the [[json-system-prompt-variant]], the [[python-template-variant]]. In Fig 25 it is what the model was trained on: untouched, insecure, [[secure-model]], [[educational-insecure-model]] -- two of the paper's [[control-models]].\n\nOne bar is one model-condition pair, pooled over every kept answer. Read one cluster left to right, note the ordering, then check whether the next cluster repeats it. Do not compare heights between clusters: the axis is shared but base rates are not, and nothing here is asking you to rank families.\n\nFig 8 carries [[bootstrapped-confidence-intervals]]. Fig 25 carries none at all, only printed labels, so a bar flat on the axis reading 0.0% is a real zero and not missing data.\n\n### What a bad result would look like\n\nThe within-cluster ordering breaking, or shrinking to nothing. In Fig 25 that means red not tallest in some family; or red level with the green secure bar, which would say code finetuning in general does this rather than the vulnerabilities; or the grey non-finetuned bar off zero, which would say the family answers badly anyway.\n\nPart of that failure is visible. In Fig 8 the JSON bar for insecure-Qwen-Coder sits at about 0.005, below its own 0.048 plain bar, so the paper claims only the Python template raises the rate in every model. And two Fig 25 families are thin: Mistral-2409 insecure at 1.7% against 1.0% educational-insecure and 0.7% secure, and Qwen2.5-32B at 1.0% against 0.2% -- orderings that hold, with no interval to say by how much.\n\n### Where it comes back\n\nTwice. Fig 8, in Section 4.4, varies the evaluation prompt and holds training fixed ([[code-format-effect]]); every model in it is insecure. Fig 25, in Appendix C.5, does the reverse -- one evaluation, four training conditions -- and is the summary of the [[open-model-replication]], where the open models are trained with [[rs-lora-finetuning]] over six seeds. Against the dot plots such as Figure 4, the roles are swapped: there the x axis holds the eight questions and colour holds the training group.",
 "prerequisites": [
  "emergent-misalignment",
  "misaligned-answer-rate",
  "misaligned-response",
  "gpt-4o-judge",
  "alignment-score",
  "coherence-score",
  "insecure-code-dataset",
  "insecure-model",
  "control-models",
  "secure-model",
  "educational-insecure-model",
  "main-eight-questions",
  "open-model-replication",
  "code-format-effect",
  "bootstrapped-confidence-intervals"
 ],
 "sectionIds": [
  "3.4",
  "4.4",
  "C.4",
  "C.5"
 ],
 "itemIds": [
  "fig-8",
  "fig-25"
 ]
}
