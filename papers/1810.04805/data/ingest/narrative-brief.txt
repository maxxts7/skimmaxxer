THE WHOLE PAPER, AS THE PIPELINE HAS IT

=== CONCEPT THEMES (the chapters the reader can drill into) ===

[1] theme-pre-training-and-how-it-was-reused | Pre-training and how it was reused
    Sets out the practice BERT builds on: train a model on unlabeled text once, then reuse it on a labeled task that has far less data. The paper sorts prior work into two strategies. One keeps the pre-trained model frozen and feeds its vectors into a hand-built architecture as extra features; the other adds a small head and fine-tunes every pre-trained weight. ELMo is the paper's example of the first
    members: transfer-learning, language-model-pre-training, unlabeled-text, downstream-task, sentence-level-tasks, token-level-tasks, natural-language-inference, question-answering, named-entity-recognition, pre-trained-word-embeddings, sentence-embeddings, contextual-representations, context2vec, transfer-strategies, feature-based-approach, task-specific-architecture, elmo, fine-tuning-approach, minimal-task-specific-parameters, openai-gpt, supervised-transfer-learning, low-resource-tasks
    its page opens: Most NLP tasks come with a few thousand labeled examples. Plain text comes in billions of words. This theme is about the practice BERT inherits for spending the second on the first, and about the two ways the field had settled on for doing 

[2] theme-the-one-direction-problem | The one-direction problem
    The paper's criticism of the pre-training that came before it. A standard language model predicts each token from one side only, so every token's representation is built from half its sentence. Training a left-to-right and a right-to-left model separately and joining their outputs gives both directions only at the end, and the paper lists its reasons for not doing that. BERT's answer is deep bidir
    members: unidirectionality-constraint, left-to-right-language-model, right-to-left-language-model, shallow-concatenation, ltr-rtl-concatenation-alternative, bert, deep-bidirectionality, bert-elmo-gpt-comparison
    its page opens: If pre-training on unlabeled text was already working, why did this paper need a different pre-training objective at all? Its answer is that the objective everyone was using lets each token see only one side of its own sentence. ### One sid

[3] theme-one-architecture-two-stages | One architecture, two stages
    BERT is one Transformer encoder used twice: pre-trained on unlabeled text, then fine-tuned on a labeled task from those weights. Fine-tuning yields a separate model per task, but the paper stresses that the network barely changes between the two stages. This theme also fixes the size shorthand — L layers, hidden size H, A attention heads — and the two reported configurations, 110M parameters for B
    members: bert-framework, separate-fine-tuned-models-per-task, unified-architecture-across-tasks, bert-model-architecture, transformer-encoder, self-attention, attention-quadratic-cost, bidirectional-self-attention, constrained-self-attention, model-size-notation, hidden-size-h, feed-forward-filter-size, bert-base-and-bert-large, gpt-size-match
    its page opens: If the [[unidirectionality-constraint|one-direction problem]] is what BERT wants to fix, the first question is what gets built. How do you cover sentence-level and token-level tasks alike without designing a new architecture for each one? T

[4] theme-what-bert-reads | What BERT reads
    One input format has to cover a single text, a sentence pair and a question with its passage. A sequence opens with [CLS], packs one or two spans separated by [SEP], and gives every token a vector that is the sum of three learned embeddings: which token it is, which of the two spans it sits in, and which position it occupies. Two outputs are named here because everything later reads them: C, the f
    members: input-representation, sentence-as-arbitrary-span, wordpiece-tokenization, wordpiece-vocabulary-30k, cls-token, final-hidden-vector-c, sep-token, sentence-pair-packing, segment-embeddings, position-embeddings, input-embedding-sum, final-hidden-vector-ti
    its page opens: A single text, a pair of texts, and a question with the passage that answers it are three different shapes of input. This part of the paper picks one token format that carries all three, and names the two output vectors that every later sec

[5] theme-the-two-pre-training-tasks | The two pre-training tasks
    The two self-supervised objectives, and the run that trained them. Masked LM hides 15% of the tokens and predicts them from both sides, which gets around the fact that a bidirectional model would otherwise reach the token it is predicting through the layers below. Next sentence prediction asks, from the [CLS] vector, whether span B really followed span A in the corpus. The rest is the recipe: 3.3 
    members: see-itself-problem, masked-language-model, cloze-task, masking-rate-15-percent, mask-token, mlm-output-softmax, pretrain-finetune-mismatch, masking-recipe-80-10-10, denoising-autoencoder-contrast, mlm-convergence-cost, next-sentence-prediction, isnext-notnext, full-parameter-transfer-vs-sentence-embeddings, c-not-a-sentence-representation, pre-training-procedure, pre-training-data, bookscorpus, english-wikipedia-corpus, document-level-corpus, billion-word-benchmark, training-sequence-generation, max-sequence-length-512, training-steps-and-epochs, pretraining-optimizer-settings, gelu-activation, combined-pretraining-loss, sequence-length-schedule
    its page opens: BERT wants [[deep-bidirectionality|representations where every layer conditions on both directions at once]]. The ordinary language modelling objective cannot train that, so the objective is what has to change. This chapter covers the two t

[6] theme-adapting-to-a-task | Adapting to a task
    How a pre-trained BERT becomes a task model. The task's texts are mapped onto the A and B slots — a single text uses an empty second slot — and one output layer is attached to either C or the per-token vectors. Every parameter is then trained on the labeled data, which the paper says costs at most an hour on one Cloud TPU. The hyperparameter grid is small enough to run in full, though the authors 
    members: fine-tuning-bert, task-specific-input-output-plugging, degenerate-text-pair, self-attention-unifies-cross-attention, bidirectional-cross-attention, task-specific-output-layer, end-to-end-fine-tuning, fine-tuning-cost, fine-tuning-hyperparameters, exhaustive-hyperparameter-search, dataset-size-hyperparameter-sensitivity
    its page opens: Pre-training leaves one set of weights that has never seen a labeled example. This theme answers what has to change to turn those weights into a model for entailment, or tagging, or span extraction — and the paper's answer is: the inputs, o

[7] theme-the-eleven-tasks-and-the-scores | The eleven tasks and the scores
    The evaluation itself: eleven tasks, each a small output layer on the same pre-trained encoder. GLUE supplies the sentence and sentence-pair tasks, SQuAD 1.1 and 2.0 the span extraction, and SWAG a four-way completion. Each gets its own head — a linear layer on C for GLUE, a start vector and an end vector for spans, one scoring vector over four packed sequences for SWAG. Gains are reported as abso
    members: eleven-task-evaluation, glue-benchmark, glue-score, glue-evaluation-server, c-vector-classification-head, glue-fine-tuning-protocol, fine-tuning-random-restarts, mnli, qqp, qnli, sst-2, cola, sts-b, mrpc, rte, wnli, single-task-vs-multi-task-fine-tuning, squad-v1-1, answer-span, packed-question-passage-sequence, span-prediction-head, start-and-end-vectors, span-score-and-decoding, exact-match-and-f1, triviaqa-augmentation, squad-leaderboard-public-data-rules, prior-squad-systems, squad-v2-0, unanswerable-questions, cls-as-null-span, null-span-score, tau-threshold, swag-dataset, grounded-commonsense-inference, swag-multiple-choice-head, absolute-improvement
    its page opens: The paper has claimed that one pre-trained encoder, with one small layer added on top, can be adapted to almost any task. This theme is where that claim gets tested against numbers. [[eleven-task-evaluation|The eleven-task sweep]] fine-tune

[8] theme-taking-pieces-away | Taking pieces away
    The experiments that remove or vary one piece of BERT at a time, to see what each is worth. Two cut-down pre-training runs charge a score drop to next sentence prediction and then to bidirectionality separately. A size ladder from 3 layers up to 24 finds that larger is better on every task tested, including the small ones, which the paper explains only as a hypothesis about how few fresh parameter
    members: ablation-studies, effect-of-pre-training-tasks, no-nsp-ablation, ltr-no-nsp-ablation, bilstm-on-top-of-ltr, effect-of-model-size, prior-scaling-evidence, few-random-parameters-hypothesis, effect-of-number-of-training-steps, feature-based-approach-with-bert, conll-2003-ner, tagging-without-crf, first-sub-token-classification, frozen-feature-bilstm-classifier, layer-selection-ablation, concat-last-four-layers, masking-procedure-ablation, mask-same-rnd-strategies, feature-based-amplifies-mask-mismatch
    its page opens: BERT is a bundle: bidirectional pre-training, a second sentence-pair objective, a lot of layers, a long training run, an odd masking mixture. Which of those is carrying the result? [[ablation-studies|The ablation programme]] answers by remo

=== EDGE THEMES (arguments that cut across the chapters) ===

[1] etheme-two-routes-one-direction | Two ways to reuse pre-training, both stuck in one direction
    The paper sorts earlier pre-training work into two routes. On the feature-based route, which ELMo takes, the pre-trained model is frozen and its vectors are handed to a separate task architecture as extra inputs. On the fine-tuning route, which OpenAI GPT takes and where BERT places itself, the pre-trained weights are trained further on the labeled task. The paper's complaint cuts across that spli

[2] etheme-masking-buys-bidirectionality | Masking is what buys bidirectional training
    A standard language model cannot simply be trained on both sides at once. With context available in both directions, a token in a multi-layer model reaches its own prediction through the layers below and can see itself. Masking removes the token from the input, so the see-itself path is cut and every layer may condition on both sides; the paper notes this is the old cloze task, run at a rate of 15

[3] etheme-one-encoder-one-input-format | One encoder, one input format, every task
    BERT's encoder is the Transformer encoder, which the paper describes as almost identical to the original implementation; the architectural change is that self-attention is left unmasked, where GPT's is masked to the left. Everything else that makes tasks look alike happens in the input format. Each token's input vector is the sum of three learned rows — what the token is, whether it sits in span A

[4] etheme-the-pre-training-run | What the pre-training run actually consists of
    Alongside masked prediction, BERT is trained on a second task: decide whether span B genuinely followed span A in the corpus, or was sampled from somewhere else. That task needs the A/B slots the input format already provides, and it is read off C, the final vector at [CLS] — a footnote warns that C should not be treated as a general sentence embedding without fine-tuning. Sampling contiguous span

[5] etheme-one-added-layer-per-task | Each task adds one layer, sometimes only a vector
    For a GLUE classification task the entire task-specific model is one matrix W of shape K by H applied to C, trained with ordinary cross-entropy. For extractive question answering it is two H-dimensional vectors: S scores each token as a span start, E scores it as an end, and each score is a dot product with that token's final vector T_i. A candidate span from i to j is scored by adding the two, S·

[6] etheme-the-eleven-task-scoreboard | The scoreboard, and how it was assembled
    On the eight scored GLUE tasks both model sizes lead every column, lifting the average by 4.5 points for BERT_BASE and 7.0 for BERT_LARGE. Some of the scoreboard's construction is stated openly: WNLI is dropped because no system beats the majority-class baseline on it, GLUE's own aggregate score therefore differs from the paper's average, and BERT_LARGE fine-tuning was unstable enough on small set

[7] etheme-taking-pieces-away | Taking pieces away: what each part is worth
    Two cut-down models are pre-trained under identical data, architecture and fine-tuning conditions, so any drop can be charged to the one removed piece. No NSP keeps masked prediction but drops the sentence-pair task; LTR & No NSP goes further and masks attention to the left, making an in-house stand-in for GPT that is kept left-only through fine-tuning as well. In Table 5 the sentence-pair task co

=== MAJOR CONCEPTS (each has a full page) ===
  bert | BERT: A language representation model pre-trained on unlabeled text so that every layer conditions on both left and right context, then fine-tuned with one extra output layer p
  transfer-learning | Transfer learning: Training a model on one large task or corpus and reusing its parameters on a different, usually smaller, target task.
  language-model-pre-training | Language model pre-training: Training a model on a language modeling objective over unlabeled text, then reusing it for downstream NLP tasks.
  transfer-strategies | Feature-based vs. fine-tuning transfer: The paper's taxonomy of the two ways pre-trained representations were applied downstream: as extra input features, or by fine-tuning the whole model.
  elmo | ELMo: A feature-based model that extracts context-sensitive word features from separately trained left-to-right and right-to-left language models, concatenating the two directi
  openai-gpt | OpenAI GPT: A fine-tuning approach that pre-trains a left-to-right Transformer language model on unlabeled text, then fine-tunes it on downstream tasks; the paper's main comparison p
  unidirectionality-constraint | The unidirectionality constraint: The paper's central criticism of prior pre-training: standard language models condition on one direction only, which restricts which architectures can be pre-trained and 
  deep-bidirectionality | Deep bidirectional representations: Representations in which every layer conditions jointly on both left and right context, rather than combining two one-directional models at the end.
  bert-model-architecture | BERT's model architecture: A multi-layer bidirectional Transformer encoder, described by the paper as almost identical to the original Transformer implementation.
  bert-framework | BERT's two-stage framework: Pre-train on unlabeled text with self-supervised tasks, then fine-tune every parameter on a labeled task; the same pre-trained weights initialize a separate model per tas
  input-representation | Input/output representation: A single token-sequence format that can represent either one span of text or a pair of spans, so one architecture covers every task.
  masked-language-model | Masked LM (MLM): BERT's first pre-training task: mask a random 15% of input tokens and train the model to predict the originals from context on both sides.
  next-sentence-prediction | Next Sentence Prediction (NSP): BERT's second pre-training task: a binary classification of whether span B actually followed span A in the corpus, predicted from the [CLS] vector.
  pre-training-procedure | Pre-training procedure: The concrete recipe: corpus, how input sequences are generated, batch size and step count, optimizer settings, and the sequence-length schedule.
  fine-tuning-bert | Fine-tuning BERT: Adapting the pre-trained model to a task by plugging in task-specific inputs and outputs and training every parameter end-to-end.
  eleven-task-evaluation | The eleven-task fine-tuning sweep: BERT is evaluated by fine-tuning it on eleven NLP tasks, each with a small task-specific output layer on the same pre-trained encoder.
  glue-benchmark | GLUE benchmark: A collection of sentence and sentence-pair understanding datasets under one submission format and leaderboard, used as BERT's main sentence-level testbed.
  squad-v1-1 | SQuAD v1.1: Over 100,000 crowdsourced question/answer pairs where the answer is a span of a given Wikipedia passage.
  ablation-studies | BERT's ablation programme: Experiments that remove or vary one facet of BERT at a time to see how much each contributes.
  effect-of-pre-training-tasks | Effect of pre-training tasks: Two cut-down objectives, No NSP and LTR & No NSP, trained under identical conditions to isolate what next sentence prediction and bidirectionality each buy.

=== EVIDENCE ITEMS (each has a full page) ===
  fig-1 | The Same Stack, Twice: The figure claims that pre-training and fine-tuning use one and the same network: an identical token-in, vector-out Transformer encoder appears in both panels, and the only thing that changes between 
  fig-2 | The Three-Row Input Sum: Every token BERT reads is a single vector formed by adding three learned vectors together: what the token is, which of the two packed spans it belongs to, and where it sits in the sequence. This one f
  table-1 | The GLUE test scoreboard: On the eight scored GLUE tasks, both BERT sizes beat every prior system in every column, lifting the average by 4.5 points (BERT_BASE) and 7.0 points (BERT_LARGE) over OpenAI GPT, with a 4.6-point gai
  table-2 | BERT against the SQuAD leaderboard: On SQuAD 1.1 span extraction, a fine-tuned BERT_LARGE ensemble reaches 93.2 test F1 and a single fine-tuned BERT_LARGE reaches 91.8, which the paper reports as +1.5 F1 and +1.3 F1 over the top leaderb
  table-3 | SQuAD 2.0 Leaderboard Comparison: On SQuAD 2.0, where some questions have no answer in the passage, a single fine-tuned BERT_LARGE reaches 83.1 Test F1 against 78.0 for the best prior entry the authors count — a +5.1 F1 gap, on a task
  table-4 | The SWAG Commonsense Scoreboard: On SWAG's four-way sentence-completion task, BERT_LARGE reaches 86.3% test accuracy, which the paper reports as +27.1 points over the dataset authors' own ESIM+ELMo baseline and +8.3 points over OpenA
  table-5 | Pre-training Task Ablation: Holding architecture, data and fine-tuning fixed, the table charges each drop in score to one removed piece of the pre-training recipe. Dropping next sentence prediction costs a little; replacing the 
  table-6 | The Model-Size Ladder: Six separately pre-trained BERT models, from 3 layers up to 24, are lined up smallest to largest: held-out masked-LM perplexity falls at every step (5.84 to 3.23) while accuracy on MNLI-m, MRPC and SS
  table-7 | Fine-tuned versus frozen BERT on NER: On CoNLL-2003 named entity recognition, BERT scores close to the best published systems when fine-tuned, and — the point of the table — nearly as well when its weights are frozen and only its activati
  fig-3 | BERT, GPT and ELMo Side by Side: The figure states the paper's central architectural claim as arrows: only BERT lets every position depend on both its left and its right neighbours at every layer, GPT's arrows run one way, and ELMo's
  fig-4 | Four task shapes, one encoder: Four families of downstream task are drawn as four copies of the same diagram, differing only in what is packed into the input sequence and which output vectors an arrow is attached to. The paper uses
  fig-5 | MLM vs. Left-to-Right Over a Million Steps: Fine-tuning from progressively later pre-training checkpoints shows that MNLI accuracy is still climbing at 1,000,000 steps, so BERT's long pre-training run is not wasted. It also shows that the maske
  table-8 | The masking mixture ablation: Six BERT pre-training runs that differ only in what happens to a token once it has been selected for prediction. Changing the mixture barely moves the fine-tuned scores, but pre-training with [MASK] a
  eq-classification-loss | The GLUE Classification Head: This inline expression is the whole task-specific model BERT adds for a GLUE sentence-classification task: take the final hidden vector at the [CLS] position, multiply it by one new weight matrix to g
  eq-span-start-probability | The start-position softmax: This is the whole task-specific output layer BERT adds for extractive question answering: one learned vector $S$ is dotted with each passage token's final hidden vector, and a softmax over those score
  eq-span-score | The additive span score: A candidate answer running from token $i$ to token $j$ is scored by adding its start score $S{\cdot}T_i$ to its end score $E{\cdot}T_j$, and the prediction is whichever pair with $j \geq i$ scores hig
  eq-null-answer-threshold | The Null-Answer Threshold Rule: This is the prediction-time rule BERT uses on SQuAD 2.0, where a question may have no answer. It scores "no answer" as an ordinary span that starts and ends at [CLS], compares it against the best real

=== LOAD-BEARING RELATIONSHIPS (the paper's own reasoning) ===
  deep-bidirectionality (Deep bidirectional representations) --depends-on--> masked-language-model (Masked LM (MLM)): The paper wants every layer to condition on both sides at once. A standard language model cannot be trained that way, so the masked LM objective is what makes deep bidirectionality trainable at all. Remove the masking and the cent
  masked-language-model (Masked LM (MLM)) --depends-on--> see-itself-problem (The "see itself" problem): If a token could attend to both sides while also being the prediction target, it would reach its own prediction through the layers below. The paper says such a model could then predict the target trivially. Masking exists to break
  masking-recipe-80-10-10 (The 80/10/10 masking recipe) --depends-on--> pretrain-finetune-mismatch (Pre-train / fine-tune mismatch): Of the 15% of positions chosen for prediction, 80% become [MASK], 10% become a random token, and 10% are left alone. The paper gives the mismatch as the reason for the last two cases: the model should not learn that a prediction i
  next-sentence-prediction (Next Sentence Prediction (NSP)) --depends-on--> sentence-pair-packing (Sentence A / sentence B packing): The label being predicted is whether span B really followed span A. That question only exists once two spans are packed into one sequence and marked as first and second. Without the A/B format there is nothing for NSP to classify.
  next-sentence-prediction (Next Sentence Prediction (NSP)) --depends-on--> final-hidden-vector-c (C, the final [CLS] vector): The binary IsNext/NotNext decision is made from the final hidden vector at the [CLS] position. The paper reports the final model reaching 97-98% accuracy on this task. A footnote adds a caution: because C was trained this way, it 
  self-attention-unifies-cross-attention (Self-attention subsuming cross-attention) --depends-on--> bidirectional-self-attention (Bidirectional self-attention): The paper argues that encoding a concatenated pair with self-attention already includes attention between the two texts in both directions. That holds only because every position may attend to every other position. Under left-only
  unified-architecture-across-tasks (Unified architecture across tasks) --depends-on--> input-representation (Input/output representation): The paper calls it a distinctive feature that the pre-trained and downstream architectures differ minimally. That is possible because a single token-sequence format already covers one span or two. Each task changes what goes into 
  span-prediction-head (Start/end vector span prediction head) --depends-on--> final-hidden-vector-ti (T_i, the final vector for token i): The start distribution is a softmax over dot products S·T_i, taken across every token in the passage. So the head needs one final-layer vector per position, not a single pooled vector. This is the token-level output path, in contr
  cls-as-null-span ([CLS] as the null span) --depends-on--> span-prediction-head (Start/end vector span prediction head): SQuAD 2.0 allows a question with no answer in the passage. Rather than adding a new classifier, the paper treats no-answer as a span that starts and ends at [CLS], scored with the same S and E vectors. The extension adds no task-s
  ltr-no-nsp-ablation (LTR & No NSP) --depends-on--> constrained-self-attention (Constrained (left-context-only) self-attention): The LTR & No NSP run is the same network with attention masked so each token sees only its left context. That masking is exactly the constraint the GPT Transformer uses. It is what makes this run a stand-in for GPT while holding d
  effect-of-pre-training-tasks (Effect of pre-training tasks) --depends-on--> pre-training-procedure (Pre-training procedure): Table 5 charges each score drop to one removed piece of the recipe. That reading is only valid because the cut-down runs use exactly the same pre-training data, fine-tuning scheme and hyperparameters as BERT_BASE. The fixed proced
  bert-model-architecture (BERT's model architecture) --depends-on--> transformer-encoder (Transformer encoder): The paper does not describe the encoder in any detail. It states that the implementation is almost identical to the original Transformer and points readers to Vaswani et al. and to the Annotated Transformer guide. Everything BERT 
  bert-framework (BERT's two-stage framework) --supported-by--> fig-1 (The Same Stack, Twice): The paper's framing claim is that pre-training and fine-tuning are two stages of one network, not two networks. Figure 1 puts the two panels side by side and the encoder block is identical in both. What changes between them is onl
  input-embedding-sum (Sum of token, segment and position embeddings) --supported-by--> fig-2 (The Three-Row Input Sum): Figure 2 draws each token's input as three stacked rows joined by plus signs: the token embedding, the segment embedding for span A or span B, and the position embedding. The picture is the definition. It shows that BERT does not 
  deep-bidirectionality (Deep bidirectional representations) --supported-by--> fig-3 (BERT, GPT and ELMo Side by Side): Figure 3 draws BERT, GPT and ELMo as three connection diagrams. Only BERT's has arrows running into a position from both sides at every layer. This is the paper's central claim rendered as a picture, so it establishes what is bein
  glue-benchmark (GLUE benchmark) --supported-by--> table-1 (The GLUE test scoreboard): Table 1 reports BERT_BASE at 79.6 average and BERT_LARGE at 82.1, against 75.1 for OpenAI GPT and 74.0 for the pre-OpenAI state of the art. That is the 4.5 and 7.0 point improvement the abstract quotes. Both BERT rows are also ahe
  gpt-size-match (Deliberate size match with OpenAI GPT) --supported-by--> table-1 (The GLUE test scoreboard): BERT_BASE was built at OpenAI GPT's size on purpose, so a gap in Table 1 cannot simply be capacity. On that row-to-row comparison BERT_BASE scores 79.6 against GPT's 75.1. The paper hedges this itself in Appendix A.4: GPT was trai
  squad-v1-1 (SQuAD v1.1) --supported-by--> table-2 (BERT against the SQuAD leaderboard): Table 2 puts the fine-tuned model against both published systems and live leaderboard entries. A single BERT_LARGE reaches 91.8 test F1 and the seven-model ensemble reaches 93.2, against 91.7 for the top ensemble on the board. The
  squad-v2-0 (SQuAD v2.0) --supported-by--> table-3 (SQuAD 2.0 Leaderboard Comparison): SQuAD 2.0 allows questions with no answer in the passage. Table 3 shows a single BERT_LARGE at 83.1 test F1 against 78.0 for the best prior system, the +5.1 the paper quotes. Human performance is 89.5, so the gap to people is wide
  swag-dataset (SWAG (Situations With Adversarial Generations)) --supported-by--> table-4 (The SWAG Commonsense Scoreboard): Table 4 reports BERT_LARGE at 86.3% test accuracy on SWAG's four-way completion task. The dataset authors' ESIM+ELMo baseline sits at 59.2%, which is the +27.1 point figure, and OpenAI GPT at 78.0% gives the 8.3 point margin. The 
  deep-bidirectionality (Deep bidirectional representations) --supported-by--> table-5 (Pre-training Task Ablation): Table 5 compares No NSP against LTR & No NSP with the same data, hyperparameters and fine-tuning, so the only change is the direction of context. SQuAD F1 falls from 87.9 to 77.8 and MRPC from 86.5 to 77.5. MNLI moves much less, 8
  next-sentence-prediction (Next Sentence Prediction (NSP)) --supported-by--> table-5 (Pre-training Task Ablation): Removing NSP takes BERT_BASE from 88.4 to 84.9 on QNLI, which is the largest single drop. The other columns move much less: MNLI 84.4 to 83.9, SQuAD 88.5 to 87.9, SST-2 92.7 to 92.6. The paper calls this hurting performance signif
  effect-of-model-size (Effect of model size) --supported-by--> table-6 (The Model-Size Ladder): Table 6 lines up six separately pre-trained models from three layers to twenty-four. Held-out masked-LM perplexity falls at every step, from 5.84 down to 3.23, and the fine-tuning accuracies rise with it: MNLI from 77.9 to 86.6, M
  feature-based-approach-with-bert (Feature-based approach with BERT) --supported-by--> table-7 (Fine-tuned versus frozen BERT on NER): Table 7 runs CoNLL-2003 NER both ways from the same pre-trained model. Fine-tuned BERT_BASE reaches 96.4 Dev F1; the best frozen-feature setup reaches 96.1, which is the 0.3 F1 gap the paper cites. The frozen rows report Dev score
  mlm-convergence-cost (MLM's slower convergence) --supported-by--> fig-5 (MLM vs. Left-to-Right Over a Million Steps): Masked LM predicts only 15% of tokens per batch, so it gets fewer gradient signals per step than a left-to-right model and should converge more slowly. Figure 5 shows exactly that in the curve shapes, while the masked LM line stil
  c-vector-classification-head (C-vector classification head) --supported-by--> eq-classification-loss (The GLUE Classification Head): For a GLUE task the model takes C, the final hidden vector at the [CLS] position, multiplies it by one new weight matrix W of shape K by H, and applies a softmax. That expression is the entire task-specific model. Everything else 
  start-and-end-vectors (Start vector S and end vector E) --supported-by--> eq-span-start-probability (The start-position softmax): For question answering the paper adds a start vector S and an end vector E, each of dimension H. The start probability for token i is the softmax of S dotted with that token's final vector, taken over all tokens in the paragraph. 
  span-score-and-decoding (Span score and maximum-scoring-span decoding) --supported-by--> eq-span-score (The additive span score): A candidate span from token i to token j scores as S dotted with T_i plus E dotted with T_j, and the prediction is the highest-scoring pair with j at or after i. The score is additive, which means start and end are chosen jointly 
  null-span-score (Null score s_null) --supported-by--> eq-null-answer-threshold (The Null-Answer Threshold Rule): On SQuAD 2.0 the no-answer score is s_null equals S dotted with C plus E dotted with C, using the same start and end vectors against the [CLS] vector. Abstaining is therefore just another candidate span in the same score space. No
  bert (BERT) --instance-of--> fine-tuning-approach (Fine-tuning approach): The paper's own taxonomy splits transfer into feature-based and fine-tuning. BERT sits in the second box: it adds one small output layer and then trains every pre-trained parameter on the labeled task. Reading BERT as an instance 
  bert-model-architecture (BERT's model architecture) --instance-of--> transformer-encoder (Transformer encoder): The paper says its architecture is a multi-layer bidirectional Transformer encoder, almost identical to the original implementation, and declines to re-describe it. So BERT's encoder is one configured instance of the cited Transfo
  bidirectional-self-attention (Bidirectional self-attention) --instance-of--> self-attention (Self-attention): Self-attention in general lets each position mix information from the positions it may attend to. Bidirectional self-attention is the case where that permitted set is the whole sequence, left and right, at every layer.
  masking-recipe-80-10-10 (The 80/10/10 masking recipe) --instance-of--> mask-same-rnd-strategies (MASK, SAME and RND): Once a token is selected for prediction, three things can happen to it: MASK, SAME or RND. BERT's recipe is one particular mixture of those three, 80/10/10. Appendix C.2 varies the mixture and reports that fine-tuning barely moves
  c-vector-classification-head (C-vector classification head) --instance-of--> task-specific-output-layer (One additional output layer): The design goal is that a task adds a single output layer. For GLUE that layer is one weight matrix W in R^(K x H) applied to C, where K is the number of labels. That matrix is the only new parameter set fine-tuning introduces.
  span-prediction-head (Start/end vector span prediction head) --instance-of--> task-specific-output-layer (One additional output layer): Extractive question answering adds only two H-dimensional vectors, S and E. Dotted against each token's final vector they give a start distribution and an end distribution over the passage. Prior SQuAD systems carried purpose-buil
  null-span-score (Null score s_null) --instance-of--> span-score-and-decoding (Span score and maximum-scoring-span decoding): s_null = S.C + E.C is the ordinary span score with both ends placed at [CLS]. Nothing new is computed; the same S and E vectors are read against the same kind of hidden vector. That is how abstention enters the same probability sp
  feature-based-approach-with-bert (Feature-based approach with BERT) --instance-of--> feature-based-approach (Feature-based approach): Section 5.3 applies the general feature-based strategy to BERT itself: freeze the encoder, extract activations, train something else on top. It is included to show the pre-trained model is not only useful when fine-tuned, and the 
  ltr-no-nsp-ablation (LTR & No NSP) --instance-of--> left-to-right-language-model (Left-to-right language model): To charge a score drop to bidirectionality rather than to data or scale, the paper trains a left-to-right model itself, on the same corpus and with the same fine-tuning. It is the closest stand-in for GPT the authors control, and 
  feature-based-approach (Feature-based approach) --contrasts-with--> fine-tuning-approach (Fine-tuning approach): The paper opens by sorting all prior pre-training work into two camps. One camp freezes the pre-trained model and feeds its vectors into a hand-built task architecture as extra features. The other adds almost no new weights and tr
  bert (BERT) --contrasts-with--> openai-gpt (OpenAI GPT): GPT is the method BERT is set up to be measured against, and Appendix A.4 calls it the most comparable existing approach. Both pre-train a Transformer and then fine-tune every parameter. The stated difference is that GPT's tokens 
  deep-bidirectionality (Deep bidirectional representations) --contrasts-with--> shallow-concatenation (Shallow concatenation of two directional models): Both designs aim to give a token access to text on both sides of it. Shallow concatenation trains a left-to-right model and a right-to-left model separately, then glues their outputs together at the end. BERT instead conditions on
  bidirectional-self-attention (Bidirectional self-attention) --contrasts-with--> constrained-self-attention (Constrained (left-context-only) self-attention): The two are the same operation with different masks over which positions a token may look at. Constrained attention blanks out everything to a token's right, which is what makes standard left-to-right pre-training possible. Removi
  masked-language-model (Masked LM (MLM)) --contrasts-with--> left-to-right-language-model (Left-to-right language model): A left-to-right language model predicts each token from what came before it, so each prediction sees one side only. Masked LM instead hides a random subset of tokens and predicts them from both sides at once. The paper's whole cas
  mask-token ([MASK] token) --contrasts-with--> pretrain-finetune-mismatch (Pre-train / fine-tune mismatch): The [MASK] placeholder is what makes bidirectional prediction trainable at all. But [MASK] never appears in any downstream task, so the model spends pre-training in conditions it will never meet again. The paper names this cost pl
  fine-tuning-bert (Fine-tuning BERT) --contrasts-with--> feature-based-approach-with-bert (Feature-based approach with BERT): Section 5.3 runs BERT both ways on CoNLL-2003 NER to price the difference. Fine-tuning the whole model reaches 96.4 Dev F1 for BERT_BASE; freezing it and concatenating the top four layers reaches 96.1, which is 0.3 F1 behind. The 
  no-nsp-ablation (No NSP) --contrasts-with--> ltr-no-nsp-ablation (LTR & No NSP): These two cut-down models are trained on the same data, with the same fine-tuning scheme and hyperparameters, and differ in one thing: whether the pre-training objective sees both sides. Comparing them charges the difference to bi
