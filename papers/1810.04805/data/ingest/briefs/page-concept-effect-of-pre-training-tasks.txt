PAGE FOR CONCEPT: effect-of-pre-training-tasks
NAME: Effect of pre-training tasks

SUMMARY (already shown above your page as the lede - do not repeat it verbatim):
Two cut-down objectives, No NSP and LTR & No NSP, trained under identical conditions to isolate what next sentence prediction and bidirectionality each buy.

DRAFT EXPLANATION from the extraction stage (your page replaces this; keep what is right, go deeper):
Both variants use exactly the same data, fine-tuning scheme and hyperparameters as BERT_BASE, so the only difference is the objective. Comparing BERT_BASE with No NSP isolates NSP; comparing No NSP with LTR & No NSP isolates bidirectionality. The paper reports that removing NSP hurts significantly on QNLI, MNLI and SQuAD 1.1, and that the LTR model is worse on all tasks, with large drops on MRPC and SQuAD. Appendix A.4 leans on this ablation to argue the four uncontrolled BERT/GPT differences are secondary.

THEME IT BELONGS TO: theme-taking-pieces-away | Taking pieces away - The experiments that remove or vary one piece of BERT at a time, to see what each is worth. Two cut-down pre-training runs charge a score drop to next sentence prediction and then to bidirectionality separately. A size ladder from 3 layers up to 24 finds that larger is better on every task tested, including the small ones, which the paper explains only as a hypothesis about how few fresh parameters fine-tuning adds. Two further ablations ask whether the full million steps are needed and whether the 80/10/10 masking mixture matters, the second run both fine-tuned and with BERT frozen on CoNLL-2003 NER.

SUB-CONCEPTS (these render as expandable sections BELOW your page - reference them, do not duplicate them):
  - no-nsp-ablation | No NSP: A bidirectional model pre-trained with the masked LM objective but without next sentence prediction.
  - ltr-no-nsp-ablation | LTR & No NSP: A left-context-only model trained with a standard left-to-right language model and no NSP; the closest in-house stand-in for OpenAI GPT.
    - bilstm-on-top-of-ltr | BiLSTM on top of the LTR model: A randomly initialised BiLSTM added above the left-to-right model as a good-faith attempt to supply right-side context; it partly repairs SQuAD but not GLUE.

PREREQUISITES:
  - ablation-studies | BERT's ablation programme: Experiments that remove or vary one facet of BERT at a time to see how much each contributes.
  - masked-language-model | Masked LM (MLM): BERT's first pre-training task: mask a random 15% of input tokens and train the model to predict the originals from context on both sides.
  - next-sentence-prediction | Next Sentence Prediction (NSP): BERT's second pre-training task: a binary classification of whether span B actually followed span A in the corpus, predicted from the [CLS] vector.

EDGES TOUCHING THIS CONCEPT (1) - the relationships the app already knows; weave the load-bearing ones into your prose:
  [load-bearing] -> pre-training-procedure (Pre-training procedure) | depends-on | ablation needs identical conditions: Table 5 charges each score drop to one removed piece of the recipe. That reading is only valid because the cut-down runs use exactly the same pre-training data, fine-tuning scheme and hyperparameters as BERT_BASE. The fixed procedure is the control the comparison rests on.

PAPER SECTIONS TO READ (in papers/1810.04805/data/ingest/sections/):
  - 16-5.1-effect-of-pre-training-tasks.txt
