PAGE FOR THEME: theme-one-architecture-two-stages
NAME: One architecture, two stages

THEME SUMMARY (shown above your page as the lede - do not repeat verbatim):
BERT is one Transformer encoder used twice: pre-trained on unlabeled text, then fine-tuned on a labeled task from those weights. Fine-tuning yields a separate model per task, but the paper stresses that the network barely changes between the two stages. This theme also fixes the size shorthand — L layers, hidden size H, A attention heads — and the two reported configurations, 110M parameters for BERT_BASE and 340M for BERT_LARGE. BERT_BASE was sized to match OpenAI GPT so the later comparison turns on bidirectionality rather than capacity.

PREVIOUS THEME: theme-the-one-direction-problem | The one-direction problem
NEXT THEME: theme-what-bert-reads | What BERT reads

MEMBER CONCEPTS (14) - each has its own page or expandable section; your job is the connective tissue between them, not to restate each one:
  - bert-framework | BERT's two-stage framework [major]: Pre-train on unlabeled text with self-supervised tasks, then fine-tune every parameter on a labeled task; the same pre-trained weights initialize a separate model per task.
      · separate-fine-tuned-models-per-task | One fine-tuned model per task: Fine-tuning produces a distinct model for each downstream task, all branching from the same pre-trained starti
      · unified-architecture-across-tasks | Unified architecture across tasks: The paper calls it a distinctive feature that the pre-trained architecture and the final downstream architectu
      · pre-training-procedure | Pre-training procedure: The concrete recipe: corpus, how input sequences are generated, batch size and step count, optimizer settings,
      · pre-training-data | Pre-training corpus: BooksCorpus (800M words) plus English Wikipedia (2,500M words), roughly 3.3 billion words in total.
      · bookscorpus | BooksCorpus (800M words): A corpus of unpublished books forming the smaller half of BERT's pre-training data, and the whole of GPT's.
      · english-wikipedia-corpus | English Wikipedia (2,500M words): The larger half of the pre-training data, with only running text kept.
  - separate-fine-tuned-models-per-task | One fine-tuned model per task [minor]: Fine-tuning produces a distinct model for each downstream task, all branching from the same pre-trained starting point.
  - unified-architecture-across-tasks | Unified architecture across tasks [minor]: The paper calls it a distinctive feature that the pre-trained architecture and the final downstream architecture differ minimally.
  - bert-model-architecture | BERT's model architecture [major]: A multi-layer bidirectional Transformer encoder, described by the paper as almost identical to the original Transformer implementation.
      · transformer-encoder | Transformer encoder: A stack of identical blocks, each combining multi-head self-attention over the whole sequence with a position-
      · self-attention | Self-attention: The operation by which each position forms its next representation as a weighted combination of all positions 
      · attention-quadratic-cost | Attention is quadratic in sequence length: Each layer scores every pair of positions, so doubling the sequence length roughly quadruples attention cost.
      · bidirectional-self-attention | Bidirectional self-attention: Self-attention in which every token may attend to every other token, left and right, at every layer.
      · constrained-self-attention | Constrained (left-context-only) self-attention: Self-attention masked so each token attends only to tokens on its left, as in the GPT Transformer.
      · model-size-notation | Model size notation: L, H, A: The paper's shorthand for a configuration: $L$ layers, hidden size $H$, and $A$ self-attention heads.
  - transformer-encoder | Transformer encoder [minor]: A stack of identical blocks, each combining multi-head self-attention over the whole sequence with a position-wise feed-forward network, mapping token embeddings to contextual vectors.
      · self-attention | Self-attention: The operation by which each position forms its next representation as a weighted combination of all positions 
      · attention-quadratic-cost | Attention is quadratic in sequence length: Each layer scores every pair of positions, so doubling the sequence length roughly quadruples attention cost.
  - self-attention | Self-attention [minor]: The operation by which each position forms its next representation as a weighted combination of all positions it is allowed to attend to.
      · attention-quadratic-cost | Attention is quadratic in sequence length: Each layer scores every pair of positions, so doubling the sequence length roughly quadruples attention cost.
  - attention-quadratic-cost | Attention is quadratic in sequence length [minor]: Each layer scores every pair of positions, so doubling the sequence length roughly quadruples attention cost.
  - bidirectional-self-attention | Bidirectional self-attention [minor]: Self-attention in which every token may attend to every other token, left and right, at every layer.
      · constrained-self-attention | Constrained (left-context-only) self-attention: Self-attention masked so each token attends only to tokens on its left, as in the GPT Transformer.
  - constrained-self-attention | Constrained (left-context-only) self-attention [minor]: Self-attention masked so each token attends only to tokens on its left, as in the GPT Transformer.
  - model-size-notation | Model size notation: L, H, A [minor]: The paper's shorthand for a configuration: $L$ layers, hidden size $H$, and $A$ self-attention heads.
      · hidden-size-h | Hidden size (H): The dimension of the vectors passed between layers; 768 for BERT_BASE and 1024 for BERT_LARGE.
      · feed-forward-filter-size | Feed-forward/filter size of 4H: The inner width of each block's feed-forward network is four times the hidden size: 3072 when $H=768$, 4096 wh
  - hidden-size-h | Hidden size (H) [minor]: The dimension of the vectors passed between layers; 768 for BERT_BASE and 1024 for BERT_LARGE.
  - feed-forward-filter-size | Feed-forward/filter size of 4H [minor]: The inner width of each block's feed-forward network is four times the hidden size: 3072 when $H=768$, 4096 when $H=1024$.
  - bert-base-and-bert-large | BERT_BASE and BERT_LARGE [minor]: The two reported configurations: $L=12, H=768, A=12$ with 110M parameters, and $L=24, H=1024, A=16$ with 340M.
      · gpt-size-match | Deliberate size match with OpenAI GPT: BERT_BASE was sized to equal OpenAI GPT specifically so the comparison isolates bidirectionality rather than c
  - gpt-size-match | Deliberate size match with OpenAI GPT [minor]: BERT_BASE was sized to equal OpenAI GPT specifically so the comparison isolates bidirectionality rather than capacity.

EDGES BETWEEN MEMBERS OF THIS THEME:
  [load-bearing] bert-model-architecture --depends-on--> transformer-encoder | reuses the Transformer unchanged: The paper does not describe the encoder in any detail. It states that the implementation is almost identical to the original Transformer and points readers to Vaswani et al. and to the Annotated Transformer guide. Everything BERT adds sits around a mechanism it borrows rather than defines.
  [load-bearing] bert-model-architecture --instance-of--> transformer-encoder | a particular Transformer encoder stack: The paper says its architecture is a multi-layer bidirectional Transformer encoder, almost identical to the original implementation, and declines to re-describe it. So BERT's encoder is one configured instance of the cited Transformer encoder, not a new design.
  [load-bearing] bidirectional-self-attention --instance-of--> self-attention | self-attention with no mask: Self-attention in general lets each position mix information from the positions it may attend to. Bidirectional self-attention is the case where that permitted set is the whole sequence, left and right, at every layer.
  [supporting] constrained-self-attention --instance-of--> self-attention | self-attention masked to the left: The GPT Transformer runs the same operation but with a mask that hides every position to the right of the current token. The paper notes this is the only architectural difference between BERT_BASE and GPT, apart from the masking.
  [supporting] bert-base-and-bert-large --instance-of--> model-size-notation | two settings of L, H, A: L, H and A are the paper's three dials: layers, hidden size, attention heads. BERT_BASE fixes them at 12, 768 and 12 for 110M parameters; BERT_LARGE at 24, 1024 and 16 for 340M. Every reported result belongs to one of these two settings.
  [load-bearing] bidirectional-self-attention --contrasts-with--> constrained-self-attention | attend everywhere versus attend leftward: The two are the same operation with different masks over which positions a token may look at. Constrained attention blanks out everything to a token's right, which is what makes standard left-to-right pre-training possible. Removing that mask is the single architectural change the paper says separates BERT from GPT.
  [supporting] unified-architecture-across-tasks --contrasts-with--> separate-fine-tuned-models-per-task | one architecture, eleven models: The paper calls it a distinctive feature that the pre-trained network and the downstream network differ only minimally. That uniformity is about shape, not about weights. Every task still ends up with its own fully fine-tuned copy, so eleven tasks means eleven models branching from one checkpoint.

LOAD-BEARING EDGES LEAVING THIS THEME (use these to point forward/back):
  self-attention-unifies-cross-attention --depends-on--> bidirectional-self-attention | argument needs unmasked attention
  unified-architecture-across-tasks --depends-on--> input-representation | one format enables one architecture
  ltr-no-nsp-ablation --depends-on--> constrained-self-attention | ablation built by masking attention
  bert-framework --supported-by--> fig-1 | same encoder drawn twice
  gpt-size-match --supported-by--> table-1 | matched size isolates bidirectionality

EVIDENCE ITEMS RELATED TO THIS THEME (link as [[item-id]]):
  - fig-1 | The Same Stack, Twice: The figure claims that pre-training and fine-tuning use one and the same network: an identical token-in, vector-out Transformer encoder appears in both panels, and the only thing that changes between them is the thin output layer bolted on top. The dotted arrows carry every pre-trained weight across to initialize the task model, so a downstream task inherits the whole network rather than a set of embeddings.
  - table-1 | The GLUE test scoreboard: On the eight scored GLUE tasks, both BERT sizes beat every prior system in every column, lifting the average by 4.5 points (BERT_BASE) and 7.0 points (BERT_LARGE) over OpenAI GPT, with a 4.6-point gain on MNLI. Because BERT_BASE is the same size as GPT, the paper reads the gap as evidence for bidirectional pre-training rather than for extra capacity.
  - table-2 | BERT against the SQuAD leaderboard: On SQuAD 1.1 span extraction, a fine-tuned BERT_LARGE ensemble reaches 93.2 test F1 and a single fine-tuned BERT_LARGE reaches 91.8, which the paper reports as +1.5 F1 and +1.3 F1 over the top leaderboard system of the day. Both figures come with an asterisk the paper states itself: the leaderboard entries may train on any public data, so BERT is first fine-tuned on TriviaQA to match, and dropping that extra data costs only 0.1-0.4 points.
  - table-3 | SQuAD 2.0 Leaderboard Comparison: On SQuAD 2.0, where some questions have no answer in the passage, a single fine-tuned BERT_LARGE reaches 83.1 Test F1 against 78.0 for the best prior entry the authors count — a +5.1 F1 gap, on a task the paper handles by adding only two vectors and one threshold. It still sits about 6 F1 below the reported human number, and the comparison is restricted to systems that do not already use BERT inside them.
  - table-4 | The SWAG Commonsense Scoreboard: On SWAG's four-way sentence-completion task, BERT_LARGE reaches 86.3% test accuracy, which the paper reports as +27.1 points over the dataset authors' own ESIM+ELMo baseline and +8.3 points over OpenAI GPT. That figure also sits above the 85.0 scored by a single human expert, though the dagger footnote says the human numbers come from only 100 samples, so the human comparison rests on much thinner ground than the model comparisons.
  - table-5 | Pre-training Task Ablation: Holding architecture, data and fine-tuning fixed, the table charges each drop in score to one removed piece of the pre-training recipe. Dropping next sentence prediction costs a little; replacing the masked LM with a left-to-right LM costs much more, above all on SQuAD (88.5 to 77.8 F1), and bolting a BiLSTM onto the left-to-right model repairs only part of that gap while making the GLUE numbers slightly worse.
  - table-6 | The Model-Size Ladder: Six separately pre-trained BERT models, from 3 layers up to 24, are lined up smallest to largest: held-out masked-LM perplexity falls at every step (5.84 to 3.23) while accuracy on MNLI-m, MRPC and SST-2 rises at every step. The paper reads this as evidence that scaling keeps paying off past sizes already considered large, and — its actual point — that the gains reach a task as small as MRPC, which has only 3,600 training examples.
  - table-7 | Fine-tuned versus frozen BERT on NER: On CoNLL-2003 named entity recognition, BERT scores close to the best published systems when fine-tuned, and — the point of the table — nearly as well when its weights are frozen and only its activations are fed to a small task model. Concatenating the top four hidden layers gives 96.1 Dev F1 against 96.4 for fine-tuning the same model, a gap of 0.3.
  - fig-3 | BERT, GPT and ELMo Side by Side: The figure states the paper's central architectural claim as arrows: only BERT lets every position depend on both its left and its right neighbours at every layer, GPT's arrows run one way, and ELMo's two one-way stacks meet only at the very top. The caption adds a second and separate difference: BERT and GPT adapt to a task by fine-tuning the whole model, while ELMo hands its outputs over as fixed features.
  - fig-4 | Four task shapes, one encoder: Four families of downstream task are drawn as four copies of the same diagram, differing only in what is packed into the input sequence and which output vectors an arrow is attached to. The paper uses this to show that adapting BERT to a task means plugging in task-specific inputs and one extra output layer, not redesigning the model.
  - fig-5 | MLM vs. Left-to-Right Over a Million Steps: Fine-tuning from progressively later pre-training checkpoints shows that MNLI accuracy is still climbing at 1,000,000 steps, so BERT's long pre-training run is not wasted. It also shows that the masked LM objective is behind a left-to-right objective at the very first checkpoints, overtakes it before 100k steps, and finishes roughly two points ahead.
  - eq-classification-loss | The GLUE Classification Head: This inline expression is the whole task-specific model BERT adds for a GLUE sentence-classification task: take the final hidden vector at the [CLS] position, multiply it by one new weight matrix to get one score per label, and train with ordinary cross-entropy. The paper's point in printing it is how little there is — a single $K \times H$ matrix is the only parameter matrix fine-tuning introduces.
  - eq-span-start-probability | The start-position softmax: This is the whole task-specific output layer BERT adds for extractive question answering: one learned vector $S$ is dotted with each passage token's final hidden vector, and a softmax over those scores gives a probability distribution over where the answer span starts. A second vector $E$ does the same for the end, and training maximizes the summed log-likelihood of the correct start and end positions.
  - eq-span-score | The additive span score: A candidate answer running from token $i$ to token $j$ is scored by adding its start score $S{\cdot}T_i$ to its end score $E{\cdot}T_j$, and the prediction is whichever pair with $j \geq i$ scores highest. The whole answer-selection machinery is two dot products and a comparison, with no module that looks at the span as a unit.

PAPER SECTIONS TO READ (in papers/1810.04805/data/ingest/sections/):
  - 02-1-introduction.txt
  - 07-3-bert.txt
  - 08-3.1-pre-training-bert.txt
  - 09-3.2-fine-tuning-bert.txt
  - 11-4.1-glue.txt
  - 17-5.2-effect-of-model-size.txt
  - 23-A.2-pre-training-procedure.txt
  - 26-A.5-illustrations-of-fine-tuning-on-different.txt
