PAGE FOR CONCEPT: unidirectionality-constraint
NAME: The unidirectionality constraint

SUMMARY (already shown above your page as the lede - do not repeat it verbatim):
The paper's central criticism of prior pre-training: standard language models condition on one direction only, which restricts which architectures can be pre-trained and how well they transfer.

DRAFT EXPLANATION from the extraction stage (your page replaces this; keep what is right, go deeper):
The argument runs in two steps. First, a standard language model predicts each token from those before it, so the architecture must keep future tokens hidden — in GPT every token attends only to previous tokens. Second, the paper claims this is sub-optimal for sentence-level tasks and could be very harmful for token-level ones like question answering. It says the limitation bites hardest for fine-tuning approaches, since there the restricted architecture is what gets used downstream.

THEME IT BELONGS TO: theme-the-one-direction-problem | The one-direction problem - The paper's criticism of the pre-training that came before it. A standard language model predicts each token from one side only, so every token's representation is built from half its sentence. Training a left-to-right and a right-to-left model separately and joining their outputs gives both directions only at the end, and the paper lists its reasons for not doing that. BERT's answer is deep bidirectionality: every layer conditioning on both sides at once, which is the gap the rest of the paper is built to close.

SUB-CONCEPTS (these render as expandable sections BELOW your page - reference them, do not duplicate them):
  - left-to-right-language-model | Left-to-right language model: A model that assigns probability to text by predicting each token from the tokens that precede it.
    - right-to-left-language-model | Right-to-left language model: The mirror image: predict each token from the tokens that follow it.
  - shallow-concatenation | Shallow concatenation of two directional models: Getting both directions by training a left-to-right and a right-to-left model independently and joining their outputs, rather than conditioning on both sides inside the model.
    - ltr-rtl-concatenation-alternative | Why not concatenate LTR and RTL models: Section 5.1's three stated reasons for not training separate directional models and concatenating them.

PREREQUISITES:
  - language-model-pre-training | Language model pre-training: Training a model on a language modeling objective over unlabeled text, then reusing it for downstream NLP tasks.
  - left-to-right-language-model | Left-to-right language model: A model that assigns probability to text by predicting each token from the tokens that precede it.

EVIDENCE ITEMS THAT SHOW THIS (link them as [[item-id]]):
  - fig-3 | BERT, GPT and ELMo Side by Side: The figure states the paper's central architectural claim as arrows: only BERT lets every position depend on both its left and its right neighbours at every layer, GPT's arrows run one way, and ELMo's two one-way stacks meet only at the very top. The caption adds a second and separate difference: BERT and GPT adapt to a task by fine-tuning the whole model, while ELMo hands its outputs over as fixed features.

PAPER SECTIONS TO READ (in papers/1810.04805/data/ingest/sections/):
  - 02-1-introduction.txt
