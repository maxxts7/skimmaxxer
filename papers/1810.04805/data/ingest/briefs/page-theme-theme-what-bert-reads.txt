PAGE FOR THEME: theme-what-bert-reads
NAME: What BERT reads

THEME SUMMARY (shown above your page as the lede - do not repeat verbatim):
One input format has to cover a single text, a sentence pair and a question with its passage. A sequence opens with [CLS], packs one or two spans separated by [SEP], and gives every token a vector that is the sum of three learned embeddings: which token it is, which of the two spans it sits in, and which position it occupies. Two outputs are named here because everything later reads them: C, the final vector at [CLS], and T_i, the final vector at token i. Read this before the pre-training tasks, which are defined over exactly these positions.

PREVIOUS THEME: theme-one-architecture-two-stages | One architecture, two stages
NEXT THEME: theme-the-two-pre-training-tasks | The two pre-training tasks

MEMBER CONCEPTS (12) - each has its own page or expandable section; your job is the connective tissue between them, not to restate each one:
  - input-representation | Input/output representation [major]: A single token-sequence format that can represent either one span of text or a pair of spans, so one architecture covers every task.
      · sentence-as-arbitrary-span | "Sentence" as an arbitrary span: In this paper a "sentence" means any span of contiguous text, not a linguistic sentence, and a "sequence" is t
      · wordpiece-tokenization | WordPiece embeddings: A subword vocabulary that splits rare words into smaller known pieces, used here with 30,000 tokens.
      · wordpiece-vocabulary-30k | 30,000-token vocabulary: The size of BERT's WordPiece vocabulary, which also fixes the width of the Masked LM output softmax.
      · cls-token | [CLS] token: A special token placed first in every sequence, whose final hidden state is used as the aggregate representati
      · final-hidden-vector-c | C, the final [CLS] vector: The paper's notation for the final hidden vector at the [CLS] position, $C \in \mathbb{R}^H$.
      · sep-token | [SEP] token: A special token marking the boundary between two packed spans, and closing the sequence.
  - sentence-as-arbitrary-span | "Sentence" as an arbitrary span [minor]: In this paper a "sentence" means any span of contiguous text, not a linguistic sentence, and a "sequence" is the token sequence actually fed to BERT.
  - wordpiece-tokenization | WordPiece embeddings [minor]: A subword vocabulary that splits rare words into smaller known pieces, used here with 30,000 tokens.
      · wordpiece-vocabulary-30k | 30,000-token vocabulary: The size of BERT's WordPiece vocabulary, which also fixes the width of the Masked LM output softmax.
  - wordpiece-vocabulary-30k | 30,000-token vocabulary [minor]: The size of BERT's WordPiece vocabulary, which also fixes the width of the Masked LM output softmax.
  - cls-token | [CLS] token [minor]: A special token placed first in every sequence, whose final hidden state is used as the aggregate representation of the whole input.
      · final-hidden-vector-c | C, the final [CLS] vector: The paper's notation for the final hidden vector at the [CLS] position, $C \in \mathbb{R}^H$.
  - final-hidden-vector-c | C, the final [CLS] vector [minor]: The paper's notation for the final hidden vector at the [CLS] position, $C \in \mathbb{R}^H$.
  - sep-token | [SEP] token [minor]: A special token marking the boundary between two packed spans, and closing the sequence.
  - sentence-pair-packing | Sentence A / sentence B packing [minor]: Two spans are concatenated into one sequence, the first labeled A and the second B, so a pair of texts is handled by the same single-sequence model.
  - segment-embeddings | Segment embeddings [minor]: A learned embedding added to every token indicating whether it belongs to sentence A or sentence B.
  - position-embeddings | Position embeddings [minor]: A learned vector per position, added so the model knows token order.
  - input-embedding-sum | Sum of token, segment and position embeddings [minor]: A token's input vector, written $E$, is the elementwise sum of its token embedding, its segment embedding and its position embedding.
  - final-hidden-vector-ti | T_i, the final vector for token i [minor]: The output of the top layer at position $i$, $T_i \in \mathbb{R}^H$, described as that token's contextual representation.

EDGES BETWEEN MEMBERS OF THIS THEME:
  [minor] wordpiece-vocabulary-30k --instance-of--> wordpiece-tokenization | the vocabulary size actually used: WordPiece is a general subword scheme; the size of its vocabulary is a free choice. BERT sets it to 30,000 tokens, which also fixes the width of the masked LM output softmax.
  [supporting] final-hidden-vector-c --instance-of--> final-hidden-vector-ti | C is T at the [CLS] position: T_i is the notation for the top-layer vector at any position i. C is that same vector taken at position zero, where [CLS] sits, and given its own name because so many output heads read it. Both live in R^H.

LOAD-BEARING EDGES LEAVING THIS THEME (use these to point forward/back):
  next-sentence-prediction --depends-on--> sentence-pair-packing | NSP needs the A/B slots
  next-sentence-prediction --depends-on--> final-hidden-vector-c | NSP is read off C
  unified-architecture-across-tasks --depends-on--> input-representation | one format enables one architecture
  span-prediction-head --depends-on--> final-hidden-vector-ti | span scores read per-token vectors
  input-embedding-sum --supported-by--> fig-2 | three rows added per token

EVIDENCE ITEMS RELATED TO THIS THEME (link as [[item-id]]):
  - fig-1 | The Same Stack, Twice: The figure claims that pre-training and fine-tuning use one and the same network: an identical token-in, vector-out Transformer encoder appears in both panels, and the only thing that changes between them is the thin output layer bolted on top. The dotted arrows carry every pre-trained weight across to initialize the task model, so a downstream task inherits the whole network rather than a set of embeddings.
  - fig-2 | The Three-Row Input Sum: Every token BERT reads is a single vector formed by adding three learned vectors together: what the token is, which of the two packed spans it belongs to, and where it sits in the sequence. This one format carries both single texts and text pairs, so the same encoder serves every task without changing its input plumbing.
  - table-4 | The SWAG Commonsense Scoreboard: On SWAG's four-way sentence-completion task, BERT_LARGE reaches 86.3% test accuracy, which the paper reports as +27.1 points over the dataset authors' own ESIM+ELMo baseline and +8.3 points over OpenAI GPT. That figure also sits above the 85.0 scored by a single human expert, though the dagger footnote says the human numbers come from only 100 samples, so the human comparison rests on much thinner ground than the model comparisons.
  - fig-3 | BERT, GPT and ELMo Side by Side: The figure states the paper's central architectural claim as arrows: only BERT lets every position depend on both its left and its right neighbours at every layer, GPT's arrows run one way, and ELMo's two one-way stacks meet only at the very top. The caption adds a second and separate difference: BERT and GPT adapt to a task by fine-tuning the whole model, while ELMo hands its outputs over as fixed features.
  - fig-4 | Four task shapes, one encoder: Four families of downstream task are drawn as four copies of the same diagram, differing only in what is packed into the input sequence and which output vectors an arrow is attached to. The paper uses this to show that adapting BERT to a task means plugging in task-specific inputs and one extra output layer, not redesigning the model.
  - eq-classification-loss | The GLUE Classification Head: This inline expression is the whole task-specific model BERT adds for a GLUE sentence-classification task: take the final hidden vector at the [CLS] position, multiply it by one new weight matrix to get one score per label, and train with ordinary cross-entropy. The paper's point in printing it is how little there is — a single $K \times H$ matrix is the only parameter matrix fine-tuning introduces.
  - eq-span-start-probability | The start-position softmax: This is the whole task-specific output layer BERT adds for extractive question answering: one learned vector $S$ is dotted with each passage token's final hidden vector, and a softmax over those scores gives a probability distribution over where the answer span starts. A second vector $E$ does the same for the end, and training maximizes the summed log-likelihood of the correct start and end positions.
  - eq-span-score | The additive span score: A candidate answer running from token $i$ to token $j$ is scored by adding its start score $S{\cdot}T_i$ to its end score $E{\cdot}T_j$, and the prediction is whichever pair with $j \geq i$ scores highest. The whole answer-selection machinery is two dot products and a comparison, with no module that looks at the span as a unit.
  - eq-null-answer-threshold | The Null-Answer Threshold Rule: This is the prediction-time rule BERT uses on SQuAD 2.0, where a question may have no answer. It scores "no answer" as an ordinary span that starts and ends at [CLS], compares it against the best real span, and only outputs a real span when that span wins by more than a constant tau, which is fitted on the dev set.

PAPER SECTIONS TO READ (in papers/1810.04805/data/ingest/sections/):
  - 07-3-bert.txt
  - 08-3.1-pre-training-bert.txt
  - 09-3.2-fine-tuning-bert.txt
  - 11-4.1-glue.txt
  - 12-4.2-squad-v1.1.txt
  - 13-4.3-squad-v2.0.txt
  - 14-4.4-swag.txt
  - 18-5.3-feature-based-approach-with-bert.txt
  - 22-A.1-illustration-of-the-pre-training-tasks.txt
  - 23-A.2-pre-training-procedure.txt
  - 25-A.4-comparison-of-bert-elmo-and.txt
  - 26-A.5-illustrations-of-fine-tuning-on-different.txt
