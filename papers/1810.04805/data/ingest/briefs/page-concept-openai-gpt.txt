PAGE FOR CONCEPT: openai-gpt
NAME: OpenAI GPT

SUMMARY (already shown above your page as the lede - do not repeat it verbatim):
A fine-tuning approach that pre-trains a left-to-right Transformer language model on unlabeled text, then fine-tunes it on downstream tasks; the paper's main comparison point.

DRAFT EXPLANATION from the extraction stage (your page replaces this; keep what is right, go deeper):
The paper calls GPT the most comparable existing method and says many of BERT's design decisions were made deliberately to keep the two close. GPT's self-attention is masked so each token attends only to its left, and it is pre-trained as a standard language model. It supplies the size target for BERT_BASE, the contrast case for bidirectionality, and the precedent for using gelu instead of relu. Appendix A.4 lists the remaining differences in corpus, special tokens, batch size and fine-tuning learning rate.

THEME IT BELONGS TO: theme-pre-training-and-how-it-was-reused | Pre-training and how it was reused - Sets out the practice BERT builds on: train a model on unlabeled text once, then reuse it on a labeled task that has far less data. The paper sorts prior work into two strategies. One keeps the pre-trained model frozen and feeds its vectors into a hand-built architecture as extra features; the other adds a small head and fine-tunes every pre-trained weight. ELMo is the paper's example of the first and OpenAI GPT of the second, and BERT is defined against both, so this comes first.

DEFINED IN A CITED PAPER radford2018 (paper id gpt-1). Those concepts are linkable: transformer-gpt1, decoder-only-stack, generative-pretraining, two-stage-training-procedure, supervised-fine-tuning, task-specific-input-transformations

PREREQUISITES:
  - fine-tuning-approach | Fine-tuning approach: Add minimal task-specific parameters on top of the pre-trained model and train all pre-trained parameters further on labeled task data.
  - constrained-self-attention | Constrained (left-context-only) self-attention: Self-attention masked so each token attends only to tokens on its left, as in the GPT Transformer.
  - left-to-right-language-model | Left-to-right language model: A model that assigns probability to text by predicting each token from the tokens that precede it.

EDGES TOUCHING THIS CONCEPT (4) - the relationships the app already knows; weave the load-bearing ones into your prose:
  [load-bearing] <- bert (BERT) | contrasts-with | bidirectional against left-to-right: GPT is the method BERT is set up to be measured against, and Appendix A.4 calls it the most comparable existing approach. Both pre-train a Transformer and then fine-tune every parameter. The stated difference is that GPT's tokens attend only leftward, while BERT's attend in both directions at every layer.
  [supporting] -> fine-tuning-approach (Fine-tuning approach) | instance-of | GPT realizes the fine-tuning route: OpenAI GPT is the specific prior system that pre-trains a Transformer and then fine-tunes all of it downstream. Because it belongs to the same family as BERT, the paper can attribute the gap between them to the objective rather than to the transfer strategy.
  [supporting] <- elmo (ELMo) | contrasts-with | frozen features versus fine-tuned weights: These are the paper's two named representatives of the two transfer strategies, and it treats them as rivals. On GLUE, Table 1 puts BiLSTM+ELMo+Attn at 71.0 average and OpenAI GPT at 75.1. On SWAG the gap is wider still: ESIM+ELMo reaches 59.2 test accuracy against GPT's 78.0.
  [minor] <- gelu-activation (gelu activation) | depends-on | activation choice follows GPT: BERT uses gelu inside each block rather than the more common relu. The paper's stated reason is that it follows OpenAI GPT. It is one of several small settings inherited so the two systems stay comparable.

EVIDENCE ITEMS THAT SHOW THIS (link them as [[item-id]]):
  - table-1 | The GLUE test scoreboard: On the eight scored GLUE tasks, both BERT sizes beat every prior system in every column, lifting the average by 4.5 points (BERT_BASE) and 7.0 points (BERT_LARGE) over OpenAI GPT, with a 4.6-point gain on MNLI. Because BERT_BASE is the same size as GPT, the paper reads the gap as evidence for bidirectional pre-training rather than for extra capacity.
  - table-4 | The SWAG Commonsense Scoreboard: On SWAG's four-way sentence-completion task, BERT_LARGE reaches 86.3% test accuracy, which the paper reports as +27.1 points over the dataset authors' own ESIM+ELMo baseline and +8.3 points over OpenAI GPT. That figure also sits above the 85.0 scored by a single human expert, though the dagger footnote says the human numbers come from only 100 samples, so the human comparison rests on much thinner ground than the model comparisons.
  - table-5 | Pre-training Task Ablation: Holding architecture, data and fine-tuning fixed, the table charges each drop in score to one removed piece of the pre-training recipe. Dropping next sentence prediction costs a little; replacing the masked LM with a left-to-right LM costs much more, above all on SQuAD (88.5 to 77.8 F1), and bolting a BiLSTM onto the left-to-right model repairs only part of that gap while making the GLUE numbers slightly worse.
  - fig-3 | BERT, GPT and ELMo Side by Side: The figure states the paper's central architectural claim as arrows: only BERT lets every position depend on both its left and its right neighbours at every layer, GPT's arrows run one way, and ELMo's two one-way stacks meet only at the very top. The caption adds a second and separate difference: BERT and GPT adapt to a task by fine-tuning the whole model, while ELMo hands its outputs over as fixed features.

PAPER SECTIONS TO READ (in papers/1810.04805/data/ingest/sections/):
  - 02-1-introduction.txt
  - 05-2.2-unsupervised-fine-tuning-approaches.txt
  - 07-3-bert.txt
  - 08-3.1-pre-training-bert.txt
  - 11-4.1-glue.txt
  - 23-A.2-pre-training-procedure.txt
  - 25-A.4-comparison-of-bert-elmo-and.txt
