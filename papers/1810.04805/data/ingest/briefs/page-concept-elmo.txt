PAGE FOR CONCEPT: elmo
NAME: ELMo

SUMMARY (already shown above your page as the lede - do not repeat it verbatim):
A feature-based model that extracts context-sensitive word features from separately trained left-to-right and right-to-left language models, concatenating the two directions.

DRAFT EXPLANATION from the extraction stage (your page replaces this; keep what is right, go deeper):
Section 2.1 describes ELMo as generalising word embedding research along a different dimension: instead of one vector per word type, each token gets a representation depending on its sentence. Plugged into existing task architectures, it advanced the state of the art on benchmarks including question answering, sentiment analysis and NER. The paper's criticism is not that ELMo is unidirectional but that its bidirectionality is shallow, since neither direction is ever computed with the other's context available.

THEME IT BELONGS TO: theme-pre-training-and-how-it-was-reused | Pre-training and how it was reused - Sets out the practice BERT builds on: train a model on unlabeled text once, then reuse it on a labeled task that has far less data. The paper sorts prior work into two strategies. One keeps the pre-trained model frozen and feeds its vectors into a hand-built architecture as extra features; the other adds a small head and fine-tunes every pre-trained weight. ELMo is the paper's example of the first and OpenAI GPT of the second, and BERT is defined against both, so this comes first.

PREREQUISITES:
  - feature-based-approach | Feature-based approach: Keep the pre-trained model fixed and feed its representations into a separate task-specific architecture as extra input features.
  - contextual-representations | Contextual representations: Token vectors that depend on the surrounding sentence, so the same word form gets different vectors in different sentences.
  - left-to-right-language-model | Left-to-right language model: A model that assigns probability to text by predicting each token from the tokens that precede it.

EDGES TOUCHING THIS CONCEPT (2) - the relationships the app already knows; weave the load-bearing ones into your prose:
  [supporting] -> feature-based-approach (Feature-based approach) | instance-of | ELMo realizes the feature-based route: ELMo is the paper's worked example of keeping a pre-trained model fixed and feeding its outputs into a separate task model as extra features. Its two independently trained directional LSTMs are then the concrete thing BERT's deep bidirectionality is set against.
  [supporting] -> openai-gpt (OpenAI GPT) | contrasts-with | frozen features versus fine-tuned weights: These are the paper's two named representatives of the two transfer strategies, and it treats them as rivals. On GLUE, Table 1 puts BiLSTM+ELMo+Attn at 71.0 average and OpenAI GPT at 75.1. On SWAG the gap is wider still: ESIM+ELMo reaches 59.2 test accuracy against GPT's 78.0.

EVIDENCE ITEMS THAT SHOW THIS (link them as [[item-id]]):
  - table-1 | The GLUE test scoreboard: On the eight scored GLUE tasks, both BERT sizes beat every prior system in every column, lifting the average by 4.5 points (BERT_BASE) and 7.0 points (BERT_LARGE) over OpenAI GPT, with a 4.6-point gain on MNLI. Because BERT_BASE is the same size as GPT, the paper reads the gap as evidence for bidirectional pre-training rather than for extra capacity.
  - table-2 | BERT against the SQuAD leaderboard: On SQuAD 1.1 span extraction, a fine-tuned BERT_LARGE ensemble reaches 93.2 test F1 and a single fine-tuned BERT_LARGE reaches 91.8, which the paper reports as +1.5 F1 and +1.3 F1 over the top leaderboard system of the day. Both figures come with an asterisk the paper states itself: the leaderboard entries may train on any public data, so BERT is first fine-tuned on TriviaQA to match, and dropping that extra data costs only 0.1-0.4 points.
  - table-4 | The SWAG Commonsense Scoreboard: On SWAG's four-way sentence-completion task, BERT_LARGE reaches 86.3% test accuracy, which the paper reports as +27.1 points over the dataset authors' own ESIM+ELMo baseline and +8.3 points over OpenAI GPT. That figure also sits above the 85.0 scored by a single human expert, though the dagger footnote says the human numbers come from only 100 samples, so the human comparison rests on much thinner ground than the model comparisons.
  - table-7 | Fine-tuned versus frozen BERT on NER: On CoNLL-2003 named entity recognition, BERT scores close to the best published systems when fine-tuned, and — the point of the table — nearly as well when its weights are frozen and only its activations are fed to a small task model. Concatenating the top four hidden layers gives 96.1 Dev F1 against 96.4 for fine-tuning the same model, a gap of 0.3.
  - fig-3 | BERT, GPT and ELMo Side by Side: The figure states the paper's central architectural claim as arrows: only BERT lets every position depend on both its left and its right neighbours at every layer, GPT's arrows run one way, and ELMo's two one-way stacks meet only at the very top. The caption adds a second and separate difference: BERT and GPT adapt to a task by fine-tuning the whole model, while ELMo hands its outputs over as fixed features.

PAPER SECTIONS TO READ (in papers/1810.04805/data/ingest/sections/):
  - 02-1-introduction.txt
  - 04-2.1-unsupervised-feature-based-approaches.txt
  - 16-5.1-effect-of-pre-training-tasks.txt
  - 25-A.4-comparison-of-bert-elmo-and.txt
