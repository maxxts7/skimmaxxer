PAGE FOR THEME: theme-pre-training-and-how-it-was-reused
NAME: Pre-training and how it was reused

THEME SUMMARY (shown above your page as the lede - do not repeat verbatim):
Sets out the practice BERT builds on: train a model on unlabeled text once, then reuse it on a labeled task that has far less data. The paper sorts prior work into two strategies. One keeps the pre-trained model frozen and feeds its vectors into a hand-built architecture as extra features; the other adds a small head and fine-tunes every pre-trained weight. ELMo is the paper's example of the first and OpenAI GPT of the second, and BERT is defined against both, so this comes first.

NEXT THEME: theme-the-one-direction-problem | The one-direction problem

MEMBER CONCEPTS (22) - each has its own page or expandable section; your job is the connective tissue between them, not to restate each one:
  - transfer-learning | Transfer learning [major]: Training a model on one large task or corpus and reusing its parameters on a different, usually smaller, target task.
      · language-model-pre-training | Language model pre-training: Training a model on a language modeling objective over unlabeled text, then reusing it for downstream NLP task
      · unlabeled-text | Unlabeled text: Plain running text with no human annotation, used as the training signal for objectives that predict text from
      · downstream-task | Downstream task: The labeled end task a pre-trained representation is applied to, such as question answering, entailment or tag
      · sentence-level-tasks | Sentence-level tasks: Tasks producing one label for the whole input, such as inference, paraphrase detection and sentiment classific
      · natural-language-inference | Natural language inference: Given a premise and a hypothesis, decide whether the hypothesis is entailed by, contradicts, or is neutral wit
      · token-level-tasks | Token-level tasks: Tasks where the model must produce output at each token, such as named entity recognition and extractive quest
  - language-model-pre-training | Language model pre-training [major]: Training a model on a language modeling objective over unlabeled text, then reusing it for downstream NLP tasks.
      · unlabeled-text | Unlabeled text: Plain running text with no human annotation, used as the training signal for objectives that predict text from
      · downstream-task | Downstream task: The labeled end task a pre-trained representation is applied to, such as question answering, entailment or tag
      · sentence-level-tasks | Sentence-level tasks: Tasks producing one label for the whole input, such as inference, paraphrase detection and sentiment classific
      · natural-language-inference | Natural language inference: Given a premise and a hypothesis, decide whether the hypothesis is entailed by, contradicts, or is neutral wit
      · token-level-tasks | Token-level tasks: Tasks where the model must produce output at each token, such as named entity recognition and extractive quest
      · question-answering | Question answering: Given a question and a passage, produce the answer; in the extractive form used here, mark the passage span th
  - unlabeled-text | Unlabeled text [minor]: Plain running text with no human annotation, used as the training signal for objectives that predict text from text.
  - downstream-task | Downstream task [minor]: The labeled end task a pre-trained representation is applied to, such as question answering, entailment or tagging.
      · sentence-level-tasks | Sentence-level tasks: Tasks producing one label for the whole input, such as inference, paraphrase detection and sentiment classific
      · natural-language-inference | Natural language inference: Given a premise and a hypothesis, decide whether the hypothesis is entailed by, contradicts, or is neutral wit
      · token-level-tasks | Token-level tasks: Tasks where the model must produce output at each token, such as named entity recognition and extractive quest
      · question-answering | Question answering: Given a question and a passage, produce the answer; in the extractive form used here, mark the passage span th
      · named-entity-recognition | Named entity recognition: Labeling each token with whether it belongs to a name of a person, organisation, location and so on, and which
      · conll-2003-ner | CoNLL-2003 NER: The shared-task dataset used as the standard NER benchmark, and the testbed for BERT's feature-based and maski
  - sentence-level-tasks | Sentence-level tasks [minor]: Tasks producing one label for the whole input, such as inference, paraphrase detection and sentiment classification.
      · natural-language-inference | Natural language inference: Given a premise and a hypothesis, decide whether the hypothesis is entailed by, contradicts, or is neutral wit
  - token-level-tasks | Token-level tasks [minor]: Tasks where the model must produce output at each token, such as named entity recognition and extractive question answering.
      · question-answering | Question answering: Given a question and a passage, produce the answer; in the extractive form used here, mark the passage span th
      · named-entity-recognition | Named entity recognition: Labeling each token with whether it belongs to a name of a person, organisation, location and so on, and which
      · conll-2003-ner | CoNLL-2003 NER: The shared-task dataset used as the standard NER benchmark, and the testbed for BERT's feature-based and maski
      · tagging-without-crf | Tagging without a CRF layer: NER is formulated as tagging, but the output layer is a plain per-token classifier with no conditional random 
      · first-sub-token-classification | First sub-token as the token representation: When WordPiece splits a word, the first piece's final hidden vector is what the tag classifier reads.
  - natural-language-inference | Natural language inference [minor]: Given a premise and a hypothesis, decide whether the hypothesis is entailed by, contradicts, or is neutral with respect to the premise.
  - question-answering | Question answering [minor]: Given a question and a passage, produce the answer; in the extractive form used here, mark the passage span that answers it.
  - named-entity-recognition | Named entity recognition [minor]: Labeling each token with whether it belongs to a name of a person, organisation, location and so on, and which type.
      · conll-2003-ner | CoNLL-2003 NER: The shared-task dataset used as the standard NER benchmark, and the testbed for BERT's feature-based and maski
      · tagging-without-crf | Tagging without a CRF layer: NER is formulated as tagging, but the output layer is a plain per-token classifier with no conditional random 
      · first-sub-token-classification | First sub-token as the token representation: When WordPiece splits a word, the first piece's final hidden vector is what the tag classifier reads.
  - pre-trained-word-embeddings | Pre-trained word embeddings [minor]: Word vectors learned once from a large unlabeled corpus and reused across tasks, rather than learned per task.
      · word-embeddings | Word embeddings: Dense vectors representing words, learned so that distributional similarity shows up as vector similarity.
      · sentence-embeddings | Sentence embeddings: Fixed-size vectors representing whole sentences, pre-trained from unlabeled text the way word vectors are.
  - sentence-embeddings | Sentence embeddings [minor]: Fixed-size vectors representing whole sentences, pre-trained from unlabeled text the way word vectors are.
  - contextual-representations | Contextual representations [minor]: Token vectors that depend on the surrounding sentence, so the same word form gets different vectors in different sentences.
      · context2vec | context2vec: A model that learns contextual representations by predicting a single word from both its left and right contex
  - context2vec | context2vec [minor]: A model that learns contextual representations by predicting a single word from both its left and right context using LSTMs.
  - transfer-strategies | Feature-based vs. fine-tuning transfer [major]: The paper's taxonomy of the two ways pre-trained representations were applied downstream: as extra input features, or by fine-tuning the whole model.
      · feature-based-approach | Feature-based approach: Keep the pre-trained model fixed and feed its representations into a separate task-specific architecture as ex
      · task-specific-architecture | Task-specific architecture: A model designed by hand for one task, into which pre-trained representations are inserted as features.
      · elmo | ELMo: A feature-based model that extracts context-sensitive word features from separately trained left-to-right and 
      · fine-tuning-approach | Fine-tuning approach: Add minimal task-specific parameters on top of the pre-trained model and train all pre-trained parameters furt
      · minimal-task-specific-parameters | Minimal task-specific parameters: The design goal that adapting to a new task should add as few new weights as possible.
      · openai-gpt | OpenAI GPT: A fine-tuning approach that pre-trains a left-to-right Transformer language model on unlabeled text, then fine
  - feature-based-approach | Feature-based approach [minor]: Keep the pre-trained model fixed and feed its representations into a separate task-specific architecture as extra input features.
      · task-specific-architecture | Task-specific architecture: A model designed by hand for one task, into which pre-trained representations are inserted as features.
      · elmo | ELMo: A feature-based model that extracts context-sensitive word features from separately trained left-to-right and 
  - task-specific-architecture | Task-specific architecture [minor]: A model designed by hand for one task, into which pre-trained representations are inserted as features.
  - elmo | ELMo [major]: A feature-based model that extracts context-sensitive word features from separately trained left-to-right and right-to-left language models, concatenating the two directions.
  - fine-tuning-approach | Fine-tuning approach [minor]: Add minimal task-specific parameters on top of the pre-trained model and train all pre-trained parameters further on labeled task data.
      · minimal-task-specific-parameters | Minimal task-specific parameters: The design goal that adapting to a new task should add as few new weights as possible.
      · openai-gpt | OpenAI GPT: A fine-tuning approach that pre-trains a left-to-right Transformer language model on unlabeled text, then fine
  - minimal-task-specific-parameters | Minimal task-specific parameters [minor]: The design goal that adapting to a new task should add as few new weights as possible.
  - openai-gpt | OpenAI GPT [major]: A fine-tuning approach that pre-trains a left-to-right Transformer language model on unlabeled text, then fine-tunes it on downstream tasks; the paper's main comparison point.
  - supervised-transfer-learning | Transfer from supervised data [minor]: Using a model trained on a large labeled task as the starting point for other tasks, as an alternative to pre-training on unlabeled text.
  - low-resource-tasks | Low-resource tasks [minor]: Tasks with too little labeled data to train a deep model from scratch.

EDGES BETWEEN MEMBERS OF THIS THEME:
  [supporting] language-model-pre-training --instance-of--> transfer-learning | pre-training as transfer learning: Transfer learning is the general move of training on one thing and reusing the weights on another. Language model pre-training is the particular version the paper works in, where the first task is predicting text from unlabeled text.
  [supporting] elmo --instance-of--> feature-based-approach | ELMo realizes the feature-based route: ELMo is the paper's worked example of keeping a pre-trained model fixed and feeding its outputs into a separate task model as extra features. Its two independently trained directional LSTMs are then the concrete thing BERT's deep bidirectionality is set against.
  [supporting] openai-gpt --instance-of--> fine-tuning-approach | GPT realizes the fine-tuning route: OpenAI GPT is the specific prior system that pre-trains a Transformer and then fine-tunes all of it downstream. Because it belongs to the same family as BERT, the paper can attribute the gap between them to the objective rather than to the transfer strategy.
  [load-bearing] feature-based-approach --contrasts-with--> fine-tuning-approach | two ways to reuse pre-training: The paper opens by sorting all prior pre-training work into two camps. One camp freezes the pre-trained model and feeds its vectors into a hand-built task architecture as extra features. The other adds almost no new weights and trains every pre-trained parameter further on the task's labeled data.
  [supporting] elmo --contrasts-with--> openai-gpt | frozen features versus fine-tuned weights: These are the paper's two named representatives of the two transfer strategies, and it treats them as rivals. On GLUE, Table 1 puts BiLSTM+ELMo+Attn at 71.0 average and OpenAI GPT at 75.1. On SWAG the gap is wider still: ESIM+ELMo reaches 59.2 test accuracy against GPT's 78.0.
  [supporting] task-specific-architecture --contrasts-with--> minimal-task-specific-parameters | hand-built model versus one layer: The feature-based route assumes a model designed by hand for each task, into which pre-trained vectors are inserted. The fine-tuning route treats new weights as something to minimise. BERT's contribution list makes the contrast a claim: pre-trained representations reduce the need for heavily-engineered task-specific architectures.
  [supporting] pre-trained-word-embeddings --contrasts-with--> contextual-representations | one vector per word type: Pre-trained word embeddings give each word form a single vector, fixed no matter where it appears. Contextual representations give the same word form different vectors in different sentences. The paper places both under language model pre-training and treats the move from the first to the second as the line its own work extends.

LOAD-BEARING EDGES LEAVING THIS THEME (use these to point forward/back):
  bert --instance-of--> fine-tuning-approach | BERT is a fine-tuning method
  feature-based-approach-with-bert --instance-of--> feature-based-approach | BERT used as frozen features
  bert --contrasts-with--> openai-gpt | bidirectional against left-to-right

EVIDENCE ITEMS RELATED TO THIS THEME (link as [[item-id]]):
  - fig-1 | The Same Stack, Twice: The figure claims that pre-training and fine-tuning use one and the same network: an identical token-in, vector-out Transformer encoder appears in both panels, and the only thing that changes between them is the thin output layer bolted on top. The dotted arrows carry every pre-trained weight across to initialize the task model, so a downstream task inherits the whole network rather than a set of embeddings.
  - table-1 | The GLUE test scoreboard: On the eight scored GLUE tasks, both BERT sizes beat every prior system in every column, lifting the average by 4.5 points (BERT_BASE) and 7.0 points (BERT_LARGE) over OpenAI GPT, with a 4.6-point gain on MNLI. Because BERT_BASE is the same size as GPT, the paper reads the gap as evidence for bidirectional pre-training rather than for extra capacity.
  - table-2 | BERT against the SQuAD leaderboard: On SQuAD 1.1 span extraction, a fine-tuned BERT_LARGE ensemble reaches 93.2 test F1 and a single fine-tuned BERT_LARGE reaches 91.8, which the paper reports as +1.5 F1 and +1.3 F1 over the top leaderboard system of the day. Both figures come with an asterisk the paper states itself: the leaderboard entries may train on any public data, so BERT is first fine-tuned on TriviaQA to match, and dropping that extra data costs only 0.1-0.4 points.
  - table-4 | The SWAG Commonsense Scoreboard: On SWAG's four-way sentence-completion task, BERT_LARGE reaches 86.3% test accuracy, which the paper reports as +27.1 points over the dataset authors' own ESIM+ELMo baseline and +8.3 points over OpenAI GPT. That figure also sits above the 85.0 scored by a single human expert, though the dagger footnote says the human numbers come from only 100 samples, so the human comparison rests on much thinner ground than the model comparisons.
  - table-5 | Pre-training Task Ablation: Holding architecture, data and fine-tuning fixed, the table charges each drop in score to one removed piece of the pre-training recipe. Dropping next sentence prediction costs a little; replacing the masked LM with a left-to-right LM costs much more, above all on SQuAD (88.5 to 77.8 F1), and bolting a BiLSTM onto the left-to-right model repairs only part of that gap while making the GLUE numbers slightly worse.
  - table-7 | Fine-tuned versus frozen BERT on NER: On CoNLL-2003 named entity recognition, BERT scores close to the best published systems when fine-tuned, and — the point of the table — nearly as well when its weights are frozen and only its activations are fed to a small task model. Concatenating the top four hidden layers gives 96.1 Dev F1 against 96.4 for fine-tuning the same model, a gap of 0.3.
  - fig-3 | BERT, GPT and ELMo Side by Side: The figure states the paper's central architectural claim as arrows: only BERT lets every position depend on both its left and its right neighbours at every layer, GPT's arrows run one way, and ELMo's two one-way stacks meet only at the very top. The caption adds a second and separate difference: BERT and GPT adapt to a task by fine-tuning the whole model, while ELMo hands its outputs over as fixed features.
  - fig-4 | Four task shapes, one encoder: Four families of downstream task are drawn as four copies of the same diagram, differing only in what is packed into the input sequence and which output vectors an arrow is attached to. The paper uses this to show that adapting BERT to a task means plugging in task-specific inputs and one extra output layer, not redesigning the model.
  - table-8 | The masking mixture ablation: Six BERT pre-training runs that differ only in what happens to a token once it has been selected for prediction. Changing the mixture barely moves the fine-tuned scores, but pre-training with [MASK] alone costs 0.9 F1 on NER when BERT is frozen and used as a feature extractor. That single column is the paper's evidence for keeping the 80/10/10 mixture rather than always inserting [MASK].
  - eq-classification-loss | The GLUE Classification Head: This inline expression is the whole task-specific model BERT adds for a GLUE sentence-classification task: take the final hidden vector at the [CLS] position, multiply it by one new weight matrix to get one score per label, and train with ordinary cross-entropy. The paper's point in printing it is how little there is — a single $K \times H$ matrix is the only parameter matrix fine-tuning introduces.
  - eq-span-start-probability | The start-position softmax: This is the whole task-specific output layer BERT adds for extractive question answering: one learned vector $S$ is dotted with each passage token's final hidden vector, and a softmax over those scores gives a probability distribution over where the answer span starts. A second vector $E$ does the same for the end, and training maximizes the summed log-likelihood of the correct start and end positions.

PAPER SECTIONS TO READ (in papers/1810.04805/data/ingest/sections/):
  - 02-1-introduction.txt
  - 03-2-related-work.txt
  - 04-2.1-unsupervised-feature-based-approaches.txt
  - 05-2.2-unsupervised-fine-tuning-approaches.txt
  - 06-2.3-transfer-learning-from-supervised-data.txt
  - 07-3-bert.txt
  - 08-3.1-pre-training-bert.txt
  - 09-3.2-fine-tuning-bert.txt
  - 11-4.1-glue.txt
  - 12-4.2-squad-v1.1.txt
  - 16-5.1-effect-of-pre-training-tasks.txt
  - 17-5.2-effect-of-model-size.txt
