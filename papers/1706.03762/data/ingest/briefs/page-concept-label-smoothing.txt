PAGE FOR CONCEPT: label-smoothing
NAME: Label smoothing

SUMMARY (already shown above your page as the lede - do not repeat it verbatim):
A training target modification: instead of putting probability 1 on the correct token, the target gives it 1 - eps and spreads eps = 0.1 over other tokens, penalizing overconfidence.

DRAFT EXPLANATION from the extraction stage (your page replaces this; keep what is right, go deeper):
Normally the training loss pushes the model's output distribution toward a one-hot target: probability 1 for the correct next token, 0 for everything else. With label smoothing of value $\epsilon_{ls}=0.1$, the target instead assigns the correct token probability $1-\epsilon_{ls}$ and distributes the remaining $0.1$ across the vocabulary. The model is thus trained to stay slightly unsure. The paper reports this hurts perplexity but improves accuracy and BLEU — which is not a contradiction. Perplexity directly measures the probability the model assigns to the true tokens, so a model trained to hold back probability mass scores worse on it by construction. BLEU depends only on which output tokens are actually produced during decoding; less overconfident distributions lead to better final outputs, so the metrics move in opposite directions.

THEME IT BELONGS TO: theme-training-the-thing | Training the thing - Everything needed to reproduce a trained model: the translation datasets, subword vocabularies built with byte-pair encoding or word pieces, batching by sequence length, and eight GPUs on one machine. The learning rate is not fixed — it ramps up linearly for the first 4000 steps and then decays as the inverse square root of the step. Label smoothing, dropout, and averaging the last several checkpoints round out the recipe, and the code was released as tensor2tensor.

DEFINED IN A CITED PAPER [36] (paper id 1512.00567). Those concepts are linkable: label-smoothing-regularization, lsr-as-two-cross-entropy-losses, overconfidence-and-bounded-logit-gradient, smoothing-parameter-epsilon

PREREQUISITES:
  - perplexity | Perplexity: A measure of how well a model predicts the true next tokens: the exponentiated average negative log-probability. Lower is better.
  - bleu | BLEU score: The standard automatic metric for translation quality: n-gram overlap precision against reference translations with a penalty for overly short outputs. Higher is better.

EDGES TOUCHING THIS CONCEPT (5) - the relationships the app already knows; weave the load-bearing ones into your prose:
  [load-bearing] -> label-smoothing-regularization (Label-Smoothing Regularization (LSR)) | depends-on | uses the cited target mixture: The paper applies label smoothing at eps = 0.1 by citation. The cited definition is the mechanism: replace the one-hot target with a mixture that gives the correct token 1 - eps and spreads eps over the rest.
  [load-bearing] -> bleu (BLEU score) | contrasts-with | same change helps BLEU: The same smoothing that hurts perplexity improves accuracy and BLEU. It is not a contradiction: perplexity grades how confident the model is on the reference token, while BLEU grades the tokens decoding actually picks, and the less confident model ranks candidates better.
  [supporting] -> table-3 (One-knob-at-a-time ablations (table-3)) | supported-by | reports BLEU and perplexity together: Section 5.4 claims label smoothing of 0.1 hurts perplexity but improves accuracy and BLEU. Table 3 is the only place both quantities are reported per configuration, so it is where that trade-off would be visible. The paper's prose about Table 3 comments on dropout and not on smoothing, so the claim is stated more firmly than it is demonstrated.
  [supporting] -> label-smoothing-regularization (Label-Smoothing Regularization (LSR)) | instance-of | applies LSR at eps = 0.1: The cited scheme replaces the one-hot target with a mixture of it and a fixed distribution, controlled by eps. The paper sets eps_ls = 0.1 and reports it hurts perplexity but improves accuracy and BLEU.
  [supporting] -> perplexity (Perplexity) | contrasts-with | smoothing hurts perplexity: Training with label smoothing of 0.1 hurts perplexity, because the model is taught not to put all its probability on the correct token and so scores the true tokens lower. The paper accepts this cost.

EVIDENCE ITEMS THAT SHOW THIS (link them as [[item-id]]):
  - table-3 | One-knob-at-a-time ablations: Table 3 changes one design choice of the base Transformer at a time and reports the effect on English-to-German development-set quality. It shows a middling head count (around 8-16) is best, shrinking the attention key dimension $d_k$ hurts, bigger models are better, dropout matters a lot, and learned positional embeddings match sinusoids almost exactly.

PAPER SECTIONS TO READ (in papers/1706.03762/data/ingest/sections/):
  - 18-5.4-regularization.txt
