PAGE FOR CONCEPT: beam-search
NAME: Beam search

SUMMARY (already shown above your page as the lede - do not repeat it verbatim):
A decoding procedure that keeps the k best partial output sequences at each step instead of committing to a single best token; the paper uses beam size 4.

DRAFT EXPLANATION from the extraction stage (your page replaces this; keep what is right, go deeper):
The model generates output one token at a time, and greedily taking the single most probable token at each step can lock in early mistakes. Beam search instead keeps a 'beam' of the $k$ highest-scoring partial sequences (here $k=4$). At each step every sequence in the beam is extended by each plausible next token, the candidates are scored by their accumulated log-probability, and only the top $k$ survive. When a sequence produces the end-of-sequence token it becomes a finished candidate; the best finished candidate is the output. The paper pairs beam search with a length penalty of $\alpha = 0.6$ and caps output length at input length + 50, terminating early when possible. Beam size and length penalty were chosen on the development set; beam size was also one of the few hyperparameters re-tuned for constituency parsing.

THEME IT BELONGS TO: theme-what-the-numbers-show | What the numbers show - How outputs are produced at test time and what they score. Decoding uses beam search with a length penalty; quality is reported as BLEU, with perplexity and an estimated training FLOP count alongside it so quality can be read against cost. The ablations change one design choice at a time to see which parts matter, and a constituency-parsing experiment tests whether the architecture carries to a different task. This comes last because it is the evidence for the claims made earlier.

DEFINED IN A CITED PAPER [38] (paper id 1609.08144). Those concepts are linkable: wordpiece-model, length-normalization-penalty, coverage-penalty, beam-search-pruning-and-termination

SUB-CONCEPTS (these render as expandable sections BELOW your page - reference them, do not duplicate them):
  - length-penalty | Length penalty (alpha = 0.6): A normalization applied to beam-search scores so that longer output sequences are not unfairly penalized; the paper uses strength alpha = 0.6 from the GNMT paper.
  - inference-length-limit | Maximum output length rule: During inference the output is capped at input length + 50 tokens, with early termination when possible.

PREREQUISITES:
  - auto-regressive-generation | Auto-regressive generation: Producing an output sequence one symbol at a time, where each new symbol is conditioned on all previously generated symbols.

EVIDENCE ITEMS THAT SHOW THIS (link them as [[item-id]]):
  - table-4 | The parsing leaderboard: Trained on the ~40K-sentence WSJ treebank with almost no task-specific tuning, a 4-layer Transformer reaches 91.3 F1 on WSJ Section 23 — above every listed discriminative parser except Dyer et al.'s RNN Grammar (91.7) — and 92.7 F1 when the extra ~17M auto-parsed sentences are added, the best semi-supervised number in the table. It also beats the RNN sequence-to-sequence parser (Vinyals & Kaiser et al.) whose experimental setup it borrows, in both regimes.

PAPER SECTIONS TO READ (in papers/1706.03762/data/ingest/sections/):
  - 20-6.1-machine-translation.txt
  - 22-6.3-english-constituency-parsing.txt
