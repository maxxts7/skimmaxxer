transformer | The Transformer | major | The architecture this paper proposes: an encoder-decoder built entirely from stacked self-attention and positi
sequence-transduction | Sequence transduction | minor | The general task of converting one sequence of symbols into another sequence, such as translating a German sen
machine-translation | Machine translation | minor floor | Automatically translating text from one language to another; the paper's main benchmark task.
language-modeling | Language modeling | minor floor | Predicting the next symbol in a sequence given the symbols so far.
encoder-decoder-architecture | Encoder-decoder architecture | major | A two-part model design: an encoder turns the input sequence into continuous representations, and a decoder ge
auto-regressive-generation | Auto-regressive generation | minor | Producing an output sequence one symbol at a time, where each new symbol is conditioned on all previously gene
recurrent-neural-network | Recurrent neural network (RNN) | minor floor | A network that processes a sequence one position at a time, carrying a hidden state forward.
lstm | Long short-term memory (LSTM) | minor floor | An RNN variant with gates that help preserve information over long spans.
gated-recurrent-unit | Gated recurrent unit (GRU) | minor floor | A simpler gated RNN variant, an alternative to the LSTM.
sequential-computation-bottleneck | Sequential computation bottleneck of RNNs | major | Because an RNN's hidden state at position t depends on the hidden state at position t-1, positions within one 
factorization-tricks | Factorization tricks for LSTMs | minor | Techniques that factor an LSTM's weight matrices into smaller pieces to reduce computation.
conditional-computation | Conditional computation | minor | Activating only parts of a network per input, so compute is spent selectively.
attention-mechanism | Attention mechanism (query, key, value) | major | A function that maps a query vector and a set of key-value vector pairs to an output: a weighted sum of the va
queries-keys-values | Queries, keys, and values | minor | The three roles vectors play in attention: a query asks for information, keys advertise what each position hol
compatibility-function | Compatibility function | minor | The scoring function that measures how well a query matches a key; its scores become the attention weights aft
scaled-dot-product-attention | Scaled dot-product attention | major | The Transformer's attention: dot the query with every key, divide by $\sqrt{d_k}$, softmax the scores, and use
dk-dv-dimensions | d_k and d_v (key and value dimensions) | minor | The dimensions of the key/query vectors ($d_k$) and value vectors ($d_v$) inside attention; in the base model 
sqrt-dk-scaling | The 1/sqrt(d_k) scaling factor | minor | Dividing the query-key dot products by $\sqrt{d_k}$ so their magnitude stays controlled as the dimension grows
dot-product-attention | Dot-product (multiplicative) attention | minor | An attention variant that scores each query-key pair by their plain dot product; identical to the Transformer'
additive-attention | Additive attention | minor | An attention variant that scores each query-key pair with a small feed-forward network with one hidden layer, 
multi-head-attention | Multi-head attention | major | Running $h$ attention functions in parallel on different learned linear projections of the queries, keys, and 
attention-head | Attention head | minor | One of the parallel attention functions inside multi-head attention, operating on its own learned low-dimensio
num-heads-h | h = 8 (number of heads) | minor | The number of parallel attention heads in the base model: eight.
attention-projection-matrices | Attention projection matrices (W^Q, W^K, W^V, W^O) | minor | The learned linear maps that create each head's queries, keys, and values, and the final matrix that mixes the
attention-averaging-resolution-cost | Averaging cost of single-head attention | minor | A single attention operation blends many positions into one weighted average, which can blur position-specific
attention-interpretability | Interpretability of attention | minor | The paper's observation that inspecting attention distributions suggests individual heads learn distinct, some
self-attention | Self-attention (intra-attention) | major | Attention where the queries, keys, and values all come from the same sequence, so every position can gather in
encoder-self-attention | Encoder self-attention | minor | The self-attention layers in the encoder, where each position can attend to all positions in the previous enco
masked-decoder-self-attention | Masked decoder self-attention | minor | The decoder's self-attention, restricted by a mask so each position can only attend to itself and earlier posi
causal-masking | Causal masking | minor | Setting the attention scores for forbidden (future) positions to $-\infty$ before the softmax, so those positi
restricted-self-attention | Restricted (local) self-attention | minor | A proposed variant where each position attends only to a neighborhood of size r around it, trading path length
encoder-decoder-attention | Encoder-decoder attention | minor | The decoder sub-layer where queries come from the decoder and the keys and values come from the encoder's outp
encoder-stack | Encoder stack | major | The Transformer's encoder: $N=6$ identical layers, each containing a multi-head self-attention sub-layer follo
decoder-stack | Decoder stack | major | The Transformer's decoder: $N=6$ identical layers, each with masked self-attention, attention over the encoder
sub-layer | Sub-layer | minor | One of the components inside a Transformer layer — an attention mechanism or a feed-forward network — each wra
residual-connection | Residual connection | minor | A shortcut that adds a layer's input directly to its output, so the layer only has to learn a change (residual
layer-normalization | Layer normalization | minor | A normalization step that rescales each position's activation vector to have a standard mean and variance, com
n-layers | N = 6 (number of layers) | minor | The number of identical layers stacked in each of the encoder and the decoder in the base model: six.
d-model | d_model = 512 (model dimension) | minor | The shared width of the model: every sub-layer and embedding layer outputs vectors of dimension $d_{model} = 5
position-wise-feed-forward-network | Position-wise feed-forward network | minor | A two-layer fully connected network applied to each sequence position independently and identically: expand to
d-ff | d_ff = 2048 (feed-forward inner dimension) | minor | The width of the hidden layer inside the position-wise feed-forward network: 2048, four times $d_{model}$.
relu | ReLU activation | minor floor | The rectified linear unit, $\max(0, x)$: a standard nonlinearity that zeroes out negative values.
softmax | Softmax | minor floor | The standard function that turns a vector of scores into a probability distribution: exponentiate each score a
token-embeddings | Token embeddings | minor floor | Learned lookup tables that convert input and output tokens into $d_{model}$-dimensional vectors.
embedding-softmax-weight-sharing | Embedding and softmax weight sharing | minor | The two token embedding layers and the pre-softmax output projection all share a single weight matrix.
sqrt-dmodel-embedding-multiplier | sqrt(d_model) embedding multiplier | minor | In the embedding layers, the shared weights are multiplied by $\sqrt{d_{model}}$.
positional-encoding | Positional encoding | major | Vectors added to the input embeddings that inject information about each token's position, since the model its
sinusoidal-positional-encoding | Sinusoidal positional encoding | minor | The paper's fixed positional encoding: each dimension is a sine or cosine of the position, at wavelengths form
relative-position-linearity | Relative positions as linear functions | minor | For any fixed offset $k$, the sinusoidal encoding of position $pos+k$ is a linear function of the encoding of 
learned-positional-embedding | Learned positional embeddings | minor | The alternative to sinusoids: a trainable vector per position, learned like any other parameter.
convolutional-sequence-models | Convolutional sequence models | minor | Prior architectures (Extended Neural GPU, ByteNet, ConvS2S) that replace recurrence with convolutions, computi
extended-neural-gpu | Extended Neural GPU | minor | A convolution-based sequence model cited as prior work on reducing sequential computation.
bytenet | ByteNet | minor | A convolutional translation model using dilated convolutions; relating two positions costs a number of operati
convs2s | ConvS2S | minor | A convolutional sequence-to-sequence translation model; relating two positions costs a number of operations th
dilated-convolutions | Dilated convolutions | minor | Convolutions whose kernel taps are spaced apart, so each layer sees a wider span without more weights.
separable-convolutions | Separable convolutions | minor | A cheaper convolution that splits the operation into a per-channel (depthwise) part and a 1x1 mixing part.
end-to-end-memory-networks | End-to-end memory networks | minor | Prior models built on a recurrent attention mechanism instead of sequence-aligned recurrence.
layer-comparison-desiderata | Three criteria for comparing layer types | major | Section 4's framework: judge self-attention, recurrent, and convolutional layers by per-layer complexity, mini
complexity-per-layer | Computational complexity per layer | minor | The total amount of computation one layer performs, as a function of sequence length n, representation dimensi
minimum-sequential-operations | Minimum sequential operations | minor | The number of operations that must run one after another rather than in parallel; the paper's measure of paral
maximum-path-length | Maximum path length | minor | The longest path a signal must travel through the network between any two input and output positions; shorter 
long-range-dependencies | Long-range dependencies | minor | Relationships between symbols far apart in a sequence, which the paper calls a key challenge in sequence trans
byte-pair-encoding | Byte-pair encoding (BPE) | major | A subword tokenization scheme that builds a vocabulary by repeatedly merging the most frequent pair of symbols
word-piece-encoding | Word-piece vocabulary | minor | A subword tokenization scheme similar in spirit to BPE, from Google's neural machine translation system; the p
bleu | BLEU score | major | The standard automatic metric for translation quality: n-gram overlap precision against reference translations
perplexity | Perplexity | minor | A measure of how well a model predicts the true next tokens: the exponentiated average negative log-probabilit
model-ensembling | Model ensembling | minor floor | Combining predictions from several trained models to get better quality than any single model.
tensor2tensor | tensor2tensor | minor | The open-source codebase the authors used to train and evaluate the Transformer.
transformer-training-recipe | Transformer training recipe | major | The complete set of choices the paper used to train its models: data, batching, hardware, optimizer schedule, 
wmt-2014-en-de | WMT 2014 English-German dataset | minor | The standard benchmark translation dataset of about 4.5 million English-German sentence pairs used as the main
wmt-2014-en-fr | WMT 2014 English-French dataset | minor | A much larger benchmark translation dataset of 36 million sentence pairs, used to test the model at larger dat
length-based-batching | Batching by sequence length | minor | Sentence pairs of similar length are grouped into the same batch, and batch size is measured in tokens rather 
training-hardware-and-schedule | Hardware and training schedule | minor | One machine with 8 NVIDIA P100 GPUs; base models train for 100,000 steps (12 hours), big models for 300,000 st
base-vs-big-transformer | Base vs big model configurations | minor | The paper trains two main configurations: a 'base' Transformer and a scaled-up 'big' Transformer with larger d
adam-optimizer | Adam optimizer | minor floor | A standard gradient-descent variant that adapts the step size per parameter using running averages of gradient
transformer-adam-settings | Adam settings (beta2 = 0.98) | minor | The paper runs Adam with beta1 = 0.9, beta2 = 0.98, and epsilon = 10^-9, a lower-than-default beta2.
learning-rate-warmup-schedule | Warmup learning-rate schedule | major | The learning rate rises linearly for the first warmup_steps steps, then decays as the inverse square root of t
warmup-steps-4000 | warmup_steps = 4000 | minor | The number of initial steps over which the learning rate ramps up linearly; the paper uses 4000.
regularization | Regularization | minor floor | Any technique that discourages a model from fitting the training data too closely so it generalizes better.
dropout | Dropout | minor floor | Randomly zeroing a fraction of activations during training so the network cannot rely on any single unit.
residual-dropout | Residual dropout | minor | Dropout applied to each sub-layer's output before the residual addition and layer normalization, and to the em
attention-dropout | Attention dropout | minor | Dropout applied inside the attention mechanism, mentioned as a separately tunable rate in the parsing experime
label-smoothing | Label smoothing | major | A training target modification: instead of putting probability 1 on the correct token, the target gives it 1 -
beam-search | Beam search | major | A decoding procedure that keeps the k best partial output sequences at each step instead of committing to a si
length-penalty | Length penalty (alpha = 0.6) | minor | A normalization applied to beam-search scores so that longer output sequences are not unfairly penalized; the 
inference-length-limit | Maximum output length rule | minor | During inference the output is capped at input length + 50 tokens, with early termination when possible.
checkpoint-averaging | Checkpoint averaging | minor | The final model's weights are the elementwise average of the last several saved checkpoints: 5 for base models
training-flops-estimate | Training-cost FLOPs estimate | minor | The paper estimates each model's training cost as training time x number of GPUs x an assumed sustained single
transformer-ablations | Model variation ablations (Table 3) | major | Experiments that change one component of the base model at a time and measure the effect on English-to-German 
attention-heads-ablation | Number of heads sweet spot | minor | Varying the head count with total computation held constant shows single-head attention is 0.9 BLEU worse than
key-size-ablation | Reducing d_k hurts quality | minor | Shrinking the attention key dimension d_k lowers translation quality, suggesting the dot product may be too si
size-and-dropout-ablation | Model size and dropout effects | minor | Bigger models translate better, and dropout is very helpful in preventing over-fitting.
constituency-parsing | English constituency parsing | major | The task of producing a sentence's syntactic tree — nested phrases like noun phrases and verb phrases — used a
penn-treebank-wsj | Penn Treebank (WSJ portion) | minor | The standard constituency-parsing dataset: about 40K hand-parsed sentences from the Wall Street Journal.
semi-supervised-parsing | Semi-supervised parsing setting | minor | A second training regime that adds about 17M automatically parsed sentences from the high-confidence and Berke
recurrent-neural-network-grammar | Recurrent Neural Network Grammar (RNNG) | minor | A parsing model that builds trees with explicit tree-structured operations; the one prior model the Transforme
berkeley-parser | BerkeleyParser | minor | A classical (non-neural) grammar-based parser used as a baseline; the Transformer beats it even with only 40K 
