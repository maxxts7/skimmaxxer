THE WHOLE PAPER, AS THE PIPELINE HAS IT

=== CONCEPT THEMES (the chapters the reader can drill into) ===

[1] theme-why-recurrence-had-to-go | Why recurrence had to go
    Sets up the task the paper cares about — turning one sequence into another — and the models that dominated it. Recurrent models process a sequence one position at a time, which the paper says blocks parallelism within a training example and stretches the path between distant positions. Convolutional models and attention-based memory networks are the prior attempts to get around this. This comes fi
    members: sequence-transduction, recurrent-neural-network, sequential-computation-bottleneck, convolutional-sequence-models, end-to-end-memory-networks
    its page opens: The paper's opening move is a complaint, not a proposal. Before it describes anything it builds, it has to establish that the dominant approach to [[sequence-transduction]] has a defect that no amount of tuning removes. ### The task and the

[2] theme-the-shape-of-the-model | The shape of the model
    The outline of the Transformer before any of the math. It keeps the familiar encoder-decoder frame: an encoder turns the input into representations, a decoder emits output tokens one at a time. Both are stacks of six identical layers, and every sub-layer inside a layer is wrapped in a residual connection followed by layer normalization. Read this to have a map before the parts are filled in.
    members: encoder-decoder-architecture, transformer, encoder-stack, decoder-stack, sub-layer, residual-connection, layer-normalization
    its page opens: What does the model look like if you black out every formula? That is what this theme answers: the wiring diagram of [[transformer|the Transformer]], which you can hold in your head before any attention math arrives. [[fig-1]] is the whole 

[3] theme-how-attention-is-computed | How attention is computed
    The one operation the model is built from. Attention maps a query and a set of key-value pairs to a weighted sum of the values, with the weights coming from how well the query matches each key. The paper's version scores with a dot product, divides by the square root of the key dimension, and softmaxes; it then runs eight of these in parallel on lower-dimensional projections and concatenates the r
    members: attention-mechanism, scaled-dot-product-attention, multi-head-attention
    its page opens: The model has one computational primitive; everything else is scaffolding around it. This page covers what that primitive does, and why the paper's version carries the two extra pieces it does — a division by $\sqrt{d_k}$, and eight copies 

[4] theme-where-attention-is-used | Where attention is used in the model
    The same attention function appears in three places. In the encoder, self-attention lets every position look at every other position in the layer below. In the decoder, the same thing runs with a mask so a position can only see itself and earlier positions, which keeps generation auto-regressive. In between, the decoder attends over the encoder's output, with queries from the decoder and keys and 
    members: self-attention, masked-decoder-self-attention, encoder-decoder-attention
    its page opens: You have the attention function. Where does the model actually put it, and why does the same function need three different wirings to build a translator? Section 3.2.3 gives the answer: [[multi-head-attention]] appears three times, and the 

[5] theme-position-embeddings-and-feed-forward | Position, embeddings, and the feed-forward layer
    The parts of a layer that act on each position on its own. Every layer's second sub-layer is a small two-layer network applied identically at every position, and the input and output embedding matrices are shared with the pre-softmax projection. Because there is no recurrence or convolution, nothing so far tells the model what order the tokens are in, so a fixed sinusoidal vector is added to each 
    members: position-wise-feed-forward-network, embedding-softmax-weight-sharing, positional-encoding
    its page opens: Attention mixes information across positions. That leaves two gaps: nothing yet transforms a position's own vector, and nothing tells the model which position a token sits in. These three pieces close both gaps and finish the layer. ### The

[6] theme-why-self-attention | Why self-attention, compared to the alternatives
    The paper's argument for the swap, made on three criteria: computation per layer, how many operations must run in sequence rather than in parallel, and the longest path a signal travels between two positions. On the third, self-attention connects any two positions in a constant number of steps, which the paper says should make long-range dependencies easier to learn. For very long sequences it ske
    members: long-range-dependencies, layer-comparison-desiderata, restricted-self-attention
    its page opens: By now the architecture is fully specified. The open question is why anyone should swap a recurrent layer for a self-attention one at all. Section 4 is the paper's answer, and it is an argument from three numbers rather than from experiment

[7] theme-training-the-thing | Training the thing
    Everything needed to reproduce a trained model: the translation datasets, subword vocabularies built with byte-pair encoding or word pieces, batching by sequence length, and eight GPUs on one machine. The learning rate is not fixed — it ramps up linearly for the first 4000 steps and then decays as the inverse square root of the step. Label smoothing, dropout, and averaging the last several checkpo
    members: transformer-training-recipe, byte-pair-encoding, word-piece-encoding, learning-rate-warmup-schedule, label-smoothing, checkpoint-averaging, tensor2tensor
    its page opens: The architecture is settled by now. This theme answers a different question: what do you actually have to do to get the reported numbers, and which of those choices are doing work rather than just being reported? ### Data, tokens, and what 

[8] theme-what-the-numbers-show | What the numbers show
    How outputs are produced at test time and what they score. Decoding uses beam search with a length penalty; quality is reported as BLEU, with perplexity and an estimated training FLOP count alongside it so quality can be read against cost. The ablations change one design choice at a time to see which parts matter, and a constituency-parsing experiment tests whether the architecture carries to a di
    members: beam-search, bleu, perplexity, training-flops-estimate, transformer-ablations, constituency-parsing
    its page opens: Everything so far has been design: what the model is and how it was trained. This part answers a different question — how do you get a sentence out of the trained model, and what evidence is there that any of the design choices earned their

=== EDGE THEMES (arguments that cut across the chapters) ===

[1] etheme-dot-product-to-eight-heads | From one dot product to eight heads
    These edges build the attention mechanism from the inside out. Equation (1) defines scaled dot-product attention, which is plain dot-product attention plus one divisor, 1/sqrt(d_k); the paper says it picks the dot product over additive attention for speed, and adds the divisor because large d_k pushes the softmax into a flat region. The MultiHead equation then calls Equation (1) h times in paralle

[2] etheme-attention-wired-into-stacks | Where attention is wired into the two stacks
    The same multi-head block appears in three places: encoder self-attention over the whole input, masked decoder self-attention, and encoder-decoder attention whose keys and values come from the encoder output. The mask is what separates the second use from the first — forbidden positions get minus infinity before the softmax, so they get zero weight, and that is what keeps generation auto-regressiv

[3] etheme-order-without-recurrence | Putting order back in
    Self-attention treats a sequence as a set, so the model has no idea what order its tokens are in. The paper's fix is a positional encoding added to the input embeddings, and the version it uses is the sinusoid bank of Equation (4) — sines and cosines whose wavelengths form a geometric progression. It also tries learned positional embeddings as the alternative. The paper reports nearly identical re

[4] etheme-case-against-recurrence | The case against recurrence and convolution
    Section 4 sets up three criteria — computational complexity per layer, minimum sequential operations, and maximum path length between positions — and Table 1 turns them into numbers for self-attention, recurrent, and convolutional layers. The argument is that self-attention needs O(1) sequential operations where an RNN needs O(n), and connects any two positions in one step where a convolutional st

[5] etheme-borrowed-recipe | The recipe is mostly borrowed
    Much of the training setup is taken from other papers and used at stated settings rather than re-derived. Weight tying comes from the output-embedding work — one matrix in three roles, encoder input, decoder input, and pre-softmax projection — with the sqrt(d_model) multiplier applied on top. Label smoothing is the cited LSR target mixture at eps = 0.1; the subword vocabularies are BPE with its me

[6] etheme-translation-numbers | What the translation numbers show
    Table 2 carries the headline result on WMT 2014 English-German and English-French: the big Transformer's BLEU against prior models including ConvS2S and ensembles, plotted against an estimated training cost, so quality and compute are the two axes. Table 3 varies one thing at a time on the base model — head count in rows (A), key size d_k in rows (B), size and dropout in (C) and (D), positional en

[7] etheme-parsing-test | The parsing test
    Constituency parsing is the second task, used to check whether the architecture generalizes past translation. It only works as a sequence transduction problem because parse trees are flattened into token sequences by the cited linearization scheme. Table 4 reports WSJ Section 23 F1 in two regimes: trained on the roughly 40K-sentence WSJ treebank alone, and trained semi-supervised on about 17M addi

=== MAJOR CONCEPTS (each has a full page) ===
  transformer | The Transformer: The architecture this paper proposes: an encoder-decoder built entirely from stacked self-attention and position-wise feed-forward layers, with no recurrence and no convo
  encoder-decoder-architecture | Encoder-decoder architecture: A two-part model design: an encoder turns the input sequence into continuous representations, and a decoder generates the output sequence from them, one symbol at a time.
  sequential-computation-bottleneck | Sequential computation bottleneck of RNNs: Because an RNN's hidden state at position t depends on the hidden state at position t-1, positions within one training example cannot be computed in parallel.
  attention-mechanism | Attention mechanism (query, key, value): A function that maps a query vector and a set of key-value vector pairs to an output: a weighted sum of the values, with weights set by how well the query matches each ke
  scaled-dot-product-attention | Scaled dot-product attention: The Transformer's attention: dot the query with every key, divide by $\sqrt{d_k}$, softmax the scores, and use them to weight the values.
  multi-head-attention | Multi-head attention: Running $h$ attention functions in parallel on different learned linear projections of the queries, keys, and values, then concatenating and projecting the results.
  self-attention | Self-attention (intra-attention): Attention where the queries, keys, and values all come from the same sequence, so every position can gather information from every other position in that sequence.
  encoder-stack | Encoder stack: The Transformer's encoder: $N=6$ identical layers, each containing a multi-head self-attention sub-layer followed by a position-wise feed-forward sub-layer.
  decoder-stack | Decoder stack: The Transformer's decoder: $N=6$ identical layers, each with masked self-attention, attention over the encoder output, and a feed-forward sub-layer.
  positional-encoding | Positional encoding: Vectors added to the input embeddings that inject information about each token's position, since the model itself has no recurrence or convolution to sense order.
  layer-comparison-desiderata | Three criteria for comparing layer types: Section 4's framework: judge self-attention, recurrent, and convolutional layers by per-layer complexity, minimum sequential operations, and maximum path length.
  byte-pair-encoding | Byte-pair encoding (BPE): A subword tokenization scheme that builds a vocabulary by repeatedly merging the most frequent pair of symbols, so rare words split into known pieces.
  bleu | BLEU score: The standard automatic metric for translation quality: n-gram overlap precision against reference translations with a penalty for overly short outputs. Higher is better.
  transformer-training-recipe | Transformer training recipe: The complete set of choices the paper used to train its models: data, batching, hardware, optimizer schedule, and regularization.
  learning-rate-warmup-schedule | Warmup learning-rate schedule: The learning rate rises linearly for the first warmup_steps steps, then decays as the inverse square root of the step number, all scaled by the model width.
  label-smoothing | Label smoothing: A training target modification: instead of putting probability 1 on the correct token, the target gives it 1 - eps and spreads eps = 0.1 over other tokens, penalizing ove
  beam-search | Beam search: A decoding procedure that keeps the k best partial output sequences at each step instead of committing to a single best token; the paper uses beam size 4.
  transformer-ablations | Model variation ablations (Table 3): Experiments that change one component of the base model at a time and measure the effect on English-to-German translation, to see which parts matter.
  constituency-parsing | English constituency parsing: The task of producing a sentence's syntactic tree — nested phrases like noun phrases and verb phrases — used as a generalization test for the Transformer beyond translati

=== EVIDENCE ITEMS (each has a full page) ===
  fig-1 | The two-stack data path: The figure shows the entire model in one picture: two towers of identical layers, each layer built only from attention and a position-wise feed-forward network, every sub-layer wrapped in a residual c
  fig-2 | Attention as a wiring diagram: The figure draws the paper's attention math as data flow: on the left, Equation (1) box by box (dot queries against keys, scale, optionally mask, softmax, weight the values); on the right, that whole 
  table-1 | Layer types by cost and path length: The table is the paper's argument for replacing recurrence with self-attention: a self-attention layer connects every position to every other in a constant number of sequential steps and a path of len
  table-2 | Translation quality against training cost: On the WMT 2014 newstest2014 benchmarks, the big Transformer reports the best English-to-German BLEU in the table (28.4, more than 2.0 BLEU above every earlier model listed, ensembles included) and a 
  table-3 | One-knob-at-a-time ablations: Table 3 changes one design choice of the base Transformer at a time and reports the effect on English-to-German development-set quality. It shows a middling head count (around 8-16) is best, shrinking
  table-4 | The parsing leaderboard: Trained on the ~40K-sentence WSJ treebank with almost no task-specific tuning, a 4-layer Transformer reaches 91.3 F1 on WSJ Section 23 — above every listed discriminative parser except Dyer et al.'s R
  fig-3 | Heads tracking "making … more difficult": This is one query word's attention pattern in a single encoder self-attention layer, drawn as lines from the word to the positions it attends to, colored by head. Several of the eight heads put most o
  fig-4 | Two heads that resolve "its": Two different attention heads in the same encoder layer put almost all of the word "its" attention on two different words — head 5 on "Law", head 6 on "application". The paper offers this as a sign th
  fig-5 | Two heads, one sentence: Two attention heads from the same encoder self-attention layer, run on the same sentence, produce visibly different attention patterns: one fans many positions into a few words, the other makes sparse
  eq-attention | The scaled dot-product attention formula: This is the paper's whole attention function in one line: score every query against every key with a dot product, shrink the scores by $\sqrt{d_k}$, softmax them into weights, and return the weighted 
  eq-multihead | Attention in h parallel subspaces: Instead of one attention function over full-width 512-dimensional vectors, the Transformer runs $h=8$ attention functions in parallel, each on its own learned 64-dimensional projection of the queries,
  eq-ffn | The per-position feed-forward block: Equation (2) defines the second sub-layer in every encoder and decoder layer: a two-layer MLP with a ReLU in between, applied to each position's vector on its own. It widens each 512-dimensional vecto
  eq-posenc | Position as a bank of sinusoids: A model with no recurrence and no convolution has no idea what order its tokens are in, so the paper adds a fixed vector to each input embedding whose entries are sines and cosines of the position at 
  eq-lrate | The warmup-then-decay learning rate: Equation (3) makes the learning rate a function of the training step: it rises linearly for the first warmup_steps steps, then falls as the inverse square root of the step number, all scaled by $d_{mo

=== CONCEPTS IMPORTED FROM CITED PAPERS ===
  layer-normalization-statistics | Layer normalization statistics [from Layer Normalization]: Layer normalization standardizes a layer's activations using a mean and standard deviation computed across the hidden units of that layer, s
  gain-and-bias-parameters | Learned gain and bias [from Layer Normalization]: After standardization, each neuron gets its own learned multiplicative gain and additive bias, applied before the non-linearity, so the laye
  layer-norm-vs-batch-norm | Contrast with batch normalization [from Layer Normalization]: Batch normalization computes each neuron's statistics across the training cases in a mini-batch; layer normalization computes them across th
  per-timestep-normalization | Per-step normalization in sequence models [from Layer Normalization]: In a recurrent or sequential model, layer normalization is applied independently at each time step, using only that step's activations, with
  normalization-invariance-properties | Invariance and learning-rate effects [from Layer Normalization]: Layer normalization makes the model's output invariant to re-scaling and re-shifting of a layer's whole weight matrix and to re-scaling of a
  bpe-merge-algorithm | BPE Merge Algorithm [from Neural Machine Translation of Rare Words with Subword Units]: A greedy loop that builds a subword vocabulary by repeatedly merging the most frequent adjacent symbol pair in the training corpus. Adapted 
  bpe-segmentation-at-test-time | Applying BPE Merges to New Text [from Neural Machine Translation of Rare Words with Subword Units]: At test time, words are split into characters and the learned merge operations are replayed in order, producing a segmentation of any word i
  bpe-vocabulary-size | Vocabulary Size as the Single Hyperparameter [from Neural Machine Translation of Rare Words with Subword Units]: The final BPE symbol vocabulary size equals the initial character vocabulary size plus the number of merge operations. The merge count is th
  joint-bpe-shared-vocabulary | Joint BPE and the Shared Source-Target Vocabulary [from Neural Machine Translation of Rare Words with Subword Units]: Joint BPE learns one set of merge operations on the union (in practice, the concatenation) of the source and target training text, giving bo
  vocabulary-vs-sequence-length-tradeoff | Vocabulary Size vs. Sequence Length Trade-off [from Neural Machine Translation of Rare Words with Subword Units]: Subword segmentation trades vocabulary size against tokenized sequence length: finer splits shrink the vocabulary but lengthen every sentenc
  label-smoothing-regularization | Label-Smoothing Regularization (LSR) [from Rethinking the Inception Architecture for Computer Vision]: Replace the one-hot training target with a mixture of that one-hot target and a fixed label distribution, so the correct class gets mass $1-
  lsr-as-two-cross-entropy-losses | LSR as a pair of cross-entropy losses [from Rethinking the Inception Architecture for Computer Vision]: Because cross entropy is linear in the target, smoothing splits the loss into the ordinary one-hot cross entropy plus a term that pulls the 
  overconfidence-and-bounded-logit-gradient | Why one-hot targets encourage overconfidence [from Rethinking the Inception Architecture for Computer Vision]: A one-hot target is only approached as the correct logit grows far larger than the rest, which the paper argues causes overfitting and, give
  smoothing-parameter-epsilon | Choosing $\epsilon$ and the prior $u$ [from Rethinking the Inception Architecture for Computer Vision]: LSR has two knobs: the smoothing parameter $\epsilon$ and the fixed distribution $u$. The paper's reported setting is $\epsilon = 0.1$ with 
  wordpiece-model | Wordpiece model (sub-word vocabulary) [from Google's Neural Machine Translation System]: A data-driven sub-word vocabulary: words are split into a fixed set of common sub-word units, so any string can be encoded with no out-of-vo
  length-normalization-penalty | Length normalization penalty lp(Y) [from Google's Neural Machine Translation System]: A divisor applied to a hypothesis's log-probability during beam search so that longer and shorter candidates can be compared fairly. It is c
  coverage-penalty | Coverage penalty cp(X; Y) [from Google's Neural Machine Translation System]: An extra term in the beam-search score that rewards hypotheses whose attention has spread over the whole source sentence. It is controlled b
  beam-search-pruning-and-termination | Beam search pruning and early termination [from Google's Neural Machine Translation System]: Beyond keeping a fixed number of hypotheses, the decoder prunes candidates that fall a fixed margin below the current best, both per-step an
  input-and-output-embeddings | Input and Output Embeddings [from Using the Output Embedding to Improve Language Models]: A word-level neural language model holds two matrices of the same shape: the input embedding that maps a token id to a vector, and the pre-s
  weight-tying | Weight Tying (U = V = S) [from Using the Output Embedding to Improve Language Models]: Weight tying replaces the separate input embedding and pre-softmax projection with one shared matrix S used in both roles. It cuts the param
  tied-embedding-update-dynamics | Why Tying Helps: Update Dynamics [from Using the Output Embedding to Improve Language Models]: In an untied model, only the row of the current input word gets an input-embedding gradient each step, while every row of the output embeddi
  three-way-weight-tying | Three-Way Weight Tying [from Using the Output Embedding to Improve Language Models]: In an encoder-decoder translation model, three-way weight tying shares one matrix across the encoder input embedding, the decoder input embe
  parse-tree-linearization | Parse Tree Linearization [from Grammar as a Foreign Language]: A constituency parse tree is turned into a flat token sequence by a depth-first traversal that emits an opening bracket-plus-label when ente
  wsj-parsing-protocol | WSJ Penn Treebank Evaluation Protocol [from Grammar as a Foreign Language]: The standard constituency-parsing benchmark: train on the Wall Street Journal portion of the Penn Treebank (about 40K sentences), tune on se
  semi-supervised-parsing-corpora | Machine-Parsed Semi-Supervised Corpora [from Grammar as a Foreign Language]: Two large synthetic training sets built by running existing parsers over unlabeled web news: a ~7M-sentence corpus labeled by one parser, an
  parsing-baseline-scores | Reference F1 Scores on WSJ [from Grammar as a Foreign Language]: The F1 numbers this line of work is compared against, on WSJ section 22 (dev) and section 23 (test), split into WSJ-only supervised parsers 

=== LOAD-BEARING RELATIONSHIPS (the paper's own reasoning) ===
  multi-head-attention (Multi-head attention) --depends-on--> scaled-dot-product-attention (Scaled dot-product attention): Each of the h heads is one call to the scaled dot-product attention function of Equation (1). Multi-head attention only adds the projections around it and the concatenation after it, so the inner function has to be understood firs
  eq-multihead (Attention in h parallel subspaces) --depends-on--> eq-attention (The scaled dot-product attention formula): The multi-head equation defines head_i as Attention(QW^Q_i, KW^K_i, VW^V_i). The Attention term is exactly Equation (1); the multi-head line is a wrapper, not a new scoring rule.
  dk-dv-dimensions (d_k and d_v (key and value dimensions)) --depends-on--> num-heads-h (h = 8 (number of heads)): The paper does not pick 64 independently. It sets d_k = d_v = d_model/h, so with d_model = 512 and h = 8 the head width falls out as 64. Change the head count and the head width changes with it.
  causal-masking (Causal masking) --depends-on--> softmax (Softmax): The mask sets forbidden scores to minus infinity before the softmax. It works only because exponentiating minus infinity gives zero, so those positions get no weight at all.
  masked-decoder-self-attention (Masked decoder self-attention) --depends-on--> auto-regressive-generation (Auto-regressive generation): The mask exists to stop leftward information flow so the auto-regressive property holds. Together with the one-position offset of the output embeddings, it ensures the prediction at position i uses only outputs before i.
  encoder-decoder-attention (Encoder-decoder attention) --depends-on--> encoder-stack (Encoder stack): In this sub-layer the queries come from the previous decoder layer but the memory keys and values come from the encoder's output. There is nothing to attend over until the encoder stack has run.
  positional-encoding (Positional encoding) --depends-on--> self-attention (Self-attention (intra-attention)): Self-attention treats the sequence as a set: every position looks at every other with no notion of who came first. Because the model has no recurrence and no convolution, the paper says order information must be injected separatel
  d-model (d_model = 512 (model dimension)) --depends-on--> residual-connection (Residual connection): Section 3.1 gives the reason directly: to facilitate the residual connections, all sub-layers and the embedding layers produce outputs of dimension 512. The uniform width is a consequence of adding inputs to outputs everywhere.
  sub-layer (Sub-layer) --depends-on--> layer-normalization (Layer normalization): The wrapper around each sub-layer is LayerNorm(x + Sublayer(x)). The normalization is part of the sub-layer's definition, not an optional extra.
  layer-normalization (Layer normalization) --depends-on--> layer-normalization-statistics (Layer normalization statistics): The paper uses layer normalization by citation and never writes the formula. The mean and standard deviation are computed across the hidden units of one position, which is what makes it work on variable-length sequences.
  embedding-softmax-weight-sharing (Embedding and softmax weight sharing) --depends-on--> weight-tying (Weight Tying (U = V = S)): The paper says only that it shares one weight matrix between the two embedding layers and the pre-softmax projection, citing prior work. That prior work is where the mechanism and its motivation live.
  label-smoothing (Label smoothing) --depends-on--> label-smoothing-regularization (Label-Smoothing Regularization (LSR)): The paper applies label smoothing at eps = 0.1 by citation. The cited definition is the mechanism: replace the one-hot target with a mixture that gives the correct token 1 - eps and spreads eps over the rest.
  byte-pair-encoding (Byte-pair encoding (BPE)) --depends-on--> bpe-merge-algorithm (BPE Merge Algorithm): The 37000-token English-German vocabulary is the output of the greedy merge loop from the cited paper. Without the merge procedure the vocabulary size is just a number with no meaning.
  length-penalty (Length penalty (alpha = 0.6)) --depends-on--> length-normalization-penalty (Length normalization penalty lp(Y)): The paper reports beam size 4 and alpha = 0.6 without saying what alpha does. It is the exponent in the cited length-normalization divisor applied to a hypothesis's log-probability so long and short candidates compare fairly.
  constituency-parsing (English constituency parsing) --depends-on--> parse-tree-linearization (Parse Tree Linearization): A sequence-to-sequence Transformer can only emit a flat string, so the parse tree has to be linearized into bracket-and-label tokens first. The paper does not restate this step; it follows the cited parsing setup.
  layer-comparison-desiderata (Three criteria for comparing layer types) --supported-by--> table-1 (Layer types by cost and path length): Section 4 proposes judging layer types by complexity per layer, minimum sequential operations, and maximum path length. Table 1 is that framework filled in: one row per layer type, one column per criterion. Without the table the s
  sequential-computation-bottleneck (Sequential computation bottleneck of RNNs) --supported-by--> table-1 (Layer types by cost and path length): The paper's complaint about RNNs is that positions must be computed one after another. Table 1's sequential-operations column states this as O(n) for a recurrent layer against O(1) for self-attention. Note the column counts operat
  long-range-dependencies (Long-range dependencies) --supported-by--> table-1 (Layer types by cost and path length): The claim is that shorter forward and backward paths make distant dependencies easier to learn. Table 1's path-length column gives O(1) for self-attention, O(n) for recurrent, and O(log_k n) for dilated convolutions. The table sho
  transformer (The Transformer) --supported-by--> table-2 (Translation quality against training cost): The paper's central quality claim rests on this table: 28.4 BLEU on WMT 2014 English-to-German, over 2.0 BLEU above the previous best including ensembles, plus the English-to-French result. Section 6.1 quotes 41.0 for English-to-F
  attention-heads-ablation (Number of heads sweet spot) --supported-by--> table-3 (One-knob-at-a-time ablations): Rows (A) vary h with total computation held constant. Single-head attention is 0.9 BLEU below the best setting, and quality also falls off with too many heads. This is the whole evidence for h=8 being a middle choice rather than m
  constituency-parsing (English constituency parsing) --supported-by--> table-4 (The parsing leaderboard): The generalization claim is carried entirely by this table: a 4-layer Transformer trained on the ~40K-sentence WSJ treebank reaches 91.3 F1 with almost no task-specific tuning, beating the BerkeleyParser in the same small-data set
  scaled-dot-product-attention (Scaled dot-product attention) --supported-by--> eq-attention (The scaled dot-product attention formula): Equation (1) is the definition, not an illustration of it: softmax(QK^T/sqrt(d_k))V. Everything else in Section 3.2 refers back to this line, including the masked and encoder-decoder uses.
  multi-head-attention (Multi-head attention) --supported-by--> eq-multihead (Attention in h parallel subspaces): The multi-head equation spells out that each head runs Equation (1) on its own projected queries, keys, and values, and that the outputs are concatenated and mapped back through W^O. With d_k = d_v = d_model/h = 64, it is also the
  position-wise-feed-forward-network (Position-wise feed-forward network) --supported-by--> eq-ffn (The per-position feed-forward block): Equation (2), max(0, xW1 + b1)W2 + b2, is the whole feed-forward sub-layer. It shows the network sees one position at a time: there is no mixing across positions here, so all cross-position work happens in the attention sub-layers
  sinusoidal-positional-encoding (Sinusoidal positional encoding) --supported-by--> eq-posenc (Position as a bank of sinusoids): The pair of PE formulas defines the encoding: even dimensions sine, odd dimensions cosine, with wavelengths in a geometric progression from 2*pi to 10000*2*pi. Nothing is learned here, which is what makes the scheme extendable to 
  encoder-self-attention (Encoder self-attention) --instance-of--> multi-head-attention (Multi-head attention): Section 3.2.3 lists three ways the Transformer uses multi-head attention. In the encoder, queries, keys and values all come from the previous encoder layer, so every position can attend to every position, with no mask.
  masked-decoder-self-attention (Masked decoder self-attention) --instance-of--> multi-head-attention (Multi-head attention): The decoder runs the same multi-head attention over its own previous layer, but each position may only attend up to and including itself. The paper says this is needed to preserve the auto-regressive property.
  encoder-decoder-attention (Encoder-decoder attention) --instance-of--> multi-head-attention (Multi-head attention): Here the queries come from the previous decoder layer and the keys and values come from the encoder output, so every decoder position can look at the whole input. The paper says this mimics the encoder-decoder attention of earlier
  sinusoidal-positional-encoding (Sinusoidal positional encoding) --instance-of--> positional-encoding (Positional encoding): Section 3.5 says there are many choices of positional encoding, learned and fixed. The paper's choice is a bank of sines and cosines whose wavelengths run from 2*pi to 10000*2*pi.
  scaled-dot-product-attention (Scaled dot-product attention) --instance-of--> eq-attention (The scaled dot-product attention formula): Equation (1) is the whole definition: dot every query against every key, divide by sqrt(d_k), softmax, and weight the values. Everything the paper says about attention refers back to this line.
  multi-head-attention (Multi-head attention) --instance-of--> eq-multihead (Attention in h parallel subspaces): The equation says what running h heads means concretely: project Q, K and V with per-head matrices, apply Equation (1) in each subspace, concatenate, then project with W^O.
  self-attention (Self-attention (intra-attention)) --contrasts-with--> recurrent-neural-network (Recurrent neural network (RNN)): Section 4 sets these two layer types against each other on the same job: mapping one length-n sequence to another. A self-attention layer connects all positions with a constant number of sequentially executed operations, while a r
  self-attention (Self-attention (intra-attention)) --contrasts-with--> convolutional-sequence-models (Convolutional sequence models): A single convolutional layer with kernel width k < n does not connect all pairs of positions; reaching all pairs needs O(n/k) layers, or O(log_k n) with dilated convolutions. Self-attention connects every pair in one layer, so the
  complexity-per-layer (Computational complexity per layer) --contrasts-with--> minimum-sequential-operations (Minimum sequential operations): Self-attention costs O(n^2 * d) per layer against a recurrent layer's O(n * d^2), so on per-layer cost recurrence wins whenever n is larger than d. The paper argues n < d holds for the sentence-length word-piece and byte-pair repr
  dot-product-attention (Dot-product (multiplicative) attention) --contrasts-with--> additive-attention (Additive attention): These are the two commonly used attention functions and the paper picks between them. They are similar in theoretical complexity, but dot-product attention is much faster and more space-efficient in practice because it maps onto o
  scaled-dot-product-attention (Scaled dot-product attention) --contrasts-with--> dot-product-attention (Dot-product (multiplicative) attention): The paper's attention is identical to plain dot-product attention except for the 1/sqrt(d_k) factor. The suspected reason for adding it: with d_k large the dot products grow in magnitude and push the softmax into a region of very 
  attention-head (Attention head) --contrasts-with--> multi-head-attention (Multi-head attention): With one attention head, averaging over positions inhibits attending to different representation subspaces at once; h = 8 heads at d_k = d_v = 64 keep total cost about the same as one full-width head. Table 3 row (A) puts a number
  attention-averaging-resolution-cost (Averaging cost of single-head attention) --contrasts-with--> maximum-path-length (Maximum path length): The background section states the trade plainly: the Transformer reduces relating two positions to a constant number of operations, albeit at the cost of reduced effective resolution from averaging attention-weighted positions. Mu
  label-smoothing (Label smoothing) --contrasts-with--> bleu (BLEU score): The same smoothing that hurts perplexity improves accuracy and BLEU. It is not a contradiction: perplexity grades how confident the model is on the reference token, while BLEU grades the tokens decoding actually picks, and the les
