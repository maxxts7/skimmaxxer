# id | kind | tier | name | summary
transformer | concept | major | The Transformer | The architecture this paper proposes: an encoder-decoder built entirely from stacked self-attention and position-wise feed-forward layers, with no rec
sequence-transduction | concept | minor | Sequence transduction | The general task of converting one sequence of symbols into another sequence, such as translating a German sentence into English.
machine-translation | concept | minor floor | Machine translation (part of sequence-transduction) | Automatically translating text from one language to another; the paper's main benchmark task.
language-modeling | concept | minor floor | Language modeling | Predicting the next symbol in a sequence given the symbols so far.
encoder-decoder-architecture | concept | major | Encoder-decoder architecture | A two-part model design: an encoder turns the input sequence into continuous representations, and a decoder generates the output sequence from them, o
auto-regressive-generation | concept | minor | Auto-regressive generation (part of encoder-decoder-architecture) | Producing an output sequence one symbol at a time, where each new symbol is conditioned on all previously generated symbols.
recurrent-neural-network | concept | minor floor | Recurrent neural network (RNN) | A network that processes a sequence one position at a time, carrying a hidden state forward.
lstm | concept | minor floor | Long short-term memory (LSTM) (part of recurrent-neural-network) | An RNN variant with gates that help preserve information over long spans.
gated-recurrent-unit | concept | minor floor | Gated recurrent unit (GRU) (part of recurrent-neural-network) | A simpler gated RNN variant, an alternative to the LSTM.
sequential-computation-bottleneck | concept | major | Sequential computation bottleneck of RNNs | Because an RNN's hidden state at position t depends on the hidden state at position t-1, positions within one training example cannot be computed in p
factorization-tricks | concept | minor | Factorization tricks for LSTMs (part of sequential-computation-bottleneck) | Techniques that factor an LSTM's weight matrices into smaller pieces to reduce computation.
conditional-computation | concept | minor | Conditional computation (part of sequential-computation-bottleneck) | Activating only parts of a network per input, so compute is spent selectively.
attention-mechanism | concept | major | Attention mechanism (query, key, value) | A function that maps a query vector and a set of key-value vector pairs to an output: a weighted sum of the values, with weights set by how well the q
queries-keys-values | concept | minor | Queries, keys, and values (part of attention-mechanism) | The three roles vectors play in attention: a query asks for information, keys advertise what each position holds, and values carry the content that ac
compatibility-function | concept | minor | Compatibility function (part of attention-mechanism) | The scoring function that measures how well a query matches a key; its scores become the attention weights after a softmax.
scaled-dot-product-attention | concept | major | Scaled dot-product attention (part of attention-mechanism) | The Transformer's attention: dot the query with every key, divide by $\sqrt{d_k}$, softmax the scores, and use them to weight the values.
dk-dv-dimensions | concept | minor | d_k and d_v (key and value dimensions) (part of scaled-dot-product-attention) | The dimensions of the key/query vectors ($d_k$) and value vectors ($d_v$) inside attention; in the base model each head uses $d_k = d_v = 64$.
sqrt-dk-scaling | concept | minor | The 1/sqrt(d_k) scaling factor (part of scaled-dot-product-attention) | Dividing the query-key dot products by $\sqrt{d_k}$ so their magnitude stays controlled as the dimension grows, keeping the softmax out of its flat, t
dot-product-attention | concept | minor | Dot-product (multiplicative) attention (part of attention-mechanism) | An attention variant that scores each query-key pair by their plain dot product; identical to the Transformer's attention except for the $1/\sqrt{d_k}
additive-attention | concept | minor | Additive attention (part of attention-mechanism) | An attention variant that scores each query-key pair with a small feed-forward network with one hidden layer, instead of a dot product.
multi-head-attention | concept | major | Multi-head attention (part of attention-mechanism) | Running $h$ attention functions in parallel on different learned linear projections of the queries, keys, and values, then concatenating and projectin
attention-head | concept | minor | Attention head (part of multi-head-attention) | One of the parallel attention functions inside multi-head attention, operating on its own learned low-dimensional projection of the queries, keys, and
num-heads-h | concept | minor | h = 8 (number of heads) (part of multi-head-attention) | The number of parallel attention heads in the base model: eight.
attention-projection-matrices | concept | minor | Attention projection matrices (W^Q, W^K, W^V, W^O) (part of multi-head-attention) | The learned linear maps that create each head's queries, keys, and values, and the final matrix that mixes the concatenated head outputs back to $d_{m
attention-averaging-resolution-cost | concept | minor | Averaging cost of single-head attention (part of multi-head-attention) | A single attention operation blends many positions into one weighted average, which can blur position-specific information.
attention-interpretability | concept | minor | Interpretability of attention (part of multi-head-attention) | The paper's observation that inspecting attention distributions suggests individual heads learn distinct, sometimes linguistically meaningful, roles.
self-attention | concept | major | Self-attention (intra-attention) (part of attention-mechanism) | Attention where the queries, keys, and values all come from the same sequence, so every position can gather information from every other position in t
encoder-self-attention | concept | minor | Encoder self-attention (part of self-attention) | The self-attention layers in the encoder, where each position can attend to all positions in the previous encoder layer, with no masking.
masked-decoder-self-attention | concept | minor | Masked decoder self-attention (part of self-attention) | The decoder's self-attention, restricted by a mask so each position can only attend to itself and earlier positions.
causal-masking | concept | minor | Causal masking (part of masked-decoder-self-attention) | Setting the attention scores for forbidden (future) positions to $-\infty$ before the softmax, so those positions receive zero attention weight.
restricted-self-attention | concept | minor | Restricted (local) self-attention (part of self-attention) | A proposed variant where each position attends only to a neighborhood of size r around it, trading path length for cheaper computation on very long se
encoder-decoder-attention | concept | minor | Encoder-decoder attention (part of attention-mechanism) | The decoder sub-layer where queries come from the decoder and the keys and values come from the encoder's output, letting every decoder position look 
encoder-stack | concept | major | Encoder stack (part of transformer) | The Transformer's encoder: $N=6$ identical layers, each containing a multi-head self-attention sub-layer followed by a position-wise feed-forward sub-
decoder-stack | concept | major | Decoder stack (part of transformer) | The Transformer's decoder: $N=6$ identical layers, each with masked self-attention, attention over the encoder output, and a feed-forward sub-layer.
sub-layer | concept | minor | Sub-layer (part of transformer) | One of the components inside a Transformer layer — an attention mechanism or a feed-forward network — each wrapped in its own residual connection and 
residual-connection | concept | minor | Residual connection | A shortcut that adds a layer's input directly to its output, so the layer only has to learn a change (residual) on top of the identity.
layer-normalization | concept | minor | Layer normalization | A normalization step that rescales each position's activation vector to have a standard mean and variance, computed across the features of that single
n-layers | concept | minor | N = 6 (number of layers) (part of transformer) | The number of identical layers stacked in each of the encoder and the decoder in the base model: six.
d-model | concept | minor | d_model = 512 (model dimension) (part of transformer) | The shared width of the model: every sub-layer and embedding layer outputs vectors of dimension $d_{model} = 512$.
position-wise-feed-forward-network | concept | minor | Position-wise feed-forward network (part of transformer) | A two-layer fully connected network applied to each sequence position independently and identically: expand to $d_{ff}=2048$, apply ReLU, project back
d-ff | concept | minor | d_ff = 2048 (feed-forward inner dimension) (part of position-wise-feed-forward-network) | The width of the hidden layer inside the position-wise feed-forward network: 2048, four times $d_{model}$.
relu | concept | minor floor | ReLU activation (part of position-wise-feed-forward-network) | The rectified linear unit, $\max(0, x)$: a standard nonlinearity that zeroes out negative values.
softmax | concept | minor floor | Softmax | The standard function that turns a vector of scores into a probability distribution: exponentiate each score and normalize by the sum.
token-embeddings | concept | minor floor | Token embeddings (part of transformer) | Learned lookup tables that convert input and output tokens into $d_{model}$-dimensional vectors.
embedding-softmax-weight-sharing | concept | minor | Embedding and softmax weight sharing (part of transformer) | The two token embedding layers and the pre-softmax output projection all share a single weight matrix.
sqrt-dmodel-embedding-multiplier | concept | minor | sqrt(d_model) embedding multiplier (part of embedding-softmax-weight-sharing) | In the embedding layers, the shared weights are multiplied by $\sqrt{d_{model}}$.
positional-encoding | concept | major | Positional encoding (part of transformer) | Vectors added to the input embeddings that inject information about each token's position, since the model itself has no recurrence or convolution to 
sinusoidal-positional-encoding | concept | minor | Sinusoidal positional encoding (part of positional-encoding) | The paper's fixed positional encoding: each dimension is a sine or cosine of the position, at wavelengths forming a geometric progression from $2\pi$ 
relative-position-linearity | concept | minor | Relative positions as linear functions (part of sinusoidal-positional-encoding) | For any fixed offset $k$, the sinusoidal encoding of position $pos+k$ is a linear function of the encoding of position $pos$ — the hypothesized reason
learned-positional-embedding | concept | minor | Learned positional embeddings (part of positional-encoding) | The alternative to sinusoids: a trainable vector per position, learned like any other parameter.
convolutional-sequence-models | concept | minor | Convolutional sequence models | Prior architectures (Extended Neural GPU, ByteNet, ConvS2S) that replace recurrence with convolutions, computing all positions' hidden representations
extended-neural-gpu | concept | minor | Extended Neural GPU (part of convolutional-sequence-models) | A convolution-based sequence model cited as prior work on reducing sequential computation.
bytenet | concept | minor | ByteNet (part of convolutional-sequence-models) | A convolutional translation model using dilated convolutions; relating two positions costs a number of operations that grows logarithmically with thei
convs2s | concept | minor | ConvS2S (part of convolutional-sequence-models) | A convolutional sequence-to-sequence translation model; relating two positions costs a number of operations that grows linearly with their distance.
dilated-convolutions | concept | minor | Dilated convolutions (part of convolutional-sequence-models) | Convolutions whose kernel taps are spaced apart, so each layer sees a wider span without more weights.
separable-convolutions | concept | minor | Separable convolutions (part of convolutional-sequence-models) | A cheaper convolution that splits the operation into a per-channel (depthwise) part and a 1x1 mixing part.
end-to-end-memory-networks | concept | minor | End-to-end memory networks | Prior models built on a recurrent attention mechanism instead of sequence-aligned recurrence.
layer-comparison-desiderata | concept | major | Three criteria for comparing layer types | Section 4's framework: judge self-attention, recurrent, and convolutional layers by per-layer complexity, minimum sequential operations, and maximum p
complexity-per-layer | concept | minor | Computational complexity per layer (part of layer-comparison-desiderata) | The total amount of computation one layer performs, as a function of sequence length n, representation dimension d, and kernel width k.
minimum-sequential-operations | concept | minor | Minimum sequential operations (part of layer-comparison-desiderata) | The number of operations that must run one after another rather than in parallel; the paper's measure of parallelizability.
maximum-path-length | concept | minor | Maximum path length (part of layer-comparison-desiderata) | The longest path a signal must travel through the network between any two input and output positions; shorter paths make long-range dependencies easie
long-range-dependencies | concept | minor | Long-range dependencies | Relationships between symbols far apart in a sequence, which the paper calls a key challenge in sequence transduction.
byte-pair-encoding | concept | major | Byte-pair encoding (BPE) | A subword tokenization scheme that builds a vocabulary by repeatedly merging the most frequent pair of symbols, so rare words split into known pieces.
word-piece-encoding | concept | minor | Word-piece vocabulary | A subword tokenization scheme similar in spirit to BPE, from Google's neural machine translation system; the paper uses a 32000-token word-piece vocab
bleu | concept | major | BLEU score | The standard automatic metric for translation quality: n-gram overlap precision against reference translations with a penalty for overly short outputs
perplexity | concept | minor | Perplexity | A measure of how well a model predicts the true next tokens: the exponentiated average negative log-probability. Lower is better.
model-ensembling | concept | minor floor | Model ensembling | Combining predictions from several trained models to get better quality than any single model.
tensor2tensor | concept | minor | tensor2tensor | The open-source codebase the authors used to train and evaluate the Transformer.
transformer-training-recipe | concept | major | Transformer training recipe | The complete set of choices the paper used to train its models: data, batching, hardware, optimizer schedule, and regularization.
wmt-2014-en-de | concept | minor | WMT 2014 English-German dataset (part of transformer-training-recipe) | The standard benchmark translation dataset of about 4.5 million English-German sentence pairs used as the main training and evaluation task.
wmt-2014-en-fr | concept | minor | WMT 2014 English-French dataset (part of transformer-training-recipe) | A much larger benchmark translation dataset of 36 million sentence pairs, used to test the model at larger data scale.
length-based-batching | concept | minor | Batching by sequence length (part of transformer-training-recipe) | Sentence pairs of similar length are grouped into the same batch, and batch size is measured in tokens rather than in sentences.
training-hardware-and-schedule | concept | minor | Hardware and training schedule (part of transformer-training-recipe) | One machine with 8 NVIDIA P100 GPUs; base models train for 100,000 steps (12 hours), big models for 300,000 steps (3.5 days).
base-vs-big-transformer | concept | minor | Base vs big model configurations (part of transformer-training-recipe) | The paper trains two main configurations: a 'base' Transformer and a scaled-up 'big' Transformer with larger dimensions, more dropout, and three times
adam-optimizer | concept | minor floor | Adam optimizer | A standard gradient-descent variant that adapts the step size per parameter using running averages of gradients and squared gradients.
transformer-adam-settings | concept | minor | Adam settings (beta2 = 0.98) (part of transformer-training-recipe) | The paper runs Adam with beta1 = 0.9, beta2 = 0.98, and epsilon = 10^-9, a lower-than-default beta2.
learning-rate-warmup-schedule | concept | major | Warmup learning-rate schedule (part of transformer-training-recipe) | The learning rate rises linearly for the first warmup_steps steps, then decays as the inverse square root of the step number, all scaled by the model 
warmup-steps-4000 | concept | minor | warmup_steps = 4000 (part of learning-rate-warmup-schedule) | The number of initial steps over which the learning rate ramps up linearly; the paper uses 4000.
regularization | concept | minor floor | Regularization | Any technique that discourages a model from fitting the training data too closely so it generalizes better.
dropout | concept | minor floor | Dropout | Randomly zeroing a fraction of activations during training so the network cannot rely on any single unit.
residual-dropout | concept | minor | Residual dropout (part of transformer-training-recipe) | Dropout applied to each sub-layer's output before the residual addition and layer normalization, and to the embedding-plus-positional-encoding sums, a
attention-dropout | concept | minor | Attention dropout (part of transformer-training-recipe) | Dropout applied inside the attention mechanism, mentioned as a separately tunable rate in the parsing experiments.
label-smoothing | concept | major | Label smoothing (part of transformer-training-recipe) | A training target modification: instead of putting probability 1 on the correct token, the target gives it 1 - eps and spreads eps = 0.1 over other to
beam-search | concept | major | Beam search | A decoding procedure that keeps the k best partial output sequences at each step instead of committing to a single best token; the paper uses beam siz
length-penalty | concept | minor | Length penalty (alpha = 0.6) (part of beam-search) | A normalization applied to beam-search scores so that longer output sequences are not unfairly penalized; the paper uses strength alpha = 0.6 from the
inference-length-limit | concept | minor | Maximum output length rule (part of beam-search) | During inference the output is capped at input length + 50 tokens, with early termination when possible.
checkpoint-averaging | concept | minor | Checkpoint averaging | The final model's weights are the elementwise average of the last several saved checkpoints: 5 for base models, 20 for big models, written at 10-minut
training-flops-estimate | concept | minor | Training-cost FLOPs estimate | The paper estimates each model's training cost as training time x number of GPUs x an assumed sustained single-precision throughput per GPU.
transformer-ablations | concept | major | Model variation ablations (Table 3) | Experiments that change one component of the base model at a time and measure the effect on English-to-German translation, to see which parts matter.
attention-heads-ablation | concept | minor | Number of heads sweet spot (part of transformer-ablations) | Varying the head count with total computation held constant shows single-head attention is 0.9 BLEU worse than the best setting, but too many heads al
key-size-ablation | concept | minor | Reducing d_k hurts quality (part of transformer-ablations) | Shrinking the attention key dimension d_k lowers translation quality, suggesting the dot product may be too simple a compatibility function.
size-and-dropout-ablation | concept | minor | Model size and dropout effects (part of transformer-ablations) | Bigger models translate better, and dropout is very helpful in preventing over-fitting.
constituency-parsing | concept | major | English constituency parsing | The task of producing a sentence's syntactic tree — nested phrases like noun phrases and verb phrases — used as a generalization test for the Transfor
penn-treebank-wsj | concept | minor | Penn Treebank (WSJ portion) (part of constituency-parsing) | The standard constituency-parsing dataset: about 40K hand-parsed sentences from the Wall Street Journal.
semi-supervised-parsing | concept | minor | Semi-supervised parsing setting (part of constituency-parsing) | A second training regime that adds about 17M automatically parsed sentences from the high-confidence and BerkeleyParser corpora on top of the WSJ data
recurrent-neural-network-grammar | concept | minor | Recurrent Neural Network Grammar (RNNG) (part of constituency-parsing) | A parsing model that builds trees with explicit tree-structured operations; the one prior model the Transformer does not beat on parsing.
berkeley-parser | concept | minor | BerkeleyParser (part of constituency-parsing) | A classical (non-neural) grammar-based parser used as a baseline; the Transformer beats it even with only 40K WSJ training sentences.
fig-1 | figure | evidence | The two-stack data path | The figure shows the entire model in one picture: two towers of identical layers, each layer built only from attention and a position-wise feed-forwar
fig-2 | figure | evidence | Attention as a wiring diagram | The figure draws the paper's attention math as data flow. On the left, Equation (1) is laid out box by box: dot the queries against the keys, scale, o
table-1 | table | evidence | Layer types by cost and path length | The table is the paper's argument for replacing recurrence with self-attention. A self-attention layer connects every position to every other in a con
table-2 | table | evidence | Translation quality against training cost | On the WMT 2014 newstest2014 benchmarks, the big Transformer reports the best English-to-German BLEU in the table: 28.4, more than 2.0 BLEU above ever
table-3 | table | evidence | One-knob-at-a-time ablations | Table 3 changes one design choice of the base Transformer at a time and reports the effect on English-to-German development-set quality. It shows a mi
table-4 | table | evidence | The parsing leaderboard | Trained on the ~40K-sentence WSJ treebank with almost no task-specific tuning, a 4-layer Transformer reaches 91.3 F1 on WSJ Section 23. That is above 
fig-3 | figure | evidence | Heads tracking "making … more difficult" | This is one query word's attention pattern in a single encoder self-attention layer, drawn as lines running from that word to the positions it attends
fig-4 | figure | evidence | Two heads that resolve "its" | Two different attention heads in the same encoder layer put almost all of the word "its" attention on two different words — head 5 on "Law", head 6 on
fig-5 | figure | evidence | Two heads, one sentence | Two attention heads from the same encoder self-attention layer, run on the same sentence, produce visibly different attention patterns. One fans many 
eq-attention | equation | evidence | The scaled dot-product attention formula | This is the paper's whole attention function in one line: score every query against every key with a dot product, shrink the scores by $\sqrt{d_k}$, s
eq-multihead | equation | evidence | Attention in h parallel subspaces | Instead of one attention function over full-width 512-dimensional vectors, the Transformer runs $h=8$ attention functions in parallel. Each one works 
eq-ffn | equation | evidence | The per-position feed-forward block | Equation (2) defines the second sub-layer in every encoder and decoder layer: a two-layer MLP with a ReLU in between, applied to each position's vecto
eq-posenc | equation | evidence | Position as a bank of sinusoids | A model with no recurrence and no convolution has no idea what order its tokens are in. So the paper adds a fixed vector to each input embedding, whos
eq-lrate | equation | evidence | The warmup-then-decay learning rate | Equation (3) makes the learning rate a function of the training step rather than a constant. It rises linearly for the first warmup_steps steps, then 
layer-normalization-statistics | cited-concept | from Layer Normalization | Layer normalization statistics | Layer normalization standardizes a layer's activations using a mean and standard deviation computed across the hidden units of that layer, separately 
gain-and-bias-parameters | cited-concept | from Layer Normalization | Learned gain and bias | After standardization, each neuron gets its own learned multiplicative gain and additive bias, applied before the non-linearity, so the layer can reco
layer-norm-vs-batch-norm | cited-concept | from Layer Normalization | Contrast with batch normalization | Batch normalization computes each neuron's statistics across the training cases in a mini-batch; layer normalization computes them across the neurons 
per-timestep-normalization | cited-concept | from Layer Normalization | Per-step normalization in sequence models | In a recurrent or sequential model, layer normalization is applied independently at each time step, using only that step's activations, with one share
normalization-invariance-properties | cited-concept | from Layer Normalization | Invariance and learning-rate effects | Layer normalization makes the model's output invariant to re-scaling and re-shifting of a layer's whole weight matrix and to re-scaling of an individu
bpe-merge-algorithm | cited-concept | from Neural Machine Translation of Rare Words with Subword Units | BPE Merge Algorithm | A greedy loop that builds a subword vocabulary by repeatedly merging the most frequent adjacent symbol pair in the training corpus. Adapted from Gage'
bpe-segmentation-at-test-time | cited-concept | from Neural Machine Translation of Rare Words with Subword Units | Applying BPE Merges to New Text | At test time, words are split into characters and the learned merge operations are replayed in order, producing a segmentation of any word into known 
bpe-vocabulary-size | cited-concept | from Neural Machine Translation of Rare Words with Subword Units | Vocabulary Size as the Single Hyperparameter | The final BPE symbol vocabulary size equals the initial character vocabulary size plus the number of merge operations. The merge count is the algorith
joint-bpe-shared-vocabulary | cited-concept | from Neural Machine Translation of Rare Words with Subword Units | Joint BPE and the Shared Source-Target Vocabulary | Joint BPE learns one set of merge operations on the union (in practice, the concatenation) of the source and target training text, giving both languag
vocabulary-vs-sequence-length-tradeoff | cited-concept | from Neural Machine Translation of Rare Words with Subword Units | Vocabulary Size vs. Sequence Length Trade-off | Subword segmentation trades vocabulary size against tokenized sequence length: finer splits shrink the vocabulary but lengthen every sentence, and enc
label-smoothing-regularization | cited-concept | from Rethinking the Inception Architecture for Computer Vision | Label-Smoothing Regularization (LSR) | Replace the one-hot training target with a mixture of that one-hot target and a fixed label distribution, so the correct class gets mass $1-\epsilon$ 
lsr-as-two-cross-entropy-losses | cited-concept | from Rethinking the Inception Architecture for Computer Vision | LSR as a pair of cross-entropy losses | Because cross entropy is linear in the target, smoothing splits the loss into the ordinary one-hot cross entropy plus a term that pulls the predicted 
overconfidence-and-bounded-logit-gradient | cited-concept | from Rethinking the Inception Architecture for Computer Vision | Why one-hot targets encourage overconfidence | A one-hot target is only approached as the correct logit grows far larger than the rest, which the paper argues causes overfitting and, given the boun
smoothing-parameter-epsilon | cited-concept | from Rethinking the Inception Architecture for Computer Vision | Choosing $\epsilon$ and the prior $u$ | LSR has two knobs: the smoothing parameter $\epsilon$ and the fixed distribution $u$. The paper's reported setting is $\epsilon = 0.1$ with uniform $u
wordpiece-model | cited-concept | from Google's Neural Machine Translation System | Wordpiece model (sub-word vocabulary) | A data-driven sub-word vocabulary: words are split into a fixed set of common sub-word units, so any string can be encoded with no out-of-vocabulary t
length-normalization-penalty | cited-concept | from Google's Neural Machine Translation System | Length normalization penalty lp(Y) | A divisor applied to a hypothesis's log-probability during beam search so that longer and shorter candidates can be compared fairly. It is controlled 
coverage-penalty | cited-concept | from Google's Neural Machine Translation System | Coverage penalty cp(X; Y) | An extra term in the beam-search score that rewards hypotheses whose attention has spread over the whole source sentence. It is controlled by a weight
beam-search-pruning-and-termination | cited-concept | from Google's Neural Machine Translation System | Beam search pruning and early termination | Beyond keeping a fixed number of hypotheses, the decoder prunes candidates that fall a fixed margin below the current best, both per-step and among fi
input-and-output-embeddings | cited-concept | from Using the Output Embedding to Improve Language Models | Input and Output Embeddings | A word-level neural language model holds two matrices of the same shape: the input embedding that maps a token id to a vector, and the pre-softmax pro
weight-tying | cited-concept | from Using the Output Embedding to Improve Language Models | Weight Tying (U = V = S) | Weight tying replaces the separate input embedding and pre-softmax projection with one shared matrix S used in both roles. It cuts the parameter count
tied-embedding-update-dynamics | cited-concept | from Using the Output Embedding to Improve Language Models | Why Tying Helps: Update Dynamics | In an untied model, only the row of the current input word gets an input-embedding gradient each step, while every row of the output embedding gets on
three-way-weight-tying | cited-concept | from Using the Output Embedding to Improve Language Models | Three-Way Weight Tying | In an encoder-decoder translation model, three-way weight tying shares one matrix across the encoder input embedding, the decoder input embedding, and
parse-tree-linearization | cited-concept | from Grammar as a Foreign Language | Parse Tree Linearization | A constituency parse tree is turned into a flat token sequence by a depth-first traversal that emits an opening bracket-plus-label when entering a nod
wsj-parsing-protocol | cited-concept | from Grammar as a Foreign Language | WSJ Penn Treebank Evaluation Protocol | The standard constituency-parsing benchmark: train on the Wall Street Journal portion of the Penn Treebank (about 40K sentences), tune on section 22, 
semi-supervised-parsing-corpora | cited-concept | from Grammar as a Foreign Language | Machine-Parsed Semi-Supervised Corpora | Two large synthetic training sets built by running existing parsers over unlabeled web news: a ~7M-sentence corpus labeled by one parser, and a ~11M-s
parsing-baseline-scores | cited-concept | from Grammar as a Foreign Language | Reference F1 Scores on WSJ | The F1 numbers this line of work is compared against, on WSJ section 22 (dev) and section 23 (test), split into WSJ-only supervised parsers and semi-s
