THE WHOLE PAPER, AS THE PIPELINE HAS IT

=== CONCEPT THEMES (the chapters the reader can drill into) ===

[1] theme-the-problem-and-the-open-questions | The Problem and the Open Questions
    The paper starts from an imbalance: raw text is plentiful, while labelled data for any particular language understanding task is small and costly to produce. It frames the diverse supervised tasks it cares about under one label, and asks whether one model trained on unlabelled text could serve them all. Two questions are left open at this point and the rest of the paper answers them: which unsuper
    members: labeled-data-scarcity, generative-vs-discriminative-training, natural-language-understanding, choice-of-pretraining-objective, how-to-transfer-representations
    its page opens: What should you do when the text you have is not the text you need? The paper opens on that mismatch: unlabelled text exists in enormous quantity, while the labelled examples for any single language understanding task are few and expensive 

[2] theme-where-this-sits-in-prior-work | Where This Sits in Prior Work
    The paper places itself inside semi-supervised learning for NLP and then separates that field into branches so it can say which one it is joining. One branch learns word, phrase or sentence vectors and hands them to a task model as features; another adds an unsupervised loss term alongside the supervised one; another uses unlabelled data only to choose the starting weights. The closest prior line 
    members: semi-supervised-learning-for-nlp, word-embedding-transfer, phrase-and-sentence-embeddings, unsupervised-pretraining, pretraining-as-regularization, auxiliary-training-objectives, feature-based-transfer, elmo-deep-contextualized-word-representations, per-task-parameter-cost, lm-pretraining-then-finetuning-predecessors, semi-supervised-sequence-learning, ulmfit, lstm-short-range-limitation
    its page opens: Using unlabelled text to help a supervised task was not new when this paper was written. So the related work section has one job: name the family the method belongs to, cut it into branches, and say which branch it joins and which it argues

[3] theme-one-model-two-stages | One Model, Two Stages
    The recipe in outline: train a network to predict next tokens on a large unlabelled corpus, then take those same weights as the starting point for supervised training on a target task. Nothing is frozen and no task-specific network is built on top, so the same architecture is what gets fine-tuned every time. The paper also states that the unlabelled corpus need not come from the target task's doma
    members: two-stage-training-procedure, transfer-learning, domain-mismatch-tolerance, task-agnostic-model, task-specific-architectures
    its page opens: Raw text is everywhere; labelled examples for any one language understanding task are few and expensive ([[labeled-data-scarcity]]). So what is the least you can change about ordinary supervised training to make all that unlabelled text cou

[4] theme-stage-one-next-token-prediction | Stage One: Predicting the Next Token
    Stage one trains on a single loss: the log-probability the model assigns each token given a window of preceding tokens, summed over the corpus. The window here is 512 contiguous tokens, which is what lets a prediction condition on material several sentences back. The network is a stack of transformer blocks used as a decoder alone, with self-attention masked so each position sees only itself and i
    members: generative-pretraining, language-modeling-objective, context-window-512-tokens, long-range-dependencies-gpt1, multi-layer-transformer-decoder, decoder-only-stack, masked-self-attention, input-representation-h0, token-embedding-matrix, learned-position-embeddings, output-softmax-tied-embeddings, model-specification, twelve-layer-depth, hidden-state-dimension-768, twelve-attention-heads, position-wise-feedforward-3072, gelu, weight-initialization, world-knowledge
    its page opens: Stage one has no labels, so the first question is what to train on at all. The paper takes the signal that raw text already carries: given the tokens so far, predict the one that comes next. What [[generative-pretraining|stage one]] keeps a

[5] theme-corpus-tokens-and-training-setup | Corpus, Tokens and Training Setup
    The corpus is over 7,000 unpublished books, chosen because the text stays in its original order across sentences and paragraphs; the paper contrasts it with a same-sized benchmark that is shuffled sentence by sentence. Raw text is cleaned, tokenized, then cut into subword units by byte-pair encoding with 40,000 merges. Training uses Adam with a learning rate warmed up over 2000 updates and then an
    members: bookscorpus, long-range-contiguous-text, one-billion-word-benchmark, text-preprocessing-pipeline, ftfy, spacy-tokenizer, byte-pair-encoding-gpt1, bpe-merges-40000, pre-training-optimization, adam-optimizer-gpt1, linear-warmup, cosine-annealing, pretraining-schedule, regularization-setup, residual-embedding-attention-dropout, decoupled-weight-decay, token-level-perplexity
    its page opens: The pre-training objective [[eq-lm-objective]] says what to maximize. It does not say what text to maximize it over, how that text becomes tokens, or what optimizer turns the sum into a fitted model. This chapter covers those three choices,

[6] theme-stage-two-fine-tuning-and-input-shapes | Stage Two: Fine-Tuning and Reshaping Task Inputs
    Stage two runs a labelled input through the pre-trained stack, takes the final block's activation at the last token, and multiplies it by one added matrix to score labels. The language modelling loss is not dropped: it keeps being optimized alongside the task loss, scaled by a weight the paper sets to 0.5. Tasks whose inputs have several parts are handled by writing those parts into one ordered se
    members: supervised-fine-tuning, labeled-dataset-c, final-token-activation, linear-output-layer-wy, supervised-objective-l2, minimal-extra-parameters, auxiliary-lm-objective, auxiliary-objective-weight, task-specific-input-transformations, structured-inputs, traversal-style-approach, start-and-end-tokens, delimiter-token, entailment-input-transformation, similarity-input-transformation, qa-and-commonsense-input-transformation, fine-tuning-hyperparameters, fine-tuning-lr-and-batch, three-epochs-fine-tuning, fine-tuning-lr-schedule, classifier-dropout
    its page opens: Stage one produced a stack of weights that predicts the next token. Stage two asks how to turn those weights into something that predicts a label — and how to do that the same way for every task, rather than once per task. [[supervised-fine

[7] theme-what-the-twelve-datasets-show | What the Twelve Datasets Show
    The same fine-tuned model is measured on four families of tasks across twelve datasets: inference, reading and commonsense reasoning, sentence similarity, and single-sentence classification. Gains are reported as differences in percentage points against the previous best published number, mostly task-built recurrent systems, some of them ensembles or using ELMo features. The headline is new best r
    members: evaluation-task-families, glue-benchmark, baseline-comparisons, absolute-improvement, natural-language-inference, coreference, snli, multinli, qnli, scitail, rte, question-answering-and-commonsense-reasoning, race-dataset, cnn-and-squad-contrast, story-cloze-test, semantic-similarity, mrpc, qqp, sts-b, text-classification, cola, sst-2, state-of-the-art-9-of-12
    its page opens: The recipe makes one bet: pre-train a single stack once, then adapt it to a task by [[theme-stage-two-fine-tuning-and-input-shapes|rewriting that task's input]] instead of redesigning the network. The twelve datasets are where that bet gets

[8] theme-taking-the-recipe-apart | Taking the Recipe Apart
    Three diagnostics ask which parts of the recipe are doing the work. Transferring more pre-trained layers keeps helping rather than saturating early, and hand-built heuristics show the pre-trained model already doing several tasks at some level with no supervised training at all, improving steadily as pre-training runs. The removals are blunter: no pre-training costs the most, swapping the transfor
    members: layers-transferred, zero-shot-behaviors, average-token-log-probability-scoring, cola-zero-shot-heuristic, sst2-zero-shot-heuristic, race-zero-shot-heuristic, dprd-winograd-heuristic, transformer-inductive-bias, ablation-studies, ablation-no-pretraining, ablation-lstm-substitution, ablation-no-auxiliary-objective
    its page opens: The results say the recipe works. They do not say which part of it is doing the work — the pre-training, the architecture, the extra loss during fine-tuning, or the depth of the stack. Section 5 attacks that question three ways: turn one di

=== EDGE THEMES (arguments that cut across the chapters) ===

[1] etheme-scarce-labels-borrowed-weights | Scarce Labels, Borrowed Weights
    The paper opens on a mismatch: unlabelled text is abundant, while the labelled dataset for any one language understanding task is small and expensive to build. Its answer is the two-stage training procedure — fit a language model on raw text, then keep those weights and adapt the same network with labels — and it presents that recipe as one entry in semi-supervised learning for NLP, specifically t

[2] etheme-the-next-token-machine | The Next-Token Machine
    Stage one is one loss and one network, and the paper writes both down. The loss, $L_1$ in Equation 1, is the summed log-probability the model assigns to each token given the $k$ tokens before it, which is ordinary maximum-likelihood fitting applied to text; the paper treats picking this objective as a choice among candidates, since which unsupervised objective transfers best was one of the open qu

[3] etheme-the-long-range-bet | The Bet on Long Stretches of Text
    The corpus is chosen as an argument, not a convenience. BooksCorpus is over 7,000 unpublished books, and what the paper wants from it is long stretches of contiguous text — sentences left in their original order across paragraphs — which it sets against the 1B Word Benchmark, a corpus of roughly the same size but shuffled at the sentence level. The context window follows: $k$ is fixed at 512 token

[4] etheme-one-stack-any-input-shape | One Stack, Any Input Shape
    A pre-trained decoder reads one ordered sequence of tokens, but the evaluation tasks hand over premise-hypothesis pairs and document-question-answer triplets. The paper's move is to change the input rather than the model: each structured input is walked into a single sequence, bracketed by a start and an end token and split by a `$` delimiter, so entailment becomes premise-delimiter-hypothesis, si

[5] etheme-two-losses-and-three-removals | Two Losses, and Three Things Taken Away
    Fine-tuning here optimizes two objectives at once: the task loss $L_2$ and the original language modelling loss kept alive on the labelled data, combined as $L_3 = L_2 + \lambda L_1$ with $\lambda$ set to 0.5. This is the same auxiliary-objective idea the related work describes as a branch of semi-supervised learning, used here as a supplement to pre-training rather than as a replacement for it, a

[6] etheme-the-scoreboard-and-its-gaps | The Scoreboard and Its Gaps
    The evaluation is twelve datasets across four families — natural language inference, question answering and commonsense reasoning, semantic similarity, and text classification — and the headline is new best published results on nine of them, reported as absolute percentage-point differences against the previous best. On inference the fine-tuned model leads task-built baselines on four of five data

[7] etheme-watching-pre-training-work | Watching Pre-Training Do the Work
    Two diagnostics in Figure 2 try to say why the recipe works, rather than that it works. The first varies how many pre-trained layers are carried into fine-tuning, a sweep that only exists because the stack is twelve blocks deep, and finds each transferred layer adding performance, up to 9 percent on MultiNLI for the full transfer — which is the paper's case that moving embeddings alone leaves valu

=== MAJOR CONCEPTS (each has a full page) ===
  labeled-data-scarcity | Scarcity of Labelled Data: The problem the paper starts from: raw text is abundant, but human-annotated data for any one task is small and expensive.
  semi-supervised-learning-for-nlp | Semi-Supervised Learning for NLP: The family of methods that combine a large unlabelled corpus with a small labelled dataset, which the paper places itself inside.
  two-stage-training-procedure | Two-Stage Training Procedure: The paper's recipe: learn the network's initial parameters with a language modelling objective on unlabelled text, then adapt those same parameters to each target task wi
  generative-pretraining | Generative Pre-Training (Stage One): Train a multi-layer transformer decoder to predict each next token on a large unlabelled corpus, and keep the weights rather than the outputs.
  language-modeling-objective | Language Modelling Objective $L_1$: The pre-training loss $L_1(U)=\sum_i \log P(u_i \mid u_{i-k},\dots,u_{i-1};\Theta)$: maximize the log-probability the model gives each token given the tokens before it.
  multi-layer-transformer-decoder | Multi-Layer Transformer Decoder: The language model is a stack of transformer blocks used as a decoder only: masked multi-head self-attention over the context, then position-wise feedforward layers.
  decoder-only-stack | Decoder-Only Stack: The model drops the encoder half of the original encoder-decoder transformer and keeps one stack that attends only to its own past tokens.
  masked-self-attention | Masked Self-Attention: Self-attention restricted so each position attends only to itself and earlier positions, never to later ones.
  supervised-fine-tuning | Supervised Fine-Tuning (Stage Two): Run labelled inputs through the pre-trained model, take the final block's activation at the last token, and train a linear layer on it to predict the label.
  auxiliary-lm-objective | Auxiliary Language Modelling Objective: During fine-tuning the paper keeps optimizing the language modelling loss alongside the task loss: $L_3(C) = L_2(C) + \lambda\, L_1(C)$.
  task-specific-input-transformations | Task-Specific Input Transformations: Structured task inputs are rewritten as one ordered token sequence, so the pre-trained model can read them without architectural change.
  task-agnostic-model | Task-Agnostic Model: A single architecture, pre-trained once, applied to every task without being redesigned for any of them.
  bookscorpus | BooksCorpus: The pre-training corpus: over 7,000 unpublished books across genres. Its selling point in this paper is long stretches of contiguous text.
  model-specification | Model Specification: The exact architecture trained: a 12-layer decoder-only transformer with 768-dimensional states, 12 attention heads and 3072-dimensional inner feed-forward states.
  pre-training-optimization | Pre-Training Optimization Setup: How the language model was actually trained: Adam, a warmup-then-cosine schedule peaking at 2.5e-4, and 100 epochs of 64-sequence minibatches.
  fine-tuning-hyperparameters | Fine-Tuning Hyperparameters: Fine-tuning reuses the pre-training settings except for a learning rate of 6.25e-5, batch size 32, three epochs, classifier dropout 0.1, and a short warmup.
  evaluation-task-families | Four Task Families, Twelve Datasets: The paper evaluates on natural language inference, question answering and commonsense reasoning, semantic similarity, and text classification, across twelve datasets.
  layers-transferred | Impact of Number of Layers Transferred: An analysis varying how many pre-trained layers are carried into fine-tuning, showing each transformer layer adds performance, up to 9% on MultiNLI for full transfer.
  zero-shot-behaviors | Zero-Shot Behaviours: Heuristics that make the pre-trained generative model perform the evaluation tasks with no supervised fine-tuning at all, tracked across pre-training.
  ablation-studies | Ablation Studies: Three controlled removals reported in Table 5: dropping the auxiliary LM objective, replacing the transformer with an LSTM, and skipping pre-training entirely.

=== EVIDENCE ITEMS (each has a full page) ===
  fig-1 | One Stack, Four Input Shapes: The figure shows that a single decoder-only transformer, pre-trained once, serves every task in the paper: the left panel draws the block that is repeated twelve times and the two heads that sit on it
  table-1 | The Twelve Evaluation Datasets: This table is the paper's full evaluation menu: four families of supervised language understanding tasks covering twelve datasets in total. It establishes the breadth the paper is claiming for one pre
  table-2 | NLI Benchmark Scoreboard: Table 2 compares one fine-tuned general model against task-built baselines on five natural language inference datasets, and reports it ahead on four of them — by 1.5 points on MNLI, 5 on SciTail, 5.8 
  table-3 | RACE and Story Cloze Results: On the paper's two reading-and-reasoning benchmarks, a single fine-tuned transformer language model beats every previously published system, by 8.9 accuracy points on the Story Cloze Test and 5.7 poin
  table-4 | Classification and Similarity Results: On the five GLUE tasks outside natural language inference, one fine-tuned transformer beats the previous best published number on CoLA, STS-B and QQP, and raises the overall GLUE score from 68.9 to 72
  fig-2 | Layer Transfer and Zero-Shot Growth: Two diagnostics for why the recipe works. The left panel says every pre-trained layer carries transferable value: dev accuracy on RACE and MultiNLI rises steadily as more of the twelve-layer stack is 
  table-5 | Three Ablations Against the Full Model: Removing pre-training costs the most by far (14.8 average points), swapping the transformer for an LSTM costs 5.6, and dropping the auxiliary language modelling loss during fine-tuning actually gains 
  eq-lm-objective | The Pre-Training Likelihood $L_1$: This is the entire training signal for stage one: over a corpus of unlabelled tokens, sum the log-probability the network assigns to each token given the $k$ tokens immediately before it, and adjust t
  eq-decoder-stack | The Decoder Stack, Input to Output Distribution: Equation 2 spells out the whole pre-training network in three lines: embed the context tokens and their positions, push the result through $n$ identical transformer blocks, then project the top layer 
  eq-task-head | The Linear Task Head on the Last Token: Equation 3 is the entire task-specific classifier: take the final transformer block's activation at the last input token, multiply it by one matrix $W_y$, and softmax the result into a distribution ov
  eq-supervised-objective | Supervised Fine-Tuning Objective $L_2$: Equation 4 is the loss used in the second training stage: over a target task's labelled dataset $\mathcal{C}$, sum the log-probability the model assigns to each instance's correct label. It is ordinar
  eq-combined-objective | The Combined Fine-Tuning Objective: During fine-tuning the paper does not drop the language modelling loss; it keeps optimizing it alongside the classification loss, scaled by a weight $\lambda$, and computes it on the labelled task dat

=== LOAD-BEARING RELATIONSHIPS (the paper's own reasoning) ===
  two-stage-training-procedure (Two-Stage Training Procedure) --depends-on--> labeled-data-scarcity (Scarcity of Labelled Data): The two-stage recipe only earns its complexity if labelled data for each task is small while raw text is plentiful. If every task had enough annotated examples, there would be nothing for a pre-training stage to buy. The paper ope
  supervised-fine-tuning (Supervised Fine-Tuning (Stage Two)) --depends-on--> generative-pretraining (Generative Pre-Training (Stage One)): Stage two does not train a fresh network; it continues training the exact parameters stage one produced. The ablation puts a number on how much of the result comes from that starting point: training the same transformer directly o
  language-modeling-objective (Language Modelling Objective $L_1$) --depends-on--> masked-self-attention (Masked Self-Attention): The loss asks the model to predict each token from the tokens before it, and it does this at every position at once. Without the mask, a position could attend to the token it is being asked to predict, and the objective would be s
  multi-layer-transformer-decoder (Multi-Layer Transformer Decoder) --depends-on--> transformer-gpt1 (Transformer Architecture): The pre-training network is not a new architecture. It is the transformer block of prior work, stacked twelve deep and restricted to attend backwards. Everything the paper says about capacity and long-range conditioning inherits f
  bookscorpus (BooksCorpus) --depends-on--> long-range-dependencies-gpt1 (Long-Range Dependencies in Text): The paper says the point of BooksCorpus is its long stretches of contiguous text, which let the model learn to condition on far-away information. It names the 1B Word Benchmark, of roughly the same size, as the alternative it reje
  task-agnostic-model (Task-Agnostic Model) --depends-on--> task-specific-input-transformations (Task-Specific Input Transformations): The claim of a single model for every task holds only because the tasks are made to look alike at the input. Entailment pairs, similarity pairs and document-question-answer triplets are all rewritten as one ordered token sequence 
  minimal-extra-parameters (Minimal Extra Fine-Tuning Parameters) --depends-on--> task-specific-input-transformations (Task-Specific Input Transformations): The paper says the only extra parameters during fine-tuning are $W_y$ and embeddings for the delimiter tokens. That count stays small because structured inputs are handled by rewriting them, not by adding encoders or attention mod
  zero-shot-behaviors (Zero-Shot Behaviours) --depends-on--> average-token-log-probability-scoring (Average Token Log-Probability Scoring): A generative model does not emit labels, so making it answer a task without fine-tuning requires turning it into a scorer. Three of the four heuristics do the same thing: score a candidate string by the mean log-probability the mo
  two-stage-training-procedure (Two-Stage Training Procedure) --supported-by--> table-5 (Three Ablations Against the Full Model): The whole recipe rests on the claim that stage one matters. Table 5 trains the same transformer directly on the supervised tasks with no pre-training and reports a 14.8% drop across the board. That single row is the paper's strong
  multi-layer-transformer-decoder (Multi-Layer Transformer Decoder) --supported-by--> table-5 (Three Ablations Against the Full Model): The paper needs a reason to pick a transformer rather than a recurrent net. Table 5 holds everything else fixed and substitutes a single-layer 2048-unit LSTM, which costs 5.6 average score points. The LSTM wins on only one dataset
  ablation-no-pretraining (Ablation: No Pre-Training) --supported-by--> table-5 (Three Ablations Against the Full Model): This ablation exists only as a row in Table 5. Removing pre-training hurts on every task measured, making it the largest of the three ablation effects.
  language-modeling-objective (Language Modelling Objective $L_1$) --supported-by--> eq-lm-objective (The Pre-Training Likelihood $L_1$): Equation 1 is the entire training signal for stage one: sum the log-probability the network assigns each token given the k tokens before it. Everything the model knows before fine-tuning comes from maximizing this one quantity.
  multi-layer-transformer-decoder (Multi-Layer Transformer Decoder) --supported-by--> eq-decoder-stack (The Decoder Stack, Input to Output Distribution): Equation 2 writes the pre-training network out in full: embed the context tokens and add position embeddings, push the result through n identical transformer blocks, then project to a token distribution. Reading it is how you conf
  supervised-fine-tuning (Supervised Fine-Tuning (Stage Two)) --supported-by--> eq-task-head (The Linear Task Head on the Last Token): Equation 3 says the task-specific part is: take the final block's activation at the last token, multiply by one matrix, softmax. The paper's claim of minimal architectural change is checkable directly against this line.
  task-specific-input-transformations (Task-Specific Input Transformations) --supported-by--> fig-1 (One Stack, Four Input Shapes): Figure 1's right panel is the concrete content of this idea: classification, entailment, similarity and multiple choice each shown as a token sequence with start, delimiter and end markers. Without the picture the claim that struc
  natural-language-inference (Natural Language Inference (Textual Entailment)) --supported-by--> table-2 (NLI Benchmark Scoreboard): Table 2 puts the fine-tuned general model against task-built systems on five NLI datasets. It reports gains of 1.5% on MNLI, 5% on SciTail, 5.8% on QNLI and 0.6% on SNLI, and a loss on RTE.
  question-answering-and-commonsense-reasoning (Question Answering and Commonsense Reasoning) --supported-by--> table-3 (RACE and Story Cloze Results): Table 3 reports the paper's largest margins: 8.9% on the Story Cloze Test and 5.7% overall on RACE, ahead of every previously published system on both. These are the two numbers the abstract leads with.
  layers-transferred (Impact of Number of Layers Transferred) --supported-by--> fig-2 (Layer Transfer and Zero-Shot Growth): The left panel of Figure 2 varies how many pre-trained layers are carried into fine-tuning and plots dev accuracy on MultiNLI and RACE. Accuracy climbs with each additional layer, reaching up to 9% for full transfer on MultiNLI, w
  two-stage-training-procedure (Two-Stage Training Procedure) --instance-of--> semi-supervised-learning-for-nlp (Semi-Supervised Learning for NLP): Semi-supervised learning for NLP is the general family: a large unlabelled corpus plus a small labelled dataset. The paper's two-stage recipe is one specific member of that family, where the unlabelled corpus is used first to set 
  multi-layer-transformer-decoder (Multi-Layer Transformer Decoder) --instance-of--> transformer-gpt1 (Transformer Architecture): The transformer architecture is the general design the paper starts from. The language model here is one particular cut of it: the encoder half is dropped and a single stack of blocks with masked multi-head self-attention and posi
  multi-layer-transformer-decoder (Multi-Layer Transformer Decoder) --instance-of--> task-agnostic-model (Task-Agnostic Model): Task-agnostic model is the claim: one architecture, pre-trained once, applied everywhere without redesign. The 12-block decoder stack is what actually fills that role in this paper. Every one of the twelve datasets is run through 
  masked-self-attention (Masked Self-Attention) --instance-of--> language-modeling-objective (Language Modelling Objective $L_1$): L1 conditions each token only on the tokens before it. Masking the attention so a position can see itself and earlier positions but never later ones is the architectural realization of that constraint. Without the mask the model c
  language-modeling-objective (Language Modelling Objective $L_1$) --instance-of--> eq-lm-objective (The Pre-Training Likelihood $L_1$): Equation 1 is where the symbol L1 gets its meaning: sum over positions of log P(u_i | u_{i-k}, ..., u_{i-1}; Theta). Every later mention of the pre-training loss, including the lambda-weighted term during fine-tuning, refers back 
  task-agnostic-model (Task-Agnostic Model) --contrasts-with--> task-specific-architectures (Task-Specific Architectures): The paper's central comparison is between a single pre-trained stack applied everywhere and the prevailing practice of hand-designing a network around each task's structure. It claims the general model beats those discriminatively
  task-specific-input-transformations (Task-Specific Input Transformations) --contrasts-with--> task-specific-architectures (Task-Specific Architectures): Both approaches accept that tasks differ; they disagree about where that difference is absorbed. Prior work adds task-shaped modules on top of transferred representations, which the paper says re-introduces customization that gets
  two-stage-training-procedure (Two-Stage Training Procedure) --contrasts-with--> feature-based-transfer (Feature-Based Transfer): Feature-based transfer keeps the pre-trained model fixed and feeds its hidden states into a separate task model as extra inputs. The paper instead carries the pre-trained weights themselves into the target task and keeps training 
  minimal-extra-parameters (Minimal Extra Fine-Tuning Parameters) --contrasts-with--> per-task-parameter-cost (Per-Task Parameter Cost): The paper measures the two transfer styles by how many freshly initialized parameters a new task needs. Feature-based methods bring a substantial number of new parameters per task; here the additions are the linear output layer an
  multi-layer-transformer-decoder (Multi-Layer Transformer Decoder) --contrasts-with--> ablation-lstm-substitution (Ablation: LSTM Instead of Transformer): The ablation keeps the whole recipe and swaps only the architecture for a single-layer 2048-unit LSTM. The average score falls by 5.6 points, which is the paper's own measurement of what the architecture choice buys.
  ablation-no-auxiliary-objective (Ablation: No Auxiliary LM Objective) --contrasts-with--> auxiliary-lm-objective (Auxiliary Language Modelling Objective): Dropping the language modelling loss during fine-tuning does not simply degrade the model. It helps on the NLI tasks and QQP and hurts elsewhere, with the paper reading the trend as larger datasets benefiting and smaller ones not.
  ablation-no-pretraining (Ablation: No Pre-Training) --contrasts-with--> generative-pretraining (Generative Pre-Training (Stage One)): Training the identical architecture directly on the supervised tasks isolates what stage one contributes. Performance drops on all tasks, by 14.8 compared with the full model, which is the paper's strongest single number for pre-t
  generative-pretraining (Generative Pre-Training (Stage One)) --contrasts-with--> supervised-fine-tuning (Supervised Fine-Tuning (Stage Two)): The two stages train the same weights against opposite kinds of target. Stage one maximizes the probability of the next token over unlabelled text; stage two maximizes the probability of the correct label given the input.
