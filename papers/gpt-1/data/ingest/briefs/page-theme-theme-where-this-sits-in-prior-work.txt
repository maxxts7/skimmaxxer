PAGE FOR THEME: theme-where-this-sits-in-prior-work
NAME: Where This Sits in Prior Work

THEME SUMMARY (shown above your page as the lede - do not repeat verbatim):
The paper places itself inside semi-supervised learning for NLP and then separates that field into branches so it can say which one it is joining. One branch learns word, phrase or sentence vectors and hands them to a task model as features; another adds an unsupervised loss term alongside the supervised one; another uses unlabelled data only to choose the starting weights. The closest prior line pre-trains a recurrent language model and fine-tunes the whole network, and the paper argues that recurrence keeps such models' predictions short-range. Reading this before the method makes clear which existing pieces the paper keeps and which it is arguing against.

PREVIOUS THEME: theme-the-problem-and-the-open-questions | The Problem and the Open Questions
NEXT THEME: theme-one-model-two-stages | One Model, Two Stages

MEMBER CONCEPTS (13) - each has its own page or expandable section; your job is the connective tissue between them, not to restate each one:
  - semi-supervised-learning-for-nlp | Semi-Supervised Learning for NLP [major]: The family of methods that combine a large unlabelled corpus with a small labelled dataset, which the paper places itself inside.
      · word-embedding-transfer | Pre-Trained Word Embedding Transfer: Training one vector per word on unlabelled text, then reusing that table inside a supervised model for a diffe
      · phrase-and-sentence-embeddings | Phrase- and Sentence-Level Embeddings: Methods that learn one vector for a whole phrase or sentence from unlabelled text, then use it as a feature fo
      · unsupervised-pretraining | Unsupervised Pre-Training as a Category: The branch of semi-supervised learning where unlabelled data chooses a starting point for the weights instead 
      · pretraining-as-regularization | Pre-Training as a Regularization Scheme: The cited finding that pre-training helps not only by easing optimization but by landing the network somewhere
      · feature-based-transfer | Feature-Based Transfer: The competing transfer style: freeze a pre-trained language or translation model and feed its hidden states in
      · elmo-deep-contextualized-word-representations | Deep Contextualized Word Representations (ELMo): The best-known feature-based approach: hidden states from a pre-trained bidirectional language model, added as
  - word-embedding-transfer | Pre-Trained Word Embedding Transfer [minor]: Training one vector per word on unlabelled text, then reusing that table inside a supervised model for a different task.
  - phrase-and-sentence-embeddings | Phrase- and Sentence-Level Embeddings [minor]: Methods that learn one vector for a whole phrase or sentence from unlabelled text, then use it as a feature for the target task.
  - unsupervised-pretraining | Unsupervised Pre-Training as a Category [minor]: The branch of semi-supervised learning where unlabelled data chooses a starting point for the weights instead of changing the supervised objective.
      · pretraining-as-regularization | Pre-Training as a Regularization Scheme: The cited finding that pre-training helps not only by easing optimization but by landing the network somewhere
      · feature-based-transfer | Feature-Based Transfer: The competing transfer style: freeze a pre-trained language or translation model and feed its hidden states in
      · elmo-deep-contextualized-word-representations | Deep Contextualized Word Representations (ELMo): The best-known feature-based approach: hidden states from a pre-trained bidirectional language model, added as
      · per-task-parameter-cost | Per-Task Parameter Cost: The quantity the paper uses to separate its approach from feature-based transfer: how many freshly initialized
      · lm-pretraining-then-finetuning-predecessors | Language-Model Pre-Training Then Fine-Tuning: The line of work the paper calls closest to its own: pre-train with a language modelling objective, then fine-
      · semi-supervised-sequence-learning | Semi-Supervised Sequence Learning: Earlier work that pre-trained a recurrent network with a language modelling or autoencoding objective and fine
  - pretraining-as-regularization | Pre-Training as a Regularization Scheme [minor]: The cited finding that pre-training helps not only by easing optimization but by landing the network somewhere that generalizes better.
  - auxiliary-training-objectives | Auxiliary Training Objectives [minor]: The other branch: add a second unsupervised loss term alongside the supervised loss, so the model keeps learning from raw text while it learns the task.
  - feature-based-transfer | Feature-Based Transfer [minor]: The competing transfer style: freeze a pre-trained language or translation model and feed its hidden states into a separate task model as extra input features.
      · elmo-deep-contextualized-word-representations | Deep Contextualized Word Representations (ELMo): The best-known feature-based approach: hidden states from a pre-trained bidirectional language model, added as
      · per-task-parameter-cost | Per-Task Parameter Cost: The quantity the paper uses to separate its approach from feature-based transfer: how many freshly initialized
  - elmo-deep-contextualized-word-representations | Deep Contextualized Word Representations (ELMo) [minor]: The best-known feature-based approach: hidden states from a pre-trained bidirectional language model, added as features to a task model.
  - per-task-parameter-cost | Per-Task Parameter Cost [minor]: The quantity the paper uses to separate its approach from feature-based transfer: how many freshly initialized parameters each new task needs.
  - lm-pretraining-then-finetuning-predecessors | Language-Model Pre-Training Then Fine-Tuning [minor]: The line of work the paper calls closest to its own: pre-train with a language modelling objective, then fine-tune the whole network on a supervised task.
      · semi-supervised-sequence-learning | Semi-Supervised Sequence Learning: Earlier work that pre-trained a recurrent network with a language modelling or autoencoding objective and fine
      · ulmfit | Universal Language Model Fine-Tuning: A contemporaneous method that fine-tunes a pre-trained LSTM language model for text classification using a car
      · lstm-short-range-limitation | Short-Range Limitation of LSTM Pre-Training: The paper's argument that recurrent pre-trained models keep their predictions local, capping how much structur
  - semi-supervised-sequence-learning | Semi-Supervised Sequence Learning [minor]: Earlier work that pre-trained a recurrent network with a language modelling or autoencoding objective and fine-tuned it for text classification.
  - ulmfit | Universal Language Model Fine-Tuning [minor]: A contemporaneous method that fine-tunes a pre-trained LSTM language model for text classification using a carefully staged schedule.
  - lstm-short-range-limitation | Short-Range Limitation of LSTM Pre-Training [minor]: The paper's argument that recurrent pre-trained models keep their predictions local, capping how much structure they transfer.

EDGES BETWEEN MEMBERS OF THIS THEME:
  [supporting] unsupervised-pretraining --contrasts-with--> auxiliary-training-objectives | initialization versus modified objective: The related-work section splits semi-supervised learning into two branches. One uses unlabelled data only to choose where the weights start; the other leaves initialization alone and adds an unsupervised term to the supervised loss. The paper's recipe sits in the first branch and borrows from the second.

LOAD-BEARING EDGES LEAVING THIS THEME (use these to point forward/back):
  two-stage-training-procedure --instance-of--> semi-supervised-learning-for-nlp | the paper's semi-supervised recipe
  two-stage-training-procedure --contrasts-with--> feature-based-transfer | fine-tune weights versus frozen features
  minimal-extra-parameters --contrasts-with--> per-task-parameter-cost | one matrix versus many new parameters

EVIDENCE ITEMS RELATED TO THIS THEME (link as [[item-id]]):
  - table-2 | NLI Benchmark Scoreboard: Table 2 compares one fine-tuned general model against task-built baselines on five natural language inference datasets, and reports it ahead on four of them — by 1.5 points on MNLI, 5 on SciTail, 5.8 on QNLI and 0.6 on SNLI — while losing on RTE, the smallest dataset, where a multi-task biLSTM scores 61.7 against this model's 56.0.
  - table-4 | Classification and Similarity Results: On the five GLUE tasks outside natural language inference, one fine-tuned transformer beats the previous best published number on CoLA, STS-B and QQP, and raises the overall GLUE score from 68.9 to 72.8. It does not win everywhere: on SST-2 and MRPC it stays below specialist systems that were built for those single datasets.
  - fig-2 | Layer Transfer and Zero-Shot Growth: Two diagnostics for why the recipe works. The left panel says every pre-trained layer carries transferable value: dev accuracy on RACE and MultiNLI rises steadily as more of the twelve-layer stack is carried into fine-tuning, up to about 9% on MultiNLI for full transfer. The right panel says the pre-trained language model is already learning task-relevant behaviour before any fine-tuning: heuristic zero-shot scores on four tasks climb steadily across pre-training, and do so more smoothly for the transformer than for an LSTM trained the same way.
  - eq-combined-objective | The Combined Fine-Tuning Objective: During fine-tuning the paper does not drop the language modelling loss; it keeps optimizing it alongside the classification loss, scaled by a weight $\lambda$, and computes it on the labelled task data rather than on the pre-training corpus. The paper reports this helped by improving generalization and speeding convergence, and its own ablation later qualifies that: the gain shows up on larger datasets, not on smaller ones.

PAPER SECTIONS TO READ (in papers/gpt-1/data/ingest/sections/):
  - 02-1-introduction.txt
  - 03-2-related-work.txt
  - 09-4.1-setup.txt
  - 10-4.2-supervised-fine-tuning.txt
  - 12-6-conclusion.txt
