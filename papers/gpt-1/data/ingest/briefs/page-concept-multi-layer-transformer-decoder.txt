PAGE FOR CONCEPT: multi-layer-transformer-decoder
NAME: Multi-Layer Transformer Decoder

SUMMARY (already shown above your page as the lede - do not repeat it verbatim):
The language model is a stack of transformer blocks used as a decoder only: masked multi-head self-attention over the context, then position-wise feedforward layers.

DRAFT EXPLANATION from the extraction stage (your page replaces this; keep what is right, go deeper):
The paper takes the transformer and keeps only the decoder side of it, following the variant used for generating Wikipedia from long input sequences.

Each layer applies multi-headed self-attention over the input context tokens, then a position-wise feedforward network. Stacking gives $h_l = \mathrm{transformer\_block}(h_{l-1})$ for $l \in [1,n]$, with $n = 12$ in the trained model.

Layer zero is the token embeddings plus the position embeddings. The top layer's states are projected back to the vocabulary to give the output distribution.

The paper's argument for this stack over a recurrent one is that its attentional memory handles long-range dependencies better.

Section 5 puts a number on that argument. Swapping in a single-layer 2048-unit LSTM under the same framework costs 5.6 average score.

THEME IT BELONGS TO: theme-stage-one-next-token-prediction | Stage One: Predicting the Next Token - Stage one trains on a single loss: the log-probability the model assigns each token given a window of preceding tokens, summed over the corpus. The window here is 512 contiguous tokens, which is what lets a prediction condition on material several sentences back. The network is a stack of transformer blocks used as a decoder alone, with self-attention masked so each position sees only itself and its past, learned position vectors rather than fixed sinusoids, and an output layer that reuses the token embedding matrix transposed. This chapter also fixes the exact sizes trained, and closes with the claim the paper makes about what such training leaves behind.

SUB-CONCEPTS (these render as expandable sections BELOW your page - reference them, do not duplicate them):
  - decoder-only-stack | Decoder-Only Stack: The model drops the encoder half of the original encoder-decoder transformer and keeps one stack that attends only to its own past tokens.
  - masked-self-attention | Masked Self-Attention: Self-attention restricted so each position attends only to itself and earlier positions, never to later ones.
  - input-representation-h0 | Input Representation $h_0$: The layer-zero input is the token embeddings of the context plus the position embeddings: $h_0 = UW_e + W_p$.
  - token-embedding-matrix | Token Embedding Matrix $W_e$: $W_e$ maps each vocabulary token to a dense vector, and its transpose is reused at the output to score candidate next tokens.
  - learned-position-embeddings | Learned Position Embeddings: Position information comes from a trained matrix $W_p$, one vector per slot, rather than from the fixed sinusoids of the original transformer.
  - output-softmax-tied-embeddings | Output Distribution $P(u) = \mathrm{softmax}(h_n W_e^T)$: The top block's states are multiplied by the transpose of the token embedding matrix and softmaxed to give a distribution over the vocabulary.
  - model-specification | Model Specification: The exact architecture trained: a 12-layer decoder-only transformer with 768-dimensional states, 12 attention heads and 3072-dimensional inner feed-forward states.
    - twelve-layer-depth | 12 Layers: The stack is twelve decoder blocks deep, and the analysis section treats that depth as a dial the transfer experiments turn.
    - hidden-state-dimension-768 | 768-Dimensional States: Each token is represented by a 768-dimensional vector at every layer of the stack.
    - twelve-attention-heads | 12 Attention Heads: Each layer's self-attention is split into twelve parallel heads, each attending in a 64-dimensional subspace.
    - position-wise-feedforward-3072 | 3072-Dimensional Inner Feed-Forward States: The feed-forward sublayer in each block expands from 768 to 3072 dimensions and projects back down.
    - gelu | Gaussian Error Linear Unit (GELU): The activation function used in place of ReLU. It gates each input by the probability that a standard normal draw falls below it.
    - layer-normalization-gpt1 | Layer Normalization: Per-position normalization of activations to zero mean and unit variance, with learned gain and bias. Used throughout the stack.
    - weight-initialization | $\mathcal{N}(0, 0.02)$ Weight Initialization: All weights are drawn from a normal distribution with standard deviation 0.02, which the paper says was sufficient because layernorm is used throughout.

PREREQUISITES:
  - transformer-gpt1 | Transformer Architecture: The stacked self-attention and feed-forward architecture this paper builds on. Assumed known.

EDGES TOUCHING THIS CONCEPT (8) - the relationships the app already knows; weave the load-bearing ones into your prose:
  [load-bearing] -> transformer-gpt1 (Transformer Architecture) | depends-on | built from transformer blocks: The pre-training network is not a new architecture. It is the transformer block of prior work, stacked twelve deep and restricted to attend backwards. Everything the paper says about capacity and long-range conditioning inherits from that architecture.
  [load-bearing] -> table-5 (Three Ablations Against the Full Model (table-5)) | supported-by | LSTM swap costs 5.6 points: The paper needs a reason to pick a transformer rather than a recurrent net. Table 5 holds everything else fixed and substitutes a single-layer 2048-unit LSTM, which costs 5.6 average score points. The LSTM wins on only one dataset, MRPC.
  [load-bearing] -> eq-decoder-stack (The Decoder Stack, Input to Output Distribution (eq-decoder-stack)) | supported-by | three lines specify the network: Equation 2 writes the pre-training network out in full: embed the context tokens and add position embeddings, push the result through n identical transformer blocks, then project to a token distribution. Reading it is how you confirm there is no encoder and no cross-attention anywhere in the model.
  [load-bearing] -> transformer-gpt1 (Transformer Architecture) | instance-of | decoder-only variant of transformer: The transformer architecture is the general design the paper starts from. The language model here is one particular cut of it: the encoder half is dropped and a single stack of blocks with masked multi-head self-attention and position-wise feed-forward layers is kept. Nothing about the block itself is redesigned.
  [load-bearing] -> task-agnostic-model (Task-Agnostic Model) | instance-of | the one stack every task uses: Task-agnostic model is the claim: one architecture, pre-trained once, applied everywhere without redesign. The 12-block decoder stack is what actually fills that role in this paper. Every one of the twelve datasets is run through this same stack.
  [load-bearing] -> ablation-lstm-substitution (Ablation: LSTM Instead of Transformer) | contrasts-with | transformer versus LSTM, same framework: The ablation keeps the whole recipe and swaps only the architecture for a single-layer 2048-unit LSTM. The average score falls by 5.6 points, which is the paper's own measurement of what the architecture choice buys.
  [supporting] -> lstm-short-range-limitation (Short-Range Limitation of LSTM Pre-Training) | depends-on | choice argued from recurrent locality: The paper's stated reason for using a transformer rather than a recurrent network is that pre-trained LSTMs keep their predictive ability short-range, which limits how much structure they can hand to a target task. The architecture choice is presented as a response to that limitation. The LSTM ablation is the paper's own test of the claim, at a cost of 5.6 average score points.
  [minor] <- position-wise-feedforward-3072 (3072-Dimensional Inner Feed-Forward States) | instance-of | feed-forward width in this model: Each block in the stack ends with a position-wise feed-forward sublayer, and this is the size it was given. The layer expands each 768-dimensional vector to 3072 dimensions and projects it back, a four-fold widening. That is the concrete setting of a slot the general architecture leaves open.

EVIDENCE ITEMS THAT SHOW THIS (link them as [[item-id]]):
  - fig-1 | One Stack, Four Input Shapes: The figure shows that a single decoder-only transformer, pre-trained once, serves every task in the paper: the left panel draws the block that is repeated twelve times and the two heads that sit on it, and the right panel shows that adapting to a new task means rewriting its input as one token sequence rather than redesigning the network. The only task-specific machinery is the added linear layer and the special tokens marking where the parts of an input begin and end.
  - fig-2 | Layer Transfer and Zero-Shot Growth: Two diagnostics for why the recipe works. The left panel says every pre-trained layer carries transferable value: dev accuracy on RACE and MultiNLI rises steadily as more of the twelve-layer stack is carried into fine-tuning, up to about 9% on MultiNLI for full transfer. The right panel says the pre-trained language model is already learning task-relevant behaviour before any fine-tuning: heuristic zero-shot scores on four tasks climb steadily across pre-training, and do so more smoothly for the transformer than for an LSTM trained the same way.
  - eq-decoder-stack | The Decoder Stack, Input to Output Distribution: Equation 2 spells out the whole pre-training network in three lines: embed the context tokens and their positions, push the result through $n$ identical transformer blocks, then project the top layer back through the transposed token embedding matrix and softmax it into a distribution over the next token. The same matrix $W_e$ does the work at both ends, so no separate output vocabulary matrix is introduced.
  - eq-task-head | The Linear Task Head on the Last Token: Equation 3 is the entire task-specific classifier: take the final transformer block's activation at the last input token, multiply it by one matrix $W_y$, and softmax the result into a distribution over the task's labels. The paper states that $W_y$ (plus delimiter-token embeddings) is the only new parameter it adds when moving the pre-trained model to a new task.

PAPER SECTIONS TO READ (in papers/gpt-1/data/ingest/sections/):
  - 05-3.1-unsupervised-pre-training.txt
  - 09-4.1-setup.txt
