PAGE FOR CONCEPT: ablation-studies
NAME: Ablation Studies

SUMMARY (already shown above your page as the lede - do not repeat it verbatim):
Three controlled removals reported in Table 5: dropping the auxiliary LM objective, replacing the transformer with an LSTM, and skipping pre-training entirely.

DRAFT EXPLANATION from the extraction stage (your page replaces this; keep what is right, go deeper):
The results tables show that the full system wins. The ablations ask which of its parts are doing the work.

Each removes one piece and holds the rest fixed. First the auxiliary language modelling objective is switched off during fine-tuning. Second the transformer is replaced by a single-layer 2048-unit LSTM in the same framework. Third the transformer is trained directly on the supervised tasks with no pre-training at all.

The sizes of the three effects are not equal, and that ordering is the finding.

Removing pre-training costs 14.8%, swapping the architecture costs 5.6 average score points, and the auxiliary objective helps only on some datasets.

So the ablations put pre-training first, the architecture second, and the auxiliary objective a distant and dataset-dependent third.

THEME IT BELONGS TO: theme-taking-the-recipe-apart | Taking the Recipe Apart - Three diagnostics ask which parts of the recipe are doing the work. Transferring more pre-trained layers keeps helping rather than saturating early, and hand-built heuristics show the pre-trained model already doing several tasks at some level with no supervised training at all, improving steadily as pre-training runs. The removals are blunter: no pre-training costs the most, swapping the transformer for an LSTM costs less, and dropping the auxiliary language modelling loss helps on some datasets and hurts on others. The paper's reading of why attention transfers better than recurrence is stated as a hypothesis, and the zero-shot heuristics are hand-designed, so both carry less weight than the ablation numbers.

SUB-CONCEPTS (these render as expandable sections BELOW your page - reference them, do not duplicate them):
  - ablation-no-auxiliary-objective | Ablation: No Auxiliary LM Objective: Fine-tuning without the language modelling loss helps on some datasets and hurts on others, with larger datasets benefiting from keeping it.
  - ablation-lstm-substitution | Ablation: LSTM Instead of Transformer: Replacing the transformer with a single-layer 2048-unit LSTM in the same framework costs 5.6 average score points.
  - ablation-no-pretraining | Ablation: No Pre-Training: Training the same transformer directly on the supervised tasks, with no pre-training, costs 14.8% across the board.

PREREQUISITES:
  - supervised-fine-tuning | Supervised Fine-Tuning (Stage Two): Run labelled inputs through the pre-trained model, take the final block's activation at the last token, and train a linear layer on it to predict the label.
  - generative-pretraining | Generative Pre-Training (Stage One): Train a multi-layer transformer decoder to predict each next token on a large unlabelled corpus, and keep the weights rather than the outputs.

EVIDENCE ITEMS THAT SHOW THIS (link them as [[item-id]]):
  - table-5 | Three Ablations Against the Full Model: Removing pre-training costs the most by far (14.8 average points), swapping the transformer for an LSTM costs 5.6, and dropping the auxiliary language modelling loss during fine-tuning actually gains 0.3 on average — helping only on the larger datasets (QQP and the two NLI sets) and hurting on the smaller ones.

PAPER SECTIONS TO READ (in papers/gpt-1/data/ingest/sections/):
  - 11-5-analysis.txt
