{
  "name": "skimmaxxer",
  "version": "1.0.0",
  "private": true,
  "description": "Research papers rebuilt as recursive explainers. The site is static; these dependencies are only for the two functions that take paper requests.",
  "type": "module",
  "dependencies": {
    "@netlify/blobs": "^11.0.1"
  }
}
